
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let sessionRueInjureIncludeUrl:String = "SOClocation"
fileprivate let modulePianoConfig:String = "R_KEYfast per pattern fast activity"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let moduleCurThroughUrl:[UInt8] = [0xfb,0xe7,0xeb,0xe3,0xed,0xfc,0xf7,0xfe,0xe1,0xec,0xed,0xe7,0xeb,0xe0,0xe9,0xfc,0xf7,0xfa,0xed,0xf9,0xfd,0xed,0xfb,0xfc,0xeb,0xe9,0xe4,0xe4,0xf7,0xe6,0xe7,0xfc,0xe1,0xee,0xe1,0xeb,0xe9,0xfc,0xe1,0xe7,0xe6]

private func largeSleeve(price num: UInt8) -> UInt8 {
    return num ^ 168
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let moduleShowValue:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let themeMobileEachEarlyTimer:[Character] = ["i","s","C","a","l","l","i"]
fileprivate let kMayPlaceData:String = "nautomatically"

/*: "SOCKET 连接成功." :*/
fileprivate let kLipNatureLogger:[Character] = ["S","O","C","K","E"]
fileprivate let styleLifetimeSettings:[Character] = ["T"," ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let componentPressureKey:[Character] = ["S","O","C","K","E","T"," ","连"]
fileprivate let commonSpendEvent:[Character] = ["接","失","\u{8d25}",","," "]
fileprivate let styleRoundTitle:String = "error:proof corner scan enterprise"

/*: "SOCKET 失败 :*/
fileprivate let appMirrorMetrics:String = "park help bond timber wideSOCKET "
fileprivate let screenPlayerPage:[Character] = ["失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let coreTableAlert:[Character] = ["次",","," ","切","换","过","S","o","c","k","e","t","域","名","重","新"]
fileprivate let widgetNoticeTimer:String = "尝试\u{ff0c}地\u{5740}\u{ff1a}"

/*: "SOCKET 已关闭, code: :*/
fileprivate let componentReplaceFormat:String = "maker arc customer white successfullySOCKET 已"
fileprivate let commonTrainSession:String = "\u{5173}闭, "

/*: , reason: :*/
fileprivate let commonHeData:[Character] = [","," ","r","e","a","s","o","n",":"]

/*: /?sid= :*/
fileprivate let featureAfterId:String = "host"
fileprivate let commonResignEvent:String = "family character inner?sid="

/*: &uid= :*/
fileprivate let appPortraitId:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let serviceFormalSession:String = "outer invitation gray&pla"
fileprivate let sessionRidRecessKey:String = "iphone&vbuild don we lifetime"
fileprivate let viewFrankFormat:String = "describe happilyersion="

/*: &packageId= :*/
fileprivate let sessionBeforeText:[Character] = ["&","p","a","c","k","a"]
fileprivate let commonSkullUtility:String = "geId=info approve artifact time word"

/*: "1v1server. :*/
fileprivate let themeAcquireRecommendMessage:String = "1v1sam meat"
fileprivate let kFourEvent:String = "terrible bound known public dialogerver."

/*: .com:7766" :*/
fileprivate let widgetChildData:String = "document"
fileprivate let k_originSession:String = "conversion body away contact narrowcom:7766"

/*: "43.129.185.194:7767" :*/
fileprivate let themeRefDisappearError:String = "43.12sun"
fileprivate let featureActEvent:String = "94:suitable"
fileprivate let styleDistributionId:String = "exposure"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let colorAdviceUrl:[UInt8] = [0xd0,0xcc,0xc0,0xc8,0xc6,0xd7,0xa3,0x64,0x3e,0x12,0x64,0x38,0x1f,0xf1,0xe6,0xe2,0xe0,0xeb,0xe2,0xe1,0xea,0xef,0xea,0xf7,0xfa,0xc0,0xeb,0xe2,0xed,0xe4,0xe6,0xe7,0x64,0x18,0x12,0x66,0x13,0x2f,0x6c,0x3f,0xf,0x65,0x15,0x2e,0x64,0x3e,0x12,0x66,0x6,0x30,0x6a,0x14,0x2e,0xad]

private func effRound(analysis num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "cmd" :*/
fileprivate let componentSupportPage:String = "cmthought"

/*: "ping" :*/
fileprivate let themeAnalyzeText:String = "PING"

/*: "callHeartbeat" :*/
fileprivate let commonAtError:String = "caquick"
fileprivate let commonRootDevice:[Character] = ["t"]

/*: "recievedDate" :*/
fileprivate let viewHugeSettings:[Character] = ["r","e","c","i"]
fileprivate let commonMaxThresholdHelper:[Character] = ["e","v","e","d","D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let widgetBrandFormat:String = "livoutput"
fileprivate let componentMaximumName:String = "red rem inside outdotBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let screenVoteEvent:String = "🔗 关闭 scount breath daily stop complaint"
fileprivate let servicePossibleData:String = "owindowet"

/*: "The user logs out" :*/
fileprivate let k_acidFormat:String = "award reserve instructionThe use"
fileprivate let appSameUrl:String = "r used drawing"
fileprivate let layoutPressStockAgoContent:String = "logs outaudience resource create of flow"

/*: "SOCKET  :*/
fileprivate let appGrayForeheadFormat:String = "SOCKET duck build fast game train"

/*: 重连. 重试次数： :*/
fileprivate let serviceMirrorPlatform:String = "intro连. intro试"
fileprivate let colorResponseMessage:[Character] = ["次","数","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let spacingAlarmUtility:String = "sauceCKE"
fileprivate let commonVoteMetrics:String = "\u{8981}重新连接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let featureHemKey:[Character] = ["S","O","C","K","E","T"," "]
fileprivate let styleProofError:[Character] = ["\u{6b63}","在","连","接"," ","不","需"]
fileprivate let k_regularAddedData:String = "要重新\u{8fde}\u{63a5}."

/*: "&resume=1" :*/
fileprivate let displayDisagreeAboveActFormat:String = "&resumgift instance all"
fileprivate let kRackSoapSettings:String = "e=1ban require if"

/*: "&lang= :*/
fileprivate let moduleComputerError:String = "yellow behavior late&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let appCleanText:String = "SOhistoric"
fileprivate let screenLackLogger:[Character] = ["\u{8fde}","\u{63a5}",","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let viewScarError:String = "storage below mm shrinkonNet"

/*: "errno" :*/
fileprivate let sessionDisableKey:String = "ERRNO"

/*: "data" :*/
fileprivate let componentHelpName:String = "dmediatmedia"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let spacingRoundingUtility:[Character] = ["S","O","C","K","E","T"," ","连","接"]
fileprivate let componentCloseeDevice:String = "保\u{6d3b}cmd:"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let sessionBusinessmanTitle:[UInt8] = [0xaa,0xb6,0xba,0xb2,0xbc,0xad,0xd9,0x8d,0x8b,0x8d,0x9a,0x1d,0x46,0x64,0x1f,0x4d,0x42,0x9a,0x94,0x9d,0xc3,0xd9,0x9a,0x98,0x95,0x95,0xb1,0x9c,0x98,0x8b,0x8d,0x9b,0x9c,0x98,0x8d,0xd7]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let kFirstData:[UInt8] = [0x21,0x3d,0x31,0x39,0x37,0x26,0x52,0x1e,0x1b,0x4,0x17,0x96,0xcd,0xef,0x94,0xc6,0xc9,0x11,0x1f,0x16,0x48,0x52,0x1e,0x1b,0x4,0x17,0x3a,0x17,0x13,0x0,0x6,0x30,0x17,0x13,0x6,0x5c]

private func sandwichRocket(female num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "onP" :*/
fileprivate let viewVisibleText:String = "transport database botonP"

/*: "msg" :*/
fileprivate let widgetSideHelper:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let viewCityPreference:String = "🔗 处理消息："
fileprivate let spacingTheoreticUrl:String = "crecognize"
fileprivate let themeLimitedPage:[Character] = ["d","N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let widgetSampleError:[Character] = [","," ","e","r","r","N"]
fileprivate let k_rushAlert:String = "o:there dog produce writing"

/*: , msg: :*/
fileprivate let componentSparkPage:String = "proof official, msg:"

/*: , data: :*/
fileprivate let layoutMannerId:String = ", "
fileprivate let sessionUnderMessage:String = "essential of writtendata:"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let displayMarginValue:String = "SseparationK"
fileprivate let viewWriteMaximError:String = "常消\u{606f}\u{ff1a}"

/*: "onReceivedGift" :*/
fileprivate let moduleChampionPath:[Character] = ["o","n","R","e","c","e"]
fileprivate let displayAudiencePreference:String = "ipaneled"

/*: "onVideoMatch" :*/
fileprivate let styleChannelHelper:[Character] = ["o","n","V","i","d","e","o"]
fileprivate let componentApologySession:String = "Matchreference already scene recess panel"

/*: "onRequestCall" :*/
fileprivate let commonBetweenGuideParkPreference:String = "onReanimal kingdom magnitude sequence"
fileprivate let layoutCommonTimer:String = "questCallobject threat all nor"

/*: "onCancelCall" :*/
fileprivate let layoutHelloPreference:[Character] = ["o","n","C","a","n","c","e","l","C","a","l"]
fileprivate let commonPowData:[Character] = ["l"]

/*: "onRefuseCall" :*/
fileprivate let displayBorderValue:[Character] = ["o","n","R","e","f","u","s"]
fileprivate let coreWarnUtility:String = "reach miceCall"

/*: "onAcceptCall" :*/
fileprivate let layoutHumanPath:[Character] = ["o","n","A","c","c","e","p"]
fileprivate let componentDismissError:String = "tCallrelation ingredient permission"

/*: "onStopCall" :*/
fileprivate let sessionLitUtility:String = "restore original supposed saveonSto"

/*: "freeCallTimes" :*/
fileprivate let colorParticleEffectLogger:[Character] = ["f","r","e","e","C","a","l","l"]
fileprivate let componentEyeAlert:String = "Timesguide map"

/*: "mfCoin" :*/
fileprivate let serviceCurDiskPage:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let serviceFiveMetrics:[Character] = ["o","n","M","i","s","s","C","o","i"]
fileprivate let k_videoProofPath:String = "lite"

/*: "acceptCall" :*/
fileprivate let colorGoHelper:[Character] = ["a","c","c","e","p","t","C","a","l"]
fileprivate let appFirmText:String = "ally"

/*: "stopCall" :*/
fileprivate let sessionThematicBestFormat:[Character] = ["s"]
fileprivate let themeReplacementGatorActorLogger:String = "topCallobserve challenge enable method"

/*: "requestCall" :*/
fileprivate let kPhaseId:String = "rstepqusteps"

/*: "[Invite you to a video call...]" :*/
fileprivate let serviceAttributeFormat:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let coreMonthTheoryEvent:String = "universalype"

/*: "[Invite you to a voice call...]" :*/
fileprivate let spacingDepthName:[UInt8] = [0xd,0x1f,0x38,0x20,0x3f,0x22,0x33,0x76,0x2f,0x39,0x23,0x76,0x22,0x39,0x76,0x37,0x76,0x20,0x39,0x3f,0x35,0x33,0x76,0x35,0x37,0x3a,0x3a,0x78,0x78,0x78,0xb]

private func magnitudeCoalCell(shrink num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "uid" :*/
fileprivate let appAbleLocalError:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let k_agentFormat:[Character] = ["n","i","c","k","n","a","m"]
fileprivate let kValidSystem:[Character] = ["e"]

/*: "headPic" :*/
fileprivate let commonResignMetrics:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let spacingSweepPickMessage:String = "necessity spread contribution place additiononCa"
fileprivate let sessionItMetrics:[Character] = ["u","m","e"]

/*: "cancelCall" :*/
fileprivate let colorAcrossText:String = "CANCEL"
fileprivate let themePointPlatform:String = "greatest editor passengerCall"

/*: "refuseCall" :*/
fileprivate let appOpenError:String = "oldfus"
fileprivate let colorHuntContent:[Character] = ["e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let k_duckAlwaysTimer:String = "onCalutility illegal"
fileprivate let componentLimitId:[Character] = ["i","v","a","t","i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let layoutPathSession:[Character] = ["o","n","L","i","v","e","W","a","r"]
fileprivate let featureCleanConfig:String = "helloihellog"

/*: "reason" :*/
fileprivate let moduleChaseError:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let layoutPickSaveerName:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let widgetEndPlatform:String = "process bad metalonStopL"
fileprivate let spacingStrikeTitle:String = "iserver"

/*: "onSendProgramGift" :*/
fileprivate let sessionStrokeText:[Character] = ["o","n","S","e","n","d","P","r","o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let componentHowMessage:[Character] = ["o"]
fileprivate let spacingAlliancePage:[Character] = ["n","G","a","m","e","F","s"]

/*: "broadStatus" :*/
fileprivate let moduleClothesPage:String = "standard project accuracybroadS"

/*: "onCallChatMsg" :*/
fileprivate let widgetAwardDomainUrl:[Character] = ["o","n","C","a","l","l","C","h","a","t","M"]
fileprivate let k_interestedValue:String = "sbegin"

/*: "onNotificationPopupWin" :*/
fileprivate let styleFoundFormat:String = "preserve discount computeronNot"
fileprivate let widgetYellowValue:String = "constraint tam find circuittionP"
fileprivate let spacingTellUtility:[Character] = ["W","i","n"]

/*: "onUserBlock" :*/
fileprivate let spacingBeatName:[Character] = ["o","n","U","s","e","r","B","l","o","c","k"]

/*: "isRecharge" :*/
fileprivate let spacingKitEvent:String = "isRechplatform eff source"
fileprivate let styleAroundEachHourContent:[Character] = ["a","r","g","e"]

/*: "source" :*/
fileprivate let k_femaleMutualPreference:String = "luckurce"

/*: "invitePop" :*/
fileprivate let sessionWelcomeUtility:String = "actual hang drop follow hypothesisinviteP"
fileprivate let colorProMessage:[Character] = ["o","p"]

/*: "videoPop" :*/
fileprivate let layoutMidSendEvent:String = "vlayerdeo"
fileprivate let serviceHelpId:[Character] = ["P","o","p"]

/*: "coin" :*/
fileprivate let spacingAssessmentSnapError:String = "cadministrativen"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TopThen.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let viewScaleDisplayPlayPlatform = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let themeNameGuidePath = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let serviceOnSession = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let screenRateName = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let commonTagId = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let spacingAddressRecordPreference = (sessionRueInjureIncludeUrl.replacingOccurrences(of: "location", with: "K") + "ET_ADD" + String(modulePianoConfig.prefix(5)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let layoutExploreMessage = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let screenMarginMessage = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let themeNeedUtility = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let componentNameText = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let appScaleSession = NSNotification.Name(rawValue: String(bytes: moduleCurThroughUrl.map{largeSleeve(price: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let spacingLogMetrics = NSNotification.Name(rawValue: String(bytes: moduleShowValue.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol ConsentErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func noWall(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol DelegateObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func originTake(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func requestData(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol PortraitObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func sound(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func banIcon(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func raw(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func conversionChat(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol EnhanceFailObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func club(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func labelCommit(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func checkChat(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class TopThen: NSObject {
    //: enum SocketRetryType: String {
    enum EditionSocketRetryType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currFamousVC: AnyObject?
    var currFamousVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PushThen.shared.direction == .rightToLeft ? 0 : moduleAdjustPath,
        //: y: StatusBarHeight+190
        y: appSceneUrl + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PushThen.shared.direction == .rightToLeft ? 0 : moduleAdjustPath,
        //: y: StatusBarHeight+190
        y: appSceneUrl + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: ConsentErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: DelegateObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: PortraitObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: EnhanceFailObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isFamous = false
    @objc dynamic var isFamous = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = TopThen()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(noteEffect(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(goOver),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(themeMobileEachEarlyTimer) + kMayPlaceData.replacingOccurrences(of: "automatically", with: "g")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (SensitiveFamousApplication.shared as! SensitiveFamousApplication).aReduce()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            
            .observeWeakly(Bool.self, "isFamous")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (SensitiveFamousApplication.shared as! SensitiveFamousApplication).aReduce()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension TopThen: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        CheckThen.placeLoad(msg: (String(kLipNatureLogger) + String(styleLifetimeSettings)))
        //: sendPing()
        closeStep()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: styleCheckAlert,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: spacingAddressRecordPreference)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        misestimation()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        CheckThen.placeLoad(msg: (String(componentPressureKey) + String(commonSpendEvent) + String(styleRoundTitle.prefix(6))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = PositionThen.share.reachability?.connection, status != .unavailable else {
        guard let status = PositionThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == screenRateName {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = socket(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            CheckThen.placeLoad(msg: (String(appMirrorMetrics.suffix(7)) + String(screenPlayerPage)) + "\(screenRateName)" + (String(coreTableAlert) + widgetNoticeTimer) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.shrinkShare(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.computeMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        CheckThen.placeLoad(msg: (String(componentReplaceFormat.suffix(8)) + commonTrainSession + "code:") + "\(code)" + (String(commonHeData)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.fragmentMerge(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == componentNameText {
            //: return
            return
        }
        //: if let status = PositionThen.share.reachability?.connection, status != .unavailable {
        if let status = PositionThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.shrinkShare(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func socket(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = PositionThen.share.appConfigMode.ws
        self.linkAddrArr = PositionThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = broadAlongsideArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: spacingAddressRecordPreference)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = PositionThen.share.loginUid
        let uid = PositionThen.share.loginUid
        //: let sid = PositionThen.share.loginSessionId
        let sid = PositionThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (featureAfterId.replacingOccurrences(of: "host", with: "/") + String(commonResignEvent.suffix(5))) + "\(sid)" + (String(appPortraitId)) + "\(uid)" + (String(serviceFormalSession.suffix(4)) + "tform=" + String(sessionRidRecessKey.prefix(8)) + String(viewFrankFormat.suffix(7))) + "\(coreReadText)" + (String(sessionBeforeText) + String(commonSkullUtility.prefix(5))) + "\(screenSaltConfig)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func broadAlongsideArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return widgetOkPlatform ? [(String(themeAcquireRecommendMessage.prefix(4)) + String(kFourEvent.suffix(6))) + "\(componentFormalPath)" + (widgetChildData.replacingOccurrences(of: "document", with: ".") + String(k_originSession.suffix(8)))] : [(themeRefDisappearError.replacingOccurrences(of: "sun", with: "9") + ".185.1" + featureActEvent.replacingOccurrences(of: "suitable", with: "77") + styleDistributionId.replacingOccurrences(of: "exposure", with: "67"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension TopThen {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func noteEffect(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            CheckThen.placeLoad(msg: String(bytes: colorAdviceUrl.map{effRound(analysis: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.fragmentMerge(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.shrinkShare(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func goOver() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.shrinkShare(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.detailEvent()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension TopThen {
    /// socket保活心跳包
    //: private func sendPing() {
    private func closeStep() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: viewScaleDisplayPlayPlatform, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.info(info: [(componentSupportPage.replacingOccurrences(of: "thought", with: "d")): (themeAnalyzeText.lowercased())])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func sprint() {
        //: endTalkingPing()
        generatePoint()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: themeNameGuidePath, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isFamous || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currFamousVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currFamousVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isFamous || (self.famousTick()?.isKind(of: TelecastingHiObjectProtocol.self) == false && self.famousTick()?.isKind(of: FrameworkViewController.self) == false) && (self.currFamousVC?.isKind(of: TelecastingHiObjectProtocol.self) == false && self.currFamousVC?.isKind(of: FrameworkViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.info(info: [(componentSupportPage.replacingOccurrences(of: "thought", with: "d")): (commonAtError.replacingOccurrences(of: "quick", with: "ll") + "Heartbea" + String(commonRootDevice))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func generatePoint() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func detailEvent() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(viewHugeSettings) + String(commonMaxThresholdHelper))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < screenSampleFormat {
                //: PositionThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                PositionThen.share.shack(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension TopThen {
    /// 开启直播心跳包
    //: func startLivePing() {
    func late() {
        //: endLivePing()
        alongPing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: serviceOnSession, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.info(info: [(componentSupportPage.replacingOccurrences(of: "thought", with: "d")): (widgetBrandFormat.replacingOccurrences(of: "output", with: "e") + "Hear" + String(componentMaximumName.suffix(5)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func alongPing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension TopThen {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func misestimation() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = PanelContainerView()
        //: let keyWindow = FragThen.getWindow()
        let keyWindow = FragThen.termsSource()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isFamous
        self.containerView?.isHidden = !self.isFamous
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func commentAcross() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard InstrumentalityReactiveCompatible.aftermath().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension TopThen {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func fragmentMerge(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(screenVoteEvent.prefix(6)) + servicePossibleData.replacingOccurrences(of: "window", with: "ck")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: componentNameText, reason: (String(k_acidFormat.suffix(7)) + String(appSameUrl.prefix(2)) + String(layoutPressStockAgoContent.prefix(8))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        generatePoint()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            commentAcross()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func stuff() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = TopThen.shared.socket(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.shrinkShare(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func shrinkShare(timeInterval: TimeInterval, type: EditionSocketRetryType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            CheckThen.placeLoad(msg: (String(appGrayForeheadFormat.prefix(7))) + "\(type.rawValue)" + (serviceMirrorPlatform.replacingOccurrences(of: "intro", with: "重") + String(colorResponseMessage)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.usFragment(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func usFragment(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                CheckThen.placeLoad(msg: (spacingAlarmUtility.replacingOccurrences(of: "sauce", with: "SO") + "T 连接正常 不需" + commonVoteMetrics))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                CheckThen.placeLoad(msg: (String(featureHemKey) + String(styleProofError) + k_regularAddedData))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isFamous {
        if self.isFamous {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(displayDisagreeAboveActFormat.prefix(6)) + String(kRackSoapSettings.prefix(3))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(moduleComputerError.suffix(6))) + "\(HarvestThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        fragmentMerge(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = commonTagId
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        CheckThen.placeLoad(msg: (appCleanText.replacingOccurrences(of: "historic", with: "CK") + "ET \u{5f00}\u{59cb}" + String(screenLackLogger)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func info(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if PositionThen.share.reachability?.connection == nil || PositionThen.share.reachability?.connection == .unavailable {
        if PositionThen.share.reachability?.connection == nil || PositionThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(componentSupportPage.replacingOccurrences(of: "thought", with: "d")): (String(viewScarError.suffix(5)) + "Disabled"),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (sessionDisableKey.lowercased()): "\(themeNeedUtility)",
                                       //: "data": [:]]
                                       (componentHelpName.replacingOccurrences(of: "media", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            computeMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(componentSupportPage.replacingOccurrences(of: "thought", with: "d"))] as? String == (themeAnalyzeText.lowercased()) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                CheckThen.placeLoad(msg: (String(spacingRoundingUtility) + componentCloseeDevice + " ping."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(componentSupportPage.replacingOccurrences(of: "thought", with: "d"))] as? String == (commonAtError.replacingOccurrences(of: "quick", with: "ll") + "Heartbea" + String(commonRootDevice)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                CheckThen.placeLoad(msg: String(bytes: sessionBusinessmanTitle.map{$0^249}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(componentSupportPage.replacingOccurrences(of: "thought", with: "d"))] as? String == (widgetBrandFormat.replacingOccurrences(of: "output", with: "e") + "Hear" + String(componentMaximumName.suffix(5))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                CheckThen.placeLoad(msg: String(bytes: kFirstData.map{sandwichRocket(female: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(componentSupportPage.replacingOccurrences(of: "thought", with: "d"))] as? String == (themeAnalyzeText.lowercased()) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(componentSupportPage.replacingOccurrences(of: "thought", with: "d")): (String(viewVisibleText.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (sessionDisableKey.lowercased()): "\(layoutExploreMessage)",
                                           //: "data": [:]]
                                           (componentHelpName.replacingOccurrences(of: "media", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                computeMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.shrinkShare(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func computeMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(componentSupportPage.replacingOccurrences(of: "thought", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(sessionDisableKey.lowercased())].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(widgetSideHelper))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (viewCityPreference.capitalized + spacingTheoreticUrl.replacingOccurrences(of: "recognize", with: "m") + String(themeLimitedPage)) + "\(cmdName)" + (String(widgetSampleError) + String(k_rushAlert.prefix(2))) + "\(errorNo)" + (String(componentSparkPage.suffix(6))) + "\(msg)" + (layoutMannerId.capitalized + String(sessionUnderMessage.suffix(5))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        self.title(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            CheckThen.placeLoad(msg: (displayMarginValue.replacingOccurrences(of: "separation", with: "OC") + "ET 接\u{6536}异" + viewWriteMaximError + "cmdName:") + "\(cmdName)" + (String(widgetSampleError) + String(k_rushAlert.prefix(2))) + "\(errorNo)" + (String(componentSparkPage.suffix(6))) + "\(msg)" + (layoutMannerId.capitalized + String(sessionUnderMessage.suffix(5))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: PositionThen.share.loginUserMode.freeCallTimes = 0
                PositionThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !PositionThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !PositionThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                SensitiveWindowManager.shared.sleeve()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.noWall(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(moduleChampionPath) + displayAudiencePreference.replacingOccurrences(of: "panel", with: "v") + "Gift") { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: componentPurchaseMessage, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(styleChannelHelper) + String(componentApologySession.prefix(5))) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.originTake(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(commonBetweenGuideParkPreference.prefix(4)) + String(layoutCommonTimer.prefix(9))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.requestData(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(layoutHelloPreference) + String(commonPowData)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(displayBorderValue) + String(coreWarnUtility.suffix(5))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(layoutHumanPath) + String(componentDismissError.prefix(5))) { // 接收方发送acceptCall回执
            //: self.isFamous = true
            self.isFamous = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            sprint()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.labelCommit(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(sessionLitUtility.suffix(5)) + "pCall") { // 接收方发送stopCall回执
            //: self.isFamous = false
            self.isFamous = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            generatePoint()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(colorParticleEffectLogger) + String(componentEyeAlert.prefix(5)))] as? Int {
                //: PositionThen.share.loginUserMode.freeCallTimes = freeCallTimes
                PositionThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: PositionThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PositionThen.share.loginUserMode.mf_coin = json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(String(serviceCurDiskPage))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            hemInmate(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: sessionOfPreference, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SensitiveFamousApplication.shared as! SensitiveFamousApplication).scar()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(serviceFiveMetrics) + k_videoProofPath.replacingOccurrences(of: "lite", with: "n")) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.conversionChat(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(colorGoHelper) + appFirmText.replacingOccurrences(of: "ally", with: "l")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isFamous = true
            self.isFamous = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            sprint()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.sound(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(sessionThematicBestFormat) + String(themeReplacementGatorActorLogger.prefix(7))) { // 接收方/拨打方 挂断了通话
            //: self.isFamous = false
            self.isFamous = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            generatePoint()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.banIcon(data: data)
            // 更新余额
            //: PositionThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PositionThen.share.loginUserMode.mf_coin = json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(String(serviceCurDiskPage))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(colorParticleEffectLogger) + String(componentEyeAlert.prefix(5)))] as? Int {
                //: PositionThen.share.loginUserMode.freeCallTimes = freeCallTimes
                PositionThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            hemInmate(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: sessionOfPreference, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SensitiveFamousApplication.shared as! SensitiveFamousApplication).scar()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (kPhaseId.replacingOccurrences(of: "step", with: "e") + "tCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: appScaleSession, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: PositionThen.share.recieve1v1TalkRequest(info: data)
                PositionThen.share.shack(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(viewHugeSettings) + String(commonMaxThresholdHelper))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: serviceAttributeFormat.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(coreMonthTheoryEvent.replacingOccurrences(of: "universal", with: "t"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: spacingDepthName.map{magnitudeCoalCell(shrink: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                FeatureReactiveCompatible.commit(uid: String(json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(String(appAbleLocalError))].intValue),
                                         //: title: data["nickname"] as? String,
                                         title: data[(String(k_agentFormat) + String(kValidSystem))] as? String,
                                         //: body: body,
                                         body: body,
                                         //: imageUrl: data["headPic"] as? String)
                                         imageUrl: data[(String(commonResignMetrics))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                FeatureReactiveCompatible.shared.everyYearBgm()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(spacingSweepPickMessage.suffix(4)) + "llRes" + String(sessionItMetrics)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isFamous = true
            self.isFamous = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            sprint()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(spacingSweepPickMessage.suffix(4)) + "llRes" + String(sessionItMetrics))] = "1"
            //: PositionThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            PositionThen.share.emotion(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (SensitiveFamousApplication.shared as! SensitiveFamousApplication).aReduce()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (colorAcrossText.lowercased() + String(themePointPlatform.suffix(4))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isFamous = false
            self.isFamous = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            generatePoint()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.raw(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                FeatureReactiveCompatible.skip(identifier: String(json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(String(appAbleLocalError))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                FeatureReactiveCompatible.shared.everyCurrent()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (appOpenError.replacingOccurrences(of: "old", with: "re") + String(colorHuntContent)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isFamous = false
            self.isFamous = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            generatePoint()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.club(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(k_duckAlwaysTimer.prefix(5)) + "lMot" + String(componentLimitId)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if PositionThen.share.loginUserMode.sex == Gender.female.rawValue {
            if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.checkChat?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(layoutPathSession) + featureCleanConfig.replacingOccurrences(of: "hello", with: "n")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            InstrumentalityReactiveCompatible.whyEnter(content: msg, reason: json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(String(moduleChaseError))].stringValue, time: json[(componentHelpName.replacingOccurrences(of: "media", with: "a"))][(String(layoutPickSaveerName))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(widgetEndPlatform.suffix(7)) + spacingStrikeTitle.replacingOccurrences(of: "server", with: "ve")) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            InstrumentalityReactiveCompatible.aftermath().personalIdentity()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(sessionStrokeText)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.quantityerproposal(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(componentHowMessage) + String(spacingAlliancePage)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(moduleClothesPage.suffix(6)) + "tatus")] as? Int ?? -1
            //: if PositionThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if PositionThen.share.appStatus == BetterQuantity.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: spacingLogMetrics, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: componentPurchaseMessage, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: spacingLogMetrics, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(widgetAwardDomainUrl) + k_interestedValue.replacingOccurrences(of: "begin", with: "g")) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            EveryThen.trick().frame(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(styleFoundFormat.suffix(5)) + "ifica" + String(widgetYellowValue.suffix(5)) + "opup" + String(spacingTellUtility)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            SensitiveWindowManager.shared.detectDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(spacingBeatName)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            InstrumentalityReactiveCompatible.aftermath().personalIdentity()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: serviceSaltUtility, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension TopThen {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func title(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (kPhaseId.replacingOccurrences(of: "step", with: "e") + "tCall") || cmdName == (String(commonBetweenGuideParkPreference.prefix(4)) + String(layoutCommonTimer.prefix(9))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if InstrumentalityReactiveCompatible.aftermath().isLive == true {
                //: NEED_RESTORE_LIVE = true
                screenInputValue = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                InstrumentalityReactiveCompatible.indicatorMax(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                InstrumentalityReactiveCompatible.aftermath().personalIdentity()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard screenInputValue == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (colorAcrossText.lowercased() + String(themePointPlatform.suffix(4))) || cmdName == (String(layoutHelloPreference) + String(commonPowData)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (appOpenError.replacingOccurrences(of: "old", with: "re") + String(colorHuntContent)) || cmdName == (String(displayBorderValue) + String(coreWarnUtility.suffix(5))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(sessionThematicBestFormat) + String(themeReplacementGatorActorLogger.prefix(7))) || cmdName == (String(sessionLitUtility.suffix(5)) + "pCall")
        {
            //: NEED_RESTORE_LIVE = false
            screenInputValue = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = HiReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                InstrumentalityReactiveCompatible.aftermath().numericalQuantity()
            }
            //: alert.show()
            alert.progress()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func hemInmate(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        NothingRatingManager.shared.sequenceOff()
        //: guard PositionThen.share.loginUserMode.sex == Gender.male.rawValue else {
        guard PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(spacingKitEvent.prefix(6)) + String(styleAroundEachHourContent))] as? Bool == false {
            //: TalkingAppPushManager.share.func_pushToDiscountRechargeView()
            ProdPushManager.share.panelView()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(k_femaleMutualPreference.replacingOccurrences(of: "luck", with: "so"))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(sessionWelcomeUtility.suffix(7)) + String(colorProMessage)) || source == (layoutMidSendEvent.replacingOccurrences(of: "layer", with: "i") + String(serviceHelpId)) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(spacingAssessmentSnapError.replacingOccurrences(of: "administrative", with: "oi"))] as? Int) ?? 0 < 60 {
            //: TalkingAppPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            ProdPushManager.share.funcHarshMakeup(webViewType: .RechargeHalfPage)
        }
    }
}
