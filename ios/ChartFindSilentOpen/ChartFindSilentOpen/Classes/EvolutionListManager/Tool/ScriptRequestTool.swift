
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let styleResolutionValue:String = "selected lust oval laboratorymf/bu"
fileprivate let displayInvestigationTimer:String = "ss/giffuture job"

/*: "type" :*/
fileprivate let viewChartUrl:[UInt8] = [0xf5,0xfa,0xf1,0xe6]

fileprivate func possibleInfo(come num: UInt8) -> UInt8 {
    let value = Int(num) + 127
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let serviceRatingHelper:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let styleZzUtility:[Character] = ["o","m","/","g"]
fileprivate let componentSeparationText:String = "iftListpurchase channel increase"

/*: "live/giftList" :*/
fileprivate let kBirthdayClearId:[Character] = ["l"]
fileprivate let themeTickMessage:String = "dark three picive/gi"

/*: "IM/getMessageList" :*/
fileprivate let styleKindFormat:String = "IM/gcollection color sibling"
fileprivate let widgetSlowHelper:[Character] = ["s"]
fileprivate let widgetChainConfig:[Character] = ["s","a","g","e","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let featureGuidelineLogger:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e"]
fileprivate let kGutUrl:[Character] = ["n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let sessionMeanwhileMetrics:[Character] = ["m","f","/","c","h","a"]
fileprivate let screenBlindDevice:[Character] = ["t","R","o","o","m","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let displayFadeItsPopulationEvent:String = "thirty yellow successfully oration verymf/bu"
fileprivate let viewRootPath:String = "jus origin loads/sen"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let kSlowSinceProcessTimer:String = "mf/chatsystem eh thirty"
fileprivate let servicePooMetrics:String = "channel maker weeRoom/s"

/*: "live/sendGift" :*/
fileprivate let widgetCircleReaderAlert:String = "live/seround more validity"
fileprivate let sessionListRunningDevice:[Character] = ["n","d","G","i","f","t"]

/*: "mf/user/getChatInfo" :*/
fileprivate let kBoyName:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","C","h","a","t"]
fileprivate let displayPopSystem:String = "include behind aspect validInfo"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let commonTownNoteSystem:String = "IM/getrem special"
fileprivate let displayNameData:String = "ageUserIndate fit stat analyze"
fileprivate let colorInstallLogger:String = "foListskull special influent perform"

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let colorProcedureTitle:String = "standard"
fileprivate let widgetVaryOneDismissError:String = "f/crsource theoretic bullet others"
fileprivate let widgetIllegalName:String = "hoLireplace body"

/*: "mf/crush/ignore" :*/
fileprivate let spacingNowMaximumFormat:[Character] = ["m","f","/","c","r","u","s","h","/","i","g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let layoutMemberMessage:[Character] = ["m","f","/","c","r","u","s","h","/"]
fileprivate let kUnitAlert:String = "fragmentike"

/*: "mf/user/getRelation" :*/
fileprivate let spacingEarlySettings:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","R","e","l","a","t","i","o"]
fileprivate let styleLineData:String = "N"

/*: "user/attention" :*/
fileprivate let kBlinkPage:[UInt8] = [0x25,0x23,0x35,0x22,0x7f,0x31,0x24,0x24,0x35,0x3e,0x24,0x39,0x3f,0x3e]

private func containerBold(missing num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "user/removeAttention" :*/
fileprivate let screenClearlyComplyAlert:String = "collection youuser/re"
fileprivate let commonMotionSystem:String = "recover offmoveA"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let spacingScriptHundredCommercialPlatform:[UInt8] = [0xea,0xc8,0xc7,0x8e,0xdd,0x89,0xcc,0xc8,0xdb,0xc7,0x89,0xd9,0xc6,0xc0,0xc7,0xdd,0xda,0x89,0xc0,0xcf,0x89,0xd0,0xc6,0xdc,0x89,0xcf,0xc6,0xc5,0xc5,0xc6,0xde,0x89,0xcc,0xc8,0xca,0xc1,0x89,0xc6,0xdd,0xc1,0xcc,0xdb,0xd7]

private func metalLeave(subject num: UInt8) -> UInt8 {
    return num ^ 169
}

/*: "follow Success" :*/
fileprivate let commonOriginallyDevice:[Character] = ["f","o","l","l","o","w"," "]
fileprivate let displaySearchRevenueProblemMetrics:[Character] = ["S","u"]
fileprivate let themeUniversalOrientSettings:[Character] = ["c","c","e","s","s"]

/*: "unfollow Success" :*/
fileprivate let colorViolationUrl:[Character] = ["u","n","f","o","l","l","o","w"," ","S","u","c","c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let serviceFoundTimer:[Character] = ["t","r","a","n","s"]
fileprivate let featureCoordinatorUtility:[Character] = ["l","a","t"]
fileprivate let viewShouldTimer:[Character] = ["e","/","m","s","g"]

/*: "msgId" :*/
fileprivate let featureScreenHelper:[UInt8] = [0xfb,0xe5,0xf1,0xdf,0xf2]

private func depthCivic(ok num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "targetLng" :*/
fileprivate let sessionMakerUtility:[UInt8] = [0x1,0x14,0x7,0x12,0x10,0x1,0x39,0x1b,0x12]

private func agencyHurry(process num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let viewRemarkLogger:String = "mf/greclick sample meat visual"
fileprivate let sessionAnotherCourseConfig:String = "inmate pot externaltAut"
fileprivate let serviceDuckData:String = "primary road remain spirit alletCon"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let themeReFormat:String = "color fallmf/gr"
fileprivate let screenResumePage:String = "left space lifetime remove joingetIn"
fileprivate let viewBeefSystem:[Character] = ["t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let coreOutputTimer:[UInt8] = [0x9,0x12,0x28,0x14,0x19]

private func insertVerbally(one num: UInt8) -> UInt8 {
    return num ^ 125
}

/*: "mf/chatRoom/info" :*/
fileprivate let viewLittleAwardDevice:String = "mf/chcalculate platform tap explore along"
fileprivate let featureMinGuideNotePreference:[Character] = ["/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let styleMatterGroupLogger:String = "mf/chrailroad track hidden maker"
fileprivate let componentLocalFormat:String = "him sale bind entity lockm/lis"
fileprivate let moduleWithPreference:String = "say"

/*: "roomId" :*/
fileprivate let screenFullSumId:[UInt8] = [0x64,0x49,0x6d,0x6f,0x6f,0x72]

/*: "needExtraInfo" :*/
fileprivate let k_displayUtterUrl:[UInt8] = [0x64,0x6f,0x6f,0x6e,0x4f,0x72,0x7e,0x78,0x6b,0x43,0x64,0x6c,0x65]

private func commonSharesForce(ad num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "mf/chatRoom/check" :*/
fileprivate let spacingTheoreticMetrics:String = "source nth rollmf/c"
fileprivate let widgetBeId:String = "oom/cadjustment location reference"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let spacingSelectPage:[Character] = ["m","f","/","c","h","a"]
fileprivate let sessionBeingPath:String = "tRoom/jstrike butterfly series"
fileprivate let viewStackKey:[Character] = ["o","i","n","O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let styleStockHelper:String = "mf/chareply dominant"
fileprivate let serviceVidPreference:[Character] = ["/","m","e","m","b","e","r","s"]

/*: "translate/text" :*/
fileprivate let screenViolationKey:String = "trmyns"
fileprivate let spacingHarshHelper:String = "relation edit jus ready hellolate/"

/*: "text" :*/
fileprivate let sessionRecoverObserverData:[UInt8] = [0xc3,0xd2,0xcf,0xc3]

private func jobDisk(upon num: UInt8) -> UInt8 {
    return num ^ 183
}

/*: "mf/user/queryStatus" :*/
fileprivate let spacingBasicTimer:String = "decrease workmf/u"
fileprivate let sessionOutdoName:String = "eryStatidentify regular"
fileprivate let sessionShallRobotAlert:[Character] = ["u","s"]

/*: "toUids" :*/
fileprivate let kContrastPlatform:[UInt8] = [0xbe,0xb9,0x9f,0xb3,0xae,0xbd]

fileprivate func viewEquivalent(argument num: UInt8) -> UInt8 {
    let value = Int(num) - 74
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "call/getCallTab" :*/
fileprivate let viewOpError:String = "status king earn platformcall/ge"
fileprivate let displayMagnitudeError:String = "past recommend protocoltC"
fileprivate let kWoodCutMetrics:String = "allTabcenter brake recording"

/*: "mf/user/remark" :*/
fileprivate let serviceCustomValue:String = "mf/usartifact feedback identity ta bird"
fileprivate let colorPremiumId:[Character] = ["e","r","/","r","e","m","a","r","k"]

/*: "content" :*/
fileprivate let layoutHealthyPage:[UInt8] = [0x5f,0x53,0x52,0x48,0x59,0x52,0x48]

private func purchaseShake(comment num: UInt8) -> UInt8 {
    return num ^ 60
}

/*: "top" :*/
fileprivate let componentNorText:[UInt8] = [0xf7,0xec,0xf3]

private func nurseChange(me num: UInt8) -> UInt8 {
    return num ^ 131
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ScriptRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class ScriptRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [SubstanceHandyJSON]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [SubstanceHandyJSON]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func pulpit(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(styleResolutionValue.suffix(5)) + "sine" + String(displayInvestigationTimer.prefix(6)) + "tList")
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: viewChartUrl.map{possibleInfo(come: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func outputCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(serviceRatingHelper) + String(styleZzUtility) + String(componentSeparationText.prefix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func freebee(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(kBirthdayClearId) + String(themeTickMessage.suffix(6)) + "ftList")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func listWith(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(styleKindFormat.prefix(4)) + "etMe" + String(widgetSlowHelper) + String(widgetChainConfig))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func visitorBy(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(featureGuidelineLogger) + String(kGutUrl))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func param(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(sessionMeanwhileMetrics) + String(screenBlindDevice))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func afterGift(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(displayFadeItsPopulationEvent.suffix(5)) + "sines" + String(viewRootPath.suffix(5)) + "dGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func purebred(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(kSlowSinceProcessTimer.prefix(7)) + String(servicePooMetrics.suffix(6)) + "endGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func precocious(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(widgetCircleReaderAlert.prefix(7)) + String(sessionListRunningDevice))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func floatingBridgeCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(kBoyName) + String(displayPopSystem.suffix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func medalSearch(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(commonTownNoteSystem.prefix(6)) + "Mess" + String(displayNameData.prefix(9)) + String(colorInstallLogger.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<SubstanceHandyJSON>.deserialize(from: result as? Array) {
            if let datas = Array<SubstanceHandyJSON>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [SubstanceHandyJSON])
                completion(succeed, datas as! [SubstanceHandyJSON])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func blockGift(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.extraCompletion(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func academeWellBeingQuasiNgoWithinLayer(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (colorProcedureTitle.replacingOccurrences(of: "standard", with: "m") + String(widgetVaryOneDismissError.prefix(4)) + "ush/w" + String(widgetIllegalName.prefix(4)) + "keMeList")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func fulfil(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(spacingNowMaximumFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func alongDirect(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(layoutMemberMessage) + kUnitAlert.replacingOccurrences(of: "fragment", with: "l"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func shine(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(spacingEarlySettings) + styleLineData.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func pickOutCompletion(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: kBlinkPage.map{containerBold(missing: $0)}, encoding: .utf8)! : (String(screenClearlyComplyAlert.suffix(7)) + String(commonMotionSystem.suffix(5)) + "ttention")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if PositionThen.share.loginUserMode.sex == Gender.female.rawValue {
                    if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        moduleProfileUrl.quantityerproposal(showMsg: String(bytes: spacingScriptHundredCommercialPlatform.map{metalLeave(subject: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        moduleProfileUrl.quantityerproposal(showMsg: (String(commonOriginallyDevice) + String(displaySearchRevenueProblemMetrics) + String(themeUniversalOrientSettings)).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    moduleProfileUrl.quantityerproposal(showMsg: (String(colorViolationUrl)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                moduleProfileUrl.colouration(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func enableSesame(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (String(serviceFoundTimer) + String(featureCoordinatorUtility) + String(viewShouldTimer))
        //: reqModel.params = ["msgId": msgId, "targetLng": FragThen.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: featureScreenHelper.map{depthCivic(ok: $0)}, encoding: .utf8)!: msgId, String(bytes: sessionMakerUtility.map{agencyHurry(process: $0)}, encoding: .utf8)!: FragThen.decideRevenue(), String(bytes: viewChartUrl.map{possibleInfo(come: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func upCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(viewRemarkLogger.prefix(6)) + "et/ge" + String(sessionAnotherCourseConfig.suffix(4)) + "oGre" + String(serviceDuckData.suffix(5)) + "tent")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func belowPressOut(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(themeReFormat.suffix(5)) + "eet/" + String(screenResumePage.suffix(5)) + String(viewBeefSystem))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: coreOutputTimer.map{insertVerbally(one: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func everyLand(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(viewLittleAwardDevice.prefix(5)) + "atRoom" + String(featureMinGuideNotePreference))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func appose(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(styleMatterGroupLogger.prefix(5)) + "atRoo" + String(componentLocalFormat.suffix(5)) + moduleWithPreference.replacingOccurrences(of: "say", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func markEnableCompletion(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: screenFullSumId.reversed(), encoding: .utf8)!: roomId, String(bytes: k_displayUtterUrl.map{commonSharesForce(ad: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(spacingTheoreticMetrics.suffix(4)) + "hatR" + String(widgetBeId.prefix(5)) + "heck")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func original(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(spacingSelectPage) + String(sessionBeingPath.prefix(7)) + String(viewStackKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func expressCurrencyCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(styleStockHelper.prefix(6)) + "tRoom" + String(serviceVidPreference))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func submitOperate(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (screenViolationKey.replacingOccurrences(of: "my", with: "a") + String(spacingHarshHelper.suffix(5)) + "text")
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: sessionRecoverObserverData.map{jobDisk(upon: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func heart(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(spacingBasicTimer.suffix(4)) + "ser/qu" + String(sessionOutdoName.prefix(7)) + String(sessionShallRobotAlert))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: kContrastPlatform.map{viewEquivalent(argument: $0)}, encoding: .utf8)!: toUids, String(bytes: viewChartUrl.map{possibleInfo(come: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func renameTransform(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(viewOpError.suffix(7)) + String(displayMagnitudeError.suffix(2)) + String(kWoodCutMetrics.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func ejaculator(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(serviceCustomValue.prefix(5)) + String(colorPremiumId))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: layoutHealthyPage.map{purchaseShake(comment: $0)}, encoding: .utf8)!: content, String(bytes: componentNorText.map{nurseChange(me: $0)}, encoding: .utf8)!: isTop, String(bytes: coreOutputTimer.map{insertVerbally(one: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
