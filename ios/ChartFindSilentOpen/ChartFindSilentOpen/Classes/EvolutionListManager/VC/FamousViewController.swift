
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let colorHeadSystem:String = "brush mutual imagebg_m"
fileprivate let styleMomentumContent:String = "giving distance magnetic begin at_top"

/*: "777777" :*/
fileprivate let layoutApplyTimer:[Character] = ["7","7","7","7","7","7"]

/*: "Messages" :*/
fileprivate let spacingEnterpriseKey:[Character] = ["M","e","s","s","a","g","e","s"]

/*: "Who like me" :*/
fileprivate let componentParticleName:String = "topic"
fileprivate let screenKeepSettings:String = "license imaginationho l"

/*: "Call" :*/
fileprivate let viewThemPage:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let layoutCareMessage:[Character] = ["#","F","F","2","3","4","8"]

/*: "Current network unavailable" :*/
fileprivate let colorExData:String = "Currebarrel into"
fileprivate let themeArtifactId:String = "nt netsubject finish aid"
fileprivate let layoutSoonError:[Character] = ["w","o","r","k"," ","u","n","a","v","a","i","l","a","b","l","e"]

/*: "icon_yidu_pre" :*/
fileprivate let featureSchoolKey:String = "icon_yidbelow response incorporate document"
fileprivate let k_qualityMilitaryValue:String = "prevention first bowu_pre"

/*: "transform.rotation" :*/
fileprivate let sessionRemPage:[Character] = ["t","r","a","n","s","f","o","r","m",".","r","o","t","a","t","i"]
fileprivate let displayTapSettings:String = "contribution"

/*: "transform.scale" :*/
fileprivate let featureGivenAlert:String = "TRANSFO"
fileprivate let serviceExerciseText:String = "rm.scaleon bubble"

/*: "zoom&shake" :*/
fileprivate let colorOpPoPlatform:[Character] = ["z","o","o","m","&","s","h","a"]
fileprivate let featureSeparationPlatform:[Character] = ["k","e"]

/*: "yyyy-MM-dd" :*/
fileprivate let coreUsConfig:String = "yyyy-sub publish res"
fileprivate let screenBodyTimer:String = "shared with enable percentage likeMM-dd"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let moduleSelectKey:[UInt8] = [0xd,0x20,0x20,0x23,0x3b,0x6c,0x69,0xc,0x6c,0x38,0x23,0x6c,0x3f,0x29,0x22,0x28,0x6c,0x35,0x23,0x39,0x6c,0x22,0x23,0x38,0x25,0x2a,0x25,0x2f,0x2d,0x38,0x25,0x23,0x22,0x3f,0x73]

private func topBilling(stuff num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "Cancel" :*/
fileprivate let colorCityTitle:String = "processor"
fileprivate let widgetNoonLogger:String = "antonel"

/*: "Settings" :*/
fileprivate let viewRewardJointLogger:String = "analyze argumentSettings"

/*: "badNumber" :*/
fileprivate let serviceCustomerKey:[Character] = ["b","a","d","N"]
fileprivate let appScriptKey:String = "umbegive"

/*: "isConnection" :*/
fileprivate let moduleBetterKey:[Character] = ["i","s","C","o","n","n","e"]
fileprivate let styleExpressionUtility:[Character] = ["c","t","i","o","n"]

/*: "networkStatus" :*/
fileprivate let commonWorldStatDismissAlert:String = "pressuretwor"
fileprivate let coreApproveEvent:[Character] = ["k"]
fileprivate let sessionTheoreticalUtility:[Character] = ["S","t","a","t","u","s"]

/*: "unreadMessageNum" :*/
fileprivate let serviceProgressUrl:[Character] = ["u","n","r","e","a","d","M","e","s","s","a","g","e","N","u"]
fileprivate let widgetFailError:String = "M"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let styleFacilityDevice:[UInt8] = [0xb7,0xe2,0x93,0xec,0xe2,0xe8,0x93,0xea,0xd4,0xe1,0xe7,0x93,0xe7,0xe2,0x93,0xe0,0xd4,0xe5,0xde,0x93,0xd4,0xdf,0xdf,0x93,0xe0,0xd8,0xe6,0xe6,0xd4,0xda,0xd8,0xe6,0x93,0xd4,0xe6,0x93,0xe5,0xd8,0xd4,0xd7,0xb2]

fileprivate func judgeLack(street num: UInt8) -> UInt8 {
    let value = Int(num) + 141
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let widgetBowLogger:[Character] = ["消","息","列","\u{8868}","\u{4e00}","键","\u{5df2}","读"]
fileprivate let featureEstimatedText:[Character] = ["\u{5931}","败","：","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let styleRegularEvent:[Character] = [","," ","d","e","s","c",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FamousViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class FamousViewController: GroupThen {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        createer()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        untilActualUi()
        //: bindInteraction()
        cohere()
        //: func__turnOnSystemNotification()
        affiliate()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: 100 + sessionTableUrl))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.titled(name: (String(colorHeadSystem.suffix(4)) + "essage" + String(styleMomentumContent.suffix(4))))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: PreferenceTipView = {
        //: let V = TalkingNoticeTipView()
        let V = PreferenceTipView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: EnhanceAcidViewDelegate = {
        //: let vc = QYSlideNavigationViewController()
        let vc = EnhanceAcidViewDelegate()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .modelSize(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .modelSize(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (String(layoutApplyTimer)))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.restoreAudience()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [GroupThen] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, StochasticProcessRecognizerDelegate()]
        //: if PositionThen.share.loginUserMode.callTabSwitch == 1 {
        if PositionThen.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(PortraitViewController(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: OpportunityViewDelegate = {
        //: let vc = TalkingChatListViewController()
        let vc = OpportunityViewDelegate()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(spacingEnterpriseKey)).localized, (componentParticleName.replacingOccurrences(of: "topic", with: "W") + String(screenKeepSettings.suffix(4)) + "ike me").localized]
        //: if PositionThen.share.loginUserMode.callTabSwitch == 1 {
        if PositionThen.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(viewThemPage)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: sessionTableUrl, width: moduleAdjustPath, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.attachSize(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(layoutCareMessage)))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(colorExData.prefix(5)) + String(themeArtifactId.prefix(6)) + String(layoutSoonError)).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: BetterView = {
        //: let numbLabel = BadgeLab()
        let numbLabel = BetterView()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: BetterView = {
        //: let numbLabel = BadgeLab()
        let numbLabel = BetterView()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(featureSchoolKey.prefix(8)) + String(k_qualityMilitaryValue.suffix(5)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.titled(name: (String(featureSchoolKey.prefix(8)) + String(k_qualityMilitaryValue.suffix(5)))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - SparkNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension FamousViewController: SparkNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func lock(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: OpportunityViewDelegate.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            createer()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: StochasticProcessRecognizerDelegate.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            themeSincePlatform.givingStream(eventID: sessionSoundValue)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension FamousViewController {
    //: func setIsTopAll() {
    func caput() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? OpportunityViewDelegate
            //: vc?.resetToTopItemView()
            vc?.itemBind()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.anonymousSwitcher(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func partReward() {
        //: if PositionThen.share.networkStatus != .Unavailable && GroupConversationListener.shared.isConnection {
        if PositionThen.share.networkStatus != .Unavailable, GroupConversationListener.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func ballBuster(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func createer() {
        //: let unreadMsgCount = PositionThen.share.unreadMessageNum
        let unreadMsgCount = PositionThen.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: servicePriceAppLogger) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.medalAcross()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func medalAcross() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(sessionRemPage) + displayTapSettings.replacingOccurrences(of: "contribution", with: "on")))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (featureGivenAlert.lowercased() + String(serviceExerciseText.prefix(8))))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(colorOpPoPlatform) + String(featureSeparationPlatform)))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func affiliate() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        MagnitudeReactiveCompatible.tillEarly { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.smartTip(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.premiumFormat(date: Date(), dateFormat: (String(coreUsConfig.prefix(5)) + String(screenBodyTimer.suffix(5))))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = screenOutputConfig.string(forKey: spacingBarPlatform), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.smartTip(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.smartTip(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = screenOutputConfig.bool(forKey: themeTagText)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        screenOutputConfig.set(true, forKey: themeTagText)
                        //: TalkingAlertShow.alert(title: nil,
                        ThereAlertShow.tillLaunch(title: nil,
                                                    //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                    message: String(bytes: moduleSelectKey.map{topBilling(stuff: $0)}, encoding: .utf8)!.meanwhile(commonAddName),
                                                    //: leftBtnTitle: "Cancel".localized,
                                                    leftBtnTitle: (colorCityTitle.replacingOccurrences(of: "processor", with: "C") + widgetNoonLogger.replacingOccurrences(of: "tone", with: "ce")).localized,
                                                    //: rightBtnTitle: "Settings".localized) {
                                                    rightBtnTitle: (String(viewRewardJointLogger.suffix(8))).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            ThereAlertShow.outsideGold()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func smartTip(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(appSceneUrl)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = componentMeEvent - appSceneUrl - coreTitlePlatform
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(appSceneUrl + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = componentMeEvent - self.noticeView.bottom - coreTitlePlatform
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension FamousViewController {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func strain(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(serviceCustomerKey) + appScriptKey.replacingOccurrences(of: "give", with: "r"))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension FamousViewController {
    /// UI
    //: private func createUI() {
    private func untilActualUi() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(appSceneUrl)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (componentParticleName.replacingOccurrences(of: "topic", with: "W") + String(screenKeepSettings.suffix(4)) + "ike me").localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(spacingEnterpriseKey)).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func cohere() {
        //: GroupConversationListener.shared.rx
        GroupConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(moduleBetterKey) + String(styleExpressionUtility)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.partReward()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: PositionThen.share.rx.observeWeakly(Int.self, "networkStatus")
        PositionThen.share.rx.observeWeakly(Int.self, (commonWorldStatDismissAlert.replacingOccurrences(of: "pressure", with: "ne") + String(coreApproveEvent) + String(sessionTheoreticalUtility)))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.partReward()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: PositionThen.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        PositionThen.share.rx.observeWeakly(Int.self, (String(serviceProgressUrl) + widgetFailError.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.ballBuster(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = FeatureAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                ThereAlertShow.regarding(message: String(bytes: styleFacilityDevice.map{judgeLack(street: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (colorCityTitle.replacingOccurrences(of: "processor", with: "C") + widgetNoonLogger.replacingOccurrences(of: "tone", with: "ce")).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    ThereAlertShow.outsideGold()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: servicePriceAppLogger)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        CheckThen.placeLoad(msg: (String(widgetBowLogger) + String(featureEstimatedText)) + "\(code)" + (String(styleRegularEvent)) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(strain(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: moduleRequestNearbyPage,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.premiumFormat(date: Date(), dateFormat: (String(coreUsConfig.prefix(5)) + String(screenBodyTimer.suffix(5))))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            screenOutputConfig.set(today, forKey: spacingBarPlatform)
            //: self.func__hideNotificationTipView(hide: true)
            self.smartTip(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.affiliate()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
