
//: Declare String Begin

/*: "LIVE_NEED_OPEN_NOTIFICATION" :*/
fileprivate let themeMakeNaturePath:[Character] = ["L","I","V","E","_","N","E","E","D","_","O","P","E","N","_"]
fileprivate let featureDefineExpressionPlatform:String = "NdeviceIFI"

/*: "LIVE_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let layoutDonConfig:[UInt8] = [0x7,0x2,0x1d,0xe,0x14,0x18,0x1f,0xa,0x1f,0x1e,0x18,0x14,0x8,0x3,0xa,0x5,0xc,0xe,0x14,0x5,0x4,0x1f,0x2,0xd,0x2,0x8,0xa,0x1f,0x2,0x4,0x5]

private func goodEffect(capacity num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "live/startCheck" :*/
fileprivate let appPeopleIndexLogger:String = "live/snap situation lower characteristic"
fileprivate let spacingNowSettings:String = "startCmale team simultaneously"

/*: "live/start" :*/
fileprivate let spacingAddressPlatform:String = "pattern approach actionlive/s"
fileprivate let screenBarMetrics:String = "TART"

/*: "live/stop" :*/
fileprivate let sessionNothingMicData:[Character] = ["l","i","v","e","/","s"]
fileprivate let displayAssertFormat:String = "TOP"

/*: "type" :*/
fileprivate let screenGoldQuitPath:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "mf/user/dataCard" :*/
fileprivate let k_vidMessage:String = "terrible channel sale dogmf/us"
fileprivate let colorCoordinateMetrics:String = "ataCardhidden char gold"

/*: "uid" :*/
fileprivate let spacingGirlData:[UInt8] = [0xb,0x17,0x1a]

private func weeWhether(choice num: UInt8) -> UInt8 {
    return num ^ 126
}

/*: "streamerUid" :*/
fileprivate let widgetSpecLogger:[UInt8] = [0xad,0xaa,0xac,0xbb,0xbf,0xb3,0xbb,0xac,0x8b,0xb7,0xba]

/*: "roomId" :*/
fileprivate let viewDecreaseKey:[Character] = ["r"]
fileprivate let sessionApologyConfig:String = "calculate auto rue invite rimeoomId"

/*: "Try again later, if you have any questions please contact customer service" :*/
fileprivate let componentRemindQuotePath:[UInt8] = [0x16,0x30,0x3b,0x62,0x23,0x25,0x23,0x2b,0x2c,0x62,0x2e,0x23,0x36,0x27,0x30,0x6e,0x62,0x2b,0x24,0x62,0x3b,0x2d,0x37,0x62,0x2a,0x23,0x34,0x27,0x62,0x23,0x2c,0x3b,0x62,0x33,0x37,0x27,0x31,0x36,0x2b,0x2d,0x2c,0x31,0x62,0x32,0x2e,0x27,0x23,0x31,0x27,0x62,0x21,0x2d,0x2c,0x36,0x23,0x21,0x36,0x62,0x21,0x37,0x31,0x36,0x2d,0x2f,0x27,0x30,0x62,0x31,0x27,0x30,0x34,0x2b,0x21,0x27]

/*: "redirectUrl" :*/
fileprivate let k_sSettings:[Character] = ["r","e","d","i","r","e","c","t"]
fileprivate let componentProductIslandConfig:[Character] = ["U","r","l"]

/*: "livePushUrl" :*/
fileprivate let spacingSchoolFileSettings:[Character] = ["l","i","v"]
fileprivate let displayAgainstKey:[Character] = ["e","P","u","s","h","U","r","l"]

/*: "FaceActionEvent_Interval_1_second" :*/
fileprivate let coreMinimizeAspectPath:[UInt8] = [0xe0,0xc7,0xc5,0xc3,0xe7,0xc5,0xd2,0xcf,0xc9,0xc8,0xe3,0xd0,0xc3,0xc8,0xd2,0xf9,0xef,0xc8,0xd2,0xc3,0xd4,0xd0,0xc7,0xca,0xf9,0x97,0xf9,0xd5,0xc3,0xc5,0xc9,0xc8,0xc2]

private func unitAchievement(bridge num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "hasFace" :*/
fileprivate let sessionSceneLogger:[Character] = ["h","a","s","F","a","c","e"]

/*: "Opening failed" :*/
fileprivate let kCurveConfig:String = "properly volume direction approveOpeni"
fileprivate let screenWhatSplitTimer:String = "aenableled"

/*: "LIVE 状态码： :*/
fileprivate let k_businessSystem:[Character] = ["L","I","V","E"," ","\u{72b6}","\u{6001}","码"]
fileprivate let colorCityError:String = "："

/*: "Network busy!" :*/
fileprivate let spacingOriginallyName:String = "history meanwhile ilk cleanNetwo"
fileprivate let kPianoMessage:[Character] = ["r","k"," ","b","u","s","y","!"]

/*: "LIVE 异常下播，evtID: :*/
fileprivate let kSampleUrl:String = "ample talk choose expectedLIVE 异常"
fileprivate let displayButSystem:[Character] = ["\u{4e0b}","\u{64ad}","，"]
fileprivate let viewAdviceTitle:[Character] = ["e","v","t","I","D",":"]

/*: "Live room exception" :*/
fileprivate let moduleSmallDevice:[Character] = ["L","i","v","e"," ","r","o","o","m"]
fileprivate let serviceAverWaitAlert:String = "security star exce"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InstrumentalityReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2023/6/15.
//

//: import TXLiteAVSDK_Professional
import TXLiteAVSDK_Professional
//: import UIKit
import UIKit

// 是否需要恢复直播
//: public var NEED_RESTORE_LIVE = false
public var screenInputValue = false
// 唤起直播功能
//: public let LIVE_NEED_OPEN_NOTIFICATION = NSNotification.Name(rawValue: "LIVE_NEED_OPEN_NOTIFICATION")
public let sessionAttributePlatform = NSNotification.Name(rawValue: (String(themeMakeNaturePath) + featureDefineExpressionPlatform.replacingOccurrences(of: "device", with: "OT") + "CATION"))
// 直播状态变更通知
//: public let LIVE_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "LIVE_STATUS_CHANGE_NOTIFICATION")
public let kSucceedCancelLogger = NSNotification.Name(rawValue: String(bytes: layoutDonConfig.map{goodEffect(capacity: $0)}, encoding: .utf8)!)

/// 直播渲染配置
//: class TalkingLiveConfig: NSObject {
class CreatorReactiveCompatible: NSObject {
    //: var livePushUrl = ""                 // 推流地址
    var livePushUrl = "" // 推流地址
    //: var isSmallMode = false
    var isSmallMode = false // 是否小窗口
    //: var isMute = false
    var isMute = false // 是否静音
    //: var isRequest = false
    var isRequest = false // 正在请求中（防止多次调用）
    //: var frontCamera = true
    var frontCamera = true // 是否前置摄像头
}

//: class TalkingLiveManager: NSObject {
class InstrumentalityReactiveCompatible: NSObject {
    // 直播间消息
    //: var liveRoomModel = TalkingLiveRoomModel()
    var liveRoomModel = MapModel()
    // 是否正在直播中（状态变更时发送通知）
    //: private(set) var isLive: Bool = false {
    private(set) var isLive: Bool = false {
        //: didSet {
        didSet {
            //: if PositionThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if PositionThen.share.appStatus == BetterQuantity.normal.rawValue {
                //: NotificationCenter.default.post(name: LIVE_STATUS_CHANGE_NOTIFICATION, object: self)
                NotificationCenter.default.post(name: kSucceedCancelLogger, object: self)
            }
        }
    }

    // TXLive
    //: var config = TalkingLiveConfig()
    var config = CreatorReactiveCompatible() // 直播渲染配置类
    //: private weak var renderLiveView: UIView?
    private weak var renderLiveView: UIView? // 直播视图视频源
    //: private var localPreviewView: TalkingTRTCVideoView?
    private var localPreviewView: BrobdingnagianThen? // 本地预览view（使用商汤美颜）
    //: private static var _instance: TalkingLiveManager?
    private static var _instance: InstrumentalityReactiveCompatible? // singleton

    //: private override init() {}
    override private init() {}
    //: class func shared() -> TalkingLiveManager {
    class func aftermath() -> InstrumentalityReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingLiveManager()
            _instance = InstrumentalityReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func nameure() {
        //: if TalkingLiveManager._instance != nil {
        if InstrumentalityReactiveCompatible._instance != nil {
            //: TalkingLiveManager._instance = nil
            InstrumentalityReactiveCompatible._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 直播小窗口悬浮窗
    //: private lazy var liveMiniView: TalkingLiveMiniView = {
    private lazy var liveMiniView: FeatureView = {
        //: let mini = TalkingLiveMiniView.buildLiveMiniView()
        let mini = FeatureView.residentView()
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 进入直播间
            //: self.live_updateTXLivePreview(self.liveRoomVC.renderView, smallWindow: false)
            self.screenDown(self.liveRoomVC.renderView, smallWindow: false)
            //: guard let currentVC = self.currentViewController() else { return }
            guard let currentVC = self.famousTick() else { return }
            // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
            //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
            if currentVC.isKind(of: ReverseBannerDelegate.self) {
                //: if var vcArr = currentVC.navigationController?.viewControllers {
                if var vcArr = currentVC.navigationController?.viewControllers {
                    //: vcArr.removeLast()
                    vcArr.removeLast()
                    //: vcArr.append(self.liveRoomVC)
                    vcArr.append(self.liveRoomVC)
                    //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                    currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                }
                //: } else {
            } else {
                //: currentVC.navigationController?.pushViewController(self.liveRoomVC, animated: true)
                currentVC.navigationController?.pushViewController(self.liveRoomVC, animated: true)
            }
        }
        //: return mini
        return mini
        //: }()
    }()

    // 直播控制器
    //: private lazy var liveRoomVC: TalkingLiveBeautifyViewController = {
    private lazy var liveRoomVC: EmplacementThen = {
        //: let vc = TalkingLiveBeautifyViewController()
        let vc = EmplacementThen()
        //: return vc
        return vc
        //: }()
    }()

    // 检测无人脸弹窗
    //: private lazy var noFaceAlert: TalkingLiveAlertView = {
    private lazy var noFaceAlert: HiReactiveCompatible = //: return HiReactiveCompatible(frame: .zero, type: .noFace)
        .init(frame: .zero, type: .noFace)
    //: }()

    // 直播推流对象
    //: private lazy var livePusher: TXLivePush = {
    private lazy var livePusher: TXLivePush = {
        //: let pushConfig = TXLivePushConfig()
        let pushConfig = TXLivePushConfig()
        //: pushConfig.frontCamera = self.config.frontCamera
        pushConfig.frontCamera = self.config.frontCamera
        //: pushConfig.customModeType |= CUSTOM_MODE_VIDEO_CAPTURE
        pushConfig.customModeType |= CUSTOM_MODE_VIDEO_CAPTURE
        // 标清
        //: let pusher = TXLivePush(config: pushConfig)
        let pusher = TXLivePush(config: pushConfig)
        //: pusher?.setVideoQuality(TX_Enum_Type_VideoQuality.VIDEO_QUALITY_STANDARD_DEFINITION,
        pusher?.setVideoQuality(TX_Enum_Type_VideoQuality.VIDEO_QUALITY_STANDARD_DEFINITION,
                                //: adjustBitrate: true,
                                adjustBitrate: true,
                                //: adjustResolution: false)
                                adjustResolution: false)
        //: return pusher!
        return pusher!
        //: }()
    }()
}

// MARK: - Request

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    /// 检测是否被禁用“开播功能”
    //: class func req_liveStartCheck(completion: @escaping FinishBlock) {
    class func pullLabel(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "live/startCheck"
        reqModel.requestPath = (String(appPeopleIndexLogger.prefix(5)) + String(spacingNowSettings.prefix(6)) + "heck")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 开启直播
    //: class func req_liveStart(completion: @escaping FinishBlock) {
    class func errReplacement(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "live/start"
        reqModel.requestPath = (String(spacingAddressPlatform.suffix(6)) + screenBarMetrics.lowercased())
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 停止直播
    /// - Parameters:
    ///   - type: 停播类型：1=主动终止；2=没有人脸；3=通话中断
    ///   - completion: 回调
    //: class func req_liveStop(type: Int, completion: FinishBlock? = nil) {
    class func indicatorMax(type: Int, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "live/stop"
        reqModel.requestPath = (String(sessionNothingMicData) + displayAssertFormat.lowercased())
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: screenGoldQuitPath.reversed(), encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion?(succeed, result, errorModel)
            completion?(succeed, result, errorModel)
        }
    }

    /// - Parameters:
    ///  - uid: 卡片用户id
    ///   - streamerUid: 主播id
    //: class func req__userCardInfo(uid: String, streamerUid: String, roomId: String? = nil , completion: @escaping FinishBlock) {
    class func loadedCompletion(uid: String, streamerUid: String, roomId: String? = nil, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/dataCard"
        reqModel.requestPath = (String(k_vidMessage.suffix(5)) + "er/d" + String(colorCoordinateMetrics.prefix(7)))
        //: var params = ["uid": uid, "streamerUid": streamerUid]
        var params = [String(bytes: spacingGirlData.map{weeWhether(choice: $0)}, encoding: .utf8)!: uid, String(bytes: widgetSpecLogger.map{$0^222}, encoding: .utf8)!: streamerUid]
        //: if let roomId = roomId {
        if let roomId = roomId {
            //: params["roomId"] = roomId
            params[(String(viewDecreaseKey) + String(sessionApologyConfig.suffix(5)))] = roomId
        }
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}

// MARK: - 权限检测，通过后开启直播

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    // 检测直播状态，未开播时打开直播
    //: func checkLiveAuthAndTurnOn(completionHandler: ((_ isSucceed: Bool) -> Void)? = nil) {
    func numericalQuantity(completionHandler: ((_ isSucceed: Bool) -> Void)? = nil) {
        //: guard self.isLive == false else { return }
        guard self.isLive == false else { return }
        //: guard self.config.isRequest == false else { return }
        guard self.config.isRequest == false else { return }
        //: self.config.isRequest = true
        self.config.isRequest = true

        // 1. 商汤美颜
        //: guard SenseTime_Use == true else {
        guard kVoiceMicPathContent == true else {
            //: self.func__showStatusBarErrorMsg(showMsg: "Try again later, if you have any questions please contact customer service".localized)
            self.colouration(showMsg: String(bytes: componentRemindQuotePath.map{$0^66}, encoding: .utf8)!.localized)
            //: self.config.isRequest = false
            self.config.isRequest = false
            //: return
            return
        }

        // 2. 权限检测
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MagnitudeReactiveCompatible.popularNet { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colouration(showMsg: displayReadPath)
                //: self.config.isRequest = false
                self.config.isRequest = false
                //: completionHandler?(false)
                completionHandler?(false)
                //: return
                return
            }
            // 3. 接口是否禁播检测
            //: TalkingLiveManager.req_liveStartCheck { succeed, result, errorModel in
            InstrumentalityReactiveCompatible.pullLabel { succeed, result, errorModel in
                //: guard succeed else {
                guard succeed else {
                    //: self.config.isRequest = false
                    self.config.isRequest = false
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? Dictionary<String, Any> {
                        if let result = result as? [String: Any] {
                            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            ProdPushManager.share.rangeGalleryThirty(urlStr: result[(String(k_sSettings) + String(componentProductIslandConfig))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.colouration(showMsg: errorModel!.errorMsg)
                    }
                    //: completionHandler?(false)
                    completionHandler?(false)
                    //: return
                    return
                }
                // 4. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isFamous == false else {
                guard TopThen.shared.isFamous == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.colouration(showMsg: spacingQualityHundredSystem)
                    //: self.config.isRequest = false
                    self.config.isRequest = false
                    //: completionHandler?(false)
                    completionHandler?(false)
                    //: return
                    return
                }

                // 校验成功，开启直播
                //: completionHandler?(true)
                completionHandler?(true)
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())

                // 进入直播间
                //: self.currentViewController()?.navigationController?.pushViewController(self.liveRoomVC, animated: true)
                self.famousTick()?.navigationController?.pushViewController(self.liveRoomVC, animated: true)
                //: self.live_createRoomAndStartPush(livePushUrl: json["livePushUrl"].stringValue)
                self.anScope(livePushUrl: json[(String(spacingSchoolFileSettings) + String(displayAgainstKey))].stringValue)
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    /// 展示直播违规弹窗
    //: static func live_showWarningView(content: String, reason: String, time: String) {
    static func whyEnter(content: String, reason: String, time: String) {
        //: let alert = TalkingLiveWarningView()
        let alert = AllView()
        //: alert.refreshUI(content: content, reason: reason, time: time)
        alert.chin(content: content, reason: reason, time: time)
        //: alert.show()
        alert.commonShow()
    }

    /// 释放资源
    //: func live_releaseAllResource() {
    func personalIdentity() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isLive = false
            self.isLive = false
            //: self.config.isRequest = false
            self.config.isRequest = false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: NotificationCenter.default.removeObserver(self)
            NotificationCenter.default.removeObserver(self) // 移除通知
            //: TalkingSocketManager.shared.endLivePing()
            TopThen.shared.alongPing() // 结束心跳包
            //: self.liveMiniView.removeFromSuperview()
            self.liveMiniView.removeFromSuperview() // 移除小窗口
            //: self.liveRoomVC.popCurrentViewController()
            self.liveRoomVC.pullBack() // 将直播视图从栈中移除
            //: self.destoryTXLiveView()
            self.inviteView()
            // 释放单例
            //: TalkingLiveManager.destroy()
            InstrumentalityReactiveCompatible.nameure()
            // 销毁弹幕
            //: TalkingDanmuManager.danmu_releaseAllResource()
            PushReactiveCompatible.pauseResource()
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (SensitiveFamousApplication.shared as! SensitiveFamousApplication).scar()
        }
    }

    /// 创建视频通话房间开始推流
    /// - Parameter livePushUrl: 推流地址
    //: func live_createRoomAndStartPush(livePushUrl: String) {
    func anScope(livePushUrl: String) {
        //: self.config.livePushUrl = livePushUrl
        self.config.livePushUrl = livePushUrl

        // 创建预览画面，开始直播（默认大屏）
        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
        //: self.live_updateTXLivePreview(self.liveRoomVC.renderView, smallWindow: false)
        self.screenDown(self.liveRoomVC.renderView, smallWindow: false)
        //: self.live_startPush(livePushUrl: livePushUrl)
        self.shadow(livePushUrl: livePushUrl)
    }

    /// 更新tx直播画面
    /// - Parameters:
    ///   - randerView: 渲染视图（默认为小窗口）
    ///   - smallWindow: 是否小窗口
    //: func live_updateTXLivePreview(_ randerView: UIView? = nil, smallWindow: Bool) {
    func screenDown(_ randerView: UIView? = nil, smallWindow: Bool) {
        //: if randerView == nil {
        if randerView == nil {
            //: self.renderLiveView = self.liveMiniView.renderView
            self.renderLiveView = self.liveMiniView.renderView
            //: } else {
        } else {
            //: self.renderLiveView = randerView
            self.renderLiveView = randerView
        }

        //: self.liveMiniView.isHidden = !smallWindow
        self.liveMiniView.isHidden = !smallWindow
        //: self.config.isSmallMode = smallWindow
        self.config.isSmallMode = smallWindow
        // 清除小窗口未读消息标识
        //: if self.config.isSmallMode {
        if self.config.isSmallMode {
            //: updateUnredMessageCount(clear: true)
            threeClear(clear: true)
        }
        //: startTXLivePreview(smallWindow: smallWindow)
        getGoingMale(smallWindow: smallWindow)
    }

    /// 前后摄像头切换
    /// - Parameter front: 是否前置
    //: func switchTRTCCamer(front: Bool) {
    func miss(front: Bool) {
        //: guard self.config.frontCamera != front else { return }
        guard self.config.frontCamera != front else { return }
        //: self.config.frontCamera = front
        self.config.frontCamera = front

        //: guard SenseTime_Use == true else {
        guard kVoiceMicPathContent == true else {
            //: livePusher.switchCamera()
            livePusher.switchCamera()
            //: return
            return
        }

        //: self.localPreviewView?.switchTRTCCamer(isFront: front)
        self.localPreviewView?.supremeFront(isFront: front)
    }
}

// MARK: - 通知事件

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    /// 添加通知监听
    //: private func addNotifications() {
    private func profileNotifications() {
        // 监听是否捕获到人脸
        //: if SenseTime_Use == true {
        if kVoiceMicPathContent == true {
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(belowSum(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_1_second"),
                                                   name: NSNotification.Name(rawValue: String(bytes: coreMinimizeAspectPath.map{unitAchievement(bridge: $0)}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillTerminate),
                                               selector: #selector(manageLicenseRestore),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 程序销毁时调用停播接口
    //: @objc private func appWillTerminate() {
    @objc private func manageLicenseRestore() {
        //: guard self.isLive == true else { return }
        guard self.isLive == true else { return }
        //: TalkingLiveManager.req_liveStop(type: 1)
        InstrumentalityReactiveCompatible.indicatorMax(type: 1)
    }
}

// MARK: - 未读消息提醒标识

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    /// 更新未读消息数量
    /// - Parameter clear: 是否清空
    //: func updateUnredMessageCount(clear: Bool = false) {
    func threeClear(clear: Bool = false) {
        //: guard self.config.isSmallMode else { return }
        guard self.config.isSmallMode else { return }
        //: if clear {
        if clear {
            //: self.liveMiniView.msgCount = 0
            self.liveMiniView.msgCount = 0
            //: } else {
        } else {
            //: self.liveMiniView.msgCount += 1
            self.liveMiniView.msgCount += 1
        }
    }
}

// MARK: - 商汤人脸检测

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    /// 商汤美颜是否捕获到人脸通知（间隔1秒）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func belowSum(notification: NSNotification) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard self.isLive == true else { return }
            guard self.isLive == true else { return }
            //: let userInfo = notification.userInfo as! [String: AnyObject]
            let userInfo = notification.userInfo as! [String: AnyObject]
            //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
            let hasFace = (userInfo[(String(sessionSceneLogger))] as! NSNumber).description.boolValue
            //: if hasFace == true {
            if hasFace == true {
                //: self.liveMiniView.noFaceTime = 0
                self.liveMiniView.noFaceTime = 0
                //: self.liveRoomVC.topView.noFaceTime = 0
                self.liveRoomVC.topView.noFaceTime = 0
                //: } else {
            } else {
                //: self.liveMiniView.noFaceTime += 1
                self.liveMiniView.noFaceTime += 1
                //: self.liveRoomVC.topView.noFaceTime += 1
                self.liveRoomVC.topView.noFaceTime += 1
            }
            // 连续X秒无人脸出现直接下播
            //: if self.liveMiniView.noFaceTime == MAX_NOFACETIME {
            if self.liveMiniView.noFaceTime == k_textDeviceName {
                //: self.interval_noFace()
                self.actorSLineHolder()
            }
            // 开启超时检测（防止直播时长时间后台、锁屏）
            //: self.interval_cancelPreviousPerformRequest()
            self.foreground()
            //: self.interval_startPerformRequest()
            self.privacyHandle()
        }
    }

    /// 开启超时检测（防止直播时长时间后台、锁屏）
    //: private func interval_startPerformRequest() {
    private func privacyHandle() {
        //: self.perform(#selector(interval_noFace), with: nil, afterDelay: 30)
        self.perform(#selector(actorSLineHolder), with: nil, afterDelay: 30)
    }

    /// 取消超时检测
    //: private func interval_cancelPreviousPerformRequest() {
    private func foreground() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self,
        NSObject.cancelPreviousPerformRequests(withTarget: self,
                                               //: selector: #selector(interval_noFace),
                                               selector: #selector(actorSLineHolder),
                                               //: object: nil)
                                               object: nil)
    }

    /// 未检测到人脸超过30秒
    //: @objc private func interval_noFace() {
    @objc private func actorSLineHolder() {
        //: guard self.isLive == true else { return }
        guard self.isLive == true else { return }
        //: TalkingLiveManager.req_liveStop(type: 2)
        InstrumentalityReactiveCompatible.indicatorMax(type: 2)
        //: self.live_releaseAllResource()
        self.personalIdentity()
        //: self.noFaceAlert.show()
        self.noFaceAlert.progress()
    }
}

// MARK: - TX直播

//: extension TalkingLiveManager {
extension InstrumentalityReactiveCompatible {
    /// tx本地预览视频画面
    /// - Parameter smallWindow: 是否自己的画面在小窗口
    //: private func startTXLivePreview(smallWindow: Bool) {
    private func getGoingMale(smallWindow: Bool) {
        //: guard SenseTime_Use == true else {
        guard kVoiceMicPathContent == true else {
            //: let beautyManger = livePusher.getBeautyManager()
            let beautyManger = livePusher.getBeautyManager()
            //: beautyManger?.setBeautyStyle(.nature)
            beautyManger?.setBeautyStyle(.nature)
            //: beautyManger?.setBeautyLevel(6)
            beautyManger?.setBeautyLevel(6)

            //: livePusher.startPreview(self.renderLiveView)
            livePusher.startPreview(self.renderLiveView)
            //: return
            return
        }

        //: if self.localPreviewView == nil {
        if self.localPreviewView == nil {
            //: self.localPreviewView = TalkingTRTCVideoView()
            self.localPreviewView = BrobdingnagianThen()
        }
        //: self.localPreviewView?.startPreView(isSmallWindow: smallWindow, livePusher: livePusher)
        self.localPreviewView?.eyePusher(isSmallWindow: smallWindow, livePusher: livePusher)
        //: if self.localPreviewView?.superview != self.renderLiveView {
        if self.localPreviewView?.superview != self.renderLiveView {
            //: self.localPreviewView?.removeFromSuperview()
            self.localPreviewView?.removeFromSuperview()
            //: self.renderLiveView?.addSubview(self.localPreviewView!)
            self.renderLiveView?.addSubview(self.localPreviewView!)
            //: self.localPreviewView?.snp.makeConstraints({ make in
            self.localPreviewView?.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
                //: })
            }
        }
    }

    /// 开始直播推流
    /// - Parameter livePushUrl: 推流地址
    //: private func live_startPush(livePushUrl: String) {
    private func shadow(livePushUrl: String) {
        //: if SenseTime_Use {
        if kVoiceMicPathContent {
            //: } else {
        } else {
            //: let beautyManger = livePusher.getBeautyManager()
            let beautyManger = livePusher.getBeautyManager()
            //: beautyManger?.setBeautyStyle(.nature)
            beautyManger?.setBeautyStyle(.nature)
            //: beautyManger?.setBeautyLevel(6)
            beautyManger?.setBeautyLevel(6)
        }

        //: livePusher.delegate = self
        livePusher.delegate = self
        //: livePusher.setMute(self.config.isMute)
        livePusher.setMute(self.config.isMute)
        //: let result = livePusher.start(livePushUrl)
        let result = livePusher.start(livePushUrl)
        //: onEnterRoom(Int(result))
        outIn(Int(result))
    }

    /// 销毁tx直播
    //: private func destoryTXLiveView() {
    private func inviteView() {
        //: if SenseTime_Use {
        if kVoiceMicPathContent {
            //: self.localPreviewView?.stopPreView()
            self.localPreviewView?.flag()
            //: } else {
        } else {
            //: self.livePusher.stopPreview()
            self.livePusher.stopPreview()
        }

        //: self.localPreviewView?.removeFromSuperview()
        self.localPreviewView?.removeFromSuperview()
        //: self.localPreviewView = nil
        self.localPreviewView = nil

        //: self.livePusher.stop()
        self.livePusher.stop()
    }

    /// 进入直播间，调用接口
    //: func onEnterRoom(_ result: Int) {
    func outIn(_ result: Int) {
        //: guard result >= 0 else {
        guard result >= 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "Opening failed".localized)
            self.colouration(showMsg: (String(kCurveConfig.suffix(5)) + "ng f" + screenWhatSplitTimer.replacingOccurrences(of: "enable", with: "i")).localized)
            //: self.live_releaseAllResource()
            self.personalIdentity()
            //: return
            return
        }

        // 加入房间成功，调用开播接口
        //: TalkingLiveManager.req_liveStart { succeed, result, errorModel in
        InstrumentalityReactiveCompatible.errReplacement { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: self.live_releaseAllResource()
                self.personalIdentity()
                //: return
                return
            }
            //: if let model = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) {
            if let model = MapModel.deserialize(from: result as? Dictionary) {
                //: self.liveRoomModel = model
                self.liveRoomModel = model
            }
            // 加入弹幕
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.liveRoomModel.chatGroupId, announcementMsg: self.liveRoomModel.notification)
            PushReactiveCompatible.deliverCompletion(groupId: self.liveRoomModel.chatGroupId, announcementMsg: self.liveRoomModel.notification)
            // 开播成功
            //: self.isLive = true
            self.isLive = true
            //: self.liveMiniView.refreshMiniView()
            self.liveMiniView.tickOrView()
            //: self.liveRoomVC.refreshLiveRoomView()
            self.liveRoomVC.look()
            //: TalkingSocketManager.shared.startLivePing()
            TopThen.shared.late()
            //: self.addNotifications()
            self.profileNotifications()
            // 销毁上传busy状态定时器
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (SensitiveFamousApplication.shared as! SensitiveFamousApplication).aReduce()
        }
    }
}

// MARK: - TXLivePushListener

//: extension TalkingLiveManager: TXLivePushListener {
extension InstrumentalityReactiveCompatible: TXLivePushListener {
    //: func onPushEvent(_ evtID: Int32, withParam param: [AnyHashable: Any]!) {
    func onPushEvent(_ evtID: Int32, withParam _: [AnyHashable: Any]!) {
        //: UploadLogTool.writeLog(msg: "LIVE 状态码：\(evtID).")
        CheckThen.placeLoad(msg: (String(k_businessSystem) + colorCityError.capitalized) + "\(evtID).")
        //: if evtID == PUSH_WARNING_NET_BUSY.rawValue {
        if evtID == PUSH_WARNING_NET_BUSY.rawValue {
            //: self.func__showStatusBarErrorMsg(showMsg: "Network busy!".localized)
            self.colouration(showMsg: (String(spacingOriginallyName.suffix(5)) + String(kPianoMessage)).localized)

            //: } else if evtID == PUSH_ERR_OPEN_CAMERA_FAIL.rawValue ||
        } else if evtID == PUSH_ERR_OPEN_CAMERA_FAIL.rawValue ||
            //: evtID == PUSH_ERR_OPEN_MIC_FAIL.rawValue ||
            evtID == PUSH_ERR_OPEN_MIC_FAIL.rawValue ||
            //: evtID == PUSH_ERR_VIDEO_ENCODE_FAIL.rawValue ||
            evtID == PUSH_ERR_VIDEO_ENCODE_FAIL.rawValue ||
            //: evtID == PUSH_ERR_AUDIO_ENCODE_FAIL.rawValue ||
            evtID == PUSH_ERR_AUDIO_ENCODE_FAIL.rawValue ||
            //: evtID == PUSH_ERR_UNSUPPORTED_RESOLUTION.rawValue ||
            evtID == PUSH_ERR_UNSUPPORTED_RESOLUTION.rawValue ||
            //: evtID == PUSH_ERR_UNSUPPORTED_SAMPLERATE.rawValue ||
            evtID == PUSH_ERR_UNSUPPORTED_SAMPLERATE.rawValue ||
            //: evtID == PUSH_ERR_NET_DISCONNECT.rawValue ||
            evtID == PUSH_ERR_NET_DISCONNECT.rawValue ||
            //: evtID == PUSH_ERR_AUDIO_SYSTEM_NOT_WORK.rawValue ||
            evtID == PUSH_ERR_AUDIO_SYSTEM_NOT_WORK.rawValue ||
            //: evtID == PUSH_ERR_INVALID_ADDRESS.rawValue ||
            evtID == PUSH_ERR_INVALID_ADDRESS.rawValue ||
            //: evtID == PUSH_ERR_CONNECT_SERVER_FAILED.rawValue ||
            evtID == PUSH_ERR_CONNECT_SERVER_FAILED.rawValue ||
            //: evtID == PUSH_ERR_NETWORK_UNAVAIABLE.rawValue ||
            evtID == PUSH_ERR_NETWORK_UNAVAIABLE.rawValue ||
            //: evtID == PUSH_ERR_SERVER_REFUSED.rawValue {
            evtID == PUSH_ERR_SERVER_REFUSED.rawValue
        {
            // 主播异常下播
            //: UploadLogTool.writeLog(msg: "LIVE 异常下播，evtID:\(evtID).")
            CheckThen.placeLoad(msg: (String(kSampleUrl.suffix(7)) + String(displayButSystem) + String(viewAdviceTitle)) + "\(evtID).")
            //: self.func__showStatusBarErrorMsg(showMsg: "Live room exception".localized)
            self.colouration(showMsg: (String(moduleSmallDevice) + String(serviceAverWaitAlert.suffix(5)) + "ption").localized)
            //: TalkingLiveManager.req_liveStop(type: 1)
            InstrumentalityReactiveCompatible.indicatorMax(type: 1)
            //: live_releaseAllResource()
            personalIdentity()
            // 重新开播弹窗
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
                let alert = HiReactiveCompatible(frame: .zero, type: .turnOnLive)
                //: alert.rightBlcok = {
                alert.rightBlcok = {
                    //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                    InstrumentalityReactiveCompatible.aftermath().numericalQuantity()
                }
                //: alert.show()
                alert.progress()
            }
        }
    }

    //: func onNetStatus(_ param: [AnyHashable: Any]!) {}
    func onNetStatus(_: [AnyHashable: Any]!) {}

    //: func onScreenCaptureStarted() {}
    func onScreenCaptureStarted() {}

    //: func onScreenCapturePaused(_ reason: Int32) {}
    func onScreenCapturePaused(_: Int32) {}

    //: func onScreenCaptureResumed(_ reason: Int32) {}
    func onScreenCaptureResumed(_: Int32) {}

    //: func onScreenCaptureStoped(_ reason: Int32) {}
    func onScreenCaptureStoped(_: Int32) {}
}
