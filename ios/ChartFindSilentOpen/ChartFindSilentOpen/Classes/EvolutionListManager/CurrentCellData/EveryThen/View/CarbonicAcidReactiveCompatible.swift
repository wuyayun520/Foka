
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appLateValue:[UInt8] = [0x2a,0x2d,0x2a,0x37,0x6b,0x20,0x2c,0x27,0x26,0x31,0x79,0x6a,0x63,0x2b,0x22,0x30,0x63,0x2d,0x2c,0x37,0x63,0x21,0x26,0x26,0x2d,0x63,0x2a,0x2e,0x33,0x2f,0x26,0x2e,0x26,0x2d,0x37,0x26,0x27]

private func totalPerson(accounting num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "Say something...     " :*/
fileprivate let spacingChannelCycleText:String = "Say soevolution weekly"
fileprivate let themeHomeData:String = "..     "

/*: "btn_video_gift_nor" :*/
fileprivate let appSurfaceTimer:[Character] = ["b","t","n","_","v","i"]
fileprivate let serviceLipLiteralSettings:[Character] = ["d","e","o","_","g","i","f","t","_","n","o","r"]

/*: "btn_live_gd_nor" :*/
fileprivate let viewReserveId:String = "btn_lfunction mystery ion"
fileprivate let spacingLogicalWorkplacePage:[Character] = ["g","d","_","n","o","r"]

/*: "btn_live_gd_pre" :*/
fileprivate let spacingOfflySession:String = "body soft tap skull truebtn_live"
fileprivate let kNativePath:[Character] = ["_","g","d","_","p","r","e"]

/*: "btn_live_sx_nor" :*/
fileprivate let viewPackName:String = "btn_liseries suite theoretic quality that"
fileprivate let colorPadMetrics:String = "ve_sxworkplace visual"

/*: "btn_live_sx_pre" :*/
fileprivate let displayRoughPlatform:String = "btn_llet index"
fileprivate let displayAllText:String = "x_preadditional restore curve end"

/*: "#FF2348" :*/
fileprivate let widgetAcquirePlatform:[Character] = ["#","F","F","2","3","4","8"]

/*: "btn_live_yx_nor" :*/
fileprivate let componentFourEvent:String = "btn_llocation position selection"
fileprivate let k_launchPlatform:[Character] = ["n","o","r"]

/*: "btn_live_yx_pre" :*/
fileprivate let commonFierceError:[Character] = ["b","t","n","_","l","i","v","e","_","y","x","_"]
fileprivate let coreScarHelper:String = "PRE"

/*: "toUid" :*/
fileprivate let sessionReloadlyMetrics:String = "floor key bench job readtoUid"

/*: "giftId" :*/
fileprivate let componentFaceBackgroundPath:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let coreHisPackUtility:[Character] = ["g"]
fileprivate let coreLogKey:String = "progress reply cloud castiftNum"

/*: "pkgItemsetId" :*/
fileprivate let appObjectAgainMetrics:String = "actor day circle artifactpkgI"

/*: "totalMfCoin" :*/
fileprivate let colorWoodDisableMessage:String = "totalMdignity successfully hardware across"
fileprivate let displayChooseJusMetrics:String = "fCoincity clothes combine"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CarbonicAcidReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/7/7.
//

//: import UIKit
import UIKit

//: @objc protocol TalkingLiveRoomBottomViewDelegate: NSObjectProtocol {
@objc protocol SheerObjectProtocol: NSObjectProtocol {
    //: func func__commentBtnClick()
    func snarl()
}

//: class TalkingLiveRoomBottomView: UIView {
class CarbonicAcidReactiveCompatible: UIView {
    //: var toUid: String?
    var toUid: String? /// 送给谁

    //: open weak var delegate: TalkingLiveRoomBottomViewDelegate?
    open weak var delegate: SheerObjectProtocol?

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        minWrite()
        //: setupSubViewsConstraint()
        visualAspect()
        //: GroupConversationListener.shared.func__addDelegate(self)
        GroupConversationListener.shared.andThenDelegate(self)
        //: refreshRedCountStatus()
        outTrack()
    }

    //: @available(*, unavailable)
    @available(*, unavailable)
    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appLateValue.map{totalPerson(accounting: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.clear
        view.backgroundColor = UIColor.clear
        //: return view
        return view
        //: }()
    }()

    //: private lazy var commentBtn: UIButton = {
    private lazy var commentBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setTitle("  " + "Say something...     ".localized, for: .normal)
        btn.setTitle("  " + (String(spacingChannelCycleText.prefix(6)) + "mething." + themeHomeData.capitalized).localized, for: .normal)
        //: btn.setTitleColor(UIColor(red: 255/255.0, green: 255/255.0, blue: 255/255.0, alpha: 0.8), for: .normal)
        btn.setTitleColor(UIColor(red: 255 / 255.0, green: 255 / 255.0, blue: 255 / 255.0, alpha: 0.8), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        btn.titleLabel?.font = UIFont.modelSize(type: .Regular, fontSize: 14)
        //: btn.setBackgroundColor(color: UIColor(red: 0/255.0, green: 0/255.0, blue: 0/255.0, alpha: 0.25), forState: .normal)
        btn.innerDoing(color: UIColor(red: 0 / 255.0, green: 0 / 255.0, blue: 0 / 255.0, alpha: 0.25), forState: .normal)
        //: btn.layer.cornerRadius = 17
        btn.layer.cornerRadius = 17
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PushThen.shared.direction == .rightToLeft {
            //: btn.contentHorizontalAlignment = .right
            btn.contentHorizontalAlignment = .right
            //: } else {
        } else {
            //: btn.contentHorizontalAlignment = .left
            btn.contentHorizontalAlignment = .left
        }
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(commentBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(askClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .leading
        v.alignment = .leading
        //: v.distribution = .equalSpacing
        v.distribution = .equalSpacing
        //: v.spacing = 10
        v.spacing = 10
        //: return v
        return v
        //: }()
    }()

    //: private lazy var giftBtn: UIButton = {
    private lazy var giftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(appSurfaceTimer) + String(serviceLipLiteralSettings))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .selected)
        btn.setImage(UIImage.titled(name: (String(appSurfaceTimer) + String(serviceLipLiteralSettings))), for: .selected)
        //: btn.addTarget(self, action: #selector(giftBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(quantityervalSort), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_live_gd_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(viewReserveId.prefix(5)) + "ive_" + String(spacingLogicalWorkplacePage))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_live_gd_pre"), for: .highlighted)
        btn.setImage(UIImage.titled(name: (String(spacingOfflySession.suffix(8)) + String(kNativePath))), for: .highlighted)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(counterrupt), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chatBtn: UIButton = {
    private lazy var chatBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_live_sx_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(viewPackName.prefix(6)) + String(colorPadMetrics.prefix(5)) + "_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_live_sx_pre"), for: .highlighted)
        btn.setImage(UIImage.titled(name: (String(displayRoughPlatform.prefix(5)) + "ive_s" + String(displayAllText.prefix(5)))), for: .highlighted)
        //: btn.addTarget(self, action: #selector(chatBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(checkFollow), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var redCountLab: UILabel = {
    private lazy var redCountLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.backgroundColor = UIColor(hex: "#FF2348")
        lab.backgroundColor = UIColor(hex: (String(widgetAcquirePlatform)))
        //: lab.layer.cornerRadius = 3.5
        lab.layer.cornerRadius = 3.5
        //: lab.layer.masksToBounds = true
        lab.layer.masksToBounds = true
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var gameBtn: UIButton = {
    private lazy var gameBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_live_yx_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(componentFourEvent.prefix(5)) + "ive_yx_" + String(k_launchPlatform))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_live_yx_pre"), for: .highlighted)
        btn.setImage(UIImage.titled(name: (String(commonFierceError) + coreScarHelper.lowercased())), for: .highlighted)
        //: btn.addTarget(self, action: #selector(gameBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(transport), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var giftView: TalkingChatGiftView = {
    private lazy var giftView: OffThen = {
        //: let giftV = TalkingChatGiftView(style: .live)
        let giftV = OffThen(style: .live)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent)
        //: return giftV
        return giftV
        //: }()
    }()

    //: private lazy var moreView: TalkingLiveRoomMoreView = {
    private lazy var moreView: AlongsideMoreView = {
        //: let v = TalkingLiveRoomMoreView()
        let v = AlongsideMoreView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ExternalThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ExternalThen()
        //: return v
        return v
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingLiveRoomBottomView {
extension CarbonicAcidReactiveCompatible {
    /// 拉起弹幕评论
    //: @objc private func commentBtnClick() {
    @objc private func askClick() {
        //: delegate?.func__commentBtnClick()
        delegate?.snarl()
    }

    /// 送礼
    //: @objc private func giftBtnClick() {
    @objc private func quantityervalSort() {
        //: func__sendGift()
        communication()
    }

    /// 聊天列表
    //: @objc private func chatBtnClick() {
    @objc private func checkFollow() {
        //: TalkingAppPushManager.share.func__pushToChatListVC(isHalfView: true)
        ProdPushManager.share.halfApply(isHalfView: true)
    }

    /// 更多
    //: @objc private func moreBtnClick() {
    @objc private func counterrupt() {
        //: moreView.showView()
        moreView.tingView()
    }

    /// 游戏
    //: @objc private func gameBtnClick() {
    @objc private func transport() {
        //: gamesView.showView(from: .LiveRoom)
        gamesView.head(from: .LiveRoom)
    }
}

// MARK: - SignatureManagerDelegate【刷新私信红点状态】

//: extension TalkingLiveRoomBottomView: IMManagerDelegate {
extension CarbonicAcidReactiveCompatible: SignatureManagerDelegate {
    //: func onUnreadMsgCountChanged(count: Int) {
    func addMid(count _: Int) {
        //: refreshRedCountStatus()
        outTrack()
    }

    /// 刷新消息未读数状态
    //: func refreshRedCountStatus() {
    func outTrack() {
        //: var msgCount = 0
        var msgCount = 0
        //: let convLists = [GroupConversationListener.shared.topConvList, GroupConversationListener.shared.norConvList]
        let convLists = [GroupConversationListener.shared.topConvList, GroupConversationListener.shared.norConvList]
        //: for list in convLists {
        for list in convLists {
            //: if let model = list.first(where: { $0.chatType == .private && $0.unreadCount > 0 }) {
            if let model = list.first(where: { $0.chatType == .private && $0.unreadCount > 0 }) {
                //: msgCount += Int(model.unreadCount)
                msgCount += Int(model.unreadCount)
                //: break
                break // 找到第一个未读消息后退出，不需要全部计算，只要有未读数就展示红点
            }
        }
        //: redCountLab.isHidden = (msgCount == 0)
        redCountLab.isHidden = (msgCount == 0)
    }
}

// MARK: - 送礼

//: extension TalkingLiveRoomBottomView {
extension CarbonicAcidReactiveCompatible {
    //: func func__sendGift() {
    func communication() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent(type: .live, completion: {
        RushReactiveCompatible.share.logClub(type: .live, completion: {
            //: self.func__showGiftChoiceView()
            self.counterest()
            //: })
        })
    }

    //: func func__showGiftChoiceView() {
    func counterest() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: PositionThen.share.loginUserMode.mf_coin)
        giftView.inmateTotal(needReload: true, mf_coin: PositionThen.share.loginUserMode.mf_coin)
        //: currentViewController()?.view.addSubview(giftView)
        famousTick()?.view.addSubview(giftView)
        //: giftView.showView()
        giftView.isContain()

        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: SizeSignatureGiftModel, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftModel: giftModel, giftNum: num)
            self.freebie(giftModel: giftModel, giftNum: num)
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func freebie(giftModel: SizeSignatureGiftModel, giftNum: String) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colouration(showMsg: serviceDetailFoundValue)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if toUid != nil {
        if toUid != nil {
            //: params["toUid"] = toUid
            params[(String(sessionReloadlyMetrics.suffix(5)))] = toUid
        }
        //: params["giftId"] = giftModel.gid
        params[(String(componentFaceBackgroundPath))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(coreHisPackUtility) + String(coreLogKey.suffix(6)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(appObjectAgainMetrics.suffix(4)) + "temsetId")] = giftModel.pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftLive(param: params, completion: { succeed, result, errorModel in
        ScriptRequestTool.precocious(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.privacyTemp(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.itemsetBy(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any])
            self.innerRequestAdjust(extral: result as! [String: Any])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any]) {
    func innerRequestAdjust(extral: [String: Any]) {
        //: let extralInfo = extral
        let extralInfo = extral

        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(colorWoodDisableMessage.prefix(6)) + String(displayChooseJusMetrics.prefix(5)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(colorWoodDisableMessage.prefix(6)) + String(displayChooseJusMetrics.prefix(5)))] as! NSNumber
            //: PositionThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PositionThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: PositionThen.share.loginUserMode.mf_coin)
        giftView.inmateTotal(needReload: false, mf_coin: PositionThen.share.loginUserMode.mf_coin)
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func privacyTemp(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard PositionThen.share.loginUserMode.status != 1 else {
            guard PositionThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    colouration(showMsg: errorStr)
                }
                //: return
                return
            }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            ProdPushManager.share.responsibility(clickEvent: k_restoreSystem, sufficient: false)
            //: giftView.dismissView()
            giftView.requestAcrossView()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = SubscribeThen(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent))
            //: view.show()
            view.exHusband()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                colouration(showMsg: errorStr)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingLiveRoomBottomView {
extension CarbonicAcidReactiveCompatible {
    /// 刷新底部视图
    //: func refreshBottomView(_ liveModel: TalkingLiveRoomModel) {
    func bottomActive(_ liveModel: MapModel) {
        //: toUid = String(liveModel.streamerInfo.uid)
        toUid = String(liveModel.streamerInfo.uid)
        // 非主播没有更多按钮
        //: let isAnchor = (String(liveModel.streamerInfo.uid) == PositionThen.share.loginUid)
        let isAnchor = (String(liveModel.streamerInfo.uid) == PositionThen.share.loginUid)
        //: if isAnchor == false {
        if isAnchor == false {
            //: stackView.removeArrangedSubview(moreBtn)
            stackView.removeArrangedSubview(moreBtn)
            //: moreBtn.isHidden = true
            moreBtn.isHidden = true
        }

        // 非审核模式 && 有游戏
        //: if PositionThen.share.appStatus != AppSkinStatus.special.rawValue,
        if PositionThen.share.appStatus != BetterQuantity.special.rawValue,
           //: liveModel.gameList.count > 0
           liveModel.gameList.count > 0
        {
            //: gameBtn.isHidden = false
            gameBtn.isHidden = false
            //: } else {
        } else {
            //: stackView.removeArrangedSubview(gameBtn)
            stackView.removeArrangedSubview(gameBtn)
            //: gameBtn.isHidden = true
            gameBtn.isHidden = true
        }
    }

    //: func setupSubviews() {
    func minWrite() {
        //: addSubview(contentView)
        addSubview(contentView)
        //: contentView.addSubview(commentBtn)
        contentView.addSubview(commentBtn)
        //: contentView.addSubview(stackView)
        contentView.addSubview(stackView)
        //: stackView.addArrangedSubview(gameBtn)
        stackView.addArrangedSubview(gameBtn)
        //: stackView.addArrangedSubview(chatBtn)
        stackView.addArrangedSubview(chatBtn)
        //: chatBtn.addSubview(redCountLab)
        chatBtn.addSubview(redCountLab)
        //: stackView.addArrangedSubview(moreBtn)
        stackView.addArrangedSubview(moreBtn)
        //: stackView.addArrangedSubview(giftBtn)
        stackView.addArrangedSubview(giftBtn)
    }

    //: func setupSubViewsConstraint() {
    func visualAspect() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.top.leading.trailing.bottom.equalTo(self)
            make.top.leading.trailing.bottom.equalTo(self)
        }
        //: commentBtn.snp.makeConstraints { make in
        commentBtn.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.height.equalTo(actualWidth(w: 33))
            make.height.equalTo(actualWidth(w: 33))
            //: make.width.equalTo(actualWidth(w: 157))
            make.width.equalTo(actualWidth(w: 157))
        }

        //: stackView.snp.makeConstraints { make in
        stackView.snp.makeConstraints { make in
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
        }

        //: giftBtn.snp.makeConstraints { make in
        giftBtn.snp.makeConstraints { make in
            //: make.width.height.equalTo(33)
            make.width.height.equalTo(33)
        }
        //: moreBtn.snp.makeConstraints { make in
        moreBtn.snp.makeConstraints { make in
            //: make.width.height.equalTo(giftBtn)
            make.width.height.equalTo(giftBtn)
        }
        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.width.height.equalTo(giftBtn)
            make.width.height.equalTo(giftBtn)
        }
        //: redCountLab.snp.makeConstraints { make in
        redCountLab.snp.makeConstraints { make in
            //: make.top.equalTo(7)
            make.top.equalTo(7)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.width.height.equalTo(7)
            make.width.height.equalTo(7)
        }
        //: gameBtn.snp.makeConstraints { make in
        gameBtn.snp.makeConstraints { make in
            //: make.width.height.equalTo(giftBtn)
            make.width.height.equalTo(giftBtn)
        }
    }
}
