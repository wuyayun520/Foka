
//: Declare String Begin

/*: "msgType" :*/
fileprivate let coreAlarmHelper:[Character] = ["m","s","g","T","y","p","e"]

/*: "tips" :*/
fileprivate let kInsideTitle:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let layoutAsideAidSystem:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let appBringMessage:String = "msgInfoothers currency"

/*: "user" :*/
fileprivate let moduleAllyKey:String = "projectser"

/*: "uid" :*/
fileprivate let kCapSettings:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let featureRecessTwelveHelper:[UInt8] = [0x46,0x41,0x4b,0x43,0x46,0x49,0x45,0x4d]

private func startPlay(rocket num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "headPic" :*/
fileprivate let sessionExclusiveUrl:[UInt8] = [0x5a,0x57,0x53,0x56,0x62,0x5b,0x51]

private func oppositeMovie(language num: UInt8) -> UInt8 {
    return num ^ 50
}

/*: "age" :*/
fileprivate let spacingItsError:[UInt8] = [0x93,0x95,0x97]

private func feedOf(joint num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "gift" :*/
fileprivate let sessionNoonHelper:String = "gifresidency"

/*: "isGroup" :*/
fileprivate let sessionShrinkLogger:String = "isGrouinmate learn resource"
fileprivate let featureToolNoName:[Character] = ["p"]

/*: "WantGift" :*/
fileprivate let kCenterSystem:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let k_courseSettings:String = "titlextitle"

/*: "img" :*/
fileprivate let kConversionName:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let appPureImitationError:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let colorCancelUtility:String = "midudio"

/*: "messageType" :*/
fileprivate let layoutBlockSystem:String = "mcommunications"
fileprivate let screenPeerPreference:[Character] = ["e"]

/*: "contentType" :*/
fileprivate let sessionBlankUrl:String = "coaim"
fileprivate let k_salePlatform:String = "wee commit library describe ourentType"

/*: "ImgMsg" :*/
fileprivate let k_appearMetrics:[Character] = ["I","m","g","M","s"]
fileprivate let layoutBrightContent:[Character] = ["g"]

/*: "imageUri" :*/
fileprivate let featureSeaCalculationLogger:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let widgetSignatureText:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let styleMortgagePage:String = "PIC"

/*: "isBurn" :*/
fileprivate let screenGuidanceSystem:String = "array given life officialisBurn"

/*: "giftId" :*/
fileprivate let serviceSkullFormat:String = "giftIdenvironment earn give not"

/*: "params" :*/
fileprivate let displayMaterialLogger:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let layoutStayChannelPlatform:String = "AUDI"
fileprivate let commonFeedbackPage:String = "ting"

/*: "len" :*/
fileprivate let featureGatorDevice:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let modulePhysicsSystem:[Character] = ["a","u","d","i","o","S","a","n","d","b"]
fileprivate let featureEditUrl:[Character] = ["o","x"]

/*: "senderId" :*/
fileprivate let commonInmateMessage:String = "senderIdbean depth regain"

/*: "AudioMsg" :*/
fileprivate let widgetMoreAlert:String = "AudioMscoal mode theoretic quality min"
fileprivate let coreQuantityLogger:String = "end"

/*: "content" :*/
fileprivate let kLanguageTitle:String = "conrailroadnt"

/*: "TxtMsg" :*/
fileprivate let k_filterFormat:String = "t"
fileprivate let screenAgainstPath:String = "xtMsgpop fruit"

/*: "message" :*/
fileprivate let layoutSampleUtility:[Character] = ["m"]
fileprivate let screenStretchName:String = "ESSAGE"

/*: "quoteMsgId" :*/
fileprivate let sessionLengthShowKey:String = "quoteMscommunication apply who required bind"
fileprivate let coreShapeSettings:[Character] = ["g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let layoutScreenTimer:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "rYMsgType" :*/
fileprivate let screenResumeTitle:String = "object validity argumentrYMsgT"
fileprivate let serviceToHelper:String = "yscare"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let componentMapUtility:String = "GJ:Sessupport method eave ta beneath"
fileprivate let commonLittlePictureAcrossData:String = "cast status border apptoGree"
fileprivate let screenListenFormat:[Character] = ["s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let viewPreferencePage:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "system" :*/
fileprivate let moduleTranslationUtility:[Character] = ["s","y","s","t","e","m"]

/*: "title" :*/
fileprivate let spacingSpotLogger:[Character] = ["t","i","t","l","e"]

/*: "icon" :*/
fileprivate let kIconTinPlatform:String = "ipathn"

/*: "all" :*/
fileprivate let moduleCommunicationPath:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let layoutAssistantName:String = "behavior native nut picture disabledicon_ch"
fileprivate let displayBestName:[Character] = ["a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let appReadId:String = "pickatus"

/*: "msgIncome" :*/
fileprivate let layoutHairTitle:[Character] = ["m","s","g","I"]
fileprivate let themeRequireMetrics:[Character] = ["n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let themeThematicUrl:[Character] = ["m","s","g","I","d"]

/*: "OrientationHashableRepresentation" :*/
fileprivate let spacingThenVarietyError:String = "trail antiTMsgTr"
fileprivate let colorRegionValue:String = "ateStamain argument some care"
fileprivate let screenBehaviorValue:String = "simultaneouslys"

/*: "TMsgTranslatedContent" :*/
fileprivate let colorOwnerMessage:String = "TMsgTrstatistical time union"
fileprivate let k_sharedLogger:[Character] = ["a","n","s","l","a","t","e","d","C","o","n","t","e","n","t"]

/*: "formatType" :*/
fileprivate let componentAnalyzeKey:String = "ffarrm"

/*: "plain" :*/
fileprivate let sessionWritId:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let kRompPath:String = "imageDathouse don import"
fileprivate let coreBackgroundMakerDevice:[Character] = ["a"]

/*: "VideoMsg" :*/
fileprivate let colorTeamEvent:String = "VideoMsgchance clothes capacity any audience"

/*: "match" :*/
fileprivate let commonAdminName:[Character] = ["m","a","t","c","h"]

/*: "userIntro" :*/
fileprivate let componentRetailerAlert:[Character] = ["u","s","e","r","I","n"]
fileprivate let widgetProcedureDevice:String = "beamro"

/*: "targetId" :*/
fileprivate let colorRouteMaximEvent:String = "sauceargesauce"
fileprivate let sessionMotionEvent:String = "Idother now recent play"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let featureSweetKey:String = "maymaymaymay"
fileprivate let screenMeetPage:String = "send med HH"
fileprivate let commonLateData:String = ":mm:ssindex article"

/*: "sendTime" :*/
fileprivate let themeCountoMessage:[Character] = ["s","e","n","d","T","i","m"]
fileprivate let serviceTransactionText:[Character] = ["e"]

/*: "isRead" :*/
fileprivate let sessionWrapMiddleAdvertisingConfig:String = "isReadmakeup cost recall"

/*: "audioData" :*/
fileprivate let appSurfaceSouAchievementSettings:String = "audiring"
fileprivate let commonBlackThroughSession:[Character] = ["D","a","t","a"]

/*: "audioUri" :*/
fileprivate let appAuctionMessage:[Character] = ["a","u"]
fileprivate let componentAboveFormat:String = "dioUribackground best motion quick"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChatThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum CurrentConstantTarget: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - ChatThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class ChatThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func getForMobile() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ThereMessageReactiveCompatible? {
    public class func betwixt(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ThereMessageReactiveCompatible? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = sweepUp(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: GroupConversationListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            GroupConversationListener.shared.realty(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ThereMessageReactiveCompatible? {
    public class func sweepUp(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ThereMessageReactiveCompatible? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(coreAlarmHelper))] = (String(kInsideTitle))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(layoutAsideAidSystem)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = drownMap(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MinimizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = StallModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? SensualModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(appBringMessage.prefix(7)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(moduleAllyKey.replacingOccurrences(of: "project", with: "u"))] = [
            //: "uid": PositionThen.share.loginUserMode.userID,
            (String(kCapSettings)): PositionThen.share.loginUserMode.userID,
            //: "nickname": PositionThen.share.loginUserMode.nickname,
            String(bytes: featureRecessTwelveHelper.map{startPlay(rocket: $0)}, encoding: .utf8)!: PositionThen.share.loginUserMode.nickname,
            //: "headPic": PositionThen.share.loginUserMode.headPic,
            String(bytes: sessionExclusiveUrl.map{oppositeMovie(language: $0)}, encoding: .utf8)!: PositionThen.share.loginUserMode.headPic,
            //: "age": PositionThen.share.loginUserMode.age,
            String(bytes: spacingItsError.map{feedOf(joint: $0)}, encoding: .utf8)!: PositionThen.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(layoutAsideAidSystem)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = drownMap(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(coreAlarmHelper))] as? String == (sessionNoonHelper.replacingOccurrences(of: "residency", with: "t")) {
            //: var data: ThereMessageReactiveCompatible
            var data: ThereMessageReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(sessionShrinkLogger.prefix(6)) + String(featureToolNoName))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ResumeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = OwnThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = OwnThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(coreAlarmHelper))] as? String == (String(kCenterSystem)) {
            //: let data = FeatureCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = FeatureCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(coreAlarmHelper))] as? String == (k_courseSettings.replacingOccurrences(of: "title", with: "t")) {
            //: var data: ThereMessageReactiveCompatible
            var data: ThereMessageReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(sessionShrinkLogger.prefix(6)) + String(featureToolNoName))] as? String == "1" { // 群聊
                //: data = FluThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = FluThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = CompartmentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = CompartmentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(coreAlarmHelper))] as? String == (String(kConversionName)) {
            //: let data = SinceFamousReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SinceFamousReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(coreAlarmHelper))] as? String == (String(appPureImitationError)) {
            //: let data = RationalCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RationalCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(coreAlarmHelper))] as? String == (colorCancelUtility.replacingOccurrences(of: "mid", with: "a")) {
            //: let data = FirstThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = FirstThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(sessionNoonHelper.replacingOccurrences(of: "residency", with: "t"))] != nil && (msgInfo[(layoutBlockSystem.replacingOccurrences(of: "communication", with: "es") + "ageTyp" + String(screenPeerPreference))] as? Int) == 4 {
            //: let data = CompartmentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CompartmentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? StallModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: ThereMessageReactiveCompatible) -> ThereMessageReactiveCompatible? {
    public class func dismissData(withData cellData: ThereMessageReactiveCompatible) -> ThereMessageReactiveCompatible? {
        // 图片
        //: if let reCellData = cellData as? SinceFamousReactiveCompatible {
        if let reCellData = cellData as? SinceFamousReactiveCompatible {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(FragThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(FragThen.acquireOnStag())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(sessionBlankUrl.replacingOccurrences(of: "aim", with: "nt") + String(k_salePlatform.suffix(7)))] = (String(k_appearMetrics) + String(layoutBrightContent))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(coreAlarmHelper))] = (String(kConversionName))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(featureSeaCalculationLogger))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(appBringMessage.prefix(7)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(widgetSignatureText))] = (String(k_appearMetrics) + String(layoutBrightContent))
            //: params["pic"] = filePath
            params[(styleMortgagePage.lowercased())] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(screenGuidanceSystem.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(screenGuidanceSystem.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(serviceSkullFormat.prefix(6)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(serviceSkullFormat.prefix(6)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(displayMaterialLogger))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = sweepUp(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? RationalCellData {
        if let reCellData = cellData as? RationalCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? FirstThen {
        if let reCellData = cellData as? FirstThen {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = UsufructuaryThen.cacheGuidance(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(layoutStayChannelPlatform.lowercased() + "oLengt" + commonFeedbackPage.replacingOccurrences(of: "ting", with: "h"))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(featureGatorDevice))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(modulePhysicsSystem) + String(featureEditUrl))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(modulePhysicsSystem) + String(featureEditUrl))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(coreAlarmHelper))] = (colorCancelUtility.replacingOccurrences(of: "mid", with: "a"))
            //: msgInfo["senderId"] = PositionThen.share.loginUserMode.userID
            msgInfo[(String(commonInmateMessage.prefix(8)))] = PositionThen.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(appBringMessage.prefix(7)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(widgetSignatureText))] = (String(widgetMoreAlert.prefix(7)) + coreQuantityLogger.replacingOccurrences(of: "end", with: "g"))
            //: dic["params"] = params
            dic[(String(displayMaterialLogger))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = sweepUp(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? CompartmentReactiveCompatible {
        if let reCellData = cellData as? CompartmentReactiveCompatible {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(kLanguageTitle.replacingOccurrences(of: "railroad", with: "te"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(coreAlarmHelper))] = (k_courseSettings.replacingOccurrences(of: "title", with: "t"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(appBringMessage.prefix(7)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(widgetSignatureText))] = (k_filterFormat.uppercased() + String(screenAgainstPath.prefix(5)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(layoutSampleUtility) + screenStretchName.lowercased())] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.indexMenu() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(sessionLengthShowKey.prefix(7)) + String(coreShapeSettings))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(layoutScreenTimer))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(displayMaterialLogger))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = sweepUp(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ThereMessageReactiveCompatible? {
    public class func paraFor(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ThereMessageReactiveCompatible? {
        //: var cellData: ThereMessageReactiveCompatible?
        var cellData: ThereMessageReactiveCompatible?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(layoutAsideAidSystem))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(appBringMessage.prefix(7)))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(moduleAllyKey.replacingOccurrences(of: "project", with: "u"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? StallModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == PositionThen.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == PositionThen.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == PositionThen.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == PositionThen.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(screenResumeTitle.suffix(6)) + serviceToHelper.replacingOccurrences(of: "scar", with: "p"))] as? String == (String(componentMapUtility.prefix(6)) + "s:Au" + String(commonLittlePictureAcrossData.suffix(6)) + "t:TxtCu" + String(screenListenFormat)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(viewPreferencePage))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = cellType(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(coreAlarmHelper))] as? String == (String(moduleTranslationUtility)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = StallModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? SensualModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MinimizeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(kLanguageTitle.replacingOccurrences(of: "railroad", with: "te"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = SinceFamousReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SinceFamousReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = RationalCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RationalCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ResumeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = OwnThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = OwnThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = innerCrushData(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(layoutAsideAidSystem))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(appBringMessage.prefix(7)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? TalkingStepTheoreticalAccountModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = FeatureCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = FeatureCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = FirstThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = FirstThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? FirstThen {
            if let audioCellData = cellData as? FirstThen {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = approximation(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = CommunicationMsgTable.rueWithIt(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = CommunicationMsgTable.mentionDb(gallery(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CorrelateEveryThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(spacingSpotLogger))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(kLanguageTitle.replacingOccurrences(of: "railroad", with: "te"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(kConversionName))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PingCrashCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = FluThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FluThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = CompartmentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CompartmentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? HeavyChatModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(kIconTinPlatform.replacingOccurrences(of: "path", with: "co"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: PositionThen.share.loginUserMode.headPic ?? ""
                        PositionThen.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = TalkingStepTheoreticalAccountModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(kLanguageTitle.replacingOccurrences(of: "railroad", with: "te"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(layoutAsideAidSystem))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = BowDownModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(moduleCommunicationPath))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(layoutAssistantName.suffix(7)) + String(displayBestName))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.snaplineContent(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(appReadId.replacingOccurrences(of: "pick", with: "st"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(layoutHairTitle) + String(themeRequireMetrics))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(layoutBlockSystem.replacingOccurrences(of: "communication", with: "es") + "ageTyp" + String(screenPeerPreference))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(themeThematicUrl))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(spacingThenVarietyError.suffix(6)) + "ansl" + String(colorRegionValue.prefix(6)) + screenBehaviorValue.replacingOccurrences(of: "simultaneously", with: "tu"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = OrientationHashableRepresentation(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(colorOwnerMessage.prefix(6)) + String(k_sharedLogger))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = HighwayModel.lastLink()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = HighwayModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ResignModel.loadUp()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ResignModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = NutModel.stillDraw()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func cellType(_ extraDic: [String: Any]) -> CurrentConstantTarget {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(layoutAsideAidSystem))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(appBringMessage.prefix(7)))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? StallModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(componentAnalyzeKey.replacingOccurrences(of: "far", with: "o") + "atType")] as? String == (String(sessionWritId)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(featureSeaCalculationLogger))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(kRompPath.prefix(8)) + String(coreBackgroundMakerDevice))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(colorTeamEvent.prefix(8))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = existent(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(commonAdminName))] as? Int) == 1 || (msgInfo?[(String(commonAdminName))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(coreAlarmHelper))] as? String == (String(kInsideTitle)) || (dic?[(layoutBlockSystem.replacingOccurrences(of: "communication", with: "es") + "ageTyp" + String(screenPeerPreference))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(componentAnalyzeKey.replacingOccurrences(of: "far", with: "o") + "atType")] as? String == "mf") && ((dic?[(layoutBlockSystem.replacingOccurrences(of: "communication", with: "es") + "ageTyp" + String(screenPeerPreference))] as? Int) == 15 || (dic?[(layoutBlockSystem.replacingOccurrences(of: "communication", with: "es") + "ageTyp" + String(screenPeerPreference))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(coreAlarmHelper))] as? String == (String(componentRetailerAlert) + widgetProcedureDevice.replacingOccurrences(of: "beam", with: "t")) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(coreAlarmHelper))] as? String == (String(kCenterSystem)) || (msgInfo?[(layoutBlockSystem.replacingOccurrences(of: "communication", with: "es") + "ageTyp" + String(screenPeerPreference))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func gallery(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.snaplineContent(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(layoutAsideAidSystem))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(appBringMessage.prefix(7)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(moduleAllyKey.replacingOccurrences(of: "project", with: "u"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(commonInmateMessage.prefix(8)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == PositionThen.share.loginUserMode.userID {
        if senderId == PositionThen.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(commonInmateMessage.prefix(8)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(colorRouteMaximEvent.replacingOccurrences(of: "sauce", with: "t") + String(sessionMotionEvent.prefix(2)))] = message.userID ?? (user?[(colorRouteMaximEvent.replacingOccurrences(of: "sauce", with: "t") + String(sessionMotionEvent.prefix(2)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(commonInmateMessage.prefix(8)))] = "\(msgInfo?[(String(kCapSettings))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(colorRouteMaximEvent.replacingOccurrences(of: "sauce", with: "t") + String(sessionMotionEvent.prefix(2)))] = "\(msgInfo?[(String(kCapSettings))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(themeThematicUrl))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(themeThematicUrl))] = "\(msgInfo?[(String(themeThematicUrl))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.snaplineContent(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(themeThematicUrl))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(themeThematicUrl))] = "\(customDic[(String(themeThematicUrl))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(themeThematicUrl))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(modulePhysicsSystem) + String(featureEditUrl))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(modulePhysicsSystem) + String(featureEditUrl))] = "\(msgInfo?[(String(modulePhysicsSystem) + String(featureEditUrl))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((layoutStayChannelPlatform.lowercased() + "oLengt" + commonFeedbackPage.replacingOccurrences(of: "ting", with: "h"))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(layoutStayChannelPlatform.lowercased() + "oLengt" + commonFeedbackPage.replacingOccurrences(of: "ting", with: "h"))] = "\(msgInfo?[(layoutStayChannelPlatform.lowercased() + "oLengt" + commonFeedbackPage.replacingOccurrences(of: "ting", with: "h"))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (featureSweetKey.replacingOccurrences(of: "may", with: "y") + "-MM-d" + String(screenMeetPage.suffix(4)) + String(commonLateData.prefix(6)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(themeCountoMessage) + String(serviceTransactionText))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == PositionThen.share.loginUserMode.userID ? "1" : "0"
        info?[(String(sessionWrapMiddleAdvertisingConfig.prefix(6)))] = senderId == PositionThen.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func approximation(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = gallery(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(themeThematicUrl))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func innerCrushData(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func drownMap(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func existent(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(coreAlarmHelper))] as? String, msgType == (colorCancelUtility.replacingOccurrences(of: "mid", with: "a")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(sessionBlankUrl.replacingOccurrences(of: "aim", with: "nt") + String(k_salePlatform.suffix(7)))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(widgetMoreAlert.prefix(7)) + coreQuantityLogger.replacingOccurrences(of: "end", with: "g"))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(appSurfaceSouAchievementSettings.replacingOccurrences(of: "ring", with: "o") + String(commonBlackThroughSession))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(appAuctionMessage) + String(componentAboveFormat.prefix(6)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
