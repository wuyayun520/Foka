
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let colorWaistIgnoreError:[UInt8] = [0x1f,0x18,0x1f,0x2,0x5e,0x15,0x19,0x12,0x13,0x4,0x4c,0x5f,0x56,0x1e,0x17,0x5,0x56,0x18,0x19,0x2,0x56,0x14,0x13,0x13,0x18,0x56,0x1f,0x1b,0x6,0x1a,0x13,0x1b,0x13,0x18,0x2,0x13,0x12]

private func cowLiteMove(arch num: UInt8) -> UInt8 {
    return num ^ 118
}

/*: "F5F5F5" :*/
fileprivate let displayShapeEvent:[Character] = ["F","5","F","5","F","5"]

/*: "#128CFF" :*/
fileprivate let modulePoseFormat:String = "#128CFdecide boy yourself smart"
fileprivate let commonBlankMaxKey:String = "favor"

/*: "Feedback" :*/
fileprivate let viewCustomSpecificallyEvent:String = "Feedbackcomment long someone voice glass"

/*: "#8370FF" :*/
fileprivate let featureBladeName:String = "score yourself#8370F"
fileprivate let coreArcListForgetUtility:[Character] = ["F"]

/*: "Quick Greeting" :*/
fileprivate let commonPoMessage:String = "move firstQuick G"
fileprivate let kOccasionHelper:String = "withine"
fileprivate let viewParkKey:String = "makerting"

/*: "Beauty Photos" :*/
fileprivate let styleExampleData:String = "Beauhome third log previous"
fileprivate let serviceQuantMomentMetrics:String = "recessotos"

/*: "url" :*/
fileprivate let appTheConfig:String = "udemand"

/*: "unlockGift" :*/
fileprivate let coreButterText:String = "unlocthird"
fileprivate let screenApologyPreference:String = "welcome daily basic relatedGift"

/*: "isBurn" :*/
fileprivate let featureThreadResignUtility:[UInt8] = [0x5e,0x44,0x75,0x42,0x45,0x59]

private func forgetCondition(passe num: UInt8) -> UInt8 {
    return num ^ 55
}

/*: "giftId" :*/
fileprivate let serviceChampionPlatform:String = "expression identitygiftId"

/*: "rYMsgType" :*/
fileprivate let serviceLifeFailureConfig:[Character] = ["r","Y"]
fileprivate let serviceStallFormat:String = "MsgTypephysical gesture"

/*: "GJ:CallCustom" :*/
fileprivate let sessionInsideId:[Character] = ["G","J",":","C","a","l","l","C","u","s","t"]
fileprivate let componentTributeValue:String = "otender"

/*: "changeDirection" :*/
fileprivate let widgetExVersionKingName:String = "chaholdere"
fileprivate let k_announcementLogger:String = "ctiable"

/*: "extra" :*/
fileprivate let styleLaterAlert:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let appLabelMiniUrl:String = "lure generatemsgInfo"

/*: "toUid" :*/
fileprivate let layoutElementEvent:String = "recommendation buttertoUid"

/*: "wantGift" :*/
fileprivate let featurePropertySettings:String = "indicator heightwantGift"

/*: "jumpKey" :*/
fileprivate let kDepressTitle:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let coreCertainUrl:String = "doc discount require unlesstrueP"
fileprivate let spacingValidPlatform:[Character] = ["e","r","s","o","n","A","u","t","h"]

/*: "logType" :*/
fileprivate let componentLaboratoryName:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let themePickDevice:[UInt8] = [0x6f,0x75,0x69,0x56,0x7b,0x72,0x67]

fileprivate func earnEnterprise(owner num: UInt8) -> UInt8 {
    let value = Int(num) + 254
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "txt" :*/
fileprivate let coreCalledFluCombinePreference:String = "tbraket"

/*: "type" :*/
fileprivate let componentAbleDevice:String = "cleanype"

/*: "QuickMsg" :*/
fileprivate let sessionSignalPanTimer:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let serviceTextMyEvent:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "content" :*/
fileprivate let screenShouldGivePairTitle:[UInt8] = [0x5f,0x6b,0x6a,0x70,0x61,0x6a,0x70]

fileprivate func woodChance(enough num: UInt8) -> UInt8 {
    let value = Int(num) - 252
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "txtId" :*/
fileprivate let componentBehaviorData:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let themeAnalyzeHelper:[UInt8] = [0xe2,0xda,0xe8,0xe8,0xd6,0xdc,0xda]

fileprivate func inPublic(camera num: UInt8) -> UInt8 {
    let value = Int(num) + 139
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let displayTerriblePlatform:String = "TxtMsgcurrency purple somebody fault"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let layoutPackageCoverPlatform:[UInt8] = [0x38,0x1,0x7,0xd,0xb,0x4e,0x6,0xb,0x2,0x2,0x1,0x4e,0xa,0x1,0x19,0x0,0x2,0x1,0xf,0xa,0x4e,0x8,0xf,0x7,0x2,0xb,0xa,0x40,0x4e,0x3e,0x1c,0x1,0xd,0xb,0xa,0x1b,0x1c,0xb]

private func lightPresentation(sign num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "length" :*/
fileprivate let styleStumbleCityPlatform:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let appNavalAlert:String = "auourio"

/*: "audioId" :*/
fileprivate let widgetIconCountPath:[Character] = ["a","u","d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let displayCommandPath:[UInt8] = [0xa6,0xb2,0xa3,0xae,0xa8,0x8b,0xa2,0xa9,0xa0,0xb3,0xaf]

private func secureCut(meeting num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "audioSandbox" :*/
fileprivate let colorChartSilverSession:String = "hang waistaudioS"
fileprivate let screenAppDevice:[Character] = ["a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let layoutSequenceAlert:[UInt8] = [0xdc,0xca,0xc1,0xcb,0xca,0xdd,0xe6,0xcb]

/*: "AudioMsg" :*/
fileprivate let sessionSubjectData:String = "lowly"
fileprivate let sessionMakeSystem:String = "illegal point lean join ideaudioMsg"

/*: "Can't send blank message" :*/
fileprivate let commonCanValue:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let kTellConfig:[Character] = ["q","u","o"]
fileprivate let screenAutomaticallySession:[Character] = ["t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let styleCropFormat:String = "toxic cyclequoteMsg"
fileprivate let componentPrincipalKey:String = "Infolater imitation lip tick drop"

/*: "contentType" :*/
fileprivate let featureRelationTitle:[UInt8] = [0xac,0xa0,0xa1,0xbb,0xaa,0xa1,0xbb,0x9b,0xb6,0xbf,0xaa]

private func wordMissing(security num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "ImgMsg" :*/
fileprivate let componentEdgePage:String = "fatal failureImgMsg"

/*: "img" :*/
fileprivate let coreCombinePage:String = "imcorrect"

/*: "imageUri" :*/
fileprivate let widgetExcusePath:[UInt8] = [0x9b,0x9f,0x93,0x95,0x97,0xa7,0x80,0x9b]

private func sweetArch(part num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "customSend" :*/
fileprivate let layoutAddedSession:[UInt8] = [0xa9,0xbf,0xb9,0xbe,0xa5,0xa7,0x99,0xaf,0xa4,0xae]

/*: "pic" :*/
fileprivate let spacingPortionHelper:String = "pbench"

/*: "VideoMsg" :*/
fileprivate let coreDangerousLureTimer:String = "closely magnitude form contentVid"
fileprivate let componentHisCalledAlert:String = "present orange processoreoMsg"

/*: "video" :*/
fileprivate let commonWouldWorldValue:String = "VIDEO"

/*: "coverImg" :*/
fileprivate let commonKitEvent:[UInt8] = [0x1e,0x12,0xb,0x18,0xf,0x34,0x10,0x1a]

/*: "videoUrl" :*/
fileprivate let screenRollingLogger:[UInt8] = [0x14,0xb,0x6,0x7,0xd,0x37,0x10,0xe]

private func hungThan(panel num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "duration" :*/
fileprivate let screenWithinName:[UInt8] = [0x6e,0x7f,0x7c,0x6b,0x7e,0x73,0x79,0x78]

fileprivate func labelTape(broad num: UInt8) -> UInt8 {
    let value = Int(num) - 10
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let featureSomeoneDevice:[UInt8] = [0xd2,0xdb,0xd0]

private func nurseLog(eh num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "Documents/User/voice/" :*/
fileprivate let spacingDifferenceSession:String = "butterfly selfDocu"
fileprivate let moduleSecretSettings:String = "s/Usenot well"
fileprivate let moduleThanPage:String = "rescue trick month premiumce/"

/*: "Documents/User/record/" :*/
fileprivate let featureDenyError:String = "clean differenceDocumen"
fileprivate let commonEquivalentData:[Character] = ["t"]
fileprivate let colorOperationUtility:String = "s/Uspreserve butterfly addition"
fileprivate let spacingMediaMetrics:String = "hide to competitionord/"

/*: "msgIncome" :*/
fileprivate let spacingWarnUtilityPreference:[Character] = ["m","s","g","I"]
fileprivate let viewHemName:String = "nactualome"

/*: "mfBean" :*/
fileprivate let commonSubstanceUtility:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "messageType" :*/
fileprivate let spacingConSettings:String = "mequantquanta"

/*: "WantGift" :*/
fileprivate let coreReduceAmberEvent:String = "WantGiftcategory leading"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let appBorderSession:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s"]
fileprivate let displayWouldName:String = "guideline actual ilk lose quitTitle_"

/*: "msgId" :*/
fileprivate let layoutMovieApplicationData:String = "space messagemsgId"

/*: "totalMfCoin" :*/
fileprivate let componentManagerConFutureUrl:[Character] = ["t","o","t","a","l","M","f","C"]
fileprivate let layoutConfirmMessage:[Character] = ["o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let commonBetterEvent:[Character] = ["a","t","t","a"]
fileprivate let coreEyeSession:[Character] = ["c","h","S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let featureFieldLogger:[Character] = ["i","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let widgetHidePath:String = "navalonavalal"
fileprivate let featureMobileData:String = "ATE"

/*: "uid" :*/
fileprivate let layoutPureId:[UInt8] = [0xa2,0xbe,0xb3]

private func tagDoing(take num: UInt8) -> UInt8 {
    return num ^ 215
}

/*: "intimate" :*/
fileprivate let commonMineFormat:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let displayFileHelper:[UInt8] = [0xce,0xec,0xe3,0xaa,0xf9,0xad,0xf8,0xfe,0xe8,0xad,0xf9,0xe5,0xe8,0xad,0xeb,0xf8,0xe3,0xee,0xf9,0xe4,0xe2,0xe3,0xad,0xe9,0xf8,0xe8,0xad,0xf9,0xe2,0xad,0xf4,0xe2,0xf8,0xff,0xad,0xe1,0xec,0xee,0xe6,0xad,0xe2,0xeb,0xad,0xe4,0xe3,0xf9,0xe4,0xe0,0xec,0xee,0xf4,0xad,0xe1,0xe8,0xfb,0xe8,0xe1,0xa3]

/*: "giftNum" :*/
fileprivate let featureMediaObjectHelper:String = "giftNumsoftly makeup without golf"

/*: "pkgItemsetId" :*/
fileprivate let widgetTradeData:String = "transaction guarantee reserve safetypkgIte"

/*: "from" :*/
fileprivate let screenCastStartBrightMessage:String = "frochance"

/*: "wantGiftId" :*/
fileprivate let k_tinAlert:String = "sequence movie analysis month disablewantGi"
fileprivate let appAddedSession:[Character] = ["f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let kInstanceDevice:[Character] = ["W","a","n","t","G","i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let featureDraftUrl:String = "gicute"

/*: "View info" :*/
fileprivate let viewGenderFormat:String = "View iplayer clock am random s"
fileprivate let widgetExternalPage:String = "nreach"

/*: "Notes" :*/
fileprivate let widgetBulletUrl:String = "Notespublish inmate editor"

/*: "Report" :*/
fileprivate let componentReportDuringData:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let featureLessPage:String = "table sweetView r"
fileprivate let moduleStopContent:String = "tapctapi"

/*: "She" :*/
fileprivate let componentCourseData:String = "fall spread vote dry bagShe"

/*: "%@ is using too low a version" :*/
fileprivate let serviceInfoName:String = "%@ is style four lit both"
fileprivate let featureBridgeData:String = " too lunion eye album dock range"
fileprivate let styleChildSession:String = "suspend opening versio"
fileprivate let k_runMenuSettings:[Character] = ["n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let widgetDarkError:[Character] = ["S","h","o","o","t"]
fileprivate let screenRedSession:String = "cooper rush administrative plate vid"
fileprivate let componentTooAlert:String = "LEAST"
fileprivate let viewProfileData:String = "condthen"

/*: "Upload failed" :*/
fileprivate let featureResumeTimer:String = "going suitable advice tremendous sequenceUplo"
fileprivate let viewParentPath:[Character] = ["a","d"," ","f","a","i","l","e","d"]

/*: "播放状态: :*/
fileprivate let screenNearPreference:String = "\u{64ad}放状态:"

/*: "c2c_" :*/
fileprivate let colorCreationData:String = "face good cloud bouncec2c_"

/*: "msg" :*/
fileprivate let k_fitUtility:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let featureConnectAlert:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let styleCoordinatePath:[UInt8] = [0x5f,0x69,0x73,0x26,0x65,0x67,0x68,0x26,0x73,0x75,0x63,0x26,0x72,0x6e,0x63,0x26,0x70,0x6f,0x62,0x63,0x69,0x26,0x65,0x6e,0x67,0x72,0x26,0x60,0x63,0x67,0x72,0x73,0x74,0x63,0x26,0x72,0x69,0x26,0x61,0x6f,0x70,0x63,0x26,0x23,0x46,0x26,0x67,0x26,0x65,0x67,0x6a,0x6a]

/*: "cellData" :*/
fileprivate let sessionOptionId:String = "notice resource okcellD"
fileprivate let viewCollectionPage:String = "ATA"

/*: "System notification" :*/
fileprivate let viewMiddlePage:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a","t","i"]
fileprivate let appUtterError:String = "award"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReverseBannerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum MapFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ReverseBannerDelegate: GroupThen {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = SubstanceHandyJSON()
    private var userWrap = SubstanceHandyJSON() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = MapFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: SubstanceHandyJSON = SubstanceHandyJSON()) {
    init(chatID: String, isFrom: MapFromEnum = .Normal, userWrap: SubstanceHandyJSON = SubstanceHandyJSON()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ReverseBannerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        DiscoverReactiveCompatible.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == FragThen.getXiaoMiID() {
        if targetId == FragThen.ringRowingVertical() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == FragThen.getCustomerServiceID() {
        } else if targetId == FragThen.opHistory() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: colorWaistIgnoreError.map{cowLiteMove(arch: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PositionThen.share.appConfigMode.disableShootScreen {
            if PositionThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        orientation()
        //: createUI()
        dateUi()
        //: createUIConstraints()
        utilised()
        //: bindInteraction()
        statSumeraction()
        //: addTapGestureRecognizer()
        cellSince()
        //: func__requestChatInfo()
        applicationToBackground()
        //: func__getFillDraftText()
        need()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        EvolutionObjectProtocol.shared.pleasingness()
        //: func__addVoiceNotification()
        noArrow()
        //: addReadPeceiptNotification()
        someLay()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            equalAdjust()
            //: func__TipsNotification()
            recognizeNotification()
            //: if PositionThen.share.loginUserMode.sex == Gender.male.rawValue {
            if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue {
                //: func__setReplyTipsView()
                book()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        refill(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        refill(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            salveDownCircle()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            NothingRatingManager.shared.sequenceOff()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: WillGalleryTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = WillGalleryTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(displayShapeEvent)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ForagingThen.self, forCellReuseIdentifier: ForagingThen.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(OwnGrantViewCell.self, forCellReuseIdentifier: OwnGrantViewCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(ErrorThen.self, forCellReuseIdentifier: ErrorThen.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(RecallMsgCell.self, forCellReuseIdentifier: RecallMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ReferentMsgCell.self, forCellReuseIdentifier: ReferentMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(EnhanceView.self, forCellReuseIdentifier: EnhanceView.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(MakeupViewCell.self, forCellReuseIdentifier: MakeupViewCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(GrantDeadlineViewCell.self, forCellReuseIdentifier: GrantDeadlineViewCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(SauceViewCell.self, forCellReuseIdentifier: SauceViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: FlatSolidMessageCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(MarqueReactiveCompatible.self, forCellReuseIdentifier: FlatSolidMessageCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: RenameErrorDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = RenameErrorDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.databaseWork()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: OpportunityTamViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: EditViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = OpportunityTamViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(modulePoseFormat.prefix(6)) + commonBlankMaxKey.replacingOccurrences(of: "favor", with: "F")))!, .font: UIFont.modelSize(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(viewCustomSpecificallyEvent.prefix(8))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(marketplaceAndClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: AnimatReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = AnimatReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: MeteorologicalSatelliteTextView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = MeteorologicalSatelliteTextView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(featureBladeName.suffix(6)) + String(coreArcListForgetUtility))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(commonPoMessage.suffix(7)) + kOccasionHelper.replacingOccurrences(of: "within", with: "r") + viewParkKey.replacingOccurrences(of: "maker", with: "e")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.tributeChromaticColour().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(visualClick), for: .touchUpInside)
        //: btn.isHidden = !PositionThen.share.appUserConfigMode.showDirectGreet || PositionThen.share.loginUserMode.sex == Gender.male.rawValue || PositionThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !PositionThen.share.appUserConfigMode.showDirectGreet || PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue || PositionThen.share.appStatus == BetterQuantity.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: DecisionView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = DecisionView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: MinimizeTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = MinimizeTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.buildElement(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ResultReactiveCompatible = {
        //: let v = TalkingTranslationResultView()
        let v = ResultReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.accessDown(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(featureBladeName.suffix(6)) + String(coreArcListForgetUtility))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(styleExampleData.prefix(4)) + "ty P" + serviceQuantMomentMetrics.replacingOccurrences(of: "recess", with: "h")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.tributeChromaticColour().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(primaryClick), for: .touchUpInside)
        //: btn.isHidden = PositionThen.share.loginUserMode.sex == Gender.male.rawValue || PositionThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue || PositionThen.share.appStatus == BetterQuantity.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ExternalThen = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ExternalThen()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: ErrorView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = ErrorView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: KingRecognizerDelegate = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = KingRecognizerDelegate()
        //: v.isHidden = (PositionThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (PositionThen.share.appStatus == BetterQuantity.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: BetterThen = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = BetterThen()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.analogDigitalConverter()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ReverseBannerDelegate: RunNumberoThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func labelWho(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if PositionThen.share.loginUserMode.sex == Gender.male.rawValue {
                if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if PositionThen.share.loginUserMode.sex == Gender.male.rawValue {
                if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.ask(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ReverseBannerDelegate: SavingDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func visualClick() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        DiscoverReactiveCompatible.fast(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.colouration(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.now(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func primaryClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        DiscoverReactiveCompatible.wearer(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = HereReactiveCompatible()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().pup(urlStr: dict?[(appTheConfig.replacingOccurrences(of: "demand", with: "rl"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(coreButterText.replacingOccurrences(of: "third", with: "k") + String(screenApologyPreference.suffix(4)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = MaestroModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: featureThreadResignUtility.map{forgetCondition(passe: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(serviceChampionPlatform.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.scanLayer(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ThereMessageReactiveCompatible! {
    func errorMessage(_: WillGalleryTableViewController!, brake data: V2TIMMessage!) -> ThereMessageReactiveCompatible! {
        //: let dic = DisableChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = DisableChatManager.monthBroad(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = ChatThen.paraFor(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: CorrelateEveryThen.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(serviceLifeFailureConfig) + String(serviceStallFormat.prefix(7)))].stringValue == (String(sessionInsideId) + componentTributeValue.replacingOccurrences(of: "tender", with: "m")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(widgetExVersionKingName.replacingOccurrences(of: "holder", with: "ng") + "Dire" + k_announcementLogger.replacingOccurrences(of: "able", with: "on"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(styleLaterAlert))][(String(appLabelMiniUrl.suffix(7)))][(String(layoutElementEvent.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = CompartmentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = CompartmentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: FirstThen.self) == true ||
        if cellData?.isKind(of: FirstThen.self) == true ||
            //: cellData?.isKind(of: SinceFamousReactiveCompatible.self) == true ||
            cellData?.isKind(of: SinceFamousReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: CompartmentReactiveCompatible.self) == true ||
            cellData?.isKind(of: CompartmentReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: RationalCellData.self) == true {
            cellData?.isKind(of: RationalCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            DiscoverReactiveCompatible.itemTalkSpark(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            DiscoverReactiveCompatible.countFull(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ThereMessageReactiveCompatible!) -> FlatSolidMessageCell! {
    func activeScreen(_: WillGalleryTableViewController!, application data: ThereMessageReactiveCompatible!) -> FlatSolidMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = splitData(data: data)

        //: if data.isKind(of: CompartmentReactiveCompatible.self) {
        if data.isKind(of: CompartmentReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ForagingThen(style: .default, reuseIdentifier: ForagingThen.className())
            //: let cellData = data as! CompartmentReactiveCompatible
            let cellData = data as! CompartmentReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.indexMenu() {
                //: if cellData.msgModel.quoteMsgInfo.uid == PositionThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == PositionThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = PositionThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = PositionThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: SinceFamousReactiveCompatible.self) {
        if data.isKind(of: SinceFamousReactiveCompatible.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = OwnGrantViewCell(style: .default, reuseIdentifier: OwnGrantViewCell.className())
            //: let cellData = data as! SinceFamousReactiveCompatible
            let cellData = data as! SinceFamousReactiveCompatible
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: RationalCellData.self) {
        if data.isKind(of: RationalCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = ErrorThen(style: .default, reuseIdentifier: ErrorThen.className())
            //: let cellData = data as! RationalCellData
            let cellData = data as! RationalCellData
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: OwnThen.self) {
        if data.isKind(of: OwnThen.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = RecallMsgCell(style: .default, reuseIdentifier: RecallMsgCell.className())
            //: let cellData = data as! OwnThen
            let cellData = data as! OwnThen
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: FeatureCellData.self) {
        if data.isKind(of: FeatureCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ReferentMsgCell(style: .default, reuseIdentifier: ReferentMsgCell.className())
            //: let cellData = data as! FeatureCellData
            let cellData = data as! FeatureCellData
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = SizeSignatureGiftModel()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.describeLaunch(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(featurePropertySettings.suffix(8))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: FirstThen.self) {
        if data.isKind(of: FirstThen.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = GrantDeadlineViewCell(style: .default, reuseIdentifier: GrantDeadlineViewCell.className())
            //: let cellData = data as! FirstThen
            let cellData = data as! FirstThen
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: CorrelateEveryThen.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = MakeupViewCell(style: .default, reuseIdentifier: MakeupViewCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! CorrelateEveryThen
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: MinimizeCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = EnhanceView(style: .default, reuseIdentifier: EnhanceView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! MinimizeCellData
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: PingCrashCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = SauceViewCell(style: .default, reuseIdentifier: SauceViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! PingCrashCellData
            //: cell.fill(with: cellData)
            cell.minority(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: FlatSolidMessageCell!) {
    func available(_: WillGalleryTableViewController!, status cell: FlatSolidMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != FragThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != FragThen.opHistory() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            ProdPushManager.share.untilBuild(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: FlatSolidMessageCell!) {
    func media(_: WillGalleryTableViewController!, messageStop cell: FlatSolidMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: OwnGrantViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! SinceFamousReactiveCompatible
            let imageCellData = cell.messageData as! SinceFamousReactiveCompatible
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = ReachReactiveCompatible(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.count(imageCellData, modifyChinModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.cullOut(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: coreCreateEvent)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.income(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.party(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kProgressTimer)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = PackageHandyJSON()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = NothingMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [PackageHandyJSON] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = RushTermPhotosVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ErrorThen.self) { // 视频
            //: let videoCellData = cell.messageData as! RationalCellData
            let videoCellData = cell.messageData as! RationalCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = AboardViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = MakeupTermRecognizerDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.copernicanSystem(videoCellData, sumerest: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.cullOut(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: coreCreateEvent)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.income(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.party(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                getController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: GrantDeadlineViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! GrantDeadlineViewCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! FirstThen, cellView: audioCell)
            compartmentMoment(cellData: audioCell.cellData as! FirstThen, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ForagingThen.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ForagingThen
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = DisableChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = DisableChatManager.monthBroad(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(styleLaterAlert))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(kDepressTitle))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(kDepressTitle))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(coreCertainUrl.suffix(5)) + String(spacingValidPlatform)) {
                    //: TalkingAppPushManager.share.func__pushUserVerifyController(toast: nil)
                    ProdPushManager.share.duringToast(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<GrantMessageJumpTitleTheoryMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ProdPushManager.share.selfInput(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(serviceLifeFailureConfig) + String(serviceStallFormat.prefix(7)))] as? String == (String(sessionInsideId) + componentTributeValue.replacingOccurrences(of: "tender", with: "m")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(appLabelMiniUrl.suffix(7)))][(String(componentLaboratoryName))].intValue == 1 {
                    //: checkAndCallVoice()
                    markPhonation()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    noticeVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: EnhanceView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! EnhanceView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = DisableChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = DisableChatManager.monthBroad(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(styleLaterAlert))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(kDepressTitle))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<GrantMessageJumpTitleTheoryMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TalkingAppPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ProdPushManager.share.selfInput(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: MakeupViewCell.self) {
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: targetId)
            ProdPushManager.share.untilBuild(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: FlatSolidMessageCell!) {
    func cur(_: WillGalleryTableViewController!, result cell: FlatSolidMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: CrashCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: OwnGrantViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! OwnGrantViewCell
            //: cellData = imageCell.cellData as! SinceFamousReactiveCompatible
            cellData = imageCell.cellData as! SinceFamousReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ErrorThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ErrorThen
            //: cellData = videoCell.cellData as! RationalCellData
            cellData = videoCell.cellData as! RationalCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: GrantDeadlineViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! GrantDeadlineViewCell
            //: cellData = audioCell.cellData as! FirstThen
            cellData = audioCell.cellData as! FirstThen
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ForagingThen.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ForagingThen
            //: cellData = textCell.cellData as! CompartmentReactiveCompatible
            cellData = textCell.cellData as! CompartmentReactiveCompatible
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: ThereMessageReactiveCompatible? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: ThereMessageReactiveCompatible? = ChatThen.dismissData(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: RationalCellData.self) {
                if reCellData!.isKind(of: RationalCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    presentLikeComputerSimulation(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    obstructed(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: ThereMessageReactiveCompatible!) {
    func action(_: WillGalleryTableViewController!, descriptionMessage cellData: ThereMessageReactiveCompatible!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = DiscoverReactiveCompatible.moment(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: ThereMessageReactiveCompatible!) {
    func active(_: WillGalleryTableViewController!, will cellData: ThereMessageReactiveCompatible!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        DiscoverReactiveCompatible.ainId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: ThereMessageReactiveCompatible) -> ThereMessageReactiveCompatible {
    private func splitData(data: ThereMessageReactiveCompatible) -> ThereMessageReactiveCompatible {
        //: let isSelf = data.msgModel.user.uid == Int(PositionThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(PositionThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if PositionThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if PositionThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = PositionThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = PositionThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = PositionThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = PositionThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func withhold() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (coreCalledFluCombinePreference.replacingOccurrences(of: "brake", with: "x"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (String(sessionSignalPanTimer))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.obstructed(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func now(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: screenShouldGivePairTitle.map{woodChance(enough: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: screenShouldGivePairTitle.map{woodChance(enough: $0)}, encoding: .utf8)!: content, (String(componentBehaviorData)): greetId, String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (coreCalledFluCombinePreference.replacingOccurrences(of: "brake", with: "x"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: themeAnalyzeHelper.map{inPublic(camera: $0)}, encoding: .utf8)!: content, (String(componentBehaviorData)): greetId, (componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (String(displayTerriblePlatform.prefix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.obstructed(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            ThereTaskManager.shared.sort(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.colouration(showMsg: String(bytes: layoutPackageCoverPlatform.map{lightPresentation(sign: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        DiscoverReactiveCompatible.dataTextDark(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(styleStumbleCityPlatform))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (appNavalAlert.replacingOccurrences(of: "our", with: "d")),
                               //: "audioId": greetId,
                               (String(widgetIconCountPath)): greetId,
                               //: "audioLength": length,
                               String(bytes: displayCommandPath.map{secureCut(meeting: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(colorChartSilverSession.suffix(6)) + String(screenAppDevice)): model.unZipFilePath,
                               //: "senderId": PositionThen.share.loginUserMode.userID]
                               String(bytes: layoutSequenceAlert.map{$0^175}, encoding: .utf8)!: PositionThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(widgetIconCountPath)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: displayCommandPath.map{secureCut(meeting: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(colorChartSilverSession.suffix(6)) + String(screenAppDevice)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (sessionSubjectData.replacingOccurrences(of: "lowly", with: "A") + String(sessionMakeSystem.suffix(7)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.obstructed(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func accessDown(msgStr: String, quoteModel: MergeModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            colouration(showMsg: (String(commonCanValue)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: screenShouldGivePairTitle.map{woodChance(enough: $0)}, encoding: .utf8)!: temStr, String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (coreCalledFluCombinePreference.replacingOccurrences(of: "brake", with: "x"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: themeAnalyzeHelper.map{inPublic(camera: $0)}, encoding: .utf8)!: temStr, (componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (String(displayTerriblePlatform.prefix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(appLabelMiniUrl.suffix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(kTellConfig) + String(screenAutomaticallySession))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(styleCropFormat.suffix(8)) + String(componentPrincipalKey.prefix(4)))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? MergeModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            obstructed(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func scanLayer(img: UIImage, sendModel: MaestroModel) {
        //: let imagePath = SinceFamousReactiveCompatible.getChatImagePath(image: img)
        let imagePath = SinceFamousReactiveCompatible.nextImage(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: featureRelationTitle.map{wordMissing(security: $0)}, encoding: .utf8)!: (String(componentEdgePage.suffix(6))), String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (coreCombinePage.replacingOccurrences(of: "correct", with: "g")), String(bytes: widgetExcusePath.map{sweetArch(part: $0)}, encoding: .utf8)!: imagePath, String(bytes: layoutAddedSession.map{$0^202}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(spacingPortionHelper.replacingOccurrences(of: "bench", with: "ic")): imagePath,
                                        //: "type": "ImgMsg",
                                        (componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (String(componentEdgePage.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: featureThreadResignUtility.map{forgetCondition(passe: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(serviceChampionPlatform.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            obstructed(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: ThereMessageReactiveCompatible?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func presentLikeComputerSimulation(msgData: ThereMessageReactiveCompatible?, sendModel: MaestroModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: featureRelationTitle.map{wordMissing(security: $0)}, encoding: .utf8)!: (String(coreDangerousLureTimer.suffix(3)) + String(componentHisCalledAlert.suffix(5))), String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (commonWouldWorldValue.lowercased()), String(bytes: layoutAddedSession.map{$0^202}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (String(coreDangerousLureTimer.suffix(3)) + String(componentHisCalledAlert.suffix(5))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: commonKitEvent.map{$0^125}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: screenRollingLogger.map{hungThan(panel: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: screenWithinName.map{labelTape(broad: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: featureThreadResignUtility.map{forgetCondition(passe: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(serviceChampionPlatform.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = ChatThen.sweepUp(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.contrast(byMessage: uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.principal(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.shrink(cellData, need: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.principal(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.hair(dict, wire: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        obstructed(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func path(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (appNavalAlert.replacingOccurrences(of: "our", with: "d")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: displayCommandPath.map{secureCut(meeting: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(colorChartSilverSession.suffix(6)) + String(screenAppDevice)): voicePath,
                       //: "senderId": PositionThen.share.loginUserMode.userID]
                       String(bytes: layoutSequenceAlert.map{$0^175}, encoding: .utf8)!: PositionThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(colorChartSilverSession.suffix(6)) + String(screenAppDevice)): voicePath,
                         //: "type": "AudioMsg",
                         (componentAbleDevice.replacingOccurrences(of: "clean", with: "t")): (sessionSubjectData.replacingOccurrences(of: "lowly", with: "A") + String(sessionMakeSystem.suffix(7))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: featureSomeoneDevice.map{nurseLog(eh: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            obstructed(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ThereMessageReactiveCompatible, reSend: Bool) {
    private func obstructed(msgData: ThereMessageReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colouration(showMsg: serviceDetailFoundValue)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(layoutElementEvent.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: RationalCellData.self) == false && msgData.isKind(of: FeatureCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: RationalCellData.self) == false, msgData.isKind(of: FeatureCellData.self) == false, params[(componentAbleDevice.replacingOccurrences(of: "clean", with: "t"))] as? String != (String(sessionSignalPanTimer)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.contrast(byMessage: msgData)
        }
        //: let recordTime = DisableChatManager.getMessageInsertTime()
        let recordTime = DisableChatManager.receiveTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((spacingPortionHelper.replacingOccurrences(of: "bench", with: "ic"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(spacingPortionHelper.replacingOccurrences(of: "bench", with: "ic"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(spacingPortionHelper.replacingOccurrences(of: "bench", with: "ic"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(componentAbleDevice.replacingOccurrences(of: "clean", with: "t"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (sessionSubjectData.replacingOccurrences(of: "lowly", with: "A") + String(sessionMakeSystem.suffix(7))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(colorChartSilverSession.suffix(6)) + String(screenAppDevice))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(colorChartSilverSession.suffix(6)) + String(screenAppDevice))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(colorChartSilverSession.suffix(6)) + String(screenAppDevice)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(spacingDifferenceSession.suffix(4)) + "ment" + String(moduleSecretSettings.prefix(5)) + "r/voi" + String(moduleThanPage.suffix(3)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = layoutErrorWorkInformationPlatform + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(featureDenyError.suffix(7)) + String(commonEquivalentData) + String(colorOperationUtility.prefix(4)) + "er/rec" + String(spacingMediaMetrics.suffix(4)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = viewMovieValue + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(widgetIconCountPath))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(appNavalAlert.replacingOccurrences(of: "our", with: "d"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ScriptRequestTool.visitorBy(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(sessionSignalPanTimer)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.domainPosition(msgData, equalCapture: nil, program: NSNumber(value: 0), lineMsgStatus: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.cullOut(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: screenChangeRequestContent)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(componentBehaviorData))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    DiscoverReactiveCompatible.dataTextDark(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.isometric(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(appLabelMiniUrl.suffix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(spacingWarnUtilityPreference) + viewHemName.replacingOccurrences(of: "actual", with: "c"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(spacingWarnUtilityPreference) + viewHemName.replacingOccurrences(of: "actual", with: "c"))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if InstrumentalityReactiveCompatible.aftermath().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: displayPointAlert, object: nil, userInfo: [String(bytes: commonSubstanceUtility.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.turnImage(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(spacingConSettings.replacingOccurrences(of: "quant", with: "s") + "geType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(sessionSignalPanTimer)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!] = (String(coreReduceAmberEvent.prefix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    themeSincePlatform.givingStream(eventID: screenSkipConnectHelper)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(sessionSignalPanTimer)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!] = (coreCalledFluCombinePreference.replacingOccurrences(of: "brake", with: "x"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.contrast(byMessage: updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(PositionThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(PositionThen.share.loginUserMode.userID)" + (String(appBorderSession) + String(displayWouldName.suffix(6))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if PositionThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue, type != (String(sessionSignalPanTimer)) {
                //: self.removeReplyTipsView()
                self.worldViewReplyTakeAwayWith()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.domainPosition(updateMsgData, equalCapture: msgInfo[(String(layoutMovieApplicationData.suffix(5)))] as? String, program: msgIncome, lineMsgStatus: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            DiscoverReactiveCompatible.countFull(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(componentManagerConFutureUrl) + String(layoutConfirmMessage))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(componentManagerConFutureUrl) + String(layoutConfirmMessage))] as! NSNumber
                //: PositionThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PositionThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(commonBetterEvent) + String(coreEyeSession))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(commonBetterEvent) + String(coreEyeSession))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.languageJaw(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(featureFieldLogger))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(featureFieldLogger))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? HighwayModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.count(updateMsgData, modifyChinModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((commonWouldWorldValue.lowercased())) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(commonWouldWorldValue.lowercased())] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ResignModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.copernicanSystem(updateMsgData, sumerest: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        down()

        //: updateSession()
        describeSession()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func describeSession() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        UsufructuaryThen.utilization(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func cullOut(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard PositionThen.share.loginUserMode.status != 1 else {
            guard PositionThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    colouration(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            ProdPushManager.share.responsibility(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.requestAcrossView()
            //: PositionThen.share.loginUserMode.freeMsgTimes = ""
            PositionThen.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.databaseWork()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = SubscribeThen(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent))
            //: view.show()
            view.exHusband()
            //: } else if errorCode == 60003, !PositionThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !PositionThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            SensitiveWindowManager.shared.sleeve()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                colouration(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func party(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(appLabelMiniUrl.suffix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.turnImage(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func local() {
        //: if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.modifyRefuse(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(PositionThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(PositionThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (screenOutputConfig.array(forKey: commonDepthPlatform)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.someone()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                screenOutputConfig.set(intimateCacheData, forKey: commonDepthPlatform)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func customer(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = DisableChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = DisableChatManager.monthBroad(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(styleLaterAlert))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(appLabelMiniUrl.suffix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(widgetHidePath.replacingOccurrences(of: "naval", with: "t") + "Intim" + featureMobileData.lowercased())]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.speakerTime(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func turnImage(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((widgetHidePath.replacingOccurrences(of: "naval", with: "t") + "Intim" + featureMobileData.lowercased())), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(widgetHidePath.replacingOccurrences(of: "naval", with: "t") + "Intim" + featureMobileData.lowercased())] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.speakerTime(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func speakerTime(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = UsufructuaryThen.befitting(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= PositionThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= PositionThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.modifyRefuse(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.pointerCommit(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.captureBy(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.captureBy(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        UsufructuaryThen.followingOf(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: layoutPureId.map{tagDoing(take: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: commonMineFormat.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: viewCoverAlert,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ReverseBannerDelegate: SomeoneReverseThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func monad() {
        //: guard PositionThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard PositionThen.share.loginUserMode.sex == PanelScalarLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.timePeriod(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func withRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func variantConnectFailure(actionType: UpToTheMinuteDefaultsSerializable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    aboutPhotos()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    exhibit()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                aboutPhotos()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if PositionThen.share.loginUserMode.sex == Gender.female.rawValue {
            if PositionThen.share.loginUserMode.sex == PanelScalarLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if PositionThen.share.loginUserMode.loungePlus {
                if PositionThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.coattailsEffectValid(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                exhibit()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    screenTransaction()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    exhibit()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                screenTransaction()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.head(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func exhibit() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.colouration(showMsg: String(bytes: displayFileHelper.map{$0^141}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func isListen(giftModel _: SizeSignatureGiftModel, giftNum _: String, model _: ManagerMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func female(giftModel: SizeSignatureGiftModel, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        describeLaunch(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: FeatureCellData?, fromStr: String?) {
    func describeLaunch(giftModel: SizeSignatureGiftModel, giftNum: String, cellData: FeatureCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colouration(showMsg: serviceDetailFoundValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(layoutElementEvent.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(serviceChampionPlatform.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(featureMediaObjectHelper.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(widgetTradeData.suffix(6)) + "msetId")] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(screenCastStartBrightMessage.replacingOccurrences(of: "chance", with: "m"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ScriptRequestTool.afterGift(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.cullOut(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_restoreSystem)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.itemsetBy(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.income(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.describeSession()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(appLabelMiniUrl.suffix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((widgetHidePath.replacingOccurrences(of: "naval", with: "t") + "Intim" + featureMobileData.lowercased())), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(widgetHidePath.replacingOccurrences(of: "naval", with: "t") + "Intim" + featureMobileData.lowercased())] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.speakerTime(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.externalBodyPart(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                themeSincePlatform.givingStream(eventID: viewSystemRandomPath)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func scan(giftModel: SizeSignatureGiftModel, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        themeSincePlatform.givingStream(eventID: moduleFoundId)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            colouration(showMsg: serviceDetailFoundValue)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= PositionThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            exhibit()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(k_tinAlert.suffix(6)) + String(appAddedSession))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(componentAbleDevice.replacingOccurrences(of: "clean", with: "t"))] = (String(kInstanceDevice))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (String(coreReduceAmberEvent.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(appLabelMiniUrl.suffix(7))): msgInfo, String(bytes: serviceTextMyEvent.reversed(), encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChatThen.sweepUp(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            obstructed(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func following(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(displayAcceptUrl - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.isochronous(true)
            self.msgView.isochronous(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func signForHighness(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.isochronous(true)
            self.msgView.isochronous(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func dismissRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.dismissRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                member(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func walking() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if EvolutionObjectProtocol.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            EvolutionObjectProtocol.shared.pullStrengthHome()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func income(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(appLabelMiniUrl.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!] = (featureDraftUrl.replacingOccurrences(of: "cute", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(appLabelMiniUrl.suffix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ChatThen.sweepUp(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.contrast(byMessage: cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(spacingWarnUtilityPreference) + viewHemName.replacingOccurrences(of: "actual", with: "c"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(spacingWarnUtilityPreference) + viewHemName.replacingOccurrences(of: "actual", with: "c"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.domainPosition(cellData, equalCapture: msgInfoDic[(String(layoutMovieApplicationData.suffix(5)))] as? String, program: msgIncome, lineMsgStatus: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        byAnnouncement(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(componentManagerConFutureUrl) + String(layoutConfirmMessage))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(componentManagerConFutureUrl) + String(layoutConfirmMessage))] as! NSNumber
            //: PositionThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PositionThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PositionThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.inmateTotal(needReload: false, mf_coin: PositionThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func equalAdjust() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.clew(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func byAnnouncement(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.rank(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func property(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.panel(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func historyPurchase() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func markPhonation() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.guideline { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.describeSession()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func noticeVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.notJaw { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.describeSession()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func marketplaceAndClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = DecisionMedalFeedbackThen()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        getController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func toAGreaterExtentClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = CrashViewDelegate(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.shareList(cellTitleList: [(String(viewGenderFormat.prefix(6)) + widgetExternalPage.replacingOccurrences(of: "reach", with: "fo")).localized, (String(widgetBulletUrl.prefix(5))).localized, (String(componentReportDuringData)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .restoreAudience()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                ProdPushManager.share.untilBuild(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                analogDigitalConverter()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = FeatureDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.three(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func analogDigitalConverter() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = OriginalReactiveCompatible()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.conUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.accostOn(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func board() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            member(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            getController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func trackInner(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ScreenReachTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.factor(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func someLay() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(cornerBuild(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: commonHalfDevice,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(shrinkPositive),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: coreRobotContent,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func cornerBuild(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard PositionThen.share.loginUserMode.loungePlus == true else { return }
        guard PositionThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func shrinkPositive() {
        //: PositionThen.share.loginUserMode.loungePlus = true
        PositionThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func zone() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? ThereMessageReactiveCompatible else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? ThereMessageReactiveCompatible else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if DiscoverReactiveCompatible.skim(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.belowIn(tipStr: (String(featureLessPage.suffix(6)) + "ead r" + moduleStopContent.replacingOccurrences(of: "tap", with: "e") + "pts?").localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ReverseBannerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func aboutPhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        MagnitudeReactiveCompatible.contendBrightEquity(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = PoetPickTool.assetActual(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.access(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func conversation() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        MagnitudeReactiveCompatible.pry(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.access(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func access(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            PositionThen.share.appStatus == BetterQuantity.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = MaestroModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.scanLayer(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = RushTermPhotosVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = MaestroModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.scanLayer(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        getController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func screenTransaction() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard DiscoverReactiveCompatible.beneath(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == PanelScalarLiteral.male.rawValue ? "He".localized : (String(componentCourseData.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.colouration(showMsg: (String(serviceInfoName.prefix(6)) + "using" + String(featureBridgeData.prefix(6)) + "ow a" + String(styleChildSession.suffix(7)) + String(k_runMenuSettings)).meanwhile(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.picker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func picker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = PoetPickTool.assetActual(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            PoetPickTool.primrosePath(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.threadStill(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.colouration(showMsg: (String(widgetDarkError) + String(screenRedSession.suffix(4)) + "eo at " + componentTooAlert.lowercased() + " 5 se" + viewProfileData.replacingOccurrences(of: "then", with: "s")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func threadStill(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: RationalEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ThatNothingRecognizerDelegate(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = MaestroModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.presentLikeComputerSimulation(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        WeatherMapThen.evolution()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        getController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: ThereMessageReactiveCompatible, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func principal(msgData: ThereMessageReactiveCompatible, sendModel: MaestroModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        MedalVideoManager.shared.slide(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.colouration(showMsg: (String(featureResumeTimer.suffix(4)) + String(viewParentPath)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.domainPosition(msgData, equalCapture: nil, program: NSNumber(value: 0), lineMsgStatus: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.presentLikeComputerSimulation(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    //: func func__addVoiceNotification() {
    func noArrow() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(iterate),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(response),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(con(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(score(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: commonHundredValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(customer(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: displayBeginText,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func refill(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            EvolutionObjectProtocol.shared.pleasingness()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            EvolutionObjectProtocol.shared.strip()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func iterate() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        refill(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func response() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        refill(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func con(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            refill(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            refill(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: FirstThen, cellView: TalkingChatAudioMsgCell) {
    func compartmentMoment(cellData: FirstThen, cellView: GrantDeadlineViewCell) {
        //: guard TalkingSocketManager.shared.isFamous == false else {
        guard TopThen.shared.isFamous == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colouration(showMsg: spacingQualityHundredSystem)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? ThereMessageReactiveCompatible)!.isKind(of: FirstThen.self) == false {
            if (indexModel as? ThereMessageReactiveCompatible)!.isKind(of: FirstThen.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! FirstThen
            let audioModel = indexModel as! FirstThen
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = UsufructuaryThen.cacheGuidance(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        EvolutionObjectProtocol.shared.pullStrengthHome()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if EvolutionObjectProtocol.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (screenNearPreference) + "\(EvolutionObjectProtocol.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if EvolutionObjectProtocol.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                EvolutionObjectProtocol.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        EvolutionObjectProtocol.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        EvolutionObjectProtocol.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: ThereMessageReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! ThereMessageReactiveCompatible
                let subModel: ThereMessageReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! ThereMessageReactiveCompatible
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: FirstThen.self) {
                if subModel.isKind(of: FirstThen.self) {
                    //: let audioModel = subModel as! FirstThen
                    let audioModel = subModel as! FirstThen
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = UsufructuaryThen.cacheGuidance(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    CommunicationMsgTable.garrison(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        EvolutionObjectProtocol.shared.week(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func applicationToBackground() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.bridgeWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.now(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? ThereMessageReactiveCompatible
                let indexModel = model as? ThereMessageReactiveCompatible
                //: if indexModel?.isKind(of: FirstThen.self) == true || indexModel?.isKind(of: SinceFamousReactiveCompatible.self) == true || indexModel?.isKind(of: CompartmentReactiveCompatible.self) == true || indexModel?.isKind(of: RationalCellData.self) == true {
                if indexModel?.isKind(of: FirstThen.self) == true || indexModel?.isKind(of: SinceFamousReactiveCompatible.self) == true || indexModel?.isKind(of: CompartmentReactiveCompatible.self) == true || indexModel?.isKind(of: RationalCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.down()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.exposure()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.zone()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func bridgeWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.imitation(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.bring(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.local()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == PositionThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == PositionThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == PositionThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == PositionThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.accostOn(userWrap)
    }

    //: func func__getFillDraftText() {
    func need() {
        //: let userID = "c2c_"+targetId
        let userID = (String(colorCreationData.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.monad()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func salveDownCircle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(colorCreationData.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    //: func func__TipsNotification() {
    func recognizeNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(panoramicViewNotification(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: colorNoData,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func panoramicViewNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: MinimizeCellData = userinfo[(String(k_fitUtility))] as! MinimizeCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.isochronous(true)
        self.msgView.isochronous(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func belowIn(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: screenShouldGivePairTitle.map{woodChance(enough: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: themePickDevice.map{earnEnterprise(owner: $0)}, encoding: .utf8)!: (String(featureConnectAlert))]
        //: var message: ThereMessageReactiveCompatible?
        var message: ThereMessageReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = ChatThen.betwixt(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ChatThen.sweepUp(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.isochronous(true)
        self.msgView.isochronous(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func exposure() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        DiscoverReactiveCompatible.alliance { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.belowIn(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func languageJaw(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.belowIn(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func down() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        DiscoverReactiveCompatible.refuseHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.belowIn(tipStr: String(bytes: styleCoordinatePath.map{$0^6}, encoding: .utf8)!.meanwhile(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func score(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: ThereMessageReactiveCompatible = dict?["cellData"] as! ThereMessageReactiveCompatible
        let cellData: ThereMessageReactiveCompatible = dict?[(String(sessionOptionId.suffix(5)) + viewCollectionPage.lowercased())] as! ThereMessageReactiveCompatible
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.isochronous(true)
        self.msgView.isochronous(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ReverseBannerDelegate: PingReactiveCompatible {
    //: func func__setReplyTipsView() {
    func book() {
        //: let key =  "\(PositionThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(PositionThen.share.loginUserMode.userID)" + (String(appBorderSession) + String(displayWouldName.suffix(6))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && PositionThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && PositionThen.share.appStatus == BetterQuantity.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.embargo()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            labelWho(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            worldViewReplyTakeAwayWith()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func match(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        accessDown(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        worldViewReplyTakeAwayWith()
    }

    //: func removeReplyTipsView() {
    func worldViewReplyTakeAwayWith() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            labelWho(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func orientation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: appBackSettings, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: componentMeEvent - displayAcceptUrl, width: moduleAdjustPath, height: componentMeEvent)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: moduleAdjustPath,
                                     //: height: ScreenHeight)
                                     height: componentMeEvent)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func member(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.famousTick() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: OpportunityViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: componentMeEvent - displayAcceptUrl,
                                     //: width: ScreenWidth,
                                     width: moduleAdjustPath,
                                     //: height: ScreenHeight)
                                     height: componentMeEvent)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ReverseBannerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func dateUi() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(viewMiddlePage) + appUtterError.replacingOccurrences(of: "award", with: "on")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func utilised() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? displayAcceptUrl : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.pianoThreshold(width: contentView.width,
                                       //: height: contentView.height,
                                       height: contentView.height,
                                       //: corners: [.topLeft, .topRight],
                                       corners: [.topLeft, .topRight],
                                       //: cornerRadii: .init(width: 8, height: 8))
                                       cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? layoutFormalUrl : sessionTableUrl)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + featureDailyHelper)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? layoutFormalUrl : sessionTableUrl)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + featureDailyHelper)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + featureDailyHelper)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func statSumeraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.board()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.toAGreaterExtentClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.variantConnectFailure(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                themeSincePlatform.givingStream(eventID: modulePageEvent, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
