
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let viewCancelFormat:[Character] = ["I","M","退","出","登","录","失"]
fileprivate let kHeAddedPreference:String = "format"
fileprivate let viewPreserveLogger:[Character] = ["，","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let colorPictureError:String = ", descprocessing cookie hypothesis random change"
fileprivate let viewRowingContent:String = "bag"

/*: "IM登录失败，code: :*/
fileprivate let spacingChooseSession:[Character] = ["I","M","\u{767b}","录","失","败","，"]
fileprivate let screenForePage:[Character] = ["c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let spacingPiUtility:String = "IM获取未blank aim main attach cost"
fileprivate let themeWriteUtility:String = "publish will reply workplace失败，cod"
fileprivate let coreReminderKey:String = "e:except patent poet rue ta"

/*: "crushBadge" :*/
fileprivate let widgetHangSettings:[Character] = ["c","r","u","s","h","B","a","d"]
fileprivate let screenAirResistValue:[Character] = ["g","e"]

/*: "badNumber" :*/
fileprivate let appWrittenSavePlatform:[UInt8] = [0xf6,0xf5,0xf0,0xda,0xe1,0xf9,0xf6,0xf1,0xe6]

private func harvestAlly(land num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "plaza" :*/
fileprivate let coreWillData:String = "plapicturea"

/*: "roomNum" :*/
fileprivate let moduleAwardWellHelper:String = "transfer interestedroomNum"

/*: "num" :*/
fileprivate let featureAcquireDevice:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let serviceLevelValue:String = "ONLY"
fileprivate let displayExerciseBeyondAlert:String = "transaction eye minimum guardId"

/*: "LIVE-CHAT" :*/
fileprivate let colorFlushEvent:String = "LIVE-emotion tic tender"

/*: "extra" :*/
fileprivate let coreIncludeSettings:String = "extrtheme"

/*: "msgType" :*/
fileprivate let themeDominantMetrics:[Character] = ["m","s"]
fileprivate let k_singleWithinPath:[Character] = ["g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let appCertainSecondMessage:[Character] = ["n","e","w","P","k","g","G"]
fileprivate let kResolutionUtility:String = "mineralt"

/*: "MF:ChatMentionMsg" :*/
fileprivate let coreAttachAlert:[Character] = ["M","F"]
fileprivate let componentManagingValue:[Character] = [":","C","h","a","t","M","e","n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let componentAnnouncementHandlePlatform:[UInt8] = [0x82,0x76,0x71]

fileprivate func alwaysContinue(discount num: UInt8) -> UInt8 {
    let value = Int(num) - 13
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let sessionIgnoreId:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let spacingWaveData:[UInt8] = [0x34,0x2e,0x13,0x38,0x2a,0x8,0x2e,0x38,0x2f]

/*: "c2c_ :*/
fileprivate let featureGlobalMessage:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let styleAdminSession:[Character] = ["I","M","获","取","会","话","\u{5931}","败","，","c","o","n","v","I","d",":"]

/*: , code: :*/
fileprivate let moduleExitName:String = "added instruction, code"
fileprivate let screenWelcomeData:[Character] = [":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let commonHypothesisUrl:String = "IM插\u{5165}"
fileprivate let viewKeyBitPreference:String = "descriptionge"
fileprivate let themeEntryPreference:String = "bar total private sometId:"

/*: "changeDirection" :*/
fileprivate let viewPicUrl:String = "csinglean"
fileprivate let k_squareDeviceConfig:String = "ECTION"

/*: "rYMsgType" :*/
fileprivate let themeFormerPreference:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let styleConstraintHelper:String = "GJ:Caremark word contribution chart resist"
fileprivate let coreApplicationPath:String = "M"

/*: "IM获取会话列表失败，code: :*/
fileprivate let screenSecurityUrl:String = "IM获\u{53d6}"
fileprivate let featureStageConfig:String = "carry town lifetime combine，code"
fileprivate let appComplyTotPlatform:String = ":"

/*: "msgInfo" :*/
fileprivate let viewDateRecommendationError:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let colorDetailedError:String = "NEED"

/*: "group_ :*/
fileprivate let kKindValue:String = "gyourselfoup"
fileprivate let kFormalBounceSettings:String = "validity"

/*: "IM删除会话失败，convID: :*/
fileprivate let serviceMineAllowValue:[Character] = ["I","M","删"]
fileprivate let moduleInstructionPreference:String = "除会create失"

/*: "IM置顶会话失败，convId: :*/
fileprivate let layoutElementPlatform:[Character] = ["I","M","置","\u{9876}","会","话","失","败","，","c","o","n","v"]
fileprivate let displayDistanceThatPlatform:String = "Id:mini exception sure residence"

/*: "SDK 正在连接到服务器" :*/
fileprivate let commonPresentCookieJawEvent:[Character] = ["S","D","K"," "]
fileprivate let featureDecisionMessage:[Character] = ["正","在","连","接","到","服","务","\u{5668}"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let viewUntilLogger:String = "SDK 已经click media terms week team"

/*: "IM连接服务器失败，code: :*/
fileprivate let commonContendUtility:[Character] = ["I","M","连","接","服","务","器","失","败","，","c","o","d","e",":"]

/*: , error: :*/
fileprivate let layoutSpaceMessage:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let screenMemoryTrailPath:[Character] = ["I"]
fileprivate let commonSharedKey:[Character] = ["M","账","\u{53f7}","被","挤","下","\u{7ebf}","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let serviceSheCanAlert:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let viewPecId:String = "Offline Nadvice deny ref oval rid"
fileprivate let componentKitMetrics:[Character] = ["o","t","i","f","i","c"]
fileprivate let moduleSituationPage:[Character] = ["a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let servicePairPath:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let viewSeemPlatform:[Character] = ["R","e"," ","L","o","g","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GroupConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol SignatureManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func to(data: [SensitiveConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func addMid(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func goaBean(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum SumercessionDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class GroupConversationListener: NSObject {
@objcMembers public class GroupConversationListener: NSObject {
    //: public static let shared = GroupConversationListener()
    public static let shared = GroupConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [SensitiveConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [SensitiveConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [SensitiveConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [SensitiveConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [SensitiveConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: SensitiveConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.remainConversation()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func table() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(commonEndLogger, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func remainConversation() {
        //: func__removeAllDelegate()
        all()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.actionList()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func actionList() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = SensitiveConversationModel.variety(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = SensitiveConversationModel.variety(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func fullAppear() -> [SensitiveConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if PositionThen.share.appStatus == AppSkinStatus.special.rawValue {
        if PositionThen.share.appStatus == BetterQuantity.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == FragThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == FragThen.opHistory() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension GroupConversationListener {
extension GroupConversationListener {
    //: func func__removeAllDelegate() {
    func all() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func andThenDelegate(_ delegate: SignatureManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func delegate(_ delegate: SignatureManagerDelegate) {
        //: self.func__invokeDelegate {
        self.underItemOne {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func underItemOne(_ invocation: (SignatureManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! SignatureManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension GroupConversationListener {
extension GroupConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func suggest() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            CheckThen.placeLoad(msg: (String(viewCancelFormat) + kHeAddedPreference.replacingOccurrences(of: "format", with: "败") + String(viewPreserveLogger)) + "\(code)" + (String(colorPictureError.prefix(6)) + viewRowingContent.replacingOccurrences(of: "bag", with: ":")) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.remainConversation()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func chiaroscuro(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = PositionThen.share.appUserConfigMode.uSig
        let uSigStr = PositionThen.share.appUserConfigMode.uSig
        //: let uidStr = PositionThen.share.loginUserMode.userID
        let uidStr = PositionThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.chooseManual()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            CheckThen.placeLoad(msg: (String(spacingChooseSession) + String(screenForePage)) + "\(code)" + (String(colorPictureError.prefix(6)) + viewRowingContent.replacingOccurrences(of: "bag", with: ":")) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func keepOccur() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || PositionThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || PositionThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.colouration(showMsg: serviceDetailFoundValue)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension GroupConversationListener {
extension GroupConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func chooseManual() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func nearby() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        ScriptRequestTool.listWith { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(widgetHangSettings) + String(screenAirResistValue))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: moduleRequestNearbyPage, object: self, userInfo: [String(bytes: appWrittenSavePlatform.map{harvestAlly(land: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func quantitativeRelation(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        ScriptRequestTool.everyLand { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(coreWillData.replacingOccurrences(of: "picture", with: "z"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(moduleAwardWellHelper.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = SensitiveConversationModel.variety(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(featureAcquireDevice))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(serviceLevelValue.lowercased() + "OneRoom" + String(displayExerciseBeyondAlert.suffix(2)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension GroupConversationListener: V2TIMAdvancedMsgListener {
extension GroupConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(colorFlushEvent.prefix(5)) + "CHAT"))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            PushReactiveCompatible.storage().silverStarMsg(msg: msg)
            //: return
            return
        }
        //: if msg.sender == PositionThen.share.loginUserMode.userID { return }
        if msg.sender == PositionThen.share.loginUserMode.userID { return }

        //: let dic = DisableChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = DisableChatManager.monthBroad(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(coreIncludeSettings.replacingOccurrences(of: "theme", with: "a"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? StallModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: displayBeginText, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(themeDominantMetrics) + String(k_singleWithinPath))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(appCertainSecondMessage) + kResolutionUtility.replacingOccurrences(of: "mineral", with: "if")) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: displayWindowRefreshSystem, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if DisableChatManager.checkVoiceMessage(message: msg) {
            if DisableChatManager.birthdayMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = ChatThen.gallery(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                CommunicationMsgTable.mentionDb(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.exceptStream(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ErrorPreferenceThen.shared.informationDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ErrorPreferenceThen.shared.accessIgnore(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(coreAttachAlert) + String(componentManagingValue)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ForeheadChatManager.share.insideInformation(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: DiscoverReactiveCompatible.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: DiscoverReactiveCompatible.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != FragThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != FragThen.ringRowingVertical() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.underItemOne { $0.goaBean?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            CircleReactiveCompatible.shared.deviceFile(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: componentAnnouncementHandlePlatform.map{alwaysContinue(discount: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: sessionIgnoreId.reversed(), encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: spacingWaveData.map{$0^93}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: commonThirdSettings,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            UsufructuaryThen.socialTime(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            DiscoverReactiveCompatible.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: commonHalfDevice, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: ThereMessageReactiveCompatible, isPrivete: Bool) {
    @objc public func realty(targetId: String, msgCellData: ThereMessageReactiveCompatible, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.mediety(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(featureGlobalMessage)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.handleItem(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.row(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                CheckThen.placeLoad(msg: (String(styleAdminSession)) + "\(convId)" + (String(moduleExitName.suffix(6)) + String(screenWelcomeData)) + "\(code)" + (String(colorPictureError.prefix(6)) + viewRowingContent.replacingOccurrences(of: "bag", with: ":")) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func mediety(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = PositionThen.share.loginUserMode.userID
        let senderStr = PositionThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                CheckThen.placeLoad(msg: (commonHypothesisUrl + "消\u{606f}失败，t" + viewKeyBitPreference.replacingOccurrences(of: "description", with: "ar") + String(themeEntryPreference.suffix(4))) + "\(targetId)" + (String(moduleExitName.suffix(6)) + String(screenWelcomeData)) + "\(code)" + (String(colorPictureError.prefix(6)) + viewRowingContent.replacingOccurrences(of: "bag", with: ":")) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func exceptStream(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(viewPicUrl.replacingOccurrences(of: "single", with: "h") + "geDir" + k_squareDeviceConfig.lowercased())].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && TalkingAppPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(themeFormerPreference))].stringValue == (String(styleConstraintHelper.prefix(5)) + "llCusto" + coreApplicationPath.lowercased()) && isSelf && ProdPushManager.share.concealed()!.isKind(of: ReverseBannerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: styleMaxWindowSystem) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: styleMaxWindowSystem)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension GroupConversationListener: V2TIMConversationListener {
extension GroupConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.handleItem(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.row(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.handleItem(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.row(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: PositionThen.share.unreadMessageNum = Int(totalUnreadCount)
        PositionThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.underItemOne {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.addMid?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func row(conVersationList: [SensitiveConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if UsufructuaryThen.work().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                UsufructuaryThen.work().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.underItemOne {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.to?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func dot(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [SensitiveConversationModel] = []

        // V2TIMConversation->SensitiveConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = SensitiveConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.quality(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [SensitiveConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = PermissionInfoTable.getWith(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.conv(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension GroupConversationListener {
extension GroupConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func address(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.dot(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.row(conVersationList: arr as? [SensitiveConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func mouthDown(targetId: String) -> SensitiveConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func handleItem(convList: [V2TIMConversation]) -> [SensitiveConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [SensitiveConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = SensitiveConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.quality(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.conv(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func conv(convModel: SensitiveConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.actionList()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: SumercessionDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - PositionThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - PositionThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = UsufructuaryThen.befitting(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = DisableChatManager.parseTXMessageData(data: elemData)
            let dict = DisableChatManager.monthBroad(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(coreIncludeSettings.replacingOccurrences(of: "theme", with: "a"))][(String(viewDateRecommendationError))][(colorDetailedError.lowercased() + "Fold")].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.ii(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.ii(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func ii(convList: [SensitiveConversationModel], dataType: SumercessionDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func schmooze(targetId: String, dataType: SumercessionDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(featureGlobalMessage)) + "\(targetId)" : (kKindValue.replacingOccurrences(of: "yourself", with: "r") + kFormalBounceSettings.replacingOccurrences(of: "validity", with: "_")) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            UsufructuaryThen.necessaryWith(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(PositionThen.share.loginUserMode.userID, toUid: targetId)
            CommunicationMsgTable.secondReserve(PositionThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.chooseManual()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            CheckThen.placeLoad(msg: (String(serviceMineAllowValue) + moduleInstructionPreference.replacingOccurrences(of: "create", with: "话") + "败，convID:") + "\(convID)" + (String(moduleExitName.suffix(6)) + String(screenWelcomeData)) + "\(code)" + (String(colorPictureError.prefix(6)) + viewRowingContent.replacingOccurrences(of: "bag", with: ":")) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func minimize(convModel: SensitiveConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.ii(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.ii(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension GroupConversationListener: V2TIMSDKListener {
extension GroupConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(commonPresentCookieJawEvent) + String(featureDecisionMessage)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(viewUntilLogger.prefix(6)) + "成功连接到服务器"))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        CheckThen.placeLoad(msg: (String(commonContendUtility)) + "\(code)" + (String(layoutSpaceMessage)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        CheckThen.placeLoad(msg: (String(screenMemoryTrailPath) + String(commonSharedKey)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        TopThen.shared.fragmentMerge(userLoginOut: true)
        //: GroupConversationListener.shared.isConnection = false
        GroupConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (SensitiveFamousApplication.shared as! SensitiveFamousApplication).famous()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: serviceSheCanAlert.map{$0^174}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = FeatureAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if HarvestThen.share.interfaceLang == WaitTransformable.ar.rawValue || HarvestThen.share.interfaceLang == WaitTransformable.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            HarvestThen.share.interfaceLang == WaitTransformable.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.celluloidSize(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        ThereAlertShow.tillLaunch()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        ThereAlertShow.tillLaunch(title: (String(viewPecId.prefix(9)) + String(componentKitMetrics) + String(moduleSituationPage)).localized, message: msg, leftBtnTitle: (String(servicePairPath)).localized, rightBtnTitle: (String(viewSeemPlatform)).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            InstrumentalityReactiveCompatible.aftermath().personalIdentity()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            ScriptReactiveCompatible.tag { t in
                //: ProgressHUD.dismiss()
                WeatherMapThen.evolution()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: serviceSaltUtility, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            ScriptReactiveCompatible.botDoing { t in
                //: if t {
                if t {
                    //: GroupConversationListener.shared.func__LogingIn { _ in
                    GroupConversationListener.shared.chiaroscuro { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if InstrumentalityReactiveCompatible.aftermath().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(InstrumentalityReactiveCompatible.aftermath().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    TopThen.shared.stuff()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
