
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let coreEticDevice:[UInt8] = [0x55,0x5a,0x55,0x60,0x14,0x4f,0x5b,0x50,0x51,0x5e,0x26,0x15,0xc,0x54,0x4d,0x5f,0xc,0x5a,0x5b,0x60,0xc,0x4e,0x51,0x51,0x5a,0xc,0x55,0x59,0x5c,0x58,0x51,0x59,0x51,0x5a,0x60,0x51,0x50]

fileprivate func airPoseCorporate(sum num: UInt8) -> UInt8 {
    let value = Int(num) + 20
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let componentHisFormat:[UInt8] = [0x93,0x87,0x82]

fileprivate func remPick(button num: UInt8) -> UInt8 {
    let value = Int(num) - 30
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let spacingDownName:String = "pap"
fileprivate let layoutPooQualityFormat:String = "eadPicwill drawing"

/*: "nickname" :*/
fileprivate let colorCurveMessage:String = "nicknhe"
fileprivate let k_boldEvent:[Character] = ["m","e"]

/*: "birthday" :*/
fileprivate let k_odeTremendousMetrics:String = "bstandardth"
fileprivate let serviceReserveVidPath:String = "dreplyy"

/*: "videoUrl" :*/
fileprivate let styleBodyPage:String = "am"
fileprivate let widgetNobodyDevice:[Character] = ["i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let styleStartName:String = "cosuitableer"

/*: "location" :*/
fileprivate let viewStaffSeaPath:String = "scopeocation"

/*: "cmd" :*/
fileprivate let sessionDecreaseDevice:[UInt8] = [0xa,0x14,0xb]

fileprivate func taStag(square num: UInt8) -> UInt8 {
    let value = Int(num) - 167
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let featureHostLogger:[Character] = ["r","e","f","u","s","e","C","a"]
fileprivate let featureThanPage:String = "LL"

/*: "data" :*/
fileprivate let corePurpleEvent:[UInt8] = [0x41,0x3e,0x51,0x3e]

fileprivate func gapReady(medium num: UInt8) -> UInt8 {
    let value = Int(num) - 221
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let kDocDirectError:String = "Call statistical injury"
fileprivate let appStepSureValue:String = "rleavet"
fileprivate let componentGuideName:[Character] = ["i","o","n"]

/*: "stopCall" :*/
fileprivate let featureStaffDryHelper:String = "view party ownerstopCall"

/*: "logId" :*/
fileprivate let featureNoSession:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let coreScreenTimer:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let spacingMustPage:String = "No answeragency platform show actor"

/*: "duration" :*/
fileprivate let widgetAppropriateError:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let coreDiskPlatform:String = "She hdisable we"
fileprivate let screenAddTotalLearnPath:String = "ung udrag both"
fileprivate let screenCrushPlatform:[Character] = ["p"]

/*: "cancelSex" :*/
fileprivate let commonReManagingHelper:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let layoutComplyConfig:String = "He halater beat"
fileprivate let componentAirLayerValue:String = "guest"

/*: "endType" :*/
fileprivate let kAdAlert:String = "bucketn"
fileprivate let stylePadComputerSettings:String = "segment four anima gold incomedType"

/*: "stopSex" :*/
fileprivate let displayWaitPlaceTurnMetrics:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let serviceRejectTimer:[UInt8] = [0x29,0xe,0x13,0x15,0x6,0x6,0x9,0x3,0x9,0x5,0xe,0x14,0x40,0x7,0xf,0xc,0x4,0x40,0x3,0xf,0x9,0xe,0x13,0x4c,0x40,0x3,0x1,0xc,0xc,0x40,0x8,0x1,0x13,0x40,0x5,0xe,0x4,0x5,0x4]

private func dealCheckion(regional num: UInt8) -> UInt8 {
    return num ^ 96
}

/*: "The other hung up the phone" :*/
fileprivate let coreLowId:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let colorNamePage:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let viewSpiritMetrics:String = "heavyu"
fileprivate let commonCurrentlyEvent:String = "mpUrlchampion before forward anyone southwest"

/*: "Cancel" :*/
fileprivate let commonImageFormat:String = "Cancelsalt defense allow"

/*: "Get gold coins" :*/
fileprivate let kFivePlatform:String = "final notice analysis mediumGet g"
fileprivate let appLaySession:String = "coihims"

/*: "addBean" :*/
fileprivate let featureCircuitMetrics:String = "observe analysis varietyaddBean"

/*: "refuseSex" :*/
fileprivate let serviceShallHelper:String = "sample net structure transform devoterefuseS"
fileprivate let widgetDonValue:[Character] = ["e","x"]

/*: "onAcceptCall" :*/
fileprivate let moduleMissKey:[Character] = ["o","n","A","c","c","e","p","t","C","a"]
fileprivate let componentConnectionUtility:[Character] = ["l","l"]

/*: "acceptCall" :*/
fileprivate let layoutLeanSession:String = "liecep"

/*: "Calls are cancelled" :*/
fileprivate let k_accessName:String = "Callsomebody separation future smooth"
fileprivate let k_dangerousName:String = "deliver component convert hang sure can"
fileprivate let styleNoseValue:[Character] = ["e","d"]

/*: "The call has ended" :*/
fileprivate let spacingUnlessPlatform:String = "staff export listener noon environmentThe c"
fileprivate let styleWouldEvent:[Character] = ["a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TelecastingHiObjectProtocol.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let widgetPlayUrl = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class TelecastingHiObjectProtocol: GroupThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: GrantChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PreferenceReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: ExternalMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: CloseScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        TopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TopThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: coreEticDevice.map{airPoseCorporate(sum: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! TamFamousControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.form()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            scouring()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            coupon(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.managerExpected()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        joinSubviews()
        //: setupSubViewsConstraint()
        roving()
        //: initNotificationObserver()
        observerResult()
        //: req_callGetUserInfo()
        suspendInfo()
        //: timeoutRules()
        rulesTimeout()
        //: setUpBGMPlayer()
        roundNose()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.start()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PositionThen.share.appConfigMode.disableShootScreen {
            if PositionThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFamous = false
        TopThen.shared.isFamous = false
        //: TalkingSocketManager.shared.isCalling = false
        TopThen.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.cause()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: DisableVideoView = {
        //: let v = TalkingBGVideoView()
        let v = DisableVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.nor(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: CounterventionSinceThen = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = CounterventionSinceThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: MedalWindowView = {
        //: let v = TalkingVideoWindowView()
        let v = MedalWindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = moduleAdjustPath - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PushThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: appSceneUrl + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.curAdmin()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: FeatureChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = FeatureChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func suspendInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        FeatureChatManager.usedCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: componentHisFormat.map{remPick(button: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(spacingDownName.replacingOccurrences(of: "pap", with: "h") + String(layoutPooQualityFormat.prefix(6)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(colorCurveMessage.replacingOccurrences(of: "he", with: "a") + String(k_boldEvent))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_odeTremendousMetrics.replacingOccurrences(of: "standard", with: "ir") + serviceReserveVidPath.replacingOccurrences(of: "reply", with: "a"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(styleBodyPage.replacingOccurrences(of: "am", with: "v") + String(widgetNobodyDevice))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(styleStartName.replacingOccurrences(of: "suitable", with: "v"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(viewStaffSeaPath.replacingOccurrences(of: "scope", with: "l"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.outOfSight()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.context()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func context() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = RationalCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = RationalCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.twelveNoonCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.host(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func scouring() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MagnitudeReactiveCompatible.popularNet { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colouration(showMsg: displayReadPath)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(featureHostLogger) + featureThanPage.lowercased()), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: componentHisFormat.map{remPick(button: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.giftWrapAnimated()
                //: return
                return
            }

            //: self.startPreview()
            self.start()
            //: self.stopBGM()
            self.backgroundBy()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PreferenceReactiveCompatible.burn(type: .Response)
            //: self.startBGM()
            self.form()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func start() {
        //: self.videoManager.startPreview()
        self.videoManager.sumeraction()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func cause() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.backgroundBy()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        EveryThen.acceptLibrary()
        //: TalkingSocketManager.shared.callerDelegate = nil
        TopThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        TopThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        TopThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func giftWrapAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.backgroundBy()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func curAdmin() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.segmentView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.context()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func custom() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.gameByPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.context()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.trot()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func trot() {
        //: guard PositionThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard PositionThen.share.loginUserMode.sex == PanelScalarLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = CloseScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.successfullyAnimation()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func observerResult() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(limitWith(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(used(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: sessionEnableBottomTitle,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(sendByUniversal(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: styleCheckAlert,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        widgetPlayUrl.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func limitWith(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            giftWrapAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func sendByUniversal(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard TopThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        TopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TopThen.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.shLeave()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func used(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.context()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension TelecastingHiObjectProtocol: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.colouration(showMsg: (String(kDocDirectError.prefix(5)) + "inter" + appStepSureValue.replacingOccurrences(of: "leave", with: "up") + String(componentGuideName)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(featureStaffDryHelper.suffix(8))), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: featureNoSession.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(coreScreenTimer)), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: featureNoSession.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(featureHostLogger) + featureThanPage.lowercased()), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: componentHisFormat.map{remPick(button: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.cornerPublishView()
                //: TalkingSocketManager.shared.currFamousVC = nil
                TopThen.shared.currFamousVC = nil
            }
            //: popCurrentViewController()
            giftWrapAnimated()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func roundNose() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? SeasonSum.Call : SeasonSum.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PreferenceReactiveCompatible.burn(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func form() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.twoPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func backgroundBy() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.explainAbout()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 超时定时器
    //: private func timeoutRules() {
    private func rulesTimeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? k_transformName : self.chatModel?.stayInPlaceLikeTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            birthdayBy()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.birthdayBy()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func birthdayBy() {
        //: guard !TalkingSocketManager.shared.isFamous else { return }
        guard !TopThen.shared.isFamous else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.colouration(showMsg: (String(spacingMustPage.prefix(9))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(coreScreenTimer)), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: featureNoSession.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(featureHostLogger) + featureThanPage.lowercased()), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: componentHisFormat.map{remPick(button: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        TopThen.shared.info(info: socketDict)
        //: popCurrentViewController()
        giftWrapAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func recordToo(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: featureNoSession.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(widgetAppropriateError))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.lineTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.previous()
            //: createTalkRoom()
            custom()
            //: self.stopBGM()
            self.backgroundBy()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func coupon(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.segmentView()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.nor(show: false)
        //: self.stopBGM()
        self.backgroundBy()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.previous()
        //: self.createTalkRoom()
        self.custom()
    }
}

// MARK: - PortraitObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension TelecastingHiObjectProtocol: PortraitObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func raw(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(coreDiskPlatform.prefix(5)) + "as h" + String(screenAddTotalLearnPath.prefix(5)) + String(screenCrushPlatform)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(commonReManagingHelper))].stringValue == PanelScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(layoutComplyConfig.prefix(5)) + "s hung " + componentAirLayerValue.replacingOccurrences(of: "guest", with: "up")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colouration(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.cornerPublishView()
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }

        //: popCurrentViewController()
        giftWrapAnimated()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func sound(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        coupon(logId: json[String(bytes: featureNoSession.reversed(), encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func banIcon(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(kAdAlert.replacingOccurrences(of: "bucket", with: "e") + String(stylePadComputerSettings.suffix(5)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(coreDiskPlatform.prefix(5)) + "as h" + String(screenAddTotalLearnPath.prefix(5)) + String(screenCrushPlatform)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(displayWaitPlaceTurnMetrics))].stringValue == PanelScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(layoutComplyConfig.prefix(5)) + "s hung " + componentAirLayerValue.replacingOccurrences(of: "guest", with: "up")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if PositionThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if PositionThen.share.appStatus == BetterQuantity.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: serviceRejectTimer.map{dealCheckion(regional: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(coreLowId)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colouration(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.cornerPublishView()
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.connectionTotal()
        //: popCurrentViewController()
        giftWrapAnimated()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func conversionChat(msg: String, data: [String: Any]) {
        //: guard PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard PositionThen.share.appStatus == BetterQuantity.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(colorNamePage))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(viewSpiritMetrics.replacingOccurrences(of: "heavy", with: "j") + String(commonCurrentlyEvent.prefix(5)))] as? String else { return }
            //: let httpsUrl = PositionThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = PositionThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ReachConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ProdPushManager.share.rangeGalleryThirty(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ThereAlertShow.tillLaunch(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(commonImageFormat.prefix(6))).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(kFivePlatform.suffix(5)) + "old " + appLaySession.replacingOccurrences(of: "him", with: "n")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ThereAlertShow.outsideGold()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ProdPushManager.share.responsibility(clickEvent: spacingMagnitudeName, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func modifyCamera(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.statusArr(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func checkChat(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(featureCircuitMetrics.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.bringHomeManagePoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.pointOfMagnitudeersectionCos(point: addBean)
        }
    }
}

// MARK: - EnhanceFailObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TelecastingHiObjectProtocol: EnhanceFailObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func club(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(coreDiskPlatform.prefix(5)) + "as h" + String(screenAddTotalLearnPath.prefix(5)) + String(screenCrushPlatform)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(serviceShallHelper.suffix(7)) + String(widgetDonValue))].stringValue == PanelScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(layoutComplyConfig.prefix(5)) + "s hung " + componentAirLayerValue.replacingOccurrences(of: "guest", with: "up")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colouration(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.cornerPublishView()
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }

        //: popCurrentViewController()
        giftWrapAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func labelCommit(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        coupon(logId: json[String(bytes: featureNoSession.reversed(), encoding: .utf8)!].intValue)
    }
}

// MARK: - ConsentErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension TelecastingHiObjectProtocol: ConsentErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func noWall(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(featureHostLogger) + featureThanPage.lowercased()) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.colouration(showMsg: (String(spacingMustPage.prefix(9))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.cornerPublishView()
                //: TalkingSocketManager.shared.currFamousVC = nil
                TopThen.shared.currFamousVC = nil
            }
            //: popCurrentViewController()
            giftWrapAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(moduleMissKey) + String(componentConnectionUtility)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PositionThen.share.appStatus == BetterQuantity.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(sufficient: false)
            ProdPushManager.share.responsibility(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(moduleMissKey) + String(componentConnectionUtility)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.colouration(showMsg: msg)
            //: popCurrentViewController()
            giftWrapAnimated()
        }
    }
}

// MARK: - SizeViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension TelecastingHiObjectProtocol: SizeViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func requestAlongside() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (layoutLeanSession.replacingOccurrences(of: "lie", with: "ac") + "tCall"), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: componentHisFormat.map{remPick(button: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func notePopular() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colouration(showMsg: (String(k_accessName.prefix(4)) + "s are" + String(k_dangerousName.suffix(4)) + "cell" + String(styleNoseValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(coreScreenTimer)), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: featureNoSession.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colouration(showMsg: (String(k_accessName.prefix(4)) + "s are" + String(k_dangerousName.suffix(4)) + "cell" + String(styleNoseValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(featureHostLogger) + featureThanPage.lowercased()), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: componentHisFormat.map{remPick(button: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.colouration(showMsg: (String(spacingUnlessPlatform.suffix(5)) + "all h" + String(styleWouldEvent)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            TopThen.shared.info(info: [String(bytes: sessionDecreaseDevice.map{taStag(square: $0)}, encoding: .utf8)!: (String(featureStaffDryHelper.suffix(8))), String(bytes: corePurpleEvent.map{gapReady(medium: $0)}, encoding: .utf8)!: [String(bytes: featureNoSession.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        giftWrapAnimated()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func bubbleTar(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.frontIn(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func spaceEnableForbidden(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.nor(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.headRank(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.context()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func skirt() {
        //: TalkingSocketManager.shared.currFamousVC = self
        TopThen.shared.currFamousVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = ExternalMiniView.littlenessView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.luckOutThrough()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.layDarkMode()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.famousTick()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        giftWrapAnimated()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func underBan() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension TelecastingHiObjectProtocol {
    /// 添加视图
    //: private func setupSubviews() {
    private func joinSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.mergeView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func roving() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
