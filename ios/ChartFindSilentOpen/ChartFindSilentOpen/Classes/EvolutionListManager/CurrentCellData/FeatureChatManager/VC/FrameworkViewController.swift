
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let coreDoinglyThresholdError:[UInt8] = [0x89,0x8e,0x89,0x94,0x48,0x83,0x8f,0x84,0x85,0x92,0x5a,0x49,0x40,0x88,0x81,0x93,0x40,0x8e,0x8f,0x94,0x40,0x82,0x85,0x85,0x8e,0x40,0x89,0x8d,0x90,0x8c,0x85,0x8d,0x85,0x8e,0x94,0x85,0x84]

fileprivate func purpleComment(mm num: UInt8) -> UInt8 {
    let value = Int(num) - 32
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let spacingScriptName:[UInt8] = [0x1c,0x10,0xb]

fileprivate func insideTrack(pending num: UInt8) -> UInt8 {
    let value = Int(num) + 89
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let viewWrapTimer:String = "here certain mouthheadPic"

/*: "nickname" :*/
fileprivate let spacingBubbleValue:String = "nicknpackageme"

/*: "birthday" :*/
fileprivate let spacingVisualEvent:String = "bistagethda"
fileprivate let featureClearlyConfig:[Character] = ["y"]

/*: "videoUrl" :*/
fileprivate let appNorPlayerConfig:String = "vmarke"

/*: "location" :*/
fileprivate let k_yourMessage:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let colorRefUrl:[UInt8] = [0x89,0x93,0x8a]

fileprivate func seeLink(select num: UInt8) -> UInt8 {
    let value = Int(num) + 218
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let componentAdviceUrl:String = "choice stroke longrefuseCa"
fileprivate let moduleRequestFormat:[Character] = ["l","l"]

/*: "data" :*/
fileprivate let featureAppealKey:[UInt8] = [0x90,0x95,0x80,0x95]

private func scriptUntil(exactly num: UInt8) -> UInt8 {
    return num ^ 244
}

/*: "Call interruption" :*/
fileprivate let widgetCalledSystem:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t"]
fileprivate let sessionPerceptSlowMetrics:[Character] = ["i","o","n"]

/*: "stopCall" :*/
fileprivate let commonExtentHelper:[Character] = ["s","t","o","p","C","a"]
fileprivate let commonModelSystem:[Character] = ["l","l"]

/*: "logId" :*/
fileprivate let themeDecreaseTitle:[UInt8] = [0x2f,0x2c,0x24,0xa,0x27]

/*: "cancelCall" :*/
fileprivate let colorMarkError:String = "CANCEL"

/*: "No answer" :*/
fileprivate let spacingRoadPaperPage:String = "No awall ref transfer"
fileprivate let styleAilAlert:String = "nswabstractr"

/*: "duration" :*/
fileprivate let appMultipleUtility:String = "bindrat"
fileprivate let displayMaximumPlatform:[Character] = ["i","o","n"]

/*: "She has hung up" :*/
fileprivate let featureSelectTimer:[Character] = ["S","h","e"," ","h","a","s"," "]
fileprivate let screenFunAlert:String = "bench exercisehung up"

/*: "cancelSex" :*/
fileprivate let sessionLabelKey:String = "linencel"
fileprivate let moduleMotionPersonCastError:String = "Sexgreet alive background lay"

/*: "He has hung up" :*/
fileprivate let displayJustPicValue:String = "He hasdata exclusive scene clock ingredient"
fileprivate let displayMeUrl:String = " hung uprandom man quality"

/*: "endType" :*/
fileprivate let appShallData:String = "endTynever task factor"
fileprivate let commonExpressionMetrics:[Character] = ["p","e"]

/*: "stopSex" :*/
fileprivate let componentContendAlert:String = "lifetime base sustain occurstopS"
fileprivate let kProcessHelper:String = "ehim"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let featureInjureObjectKey:[UInt8] = [0xb7,0x90,0x8d,0x8b,0x98,0x98,0x97,0x9d,0x97,0x9b,0x90,0x8a,0xde,0x99,0x91,0x92,0x9a,0xde,0x9d,0x91,0x97,0x90,0x8d,0xd2,0xde,0x9d,0x9f,0x92,0x92,0xde,0x96,0x9f,0x8d,0xde,0x9b,0x90,0x9a,0x9b,0x9a]

/*: "The other hung up the phone" :*/
fileprivate let kCompanyContent:String = "accelerate true big production layerThe o"
fileprivate let displayTrickAlert:String = "hbuttong"
fileprivate let spacingLipLogger:String = "host wave already personalhe p"

/*: "type" :*/
fileprivate let moduleMotionLoopAlarmLogger:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let k_guidelineSession:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let styleMentionMessage:String = "Cancelquote database"

/*: "Get gold coins" :*/
fileprivate let viewLetterPreference:String = "Get govolume sh stumble contain detail"
fileprivate let styleHemTimer:String = "ogreets"

/*: "addBean" :*/
fileprivate let featureProblemPage:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let commonVaryMetrics:String = "rjusu"

/*: "onAcceptCall" :*/
fileprivate let styleGreetSession:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let moduleEstimatedFormat:String = "abelowep"
fileprivate let sessionCompleteDevice:[Character] = ["l"]

/*: "Calls are cancelled" :*/
fileprivate let sessionDestinationAlert:[Character] = ["C","a","l","l","s"," ","a","r","e"," "]
fileprivate let screenMannerPriceAlert:String = "CANCE"

/*: "The call has ended" :*/
fileprivate let styleChinFierceConfig:[Character] = ["T","h","e"," ","c","a","l","l"]
fileprivate let servicePopularText:String = " hasgive request tip"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FrameworkViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let layoutPlainSystem = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class FrameworkViewController: GroupThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: GrantChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PreferenceReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: FragFamousReactiveCompatible? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        TopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TopThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: coreDoinglyThresholdError.map{purpleComment(mm: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! TamFamousControllerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.mConnectProgramRefresh()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            existentArc()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.pastWarningMode()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        teleportHide()
        //: setupSubViewsConstraint()
        writeSwaddlingClothes()
        //: initNotificationObserver()
        includeObserver()
        //: req_callGetUserInfo()
        dauntless()
        //: timeoutRules()
        wordName()
        //: resumeTalk()
        sinceFollowTitle()
        //: setUpBGMPlayer()
        only()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PositionThen.share.appConfigMode.disableShootScreen {
            if PositionThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ShieldView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFamous = false
        TopThen.shared.isFamous = false
        //: TalkingSocketManager.shared.isCalling = false
        TopThen.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.combineEvery()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: FamousView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = FamousView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: ReachBalanceReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = ReachBalanceReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func dauntless() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        FeatureChatManager.usedCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: spacingScriptName.map{ChartFindSilentOpen.insideTrack(pending: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(viewWrapTimer.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(spacingBubbleValue.replacingOccurrences(of: "package", with: "a"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(spacingVisualEvent.replacingOccurrences(of: "stage", with: "r") + String(featureClearlyConfig))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(appNorPlayerConfig.replacingOccurrences(of: "mark", with: "id") + "oUrl")].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(k_yourMessage))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.betweenInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func existentArc() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        MagnitudeReactiveCompatible.streetwiseBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colouration(showMsg: displayReadPath)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (String(componentAdviceUrl.suffix(8)) + String(moduleRequestFormat)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: spacingScriptName.map{ChartFindSilentOpen.insideTrack(pending: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.viewWho()
                //: return
                return
            }

            //: self.stopBGM()
            self.occasionDialog()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PreferenceReactiveCompatible.burn(type: .Response)
            //: self.startBGM()
            self.mConnectProgramRefresh()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func combineEvery() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.occasionDialog()
        //: TalkingSocketManager.shared.callerDelegate = nil
        TopThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        TopThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        TopThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func viewWho(animated: Bool = true) {
        //: self.stopBGM()
        self.occasionDialog()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func characterDown() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.styleId(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func sinceFollowTitle() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            characterDown()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func includeObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(insideTrack(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(scope(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: styleCheckAlert,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        layoutPlainSystem.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func insideTrack(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            viewWho()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func scope(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard TopThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        TopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        TopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        TopThen.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.up(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension FrameworkViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.colouration(showMsg: (String(widgetCalledSystem) + String(sessionPerceptSlowMetrics)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (String(commonExtentHelper) + String(commonModelSystem)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (colorMarkError.lowercased() + "Call"), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (String(componentAdviceUrl.suffix(8)) + String(moduleRequestFormat)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: spacingScriptName.map{ChartFindSilentOpen.insideTrack(pending: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.cornerPublishView()
                //: TalkingSocketManager.shared.currFamousVC = nil
                TopThen.shared.currFamousVC = nil
            }
            //: popCurrentViewController()
            viewWho()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.markLog(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func only() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? SeasonSum.Call : SeasonSum.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PreferenceReactiveCompatible.burn(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func mConnectProgramRefresh() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.twoPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func occasionDialog() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.explainAbout()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func wordName() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? k_transformName : self.chatModel?.stayInPlaceLikeTime()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            todayAnima()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.todayAnima()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func todayAnima() {
        //: guard !TalkingSocketManager.shared.isFamous else { return }
        guard !TopThen.shared.isFamous else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.colouration(showMsg: (String(spacingRoadPaperPage.prefix(4)) + styleAilAlert.replacingOccurrences(of: "abstract", with: "e")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (colorMarkError.lowercased() + "Call"), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (String(componentAdviceUrl.suffix(8)) + String(moduleRequestFormat)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: spacingScriptName.map{ChartFindSilentOpen.insideTrack(pending: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        TopThen.shared.info(info: socketDict)
        //: popCurrentViewController()
        viewWho()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func detail(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(appMultipleUtility.replacingOccurrences(of: "bind", with: "du") + String(displayMaximumPlatform))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.number(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.lengthIconStage()
            //: createTalkRoom()
            characterDown()
            //: self.stopBGM()
            self.occasionDialog()
        }
    }
}

// MARK: - PortraitObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension FrameworkViewController: PortraitObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func raw(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(featureSelectTimer) + String(screenFunAlert.suffix(7))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(sessionLabelKey.replacingOccurrences(of: "line", with: "ca") + String(moduleMotionPersonCastError.prefix(3)))].stringValue == PanelScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(displayJustPicValue.prefix(6)) + String(displayMeUrl.prefix(8))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colouration(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.cornerPublishView()
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }

        //: popCurrentViewController()
        viewWho()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func sound(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.occasionDialog()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.lengthIconStage()
        //: self.createTalkRoom()
        self.characterDown()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func banIcon(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(appShallData.prefix(5)) + String(commonExpressionMetrics))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(featureSelectTimer) + String(screenFunAlert.suffix(7))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(componentContendAlert.suffix(5)) + kProcessHelper.replacingOccurrences(of: "him", with: "x"))].stringValue == PanelScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(displayJustPicValue.prefix(6)) + String(displayMeUrl.prefix(8))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if PositionThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if PositionThen.share.appStatus == BetterQuantity.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: featureInjureObjectKey.map{$0^254}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kCompanyContent.suffix(5)) + "ther " + displayTrickAlert.replacingOccurrences(of: "button", with: "un") + " up t" + String(spacingLipLogger.suffix(4)) + "hone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colouration(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.cornerPublishView()
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.theOutputCost()
        //: self.popCurrentViewController()
        self.viewWho()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: sessionOfPreference, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func conversionChat(msg: String, data: [String: Any]) {
        //: guard PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard PositionThen.share.appStatus == BetterQuantity.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(moduleMotionLoopAlarmLogger))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_guidelineSession))] as? String else { return }
            //: let httpsUrl = PositionThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = PositionThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ReachConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ProdPushManager.share.rangeGalleryThirty(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ThereAlertShow.tillLaunch(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(styleMentionMessage.prefix(6))).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(viewLetterPreference.prefix(6)) + "ld c" + styleHemTimer.replacingOccurrences(of: "greet", with: "in")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ThereAlertShow.outsideGold()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ProdPushManager.share.responsibility(clickEvent: spacingMagnitudeName, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func checkChat(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(featureProblemPage))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.overlook(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.roundSimultaneously(point: addBean)
        }
    }
}

// MARK: - EnhanceFailObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FrameworkViewController: EnhanceFailObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func club(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(featureSelectTimer) + String(screenFunAlert.suffix(7))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(commonVaryMetrics.replacingOccurrences(of: "jus", with: "ef") + "seSex")].stringValue == PanelScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(displayJustPicValue.prefix(6)) + String(displayMeUrl.prefix(8))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.colouration(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.cornerPublishView()
            //: TalkingSocketManager.shared.currFamousVC = nil
            TopThen.shared.currFamousVC = nil
        }
        //: popCurrentViewController()
        viewWho()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func labelCommit(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.occasionDialog()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.lengthIconStage()
        //: self.createTalkRoom()
        self.characterDown()
    }
}

// MARK: - ConsentErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension FrameworkViewController: ConsentErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func noWall(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(componentAdviceUrl.suffix(8)) + String(moduleRequestFormat)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.colouration(showMsg: (String(spacingRoadPaperPage.prefix(4)) + styleAilAlert.replacingOccurrences(of: "abstract", with: "e")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.cornerPublishView()
                //: TalkingSocketManager.shared.currFamousVC = nil
                TopThen.shared.currFamousVC = nil
            }
            //: self.popCurrentViewController()
            self.viewWho()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(styleGreetSession)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PositionThen.share.appStatus == BetterQuantity.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(sufficient: false)
            ProdPushManager.share.responsibility(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(styleGreetSession)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.colouration(showMsg: msg)
            //: self.popCurrentViewController()
            self.viewWho()
        }
    }
}

// MARK: - AlongsideObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension FrameworkViewController: AlongsideObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func pristine() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (moduleEstimatedFormat.replacingOccurrences(of: "below", with: "cc") + "tCal" + String(sessionCompleteDevice)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: spacingScriptName.map{ChartFindSilentOpen.insideTrack(pending: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func disappearPlace() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colouration(showMsg: (String(sessionDestinationAlert) + screenMannerPriceAlert.lowercased() + "lled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (colorMarkError.lowercased() + "Call"), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.colouration(showMsg: (String(sessionDestinationAlert) + screenMannerPriceAlert.lowercased() + "lled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (String(componentAdviceUrl.suffix(8)) + String(moduleRequestFormat)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: spacingScriptName.map{ChartFindSilentOpen.insideTrack(pending: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.colouration(showMsg: (String(styleChinFierceConfig) + String(servicePopularText.prefix(4)) + " ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            TopThen.shared.info(info: [String(bytes: colorRefUrl.map{seeLink(select: $0)}, encoding: .utf8)!: (String(commonExtentHelper) + String(commonModelSystem)), String(bytes: featureAppealKey.map{scriptUntil(exactly: $0)}, encoding: .utf8)!: [String(bytes: themeDecreaseTitle.map{$0^67}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.theOutputCost()
        //: self.popCurrentViewController()
        self.viewWho()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func marshal(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.narrow(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func uSGovernment(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.theHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func funTin() {
        //: TalkingSocketManager.shared.currFamousVC = self
        TopThen.shared.currFamousVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = FragFamousReactiveCompatible.app()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.famousTick()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.propertyLine()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.openceNameure(talkingTime)
        //: popCurrentViewController()
        viewWho()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func detectJoin(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.openceNameure(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func receiveForArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.seeThrough(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension FrameworkViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func teleportHide() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func writeSwaddlingClothes() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
