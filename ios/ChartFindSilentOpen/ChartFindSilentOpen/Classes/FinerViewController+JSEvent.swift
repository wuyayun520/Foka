
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let stylePendingAppealPreference:[UInt8] = [0xd9,0xc7,0xc4,0xd8,0xcb,0xc7,0xd9,0xe1,0xd6,0xca,0xcb,0xd4,0xc6,0xe1,0xd4,0xc7,0xc8,0xd4,0xc7,0xd5,0xca,0xc5,0xd1,0xcb,0xd0,0xe1,0xd0,0xd1,0xd6,0xcb,0xc8,0xcb,0xc5,0xc3,0xd6,0xcb,0xd1,0xd0]

fileprivate func lieSumro(spark num: UInt8) -> UInt8 {
    let value = Int(num) + 126
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "goodsId" :*/
fileprivate let viewTermsSettings:[Character] = ["g"]
fileprivate let spacingHolderFormat:String = "oodsIdtext strong sequence barrel"

/*: "source" :*/
fileprivate let sessionPlateGreenEvent:[Character] = ["s","o","u","r","c"]
fileprivate let themeOndConfig:[Character] = ["e"]

/*: "type" :*/
fileprivate let screenChooseAwayConfig:String = "TYPE"

/*: "title" :*/
fileprivate let displayRemPage:String = "ttaskle"

/*: "url" :*/
fileprivate let displayNameMessage:String = "URL"

/*: "money" :*/
fileprivate let widgetCreaturePreference:String = "mjoinney"

/*: "Other" :*/
fileprivate let displayEqualTimer:[Character] = ["O","t","h","e","r"]

/*: "scene" :*/
fileprivate let appEraseKey:[Character] = ["s","c","e","n","e"]

/*: "show" :*/
fileprivate let screenEliteAlert:String = "SHOW"

/*: "target" :*/
fileprivate let styleContactError:[Character] = ["t","a","r","g","e","t"]

/*: "eventName" :*/
fileprivate let kShareText:String = "choicee"
fileprivate let spacingThoughtKey:String = "force digital middlentName"

/*: "jsonString" :*/
fileprivate let displayWaitEvent:String = "cow stuffjsonStrin"
fileprivate let componentSumAlert:[Character] = ["g"]

/*: "coin" :*/
fileprivate let sessionDatabasePath:String = "processin"

/*: "uid" :*/
fileprivate let colorWantLastEvent:[Character] = ["u","i","d"]

/*: "closeWeb" :*/
fileprivate let screenCrossPopularLogger:String = "closhang"
fileprivate let featureLowCloudHelper:String = "Webrowing max birth lack"

/*: "未实现的js事件： :*/
fileprivate let moduleIlkUtility:String = "未实现的"
fileprivate let moduleLifePreference:String = "js\u{4e8b}件："

/*: "PurchaseClick" :*/
fileprivate let featureRetainSystem:String = "turn replacement hurry core selectedPurchas"
fileprivate let serviceAssertName:String = "eClickstuff restore her back clearly"

/*: "Retry After or Go to " :*/
fileprivate let screenThoughtUtility:String = "Retry Atrigger remove"
fileprivate let commonActPlatform:[Character] = ["f","t","e","r"," ","o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let appOurSettings:String = "regain root like stop conditionFeedba"
fileprivate let k_brushKey:String = "length"

/*: " to contact us" :*/
fileprivate let viewWrapError:String = "demand tun portrait anti policeman to co"
fileprivate let sessionFloorId:String = "t usthree wander deny height medium"

/*: "Apple" :*/
fileprivate let k_dominantError:String = "resign telephoneApple"

/*: " apple支付充值失败： :*/
fileprivate let kPressHelper:String = "\u{f8ff} apple支"
fileprivate let displayTonePath:[Character] = ["\u{4ed8}","充","值","失","\u{8d25}","："]

/*: "payResultCallback();" :*/
fileprivate let coreLightUrl:String = "easterny"
fileprivate let styleMagneticHelper:String = "Resultpurple explore enjoy long sort"
fileprivate let moduleHarshSession:String = "civic youth same youck();"

/*: "USD" :*/
fileprivate let commonManageDevice:String = "USbench"

/*: "amount" :*/
fileprivate let appBladeSession:[UInt8] = [0xb8,0xb4,0xb6,0xac,0xb7,0xad]

private func birdAm(somebody num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let layoutGuestSession:[UInt8] = [0xa5,0xa7,0xb6,0x91,0xbb,0xb1,0xb6,0xa7,0xaf,0x8c,0xad,0xb6,0xab,0xa4,0xab,0xa1,0xa3,0xb6,0xab,0xad,0xac,0xb6,0x91,0xb6,0xa3,0xb6,0xb7,0xb1,0xea,0xb6,0xb0,0xb7,0xa7,0xeb]

private func antiCourse(outdo num: UInt8) -> UInt8 {
    return num ^ 194
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let sessionHighwayEvent:[UInt8] = [0x6,0x4,0x15,0x32,0x18,0x12,0x15,0x4,0xc,0x2f,0xe,0x15,0x8,0x7,0x8,0x2,0x0,0x15,0x8,0xe,0xf,0x15,0x32,0x15,0x0,0x15,0x14,0x12,0x49,0x7,0x0,0xd,0x12,0x4,0x48]

private func jobNight(tip num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "mfBean" :*/
fileprivate let commonRiskyFormat:[UInt8] = [0xd8,0xd1,0xad,0xd0,0xcc,0xd9]

fileprivate func takeKit(workplace num: UInt8) -> UInt8 {
    let value = Int(num) + 149
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FinerViewController+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let commonNetworkId = NSNotification.Name(rawValue: String(bytes: stylePendingAppealPreference.map{lieSumro(spark: $0)}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension FinerViewController {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func scriptCover(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = ButterflyKeyRepresentable(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.observeAction(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            nativeHuman()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            arch(productId: json[(String(viewTermsSettings) + String(spacingHolderFormat.prefix(6)))].stringValue, source: json[(String(sessionPlateGreenEvent) + String(themeOndConfig))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(screenChooseAwayConfig.lowercased())].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                k_rankText = componentNameKey
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(screenChooseAwayConfig.lowercased())].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                k_rankText = componentNameKey
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            project(type: json[(screenChooseAwayConfig.lowercased())].stringValue, productId: json[(String(viewTermsSettings) + String(spacingHolderFormat.prefix(6)))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            plankDown(title: json[(displayRemPage.replacingOccurrences(of: "task", with: "it"))].stringValue, url: json[(displayNameMessage.lowercased())].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            auctionMode(price: Double(json[(widgetCreaturePreference.replacingOccurrences(of: "join", with: "o"))].stringValue) ?? 0, payMode: (String(displayEqualTimer)), scene: json[(String(appEraseKey))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            broadcastMaxim(show: json[(screenEliteAlert.lowercased())].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            duringDit(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: TalkingAppPushManager.share.func__pushToPriveteChatVC(chatID: FragThen.getCustomerServiceID())
            ProdPushManager.share.monthCompletion(chatID: FragThen.opHistory())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(String(styleContactError))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: TalkingAppPushManager.share.func__pushToSubscribePageWebVC()
                ProdPushManager.share.feather()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = BetterRecognizerDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                deviceJus()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                micType(type: SignatureHashableRepresentation.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: widgetTransformConfig,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                micType(type: SignatureHashableRepresentation.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                micType(type: SignatureHashableRepresentation.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                micType(type: SignatureHashableRepresentation.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = AutoExpectThen()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                micType(type: SignatureHashableRepresentation.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(screenChooseAwayConfig.lowercased())].intValue
            //: changeNotifaStatus(type: type)
            cooper(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            scanLink()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            alongRankStr(eventName: json[(kShareText.replacingOccurrences(of: "choice", with: "ev") + String(spacingThoughtKey.suffix(6)))].stringValue, jsonStr: json[(String(displayWaitEvent.suffix(9)) + String(componentSumAlert))].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            isTribute(coin: json[(sessionDatabasePath.replacingOccurrences(of: "process", with: "co"))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            silentPackage()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            titleile()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            RushThen.againOf { _, _, _ in
                //: if PositionThen.share.loginUserMode.jumpType == 1 {
                if PositionThen.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.observeAction(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: TalkingAppPushManager.share.func__pushToRandomVideoVC(isBeginRand: false)
                        ProdPushManager.share.noBrainerRand(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: TalkingAppPushManager.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            ProdPushManager.share.monthCompletion(chatID: json[(String(colorWantLastEvent))].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: TalkingAppPushManager.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            ProdPushManager.share.untilBuild(uid: json[(String(colorWantLastEvent))].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: TalkingAppPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            ProdPushManager.share.funcHarshMakeup(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: let view = TalkingRechargeDetentionView.init()
            let view = DeadlineDetentionReactiveCompatible()
            //: view.dtype = json["type"].intValue
            view.dtype = json[(screenChooseAwayConfig.lowercased())].intValue
            //: view.closeWeb = json["closeWeb"].boolValue
            view.closeWeb = json[(screenCrossPopularLogger.replacingOccurrences(of: "hang", with: "e") + String(featureLowCloudHelper.prefix(3)))].boolValue
            //: view.show()
            view.prep()
            //: view.closWebClock = { [weak self] in
            view.closWebClock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: self.func__closeAction(sender: nil)
                self.observeAction(sender: nil)
            }

        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            unneededQuality(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (moduleIlkUtility.capitalized + moduleLifePreference) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func project(type _: String, productId: String, payType: ForthType) {
        //: applePay(productId: productId, payType: payType)
        arch(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func arch(productId: String, source: Int = -1, payType: ForthType) {
        //: if PositionThen.share.loginUid.isEmptyString {
        if PositionThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        FailAnalyticsManager.share.eventCompanyNameNurseLogParameters(name: (String(featureRetainSystem.suffix(7)) + String(serviceAssertName.prefix(6))))

        //: ProgressHUD.show()
        WeatherMapThen.depressCalculate()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        ManagerReactiveCompatible.shared.linkDock(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            WeatherMapThen.evolution()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    self.showUsedAppreciate()
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    printing(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    auctionMode(price: reportMoney, payMode: (String(k_dominantError.suffix(5))), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (kPressHelper + String(displayTonePath)) + "\(status.rawValue)")
                }
            }
        }
    }

    func showUsedAppreciate() {
        WeatherMapThen.usedAppreciate((String(screenThoughtUtility.prefix(7)) + String(commonActPlatform)) + "\"" + (String(appOurSettings.suffix(6)) + k_brushKey.replacingOccurrences(of: "length", with: "ck")) + "\"" + (String(viewWrapError.suffix(6)) + "ntac" + String(sessionFloorId.prefix(4))).localized)
    }
    
    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func plankDown(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = GrantRushTalkingButton()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.childMagnitude(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(wandering), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(displayNameMessage.lowercased()): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func wandering(button: GrantRushTalkingButton) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(displayNameMessage.lowercased())] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        noRequestDeal()
    }

    /// 退出登录
    //: private func needLogin() {
    private func nativeHuman() {
        //: guard Int(PositionThen.share.loginUid) ?? 0 > 0 else {
        guard Int(PositionThen.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: serviceSaltUtility,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func printing(price: Double, payType: ForthType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((coreLightUrl.replacingOccurrences(of: "eastern", with: "pa") + String(styleMagneticHelper.prefix(6)) + "Callba" + String(moduleHarshSession.suffix(5)))) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        auctionMode(price: price, payMode: (String(k_dominantError.suffix(5))), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func auctionMode(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: moduleMethodAcceptPage, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: viewCaptureName, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: viewSumervalMicName, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: coreRobotContent, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            SparkReactiveCompatible.share.passenger(price: price, currency: (commonManageDevice.replacingOccurrences(of: "bench", with: "D")))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            SparkReactiveCompatible.share.beefcakeCurrency(price: price, currency: (commonManageDevice.replacingOccurrences(of: "bench", with: "D")))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        FailAnalyticsManager.share.theoretic(payType: payMode, price: price, currency: (commonManageDevice.replacingOccurrences(of: "bench", with: "D")))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if k_rankText.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            themeSincePlatform.lance(eventID: k_rankText, parameterStr: [String(bytes: appBladeSession.map{birdAm(somebody: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: commonNetworkId, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func broadcastMaxim(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func deviceJus() {
        //: TalkingAppPushManager.share.func__pushUserVerifyController(toast: nil)
        ProdPushManager.share.duringToast(toast: nil)
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func micType(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func cooper(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            RushThen.harshCity { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            RushThen.againOf { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            MagnitudeReactiveCompatible.tillEarly { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: layoutGuestSession.map{antiCourse(outdo: $0)}, encoding: .utf8)! : String(bytes: sessionHighwayEvent.map{jobNight(tip: $0)}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func scanLink() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        RushThen.againOf { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if InstrumentalityReactiveCompatible.aftermath().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: displayPointAlert, object: nil, userInfo: [String(bytes: commonRiskyFormat.map{takeKit(workplace: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func isTribute(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            RushThen.jaw { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(PositionThen.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(PositionThen.share.loginUserMode.mf_coin)! + value
            //: PositionThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            PositionThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func silentPackage() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: displayWindowRefreshSystem, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func alongRankStr(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        themeSincePlatform.givingStream(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func titleile() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
