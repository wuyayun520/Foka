
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let viewBeneathKey:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let componentBoySaveConfig:[Character] = ["C","a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let layoutPlayerOldLogger:[Character] = ["S","u","b","m","i","t"]

/*: "Please tell us your problems                   " :*/
fileprivate let displayFieldUrl:[UInt8] = [0xc8,0xf4,0xfd,0xf9,0xeb,0xfd,0xb8,0xec,0xfd,0xf4,0xf4,0xb8,0xed,0xeb,0xb8,0xe1,0xf7,0xed,0xea,0xb8,0xe8,0xea,0xf7,0xfa,0xf4,0xfd,0xf5,0xeb,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8,0xb8]

private func weeOrigin(route num: UInt8) -> UInt8 {
    return num ^ 152
}

/*: "You need to choose at least one." :*/
fileprivate let screenNoticeName:[UInt8] = [0x2e,0x65,0x6e,0x6f,0x20,0x74,0x73,0x61,0x65,0x6c,0x20,0x74,0x61,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x59]

/*: "Please tell us your problems." :*/
fileprivate let serviceConversationUtility:String = "Pleasscript wide would cos mutual"
fileprivate let spacingEnhanceError:[Character] = [" ","u","s"," ","y","o","u","r"," ","p","r","o","b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let screenAdvertisingEvent:String = "app/userothers build ring"
fileprivate let commonGraphicSettings:String = "Reportguard than snap blink"

/*: "reason" :*/
fileprivate let layoutGolfDevice:String = "reastorageon"

/*: "desc" :*/
fileprivate let colorInsertTimer:[UInt8] = [0x70,0x71,0x67,0x77]

private func clothesDistance(stall num: UInt8) -> UInt8 {
    return num ^ 20
}

/*: "mf/moment/report" :*/
fileprivate let coreDeviceMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/"]
fileprivate let kRemainId:String = "rcustomport"

/*: "momentId" :*/
fileprivate let colorTabValue:[UInt8] = [0xb,0x9,0xb,0x3,0x8,0x12,0x2f,0x2]

/*: "mf/momentReply/report" :*/
fileprivate let coreHourMultiPage:String = "mf/mowelcome image"
fileprivate let appSkipTempPath:[Character] = ["m","e","n","t","R","e","p","l","y","/","r","e","p","o","r","t"]

/*: "app/videoReport" :*/
fileprivate let appChaseTimer:[Character] = ["a","p","p","/","v","i"]
fileprivate let displayHouseUrl:String = "deoRyear off reward live"

/*: "logId" :*/
fileprivate let viewResourceLogicalPlatform:[UInt8] = [0x57,0x54,0x5c,0x72,0x5f]

private func bounceKingBeing(the num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "Submitted successfully" :*/
fileprivate let componentSkullData:String = "Submhunt then disappear judge gap"
fileprivate let sessionElementText:String = "contrast favor contend cloud succe"

/*: "Nudity" :*/
fileprivate let commonAnotherError:String = "Nuditymultiple scan at four"

/*: "Violence" :*/
fileprivate let moduleHostTimer:String = "Violemanual definition certain job"
fileprivate let colorEffectConfig:String = "ncreminder"

/*: "Harassment" :*/
fileprivate let colorChinPhoneHomeLogger:String = "Harafollow resolution calculation charm"
fileprivate let serviceArtifactUrl:[Character] = ["s","s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let appBehaviorLogger:String = "Suicnecessary exclusive residency secure decrease"
fileprivate let kAuctionPage:String = "auto angleor S"
fileprivate let kRandomMessage:[Character] = ["u","r","y"]

/*: "False Information" :*/
fileprivate let styleScopeText:[Character] = ["F","a","l","s","e"," ","I","n","f","o","r","m","a"]
fileprivate let serviceHelpBusinessmanData:[Character] = ["t","i","o","n"]

/*: "Spam" :*/
fileprivate let kHiddenPlatform:String = "voice carry ratingSpam"

/*: "Hate Speech" :*/
fileprivate let coreGreetAlert:String = "Hate what muscle"
fileprivate let sessionMissingKingPath:[Character] = ["c","h"]

/*: "Terrorism" :*/
fileprivate let appProcessingThoughtData:[Character] = ["T","e","r","r","o","r","i"]
fileprivate let spacingHelloEvent:String = "them"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let coreEastContent:[UInt8] = [0xb9,0x8b,0x86,0x8f,0xca,0x85,0x8c,0xca,0xa3,0x86,0x86,0x8f,0x8d,0x8b,0x86,0xca,0x85,0x98,0xca,0xb8,0x8f,0x8d,0x9f,0x86,0x8b,0x9e,0x8f,0x8e,0xca,0xad,0x85,0x85,0x8e,0x99]

private func padBind(put num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "Sending disgusting messages" :*/
fileprivate let widgetMaxSquareSession:String = "Sendwarning tell zone key"
fileprivate let colorActionRobotSettings:String = "disgblackt"
fileprivate let viewHurryName:[Character] = ["i"]
fileprivate let sessionRetailerStaffSystem:String = "ng mcreate transaction petition"

/*: "Using photos of nudity or drugs" :*/
fileprivate let k_liteAdditionSettings:[UInt8] = [0x73,0x67,0x75,0x72,0x64,0x20,0x72,0x6f,0x20,0x79,0x74,0x69,0x64,0x75,0x6e,0x20,0x66,0x6f,0x20,0x73,0x6f,0x74,0x6f,0x68,0x70,0x20,0x67,0x6e,0x69,0x73,0x55]

/*: "Harassing or sending ads" :*/
fileprivate let kNurseAimPreference:String = "Harassisoft passe"
fileprivate let widgetSideFoundationData:String = "ng otransaction history closely manager sample"
fileprivate let kInstanceSettings:String = "ncontactin"

/*: "Defraud or using fake info" :*/
fileprivate let themeApartId:String = "aggression prove join reduceDefr"
fileprivate let coreAmberDevice:String = "popular can suite contrastor usi"
fileprivate let kLessValue:[Character] = ["e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let viewGreenPlatform:[Character] = ["O","t","h","e","r"]
fileprivate let modulePremiumSession:String = "normal"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FeatureDataSource.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum MinimiseCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class FeatureDataSource: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: MinimiseCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [ButterflyThen] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = ButterflyThen()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: MinimiseCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        indicator()
        //: setupSubViewsConstraint()
        back()
        //: setupReasons()
        matchAcross()
        //: bindInteraction()
        cornerSumeraction()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: viewBeneathKey.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(componentBoySaveConfig)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.tributeChromaticColour(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.tributeChromaticColour().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(dismiss), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(layoutPlayerOldLogger)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.totalelMention(colors: UIColor.motion(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(inscribeLikeHumanActivitySpread), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: moduleAdjustPath - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.childMagnitude(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = FeatureReachViewDelegate(placeholder: String(bytes: displayFieldUrl.map{weeOrigin(route: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.vary(), frame: CGRect(x: 10, y: 10, width: moduleAdjustPath - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .modelSize(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.restoreAudience()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .modelSize(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.childMagnitude(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(HugeDescribeViewCell.self, forCellReuseIdentifier: HugeDescribeViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension FeatureDataSource {
    //: @objc func func__TapGestureRecognizer() {
    @objc func screenRecognizer() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func three(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = FragThen.getWindow()
            let window = FragThen.termsSource()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func sortAcross(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func dismiss() {
        //: self.dismissReportView(succeed: false)
        self.sortAcross(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func inscribeLikeHumanActivitySpread() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.colouration(showMsg: String(bytes: screenNoticeName.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.colouration(showMsg: (String(serviceConversationUtility.prefix(5)) + "e tell" + String(spacingEnhanceError)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(screenAdvertisingEvent.prefix(8)) + String(commonGraphicSettings.prefix(6)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (layoutGolfDevice.replacingOccurrences(of: "storage", with: "s")): selectedModel.type, String(bytes: colorInsertTimer.map{clothesDistance(stall: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(coreDeviceMessage) + kRemainId.replacingOccurrences(of: "custom", with: "e"))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: colorTabValue.map{$0^102}, encoding: .utf8)!: tempId, (layoutGolfDevice.replacingOccurrences(of: "storage", with: "s")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(coreHourMultiPage.prefix(5)) + String(appSkipTempPath))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (layoutGolfDevice.replacingOccurrences(of: "storage", with: "s")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(appChaseTimer) + String(displayHouseUrl.prefix(4)) + "eport")
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: viewResourceLogicalPlatform.map{bounceKingBeing(the: $0)}, encoding: .utf8)!: tempId, (layoutGolfDevice.replacingOccurrences(of: "storage", with: "s")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        FeatureThen.anonymous(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.quantityerproposal(showMsg: (String(componentSkullData.prefix(4)) + "itted" + String(sessionElementText.suffix(6)) + "ssfully").localized)
                //: self.dismissReportView(succeed: true)
                self.sortAcross(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension FeatureDataSource: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: HugeDescribeViewCell.className()) as! HugeDescribeViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.section(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension FeatureDataSource {
    /// 添加视图
    //: private func setupSubviews() {
    private func indicator() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func back() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func matchAcross() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(commonAnotherError.prefix(6))).localized,
                           //: "Violence".localized,
                           (String(moduleHostTimer.prefix(5)) + colorEffectConfig.replacingOccurrences(of: "reminder", with: "e")).localized,
                           //: "Harassment".localized,
                           (String(colorChinPhoneHomeLogger.prefix(4)) + String(serviceArtifactUrl)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(appBehaviorLogger.prefix(4)) + "ide " + String(kAuctionPage.suffix(4)) + "elf-inj" + String(kRandomMessage)).localized,
                           //: "False Information".localized,
                           (String(styleScopeText) + String(serviceHelpBusinessmanData)).localized,
                           //: "Spam".localized,
                           (String(kHiddenPlatform.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(coreGreetAlert.prefix(5)) + "Spee" + String(sessionMissingKingPath)).localized,
                           //: "Terrorism".localized,
                           (String(appProcessingThoughtData) + spacingHelloEvent.replacingOccurrences(of: "them", with: "sm")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: coreEastContent.map{padBind(put: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(widgetMaxSquareSession.prefix(4)) + "ing " + colorActionRobotSettings.replacingOccurrences(of: "black", with: "us") + String(viewHurryName) + String(sessionRetailerStaffSystem.prefix(4)) + "essages").localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: k_liteAdditionSettings.reversed(), encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(kNurseAimPreference.prefix(7)) + String(widgetSideFoundationData.prefix(4)) + "r se" + kInstanceSettings.replacingOccurrences(of: "contact", with: "d") + "g ads").localized,
                           //: "Defraud or using fake info".localized,
                           (String(themeApartId.suffix(4)) + "aud " + String(coreAmberDevice.suffix(6)) + "ng fak" + String(kLessValue)).localized,
                           //: "Others".localized]
                           (String(viewGreenPlatform) + modulePremiumSession.replacingOccurrences(of: "normal", with: "s")).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = PositionThen.share.appConfigMode.videoReport["reason"] {
            if let reason = PositionThen.share.appConfigMode.videoReport[(layoutGolfDevice.replacingOccurrences(of: "storage", with: "s"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = ButterflyThen()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func cornerSumeraction() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
