
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let componentTabLogger:[UInt8] = [0x3d,0x42,0x3d,0x48,0xfc,0x37,0x43,0x38,0x39,0x46,0xe,0xfd,0xf4,0x3c,0x35,0x47,0xf4,0x42,0x43,0x48,0xf4,0x36,0x39,0x39,0x42,0xf4,0x3d,0x41,0x44,0x40,0x39,0x41,0x39,0x42,0x48,0x39,0x38]

fileprivate func logKind(capable num: UInt8) -> UInt8 {
    let value = Int(num) + 44
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Text greeting" :*/
fileprivate let commonOriginSeekTitle:String = "Text build up"
fileprivate let screenLanceEvent:[Character] = ["g","r","e","e","t","i","n","g"]

/*: "Add" :*/
fileprivate let featureBlindStayPath:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let coreModeOrientMessage:String = "tyshake"

/*: "addStatus" :*/
fileprivate let sessionExecutePath:String = "addStatumind assist breath us"
fileprivate let appGenderConfig:String = "greatest"

/*: "list" :*/
fileprivate let moduleContendResponseDevice:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let appAllyFoundationSystem:[UInt8] = [0x3e,0xd,0x1a,0x5f,0x6,0x10,0xa,0x5f,0xc,0xa,0xd,0x1a,0x5f,0xb,0x10,0x5f,0x1b,0x1a,0x13,0x1a,0xb,0x1a,0x5f,0xb,0x17,0x16,0xc,0x5f,0x12,0x1a,0xc,0xc,0x1e,0x18,0x1a,0x90,0xc3,0xe0]

private func tellThrough(spend num: UInt8) -> UInt8 {
    return num ^ 127
}

/*: "Cancel" :*/
fileprivate let appCorrectAllZzSettings:String = "Cancelwide beautiful number"

/*: "Delete" :*/
fileprivate let componentChangeDevice:String = "photo number terribleDelete"

/*: "The upper limit has been reached" :*/
fileprivate let coreVisibleStretchLogger:[UInt8] = [0xfe,0x12,0xf,0xca,0x1f,0x1a,0x1a,0xf,0x1c,0xca,0x16,0x13,0x17,0x13,0x1e,0xca,0x12,0xb,0x1d,0xca,0xc,0xf,0xf,0x18,0xca,0x1c,0xf,0xb,0xd,0x12,0xf,0xe]

fileprivate func presentationLowly(opportunity num: UInt8) -> UInt8 {
    let value = Int(num) - 170
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "text" :*/
fileprivate let k_blackTimer:String = "tepress"

/*: "Successfully added, please wait for approval" :*/
fileprivate let spacingLaunchWithSystem:[UInt8] = [0xe8,0xce,0xd8,0xd8,0xde,0xc8,0xc8,0xdd,0xce,0xd7,0xd7,0xc2,0x9b,0xda,0xdf,0xdf,0xde,0xdf,0x97,0x9b,0xcb,0xd7,0xde,0xda,0xc8,0xde,0x9b,0xcc,0xda,0xd2,0xcf,0x9b,0xdd,0xd4,0xc9,0x9b,0xda,0xcb,0xcb,0xc9,0xd4,0xcd,0xda,0xd7]

private func actualLowly(curve num: UInt8) -> UInt8 {
    return num ^ 187
}

/*: "Any-Hex/Java" :*/
fileprivate let commonMoreTimer:String = "about recording bird objectAny-Hex"
fileprivate let kLicenseId:String = "argument harassment sh/Java"

/*: "#FF506D" :*/
fileprivate let commonMySettings:[Character] = ["#","F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let serviceStatisticalMetrics:String = "btn_merequired threat"
fileprivate let appWeekCenterFinancialPage:[Character] = ["s","s"]
fileprivate let viewPoQuantSession:String = "age_tip warn pop"
fileprivate let coreActTitle:String = "rent scale following_nor"

/*: "Click the button below to add\ncontent" :*/
fileprivate let appYaFighterData:[UInt8] = [0xa5,0x8a,0x8f,0x85,0x8d,0xc6,0x92,0x8e,0x83,0xc6,0x84,0x93,0x92,0x92,0x89,0x88,0xc6,0x84,0x83,0x8a,0x89,0x91,0xc6,0x92,0x89,0xc6,0x87,0x82,0x82,0xec,0x85,0x89,0x88,0x92,0x83,0x88,0x92]

private func accessCommand(equally num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let serviceOwnTapeAlert:[UInt8] = [0x8,0x37,0x3a,0x31,0x7f,0x3e,0x3b,0x3b,0x36,0x31,0x38,0x7f,0x32,0x2a,0x33,0x2b,0x36,0x2f,0x33,0x3a,0x7f,0x2b,0x3a,0x27,0x2b,0x7f,0x32,0x3a,0x2c,0x2c,0x3e,0x38,0x3a,0x73,0x7f,0x2b,0x37,0x3a,0x7f,0x2c,0x26,0x2c,0x2b,0x7f,0x3a,0x32,0x7f,0x28,0x36,0x33,0x33,0x7f,0x2c,0x3a,0x31,0x3b,0x7f,0x2b,0x37,0x3a,0x32,0x7f,0x3d,0x26,0x7f,0x2b,0x2a,0x2d,0x31,0x2c,0x64,0x2c,0x33,0x36,0x3b,0x3a,0x7f,0x2b,0x30,0x7f,0x2b,0x37,0x3a,0x7f,0x32,0x3a,0x2c,0x2c,0x3e,0x38,0x3a,0x7f,0x2b,0x30,0x7f,0x3b,0x3a,0x33,0x3a,0x2b,0x3a]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SmallTextVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class SmallTextVc: GroupThen {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: GrantSaluteThen?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: componentTabLogger.map{logKind(capable: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(commonOriginSeekTitle.prefix(5)) + String(screenLanceEvent)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.layerColor()
        //: designView()
        atView()
        //: getAudioListData()
        offer()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [CrashMeasurable] = {
        //: var array = [TalkingGreetModel]()
        var array = [CrashMeasurable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(featureBlindStayPath)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.totalelMention(colors: UIColor.motion(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cover), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension SmallTextVc {
    //: func getAudioListData() {
    func offer() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(coreModeOrientMessage.replacingOccurrences(of: "shake", with: "pe"))] = 2
        //: ProgressHUD.show()
        WeatherMapThen.depressCalculate()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ToolThen.queryion(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            WeatherMapThen.evolution()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.profileView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(sessionExecutePath.prefix(8)) + appGenderConfig.replacingOccurrences(of: "greatest", with: "s"))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(moduleContendResponseDevice))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [CrashMeasurable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<CrashMeasurable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func breakUp(index: Int) {
        //: let config = ShowAlertConfig()
        let config = FeatureAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.modelSize(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        ThereAlertShow.regarding(message: String(bytes: appAllyFoundationSystem.map{tellThrough(spend: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(appCorrectAllZzSettings.prefix(6))).localized, rightBtnTitle: (String(componentChangeDevice.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            ThereAlertShow.outsideGold()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            ThereAlertShow.outsideGold()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: CrashMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            WeatherMapThen.depressCalculate()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ToolThen.element(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WeatherMapThen.evolution()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.offer()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension SmallTextVc {
    //: @objc func addButtonClick() {
    @objc func cover() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.colouration(showMsg: String(bytes: coreVisibleStretchLogger.map{presentationLowly(opportunity: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = GrantSaluteThen(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.circuit()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.unicode(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(coreModeOrientMessage.replacingOccurrences(of: "shake", with: "pe"))] = 2
            //: dict["text"] = tempStr
            dict[(k_blackTimer.replacingOccurrences(of: "press", with: "xt"))] = tempStr
            //: ProgressHUD.show()
            WeatherMapThen.depressCalculate()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            ToolThen.addCompletion(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WeatherMapThen.evolution()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.quantityerproposal(showMsg: String(bytes: spacingLaunchWithSystem.map{actualLowly(curve: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.offer()
                //: self.recordView?.dismiss()
                self.recordView?.threadTab()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func unicode(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(commonMoreTimer.suffix(7)) + String(kLicenseId.suffix(5))) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension SmallTextVc: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: moduleAdjustPath - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.modelSize(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = TamContentCell.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: TamContentCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TamContentCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.harshCreate(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(componentChangeDevice.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.breakUp(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(commonMySettings)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.breakUp(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.titled(name: (String(serviceStatisticalMetrics.prefix(6)) + String(appWeekCenterFinancialPage) + String(viewPoQuantSession.prefix(4)) + "delete" + String(coreActTitle.suffix(4)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = MedalImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension SmallTextVc {
    //: private func setupEmptyDataView() {
    private func profileView() {
        //: var style = EmptyStyle()
        var style = EarnEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: appYaFighterData.map{accessCommand(equally: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .layerColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func atView() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(TamContentCell.self, forCellReuseIdentifier: TamContentCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.vary()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.attachSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: serviceOwnTapeAlert.map{$0^95}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
