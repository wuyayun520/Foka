
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kClubName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Voice greeting" :*/
fileprivate let spacingBlindLogger:String = "none how albumVoic"
fileprivate let screenHangMessage:String = "address"
fileprivate let commonRepresentativeAlert:[Character] = ["e","t","i","n","g"]

/*: "Add" :*/
fileprivate let appPairSession:String = "Addmarketplace basic volume writing"

/*: "type" :*/
fileprivate let k_easternUrl:String = "typrounding"

/*: "addStatus" :*/
fileprivate let stylePressureData:[Character] = ["a","d","d","S","t","a","t","u","s"]

/*: "list" :*/
fileprivate let commonWithinDevice:String = "lipair"

/*: "audio" :*/
fileprivate let k_capValue:[Character] = ["a","u","d","i","o"]

/*: "length" :*/
fileprivate let sessionStrokePhoneData:[Character] = ["l"]
fileprivate let k_everyoneName:[Character] = ["e","n","g","t","h"]

/*: "url" :*/
fileprivate let featurePitchPlanError:String = "uractivity"

/*: "Successfully added, please wait for approval" :*/
fileprivate let commonCalledSoundFormat:[UInt8] = [0x49,0x6b,0x59,0x59,0x5b,0x69,0x69,0x5c,0x6b,0x62,0x62,0x6f,0x16,0x57,0x5a,0x5a,0x5b,0x5a,0x22,0x16,0x66,0x62,0x5b,0x57,0x69,0x5b,0x16,0x6d,0x57,0x5f,0x6a,0x16,0x5c,0x65,0x68,0x16,0x57,0x66,0x66,0x68,0x65,0x6c,0x57,0x62]

fileprivate func sectionObserver(still num: UInt8) -> UInt8 {
    let value = Int(num) - 246
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Are you sure to delete this message？" :*/
fileprivate let sessionAntiMotionUrl:[UInt8] = [0xd7,0xe4,0xf3,0xb6,0xef,0xf9,0xe3,0xb6,0xe5,0xe3,0xe4,0xf3,0xb6,0xe2,0xf9,0xb6,0xf2,0xf3,0xfa,0xf3,0xe2,0xf3,0xb6,0xe2,0xfe,0xff,0xe5,0xb6,0xfb,0xf3,0xe5,0xe5,0xf7,0xf1,0xf3,0x79,0x2a,0x9]

private func exceptionResist(ta num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "Cancel" :*/
fileprivate let themeAcceptData:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let styleCowRecordTimer:String = "restore pairDelete"

/*: "Please wait for the audit result" :*/
fileprivate let widgetIdeaTitle:[UInt8] = [0xd0,0xec,0xe5,0xe1,0xf3,0xe5,0xa0,0xf7,0xe1,0xe9,0xf4,0xa0,0xe6,0xef,0xf2,0xa0,0xf4,0xe8,0xe5,0xa0,0xe1,0xf5,0xe4,0xe9,0xf4,0xa0,0xf2,0xe5,0xf3,0xf5,0xec,0xf4]

fileprivate func beautyStop(task num: UInt8) -> UInt8 {
    let value = Int(num) + 128
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The upper limit has been reached" :*/
fileprivate let commonWalkingUrl:[UInt8] = [0x3a,0x4e,0x4b,0x6,0x5b,0x56,0x56,0x4b,0x58,0x6,0x52,0x4f,0x53,0x4f,0x5a,0x6,0x4e,0x47,0x59,0x6,0x48,0x4b,0x4b,0x54,0x6,0x58,0x4b,0x47,0x49,0x4e,0x4b,0x4a]

fileprivate func rueHouse(others num: UInt8) -> UInt8 {
    let value = Int(num) - 230
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "filePath" :*/
fileprivate let widgetWeeklyLowerQuickPreference:[Character] = ["f","i","l"]
fileprivate let moduleBlinkValue:String = "ePathhand spend"

/*: "time" :*/
fileprivate let appFemaleCommentLogger:String = "timinde"

/*: "#FF506D" :*/
fileprivate let featureNothingBindTimer:[Character] = ["#","F","F","5","0"]
fileprivate let serviceReaderAlert:String = "to representation capable person6D"

/*: "btn_message_delete_nor" :*/
fileprivate let moduleEditPage:[Character] = ["b","t","n","_","m"]
fileprivate let colorMustUtility:[Character] = ["e","s","s","a","g","e","_","d","e","l","e","t","e","_","n","o","r"]

/*: "Currently mute" :*/
fileprivate let widgetLipDevice:String = "Currendecide lowly factor fragment mortgage"
fileprivate let featureProductionByError:String = "mutobserver"

/*: "Edit Title" :*/
fileprivate let themeOriginWaterPage:[Character] = ["E","d","i","t"," ","T","i","t","l","e"]

/*: "remark" :*/
fileprivate let serviceMomentumMessage:[Character] = ["r"]
fileprivate let k_bladeFormat:String = "emrevenuerk"

/*: "Click the button below to add\ncontent" :*/
fileprivate let viewFloorPlatform:[UInt8] = [0x1e,0x31,0x34,0x3e,0x36,0x7d,0x29,0x35,0x38,0x7d,0x3f,0x28,0x29,0x29,0x32,0x33,0x7d,0x3f,0x38,0x31,0x32,0x2a,0x7d,0x29,0x32,0x7d,0x3c,0x39,0x39,0x57,0x3e,0x32,0x33,0x29,0x38,0x33,0x29]

private func ternExplain(head num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let coreCropSettings:[UInt8] = [0x71,0x54,0x54,0x10,0x5d,0x45,0x5c,0x44,0x59,0x40,0x5c,0x55,0x10,0x46,0x5f,0x59,0x53,0x55,0x10,0x57,0x42,0x55,0x55,0x44,0x59,0x5e,0x57,0x43,0x1c,0x10,0x51,0x5e,0x54,0x10,0x44,0x58,0x55,0x10,0x43,0x49,0x43,0x44,0x55,0x5d,0x10,0x47,0x59,0x5c,0x5c,0x10,0x43,0x55,0x5e,0x54,0x10,0x46,0x5f,0x59,0x53,0x55,0x10,0x5d,0x55,0x43,0x43,0x51,0x57,0x55,0x43,0x10,0x52,0x49,0x10,0x44,0x45,0x42,0x5e,0x43,0xb,0x3a,0x73,0x5c,0x59,0x53,0x5b,0x10,0x44,0x58,0x55,0x10,0x52,0x45,0x52,0x52,0x5c,0x55,0x10,0x44,0x5f,0x10,0x40,0x5c,0x51,0x49,0x10,0x44,0x58,0x55,0x10,0x46,0x5f,0x59,0x53,0x55,0x1c,0x10,0x43,0x5c,0x59,0x54,0x55,0x10,0x44,0x5f,0x10,0x44,0x58,0x55,0x10,0x46,0x5f,0x59,0x53,0x55,0x10,0x51,0x42,0x55,0x51,0x10,0x44,0x5f,0x10,0x54,0x55,0x5c,0x55,0x44,0x55,0x10,0x59,0x44,0x1c,0x10,0x51,0x5e,0x54,0x10,0x44,0x58,0x55,0x10,0x46,0x5f,0x59,0x53,0x55,0x10,0x44,0x59,0x44,0x5c,0x55,0x10,0x53,0x51,0x5e,0x10,0x52,0x55,0x10,0x55,0x54,0x59,0x44,0x55,0x54,0x1e]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ForeheadViewDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class ForeheadViewDelegate: GroupThen {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: SolarArrayThen?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: RationalReactiveCompatible?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: PaneView?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kClubName.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(spacingBlindLogger.suffix(4)) + "e gr" + screenHangMessage.replacingOccurrences(of: "address", with: "e") + String(commonRepresentativeAlert)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.layerColor()
        //: designView()
        viewCut()
        //: getAudioListData()
        anyExecute()
        //: setMutedDetection()
        beforeWhite()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(notificationPlayerAudioCancel(notification:)), name: appScaleSession, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(resign(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(screenSpread),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: appScaleSession,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.channelList()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [CrashMeasurable] = {
        //: var array = [TalkingGreetModel]()
        var array = [CrashMeasurable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(appPairSession.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.totalelMention(colors: UIColor.motion(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(submerge), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension ForeheadViewDelegate {
    //: func getAudioListData() {
    func anyExecute() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(k_easternUrl.replacingOccurrences(of: "rounding", with: "e"))] = 1
        //: ProgressHUD.show()
        WeatherMapThen.depressCalculate()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ToolThen.queryion(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            WeatherMapThen.evolution()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.selected()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(stylePressureData))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(commonWithinDevice.replacingOccurrences(of: "pair", with: "st"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [CrashMeasurable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<CrashMeasurable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func count(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(k_easternUrl.replacingOccurrences(of: "rounding", with: "e"))] = 1
        //: dict["audio"] = data
        dict[(String(k_capValue))] = data
        //: dict["length"] = Int(time)
        dict[(String(sessionStrokePhoneData) + String(k_everyoneName))] = Int(time)

        //: ProgressHUD.show()
        WeatherMapThen.depressCalculate()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        ToolThen.addCompletion(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            WeatherMapThen.evolution()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(featurePitchPlanError.replacingOccurrences(of: "activity", with: "l"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.quantityerproposal(showMsg: String(bytes: commonCalledSoundFormat.map{sectionObserver(still: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.anyExecute()
                //: self.recordView?.dismiss()
                self.recordView?.mouthDismiss()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func choose(index: Int) {
        //: let config = ShowAlertConfig()
        let config = FeatureAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.modelSize(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        ThereAlertShow.regarding(message: String(bytes: sessionAntiMotionUrl.map{exceptionResist(ta: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(themeAcceptData)).localized, rightBtnTitle: (String(styleCowRecordTimer.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            ThereAlertShow.outsideGold()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            ThereAlertShow.outsideGold()
            //: self.stopPlay()
            self.screenSpread()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: CrashMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ToolThen.element(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.anyExecute()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension ForeheadViewDelegate {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func notificationPlayerAudioCancel(notification _: NSNotification) {
        //: stopPlay()
        screenSpread()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func resign(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            screenSpread()
        }
    }

    //: @objc func addButtonClick() {
    @objc func submerge() {
        //: guard TalkingSocketManager.shared.isFamous == false else {
        guard TopThen.shared.isFamous == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colouration(showMsg: spacingQualityHundredSystem)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        MagnitudeReactiveCompatible.streetwiseBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.colouration(showMsg: displayReadPath)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.colouration(showMsg: String(bytes: widgetIdeaTitle.map{beautyStop(task: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.colouration(showMsg: String(bytes: commonWalkingUrl.map{rueHouse(others: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.screenSpread()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = SolarArrayThen(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.native()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(widgetWeeklyLowerQuickPreference) + String(moduleBlinkValue.prefix(5)))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.count(data: data!, time: dict[(appFemaleCommentLogger.replacingOccurrences(of: "mind", with: "m"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension ForeheadViewDelegate {
    //: func setMutedDetection() {
    func beforeWhite() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        EnhanceThen.shared.instantiate()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        EnhanceThen.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension ForeheadViewDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = PaneView.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: PaneView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! PaneView

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.networkIn(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.atTheSameTime(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.atTheSameTime(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            EnhanceThen.shared.instantiate()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.packageFor(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.screenSpread()
            //: self.editTitle(index: indexPath.section)
            self.gamut(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(styleCowRecordTimer.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.choose(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(featureNothingBindTimer) + String(serviceReaderAlert.suffix(2))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.choose(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.titled(name: (String(moduleEditPage) + String(colorMustUtility))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = MedalImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func packageFor(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isFamous else {
        guard !TopThen.shared.isFamous else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colouration(showMsg: spacingQualityHundredSystem)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.channelList()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? PaneView
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.atTheSameTime(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.colouration(showMsg: (String(widgetLipDevice.prefix(6)) + "tly " + featureProductionByError.replacingOccurrences(of: "observer", with: "e")).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = RationalReactiveCompatible.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.channelList()
        //: self.player?.play(urlStr: model.content)
        self.player?.third(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? PaneView
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.atTheSameTime(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func gamut(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = PromiseThen(title: (String(themeOriginWaterPage)).localized, leftBtnTitle: (String(themeAcceptData)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.stageInShow()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            WeatherMapThen.depressCalculate()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: CrashMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(String(serviceMomentumMessage) + k_bladeFormat.replacingOccurrences(of: "revenue", with: "a"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            ToolThen.holderPlay(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WeatherMapThen.evolution()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func screenSpread() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.channelList()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.atTheSameTime(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension ForeheadViewDelegate: ScreenToolDelegate {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func threshold(status: OpportunityTotalLiteral) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            screenSpread()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            screenSpread()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension ForeheadViewDelegate {
    //: private func setupEmptyDataView() {
    private func selected() {
        //: var style = EmptyStyle()
        var style = EarnEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: viewFloorPlatform.map{ternExplain(head: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .layerColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func viewCut() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(PaneView.self, forCellReuseIdentifier: PaneView.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.vary()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.attachSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: coreCropSettings.map{$0^48}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
