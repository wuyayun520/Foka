
//: Declare String Begin

/*: "cmd" :*/
fileprivate let spacingDomainUtility:[UInt8] = [0x2d,0x23,0x2a]

private func affairFill(secure num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "cancelCall" :*/
fileprivate let sessionAccessUtility:String = "cancelCmetal lit multiple request"
fileprivate let spacingFollowingValue:String = "aarch"

/*: "data" :*/
fileprivate let layoutOldPage:[UInt8] = [0x1e,0x1b,0x2e,0x1b]

fileprivate func sureFrameStrain(reply num: UInt8) -> UInt8 {
    let value = Int(num) + 70
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let coreLiveGlassTitle:String = "logIdstat sea"

/*: "#322870" :*/
fileprivate let colorHistoricUrl:String = "#timber2287"
fileprivate let spacingMoreMetrics:[Character] = ["0"]

/*: "Tap to start video chatting" :*/
fileprivate let layoutEnableEvent:String = "Tap tonecessity inmate"
fileprivate let colorFollowAlwaysEvent:String = "actual volume replacement generate vide"
fileprivate let viewLackData:String = "mouth"

/*: "icon_close_nor" :*/
fileprivate let displayOpportunityMessage:[Character] = ["i","c","o","n","_","c","l"]
fileprivate let widgetAnotherSystem:String = "ose_norlap finite pose"

/*: "icon_views_number" :*/
fileprivate let colorDryData:String = "icon_muscle beauty composition delay"
fileprivate let screenSauceError:String = "our empty specifically blacks_num"
fileprivate let colorModeHelper:[Character] = ["b","e","r"]

/*: "%@ chatting" :*/
fileprivate let spacingControlFormat:String = "pap app rank%@ c"
fileprivate let widgetStretchName:String = "addresstting"

/*: "icon_lounge" :*/
fileprivate let widgetNetSettings:String = "describe option actor will leasticon_"
fileprivate let viewBothPage:String = "titleunge"

/*: "icon_rank_coin" :*/
fileprivate let widgetDigAlert:String = "icobegin"
fileprivate let featureHoldHelper:String = "_coincreature reply chance"

/*: "vip_btn_top" :*/
fileprivate let kEffFormat:[Character] = ["v","i","p","_","b","t","n","_","t","o","p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let kApartmentTitle:String = "btn_nquick operation motion"
fileprivate let coreDueTimer:String = "beat until equivalent convert occasion_setti"
fileprivate let componentPillBasicTimer:String = "ngs_norsolar launch"

/*: "%@ Coins/min" :*/
fileprivate let kScheduleMessage:String = "nose length holiday%@ Co"
fileprivate let displayCollectionData:String = "narrow prettyins/min"

/*: "get json error" :*/
fileprivate let serviceMyMayHelper:String = "thirty builder soldier attachget j"
fileprivate let spacingSweetTitle:String = "watern"
fileprivate let styleKingName:String = " erroradvice input"

/*: "matchId" :*/
fileprivate let kInputValue:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let styleAnimalPath:[UInt8] = [0x6b,0x7c,0x7c,0x60,0x61]

/*: "VIPFee" :*/
fileprivate let commonPromptEliteTimer:String = "bean"
fileprivate let serviceAtmosphereUrl:[Character] = ["I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let themeYesterdaySession:String = "evaluate contactMatc"
fileprivate let coreNumbSystem:[Character] = ["w"]

/*: "userTouch" :*/
fileprivate let kLanguageError:String = "rent jaw profile resistance commentuserTo"
fileprivate let layoutDrawSystem:String = "reminderh"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let kUpperPlatform:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let appFormerHuntAlert:[UInt8] = [0x1c,0x28,0x25,0x23,0xa,0x2f,0x26,0x1b]

fileprivate func colonialGreatest(policeman num: UInt8) -> UInt8 {
    let value = Int(num) + 74
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let appYourselfId:[UInt8] = [0x6a,0x5c,0x46,0x14,0x45,0x56,0x13,0x51,0x56,0x56,0x5d,0x13,0x51,0x52,0x41,0x41,0x56,0x57,0x13,0x55,0x41,0x5c,0x5e,0x13,0x41,0x56,0x50,0x56,0x5a,0x45,0x5a,0x5d,0x54,0x13,0x50,0x52,0x5f,0x5f,0x40]

private func readyPhotoMuscle(message num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "source" :*/
fileprivate let sessionItData:[UInt8] = [0x65,0x63,0x72,0x75,0x6f,0x73]

/*: "type" :*/
fileprivate let widgetViewFormat:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let layoutYouthValue:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let themeDogKey:[Character] = ["f","r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let serviceLieUtility:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let layoutGreenSystem:String = "entry interval decrease glassonVideo"
fileprivate let kDetectSoapConfig:[Character] = ["M","a","t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let widgetUtilityError:[UInt8] = [0x1c,0x6,0x32,0x33,0x28,0x2a,0x26,0x33,0x2e,0x24,0x67,0x31,0x2e,0x23,0x22,0x28,0x1a,0x67,0x15,0x22,0x24,0x22,0x2e,0x31,0x22,0x67,0x31,0x2e,0x23,0x22,0x28,0x67,0x24,0x26,0x2b,0x2b,0x67,0x2e,0x29,0x24,0x28,0x2a,0x2e,0x29,0x20]

/*: "refuseCall" :*/
fileprivate let themeLikeEvent:[Character] = ["r","e","f","u","s","e","C","a"]
fileprivate let screenMemberActualMessage:String = "futurefuture"

/*: "onCancelCall" :*/
fileprivate let colorSeparationData:[Character] = ["o","n","C","a"]
fileprivate let colorOrientText:String = "ncelCallatmosphere throw sight"

/*: "retryAfter" :*/
fileprivate let sessionSomeoneUrl:String = "repacky"

/*: "onRequestCall" :*/
fileprivate let featureExposureFormat:String = "onRequfile detect railroad interested"
fileprivate let colorWaistFitHeartPreference:[Character] = ["e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let serviceHairConfig:String = "remain handle random weekly classfailTy"
fileprivate let moduleReceiveContent:String = "psight"

/*: "callWaitingTime" :*/
fileprivate let componentReFieldEvent:[Character] = ["c","a","l","l","W","a"]
fileprivate let styleTradeSettings:[Character] = ["i","t","i","n","g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let kDistantName:String = "unit random lucktoUid"

/*: "Free 1 min" :*/
fileprivate let styleAnonymousPleaseMetrics:String = "quantity entryFree "
fileprivate let commonThreatTitle:[Character] = ["1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let themeBigSystem:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SatelliteRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class SatelliteRecognizerDelegate: GroupThen {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.tooOf()
            }
        }
        //: self.resetCamera()
        self.screenCineCameraDraw()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        subviewsTo()
        //: setupSubViewsConstraint()
        rescueOperationShrinkConfinement()
        //: bindInteraction()
        orient()
        //: videoManager.startPreview(isMatch: true)
        videoManager.sumeraction(isMatch: true)
        //: setPlayer(isRandon: false)
        modulate(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            videoLate()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            TopThen.shared.info(info: [String(bytes: spacingDomainUtility.map{affairFill(secure: $0)}, encoding: .utf8)!: (String(sessionAccessUtility.prefix(7)) + spacingFollowingValue.replacingOccurrences(of: "arch", with: "ll")), String(bytes: layoutOldPage.map{sureFrameStrain(reply: $0)}, encoding: .utf8)!: [(String(coreLiveGlassTitle.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        destroyGreetFire()
        //: if TalkingSocketManager.shared.isFamous == false {
        if TopThen.shared.isFamous == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            TopThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            TopThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            TopThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            TopThen.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: FeatureChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = FeatureChatManager()
        //: let info = TalkingVideoChatModel()
        let info = GrantChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .restoreAudience()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(videoLate), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: moduleAdjustPath + 1, height: componentMeEvent + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (colorHistoricUrl.replacingOccurrences(of: "timber", with: "3") + String(spacingMoreMetrics)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(layoutEnableEvent.prefix(6)) + " start" + String(colorFollowAlwaysEvent.suffix(5)) + "o chattin" + viewLackData.replacingOccurrences(of: "mouth", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(displayOpportunityMessage) + String(widgetAnotherSystem.prefix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(instructionTap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: GrantRushTalkingButton = {
        //: let btn = TalkingButton()
        let btn = GrantRushTalkingButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(colorDryData.prefix(5)) + "view" + String(screenSauceError.suffix(5)) + String(colorModeHelper))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.titled(name: (String(colorDryData.prefix(5)) + "view" + String(screenSauceError.suffix(5)) + String(colorModeHelper))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.modelSize(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(spacingControlFormat.suffix(4)) + widgetStretchName.replacingOccurrences(of: "address", with: "ha")).meanwhile(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.titled(name: (String(widgetNetSettings.suffix(5)) + viewBothPage.replacingOccurrences(of: "title", with: "lo")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.titled(name: (widgetDigAlert.replacingOccurrences(of: "begin", with: "n") + "_rank" + String(featureHoldHelper.prefix(5))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.modelSize(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.titled(name: (String(kEffFormat)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: GrantRushTalkingButton = {
        //: let btn = TalkingButton()
        let btn = GrantRushTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(partnerFun), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.titled(name: (String(kApartmentTitle.prefix(5)) + "earby" + String(coreDueTimer.suffix(6)) + String(componentPillBasicTimer.prefix(7)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.titled(name: (widgetDigAlert.replacingOccurrences(of: "begin", with: "n") + "_rank" + String(featureHoldHelper.prefix(5))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .birthControl()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(kScheduleMessage.suffix(5)) + String(displayCollectionData.suffix(7))).meanwhile(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.modelSize(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: HiReactiveCompatible = //: return HiReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension SatelliteRecognizerDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func modulate(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = OwnEffectTool.default.subscriber(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(serviceMyMayHelper.suffix(5)) + spacingSweetTitle.replacingOccurrences(of: "water", with: "so") + String(styleKingName.prefix(6))))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func representativeScreen(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(kInputValue)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: spacingDomainUtility.map{affairFill(secure: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: styleAnimalPath.map{$0^14}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        BrandVideoManager.dayOfTheMonth(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.offerResource()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(kInputValue))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func fieldFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        BrandVideoManager.immobilise { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(commonPromptEliteTimer.replacingOccurrences(of: "bean", with: "V") + String(serviceAtmosphereUrl))].intValue
            //: self.refreshVideoCallPrice()
            self.tooOf()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension SatelliteRecognizerDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func partnerFun() {
        //: TalkingAppPushManager.share.func__pushToSubscribeAlert()
        ProdPushManager.share.subscribeBasic()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func instructionTap() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        pussyfootAnalogDigitalConverter()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        themeSincePlatform.givingStream(eventID: componentCountRequestSystem)

        //: if PositionThen.share.appConfigMode.displayMatchV4PopUp, PositionThen.share.loginUserMode.freeCallTimes > 0,
        if PositionThen.share.appConfigMode.displayMatchV4PopUp, PositionThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.progress()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        famousTick()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func videoLate() {
        //: guard TalkingSocketManager.shared.isFamous == false else {
        guard TopThen.shared.isFamous == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colouration(showMsg: spacingQualityHundredSystem)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        enveloping()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(themeYesterdaySession.suffix(4)) + "hing no" + String(coreNumbSystem)).localized
        //: setPlayer(isRandon: true)
        modulate(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if PositionThen.share.loginUserMode.randomVideoType == 2 {
        if PositionThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            themeSincePlatform.givingStream(eventID: displayFeaturePlatform)
            //: req_getVideoInfo()
            snip()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            themeSincePlatform.givingStream(eventID: screenLevelMeetingPlatform)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            TopThen.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            TopThen.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            TopThen.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            TopThen.shared.errorDelegate = self
            //: restart_randomVideo()
            storageEdit()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func storageEdit(cmd: String = (String(kLanguageError.suffix(6)) + layoutDrawSystem.replacingOccurrences(of: "reminder", with: "uc")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        destroyGreetFire()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        representativeScreen(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func offerResource() {
        // 停止定时器
        //: destroyWaitingTimer()
        destroyGreetFire()
        //: showCloseBtn()
        pussyfootAnalogDigitalConverter()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(layoutEnableEvent.prefix(6)) + " start" + String(colorFollowAlwaysEvent.suffix(5)) + "o chattin" + viewLackData.replacingOccurrences(of: "mouth", with: "g")).localized
        //: setPlayer(isRandon: false)
        modulate(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isFamous == false {
        if TopThen.shared.isFamous == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            TopThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            TopThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            TopThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            TopThen.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        tooOf()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func destroyGreetFire() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func snip() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(kUpperPlatform))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: appFormerHuntAlert.map{colonialGreatest(policeman: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.offerResource()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: PositionThen.share.loginUserMode.randomVideoType = 1
                    PositionThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.videoLate()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.colouration(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = VideoHandyJSON.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = EarnThen()
                //: player.setMute(bEnable: true)
                player.telegraphicSignal(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.recognizeView(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                NaturalLanguageThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                MagnitudeReactiveCompatible.popularNet { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.array()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func array() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        themeSincePlatform.givingStream(eventID: spacingPopError)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard NaturalLanguageThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.colouration(showMsg: String(bytes: appYourselfId.map{readyPhotoMuscle(message: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            NaturalLanguageThen.shared.circumference(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.offerResource()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = InstantiatePlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.famousTick()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.offerResource()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func enveloping() {
        //: if PositionThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if PositionThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(PositionThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(PositionThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func pussyfootAnalogDigitalConverter() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func screenCineCameraDraw() {
        //: guard TalkingSocketManager.shared.isFamous == false else {
        guard TopThen.shared.isFamous == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.sumeraction()
        }
    }
}

// MARK: - DelegateObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension SatelliteRecognizerDelegate: DelegateObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func originTake(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = FirstModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(kInputValue)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: sessionItData.reversed(), encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: widgetViewFormat.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: layoutYouthValue.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = ErrorSwitchsetTarget.randow
        //: if PositionThen.share.loginUserMode.freeCallTimes > 0 && PositionThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if PositionThen.share.loginUserMode.freeCallTimes > 0 && PositionThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(themeDogKey)))
            //: TalkingSocketManager.shared.isFreeCall = true
            TopThen.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: spacingDomainUtility.map{affairFill(secure: $0)}, encoding: .utf8)!: (String(serviceLieUtility)), String(bytes: layoutOldPage.map{sureFrameStrain(reply: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        TopThen.shared.info(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            TopThen.shared.info(info: [String(bytes: spacingDomainUtility.map{affairFill(secure: $0)}, encoding: .utf8)!: (String(sessionAccessUtility.prefix(7)) + spacingFollowingValue.replacingOccurrences(of: "arch", with: "ll")), String(bytes: layoutOldPage.map{sureFrameStrain(reply: $0)}, encoding: .utf8)!: [(String(coreLiveGlassTitle.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.storageEdit(cmd: (String(layoutGreenSystem.suffix(7)) + String(kDetectSoapConfig)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func requestData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(coreLiveGlassTitle.prefix(5)))].intValue
    }
}

// MARK: - PortraitObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension SatelliteRecognizerDelegate: PortraitObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func raw(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func banIcon(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func conversionChat(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func sound(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            FeatureReactiveCompatible.commit(body: String(bytes: widgetUtilityError.map{$0^71}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = FirstModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            enableicialUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        offerResource()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.roundPop()
        //: PositionThen.share.start1v1TalkCall(info: data, situation: .Calling)
        PositionThen.share.thresholdSituation(info: data, situation: .Calling)
    }
}

// MARK: - EnhanceFailObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension SatelliteRecognizerDelegate: EnhanceFailObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func club(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        storageEdit(cmd: (String(themeLikeEvent) + screenMemberActualMessage.replacingOccurrences(of: "future", with: "l")))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func labelCommit(data _: [String: Any]) {}
}

// MARK: - ConsentErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension SatelliteRecognizerDelegate: ConsentErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func noWall(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(colorSeparationData) + String(colorOrientText.prefix(8))) { // 取消通话异常
            //: stop_randomVideo()
            offerResource()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(layoutGreenSystem.suffix(7)) + String(kDetectSoapConfig)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            offerResource()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(kInputValue))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.colouration(showMsg: msg)
            //: uploadFailTime(type: 2)
            tinType(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(layoutGreenSystem.suffix(7)) + String(kDetectSoapConfig)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(sessionSomeoneUrl.replacingOccurrences(of: "pack", with: "tr") + "After")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(kInputValue))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.storageEdit(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(featureExposureFormat.prefix(6)) + String(colorWaistFitHeartPreference)), errorNo == CannonballAlongTotal.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            offerResource()
            //: guard PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PositionThen.share.appStatus == BetterQuantity.normal.rawValue else { return }
            //: TalkingAppPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            ProdPushManager.share.responsibility(clickEvent: styleVideoData, sufficient: false)
            //: uploadFailTime(type: 1)
            tinType(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        storageEdit(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension SatelliteRecognizerDelegate {
    //: func uploadFailTime(type: Int) {
    func tinType(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(serviceHairConfig.suffix(6)) + moduleReceiveContent.replacingOccurrences(of: "sight", with: "e"))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(componentReFieldEvent) + String(styleTradeSettings))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        themeSincePlatform.lance(eventID: layoutEnableTitle, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func enableicialUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(componentReFieldEvent) + String(styleTradeSettings))] = time
        //: parm["toUid"] = toUid
        parm[(String(kDistantName.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        themeSincePlatform.lance(eventID: spacingCameraWindowUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension SatelliteRecognizerDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func tooOf() {
        //: guard PositionThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard PositionThen.share.appStatus == BetterQuantity.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !PositionThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !PositionThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = ErrorSwitchsetTarget.randow
        //: if PositionThen.share.loginUserMode.freeCallTimes > 0 && PositionThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if PositionThen.share.loginUserMode.freeCallTimes > 0, PositionThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if PositionThen.share.appUserConfigMode.callBtnStyle == 1 {
            if PositionThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(styleAnonymousPleaseMetrics.suffix(5)) + String(commonThreatTitle)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = PositionThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = PositionThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(themeBigSystem)).meanwhile(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(themeBigSystem)).meanwhile(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.celluloidSize(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = PositionThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = PositionThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = PositionThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = PositionThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !PositionThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !PositionThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func subviewsTo() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func rescueOperationShrinkConfinement() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(appSceneUrl + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func orient() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(tooOf),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: spacingFirstId,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(fieldFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: layoutConversationName,
                                               //: object: nil)
                                               object: nil)
    }
}
