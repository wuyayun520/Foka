
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let colorDateAlert:[UInt8] = [0x25,0x22,0x25,0x38,0x64,0x2f,0x23,0x28,0x29,0x3e,0x76,0x65,0x6c,0x24,0x2d,0x3f,0x6c,0x22,0x23,0x38,0x6c,0x2e,0x29,0x29,0x22,0x6c,0x25,0x21,0x3c,0x20,0x29,0x21,0x29,0x22,0x38,0x29,0x28]

private func phaseWhenPoliceman(pap num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "More attractive photo more calls" :*/
fileprivate let k_beanLogger:[UInt8] = [0x73,0x6c,0x6c,0x61,0x63,0x20,0x65,0x72,0x6f,0x6d,0x20,0x6f,0x74,0x6f,0x68,0x70,0x20,0x65,0x76,0x69,0x74,0x63,0x61,0x72,0x74,0x74,0x61,0x20,0x65,0x72,0x6f,0x4d]

/*: "Upload any photo you like" :*/
fileprivate let appLogSettings:String = "Uploameanwhile abstract chase examine"
fileprivate let sessionAddedPage:String = "input awake photo yo"
fileprivate let styleMilitaryName:String = "u liketar it attention procedure"

/*: "F4F4F4" :*/
fileprivate let modulePrepareClimateData:String = "f4f4f4"

/*: "btn_add_head_cover_nor" :*/
fileprivate let screenMutualUtility:String = "btn_side delivery whether physics large"
fileprivate let themeApartmentError:String = "heawho"
fileprivate let k_positionSession:[Character] = ["_","c","o","v","e","r","_","n","o","r"]

/*: "Please upload a clear and beautiful photo of yourself" :*/
fileprivate let sessionDivisionUtility:[UInt8] = [0xca,0xf6,0xff,0xfb,0xe9,0xff,0xba,0xef,0xea,0xf6,0xf5,0xfb,0xfe,0xba,0xfb,0xba,0xf9,0xf6,0xff,0xfb,0xe8,0xba,0xfb,0xf4,0xfe,0xba,0xf8,0xff,0xfb,0xef,0xee,0xf3,0xfc,0xef,0xf6,0xba,0xea,0xf2,0xf5,0xee,0xf5,0xba,0xf5,0xfc,0xba,0xe3,0xf5,0xef,0xe8,0xe9,0xff,0xf6,0xfc]

private func quantConclusion(example num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "No violence, pornography, or politics allowed" :*/
fileprivate let featureProfileId:[UInt8] = [0xe4,0xc5,0x8a,0xdc,0xc3,0xc5,0xc6,0xcf,0xc4,0xc9,0xcf,0x86,0x8a,0xda,0xc5,0xd8,0xc4,0xc5,0xcd,0xd8,0xcb,0xda,0xc2,0xd3,0x86,0x8a,0xc5,0xd8,0x8a,0xda,0xc5,0xc6,0xc3,0xde,0xc3,0xc9,0xd9,0x8a,0xcb,0xc6,0xc6,0xc5,0xdd,0xcf,0xce]

private func lowNumbero(player num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "Next" :*/
fileprivate let widgetSightWeekUrl:String = "media dominantNext"

/*: "edit_head_Image_male_ :*/
fileprivate let serviceLayMemoryMessage:String = "egesturet"
fileprivate let commonNameureContent:String = "prompt future mirror meeting range_Ima"
fileprivate let coreWarnSession:[Character] = ["g","e","_"]
fileprivate let coreEscapeExcuseAgoPreference:[Character] = ["m","a","l","e","_"]

/*: "edit_head_Image_ :*/
fileprivate let kHisGoldTitle:[Character] = ["e","d","i","t","_","h"]
fileprivate let displayBusyAlert:[Character] = ["e","a","d","_","I","m","a","g","e","_"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OpportunityView.swift
//  AbroadTalking
//
//  Created by Charlotte on 2024/5/24.
//

//: import UIKit
import UIKit

//: class TalkingLoginEditPhotoView: UIView {
class OpportunityView: UIView {
    //: typealias InfoActionBlock = (_ type: EditInfoType) -> Void
    typealias InfoActionBlock = (_ type: WordAlongsideInfoType) -> Void

    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()
    //: var btnBlock: InfoActionBlock?
    var btnBlock: InfoActionBlock?

    //: private var nameCount: Int = 20
    private var nameCount: Int = 20

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.backgroundColor = UIColor.white
        self.backgroundColor = UIColor.white

        //: self.setupSubviews()
        self.compare()
        //: self.setupSubViewsConstraint()
        self.it()
        //: self.bindInteraction()
        self.notAgeTotaleraction()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: colorDateAlert.map{phaseWhenPoliceman(pap: $0)}, encoding: .utf8)!)
    }

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let contentView = UIView.init()
        let contentView = UIView()
        //: return contentView
        return contentView
        //: }()
    }()

    //: lazy var tipsLab: UILabel = {
    lazy var tipsLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .modelSize(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .restoreAudience()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: if PositionThen.share.userFillInfoMode.sex == Gender.female.rawValue {
        if PositionThen.share.userFillInfoMode.sex == PanelScalarLiteral.female.rawValue {
            //: label.text = "More attractive photo more calls".localized
            label.text = String(bytes: k_beanLogger.reversed(), encoding: .utf8)!.localized
            //: } else {
        } else {
            //: label.text = "Upload any photo you like".localized
            label.text = (String(appLogSettings.prefix(5)) + "d any" + String(sessionAddedPage.suffix(9)) + String(styleMilitaryName.prefix(6))).localized
        }
        //: return label
        return label
        //: }()
    }()

    //: lazy var addHeadButton: UIButton = {
    lazy var addHeadButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = UIColor(hex: "F4F4F4")
        btn.backgroundColor = UIColor(hex: (modulePrepareClimateData.uppercased()))
        //: btn.layer.cornerRadius = 5
        btn.layer.cornerRadius = 5
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true

        //: let headImg = PositionThen.share.userFillInfoMode.headImage ?? nil
        let headImg = PositionThen.share.userFillInfoMode.headImage ?? nil
        //: if headImg != nil {
        if headImg != nil {
            //: btn.setImage(headImg, for: .normal)
            btn.setImage(headImg, for: .normal)
            //: } else {
        } else {
            //: btn.setImage(UIImage.BundleImageNamed(name: "btn_add_head_cover_nor"), for: .normal)
            btn.setImage(UIImage.titled(name: (String(screenMutualUtility.prefix(4)) + "add_" + themeApartmentError.replacingOccurrences(of: "who", with: "d") + String(k_positionSession))), for: .normal)
        }
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var msgLab: UILabel = {
    lazy var msgLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .modelSize(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .valueColor()
        //: if PositionThen.share.userFillInfoMode.sex == Gender.female.rawValue {
        if PositionThen.share.userFillInfoMode.sex == PanelScalarLiteral.female.rawValue {
            //: label.text = "Please upload a clear and beautiful photo of yourself".localized
            label.text = String(bytes: sessionDivisionUtility.map{quantConclusion(example: $0)}, encoding: .utf8)!.localized
            //: } else {
        } else {
            //: label.text = "No violence, pornography, or politics allowed".localized
            label.text = String(bytes: featureProfileId.map{lowNumbero(player: $0)}, encoding: .utf8)!.localized
        }
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: return label
        return label
        //: }()
    }()

    //: lazy var commitButton: UIButton = {
    lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(widgetSightWeekUrl.suffix(4))).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.totalelMention(colors: UIColor.motion(), size: CGSize(width: moduleAdjustPath - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Layout

//: extension TalkingLoginEditPhotoView {
extension OpportunityView {
    // 添加视图
    //: private func setupSubviews() {
    private func compare() {
        //: addSubview(contentView)
        addSubview(contentView)
        //: contentView.addSubview(tipsLab)
        contentView.addSubview(tipsLab)
        //: contentView.addSubview(addHeadButton)
        contentView.addSubview(addHeadButton)
        //: contentView.addSubview(commitButton)
        contentView.addSubview(commitButton)
        //: contentView.addSubview(msgLab)
        contentView.addSubview(msgLab)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func it() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.leading.equalTo(24)
            make.leading.equalTo(24)
            //: make.height.equalTo(28)
            make.height.equalTo(28)
            //: make.top.equalTo(34)
            make.top.equalTo(34)
        }

        //: addHeadButton.snp.makeConstraints { make in
        addHeadButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.height.equalTo(159)
            make.width.height.equalTo(159)
            //: make.top.equalTo(tipsLab.snp.bottom).offset(34)
            make.top.equalTo(tipsLab.snp.bottom).offset(34)
        }

        //: let imgWidth: CGFloat = (ScreenWidth - 76) / 3.0
        let imgWidth: CGFloat = (moduleAdjustPath - 76) / 3.0
        //: let imgHeight: CGFloat = imgWidth
        let imgHeight: CGFloat = imgWidth

        //: for i in 0 ..< 3 {
        for i in 0 ..< 3 {
            //: let imageView = UIImageView.init()
            let imageView = UIImageView()
            //: if PositionThen.share.userFillInfoMode.sex == "1" {
            if PositionThen.share.userFillInfoMode.sex == "1" {
                //: imageView.image = UIImage.BundleImageNamed(name: "edit_head_Image_male_\(i)")
                imageView.image = UIImage.titled(name: (serviceLayMemoryMessage.replacingOccurrences(of: "gesture", with: "di") + "_head" + String(commonNameureContent.suffix(4)) + String(coreWarnSession) + String(coreEscapeExcuseAgoPreference)) + "\(i)")
                //: } else {
            } else {
                //: imageView.image = UIImage.BundleImageNamed(name: "edit_head_Image_\(i)")
                imageView.image = UIImage.titled(name: (String(kHisGoldTitle) + String(displayBusyAlert)) + "\(i)")
            }
            //: contentView.addSubview(imageView)
            contentView.addSubview(imageView)
            //: imageView.snp.makeConstraints { make in
            imageView.snp.makeConstraints { make in
                //: make.top.equalTo(addHeadButton.snp.bottom).offset(30)
                make.top.equalTo(addHeadButton.snp.bottom).offset(30)
                //: make.leading.equalTo(30 + (imgWidth+8)*CGFloat(i))
                make.leading.equalTo(30 + (imgWidth + 8) * CGFloat(i))
                //: make.width.equalTo(imgWidth)
                make.width.equalTo(imgWidth)
                //: make.height.equalTo(imgHeight)
                make.height.equalTo(imgHeight)
            }
        }
        //: msgLab.snp.makeConstraints { make in
        msgLab.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(addHeadButton.snp.bottom).offset(146)
            make.top.equalTo(addHeadButton.snp.bottom).offset(146)
        }

        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(msgLab.snp.bottom).offset(30)
            make.top.equalTo(msgLab.snp.bottom).offset(30)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func notAgeTotaleraction() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if self.btnBlock != nil {
                if self.btnBlock != nil {
                    //: self.btnBlock!(.Finish)
                    self.btnBlock!(.Finish)
                }
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: addHeadButton.rx.tap
        addHeadButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if self.btnBlock != nil {
                if self.btnBlock != nil {
                    //: self.btnBlock!(.HeadPic)
                    self.btnBlock!(.HeadPic)
                }
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
