
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let styleFeedbackUrl:String = "mp strike articlesetPas"

/*: "user/appleLogin" :*/
fileprivate let moduleStallEvent:String = "usube"
fileprivate let colorOriginHouseSystem:String = "instruction supposed chainr/appl"

/*: "user/googleLogin" :*/
fileprivate let featureIonKey:String = "prettyser"
fileprivate let moduleVerticalMetrics:String = "leLogrequired filter business"
fileprivate let sessionTheoryConfig:[Character] = ["i","n"]

/*: "user/smsLogin" :*/
fileprivate let k_advancedSettings:String = "user/smside subject evolution how"
fileprivate let layoutStackNoBotAlert:[Character] = ["s","L","o","g","i","n"]

/*: "user/emailVcodeLogin" :*/
fileprivate let appPromotionKey:String = "usprojectr"
fileprivate let kMediaInformationSystem:String = "air shapeilVco"
fileprivate let screenAspectTakeText:String = "month broaddeLogin"

/*: "user/pwdLogin" :*/
fileprivate let themeStrainMessage:String = "sun category spirit reach charmuser/"

/*: "user/emailPwdLogin" :*/
fileprivate let styleReadyName:String = "usconstraintr"
fileprivate let appSurfaceStrongPlatform:String = "ilPwbusinessman reason end added score"

/*: "mobile" :*/
fileprivate let stylePublishRemarkDismissId:[UInt8] = [0xfd,0xff,0xf2,0xf9,0xfc,0xf5]

private func modelTask(big num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "areaCode" :*/
fileprivate let coreAggressionDotEnterpriseSession:[UInt8] = [0x5b,0x48,0x5f,0x5b,0x79,0x55,0x5e,0x5f]

private func unaccompaniedCollect(plus num: UInt8) -> UInt8 {
    return num ^ 58
}

/*: "user/sendLoginSms" :*/
fileprivate let kRocketHelper:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g","i"]
fileprivate let spacingDescriptionId:String = "pass ago intro clearnSms"

/*: "RSA加密失败" :*/
fileprivate let moduleMoreAlert:String = "resource lie flag single exRSA加密失败"

/*: "password" :*/
fileprivate let styleFeedbackTitle:[UInt8] = [0x1f,0xe,0x1c,0x1c,0x18,0x0,0x1d,0xb]

/*: "vcode" :*/
fileprivate let coreFamousAnyoneThirdData:[UInt8] = [0xc2,0xd7,0xdb,0xd0,0xd1]

private func groundLabel(female num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "user/modifyPassword" :*/
fileprivate let k_agreeId:String = "user/minjure next plus eastern"
fileprivate let k_ideaUtility:String = "ODIF"

/*: "user/sendModifyPwdSms" :*/
fileprivate let sessionLaunchDevice:String = "paper postuser/s"
fileprivate let displayLipId:[Character] = ["d","i","f","y","P","w","d","S","m","s"]

/*: "user/sendModifyPwdEmail" :*/
fileprivate let coreAmpleRompText:[Character] = ["u","s","e","r","/","s","e","n","d","M","o"]
fileprivate let colorCropAlert:[Character] = ["d","i","f","y","P","w","d","E","m","a","i","l"]

/*: "mf/user/getMyInfo" :*/
fileprivate let k_domainDevice:String = "anonymous gift work suggest leadingmf/u"
fileprivate let commonBulletMessage:String = "getMyincorporate in heavy please"
fileprivate let moduleUniformKingPage:[Character] = ["I","n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let themeIconAlert:String = "equal i body neverinvite/b"
fileprivate let appRentDisappearFormat:String = "responsibility your regardingindI"

/*: "invite/isBind" :*/
fileprivate let styleEventFormat:String = "ipartyvi"
fileprivate let k_minimumText:String = "flow right hide jus observerte/i"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let colorFollowingTimer:String = "income plan nature proofmf/use"
fileprivate let colorAllowMakeupName:String = "eckInoriginally regular"
fileprivate let featureRenderPath:[Character] = ["d","e"]

/*: "mf/user/updateInfo" :*/
fileprivate let appUnknownUtility:[Character] = ["m","f","/","u","s","e","r","/","u","p","d","a","t","e","I","n"]
fileprivate let commonStartData:[Character] = ["f","o"]

/*: "email" :*/
fileprivate let sessionBestMessage:[UInt8] = [0x53,0x5b,0x57,0x5f,0x5a]

/*: "user/getEmailBindVCode" :*/
fileprivate let themeCountryShText:String = "usking"
fileprivate let spacingActivityMotionData:String = "varyil"
fileprivate let colorBestHeadSystem:String = "Codefair decrease final manual fall"

/*: "user/sendLoginEmail" :*/
fileprivate let coreResistanceMyFormat:String = "user/seastern interval little"
fileprivate let coreRootConfig:[Character] = ["i"]
fileprivate let componentBoxMetrics:String = "nEmailprogram content conclusion length finis"

/*: "user/emailBind" :*/
fileprivate let kReceiveAmbitPath:[Character] = ["u","s","e","r","/","e","m"]
fileprivate let coreAnotherError:[Character] = ["a","i","l","B","i","n","d"]

/*: "user/logout" :*/
fileprivate let coreActionUtility:String = "mate pending one ableuser/l"
fileprivate let widgetNavalLogger:String = "owithoutout"

/*: "app/countryList" :*/
fileprivate let moduleChampionEffectSession:String = "app/good mini lean"
fileprivate let featureToolTimer:String = "tryLismatch drag native"
fileprivate let widgetMeetingFeedbackValue:String = "nut"

/*: "user/getMobileBindVCode" :*/
fileprivate let serviceLabProblemPage:String = "user/genterprise essential bright border mm"
fileprivate let k_zoneScoreSession:String = "etMobtoxic mutual official"
fileprivate let componentDisableSettings:String = "indVCodemember put self"

/*: "user/mobileBind" :*/
fileprivate let screenWhoDevice:[Character] = ["u","s","e","r","/","m","o","b","i","l"]
fileprivate let layoutSeekPath:[Character] = ["e","B","i","n","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ScriptReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class ScriptReactiveCompatible: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func actualInGlobal(type: ExpectUTF8Initializable, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.signatureHaveParams(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(styleFeedbackUrl.suffix(6)) + "sword")].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: TalkingAppPushManager.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                ProdPushManager.share.circleOfCurvature(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            ScriptReactiveCompatible.unlessTill(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func signatureHaveParams(type: ExpectUTF8Initializable, params: [String: String]) -> WorkplaceQueryRequestModel {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (moduleStallEvent.replacingOccurrences(of: "sub", with: "s") + String(colorOriginHouseSystem.suffix(6)) + "eLogin")
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (featureIonKey.replacingOccurrences(of: "pretty", with: "u") + "/goog" + String(moduleVerticalMetrics.prefix(5)) + String(sessionTheoryConfig))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(k_advancedSettings.prefix(7)) + String(layoutStackNoBotAlert))
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (appPromotionKey.replacingOccurrences(of: "project", with: "e") + "/ema" + String(kMediaInformationSystem.suffix(5)) + String(screenAspectTakeText.suffix(7)))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(themeStrainMessage.suffix(5)) + "pwdLogin")
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (styleReadyName.replacingOccurrences(of: "constraint", with: "e") + "/ema" + String(appSurfaceStrongPlatform.prefix(4)) + "dLogin")
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func stop(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: stylePublishRemarkDismissId.map{modelTask(big: $0)}, encoding: .utf8)!: reslutStr, String(bytes: coreAggressionDotEnterpriseSession.map{unaccompaniedCollect(plus: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = WorkplaceQueryRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(kRocketHelper) + String(spacingDescriptionId.suffix(4)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func clubCompletion(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: stylePublishRemarkDismissId.map{modelTask(big: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: coreAggressionDotEnterpriseSession.map{unaccompaniedCollect(plus: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: styleFeedbackTitle.map{$0^111}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            ScriptReactiveCompatible.actualInGlobal(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func mark(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: styleFeedbackTitle.map{$0^111}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: coreFamousAnyoneThirdData.map{groundLabel(female: $0)}, encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = WorkplaceQueryRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(k_agreeId.prefix(6)) + k_ideaUtility.lowercased() + "yPassword")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func calculateStyleCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(sessionLaunchDevice.suffix(6)) + "endMo" + String(displayLipId))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func greet(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (String(coreAmpleRompText) + String(colorCropAlert))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func unlessTill(_ type: ExpectUTF8Initializable, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(k_domainDevice.suffix(4)) + "ser/" + String(commonBulletMessage.prefix(5)) + String(moduleUniformKingPage))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            screenOutputConfig.set(result, forKey: serviceFeatureDevice)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<LanguageThen>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: PositionThen.share.loginUserMode = userModel
                PositionThen.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                screenOutputConfig.set(type.rawValue, forKey: viewLevelConfig)
                //: if PositionThen.share.loginUserMode.updateInfo == false {
                if PositionThen.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: TalkingAppPushManager.share.func__pushToFillUserInfoVC()
                    ProdPushManager.share.puncture()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: viewNameSystem, object: nil, userInfo: result as! [String: Any])

                //: if PositionThen.share.loginUserMode.remindBindEmail == true {
                if PositionThen.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: TalkingAppPushManager.share.func__pushToLockUserEmailVC(isShowBack: false)
                        ProdPushManager.share.varyProminent(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func record(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (String(themeIconAlert.suffix(8)) + String(appRentDisappearFormat.suffix(4)) + "nviter")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func video(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (styleEventFormat.replacingOccurrences(of: "party", with: "n") + String(k_minimumText.suffix(4)) + "sBind")

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func chockFull(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(colorFollowingTimer.suffix(6)) + "r/Ch" + String(colorAllowMakeupName.prefix(5)) + "viteCo" + String(featureRenderPath))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func detectFailure(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(appUnknownUtility) + String(commonStartData))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.achievementMortgage()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(spacingTextKey)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.availableLink(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func face(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: sessionBestMessage.map{$0^54}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = WorkplaceQueryRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (themeCountryShText.replacingOccurrences(of: "king", with: "er") + "/getE" + spacingActivityMotionData.replacingOccurrences(of: "vary", with: "ma") + "BindV" + String(colorBestHeadSystem.prefix(4)))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(coreResistanceMyFormat.prefix(6)) + "endLog" + String(coreRootConfig) + String(componentBoxMetrics.prefix(6)))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func reportAcross(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(kReceiveAmbitPath) + String(coreAnotherError))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: coreFamousAnyoneThirdData.map{groundLabel(female: $0)}, encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func execute(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: sessionBestMessage.map{$0^54}, encoding: .utf8)!: resultEmailStr, String(bytes: styleFeedbackTitle.map{$0^111}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            ScriptReactiveCompatible.actualInGlobal(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func tag(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(coreActionUtility.suffix(6)) + widgetNavalLogger.replacingOccurrences(of: "without", with: "g"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func botDoing(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(k_domainDevice.suffix(4)) + "ser/" + String(commonBulletMessage.prefix(5)) + String(moduleUniformKingPage))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                screenOutputConfig.set(result, forKey: serviceFeatureDevice)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<LanguageThen>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: PositionThen.share.loginUserMode = userModel
                    PositionThen.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func governmentalAbstractEntitySessionCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = WorkplaceQueryRequestModel()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(moduleChampionEffectSession.prefix(4)) + "coun" + String(featureToolTimer.prefix(6)) + widgetMeetingFeedbackValue.replacingOccurrences(of: "nut", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func sharedWith(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: stylePublishRemarkDismissId.map{modelTask(big: $0)}, encoding: .utf8)!: reslutStr, String(bytes: coreAggressionDotEnterpriseSession.map{unaccompaniedCollect(plus: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = WorkplaceQueryRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (String(serviceLabProblemPage.prefix(6)) + String(k_zoneScoreSession.prefix(5)) + "ileB" + String(componentDisableSettings.prefix(8)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func afterPromptGift(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: screenResolutionSystem)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: stylePublishRemarkDismissId.map{modelTask(big: $0)}, encoding: .utf8)!: reslutStr, String(bytes: coreAggressionDotEnterpriseSession.map{unaccompaniedCollect(plus: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: coreFamousAnyoneThirdData.map{groundLabel(female: $0)}, encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = WorkplaceQueryRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(screenWhoDevice) + String(layoutSeekPath))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            viewEnterBeginAgeEvent.resist(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(moduleMoreAlert.suffix(7))))
        }
    }
}
