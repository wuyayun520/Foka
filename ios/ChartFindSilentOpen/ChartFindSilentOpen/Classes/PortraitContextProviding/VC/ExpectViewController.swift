
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let displayPillStrongData:[UInt8] = [0xbf,0xb8,0xbf,0xa2,0xfe,0xb5,0xb9,0xb2,0xb3,0xa4,0xec,0xff,0xf6,0xbe,0xb7,0xa5,0xf6,0xb8,0xb9,0xa2,0xf6,0xb4,0xb3,0xb3,0xb8,0xf6,0xbf,0xbb,0xa6,0xba,0xb3,0xbb,0xb3,0xb8,0xa2,0xb3,0xb2]

/*: "Personal information" :*/
fileprivate let moduleTranslateUtility:[Character] = ["P","e","r","s","o","n","a","l"," ","i","n","f","o","r"]
fileprivate let displayAtmosphereUtility:[Character] = ["m"]
fileprivate let widgetSoftlyApplicationEvent:String = "atlown"

/*: "PanelScalarLiteral" :*/
fileprivate let themeQueryionTrackDevice:[Character] = ["G","e","n","d","e","r"]

/*: "Once gender is set, it Cannot be changed." :*/
fileprivate let styleGallerySettings:[UInt8] = [0xe4,0xc5,0xc8,0xce,0x8b,0xcc,0xce,0xc5,0xcf,0xce,0xd9,0x8b,0xc2,0xd8,0x8b,0xd8,0xce,0xdf,0x87,0x8b,0xc2,0xdf,0x8b,0xe8,0xca,0xc5,0xc5,0xc4,0xdf,0x8b,0xc9,0xce,0x8b,0xc8,0xc3,0xca,0xc5,0xcc,0xce,0xcf,0x85]

private func shinePlatform(vid num: UInt8) -> UInt8 {
    return num ^ 171
}

/*: "#2ABBFF" :*/
fileprivate let themePlayUrl:[Character] = ["#","2","A","B","B","F","F"]

/*: "btn_male_nv_nor" :*/
fileprivate let appCommitSettings:String = "tremendous gator reportbtn_"
fileprivate let moduleArcError:String = "_nv_norecording nothing"
fileprivate let widgetFeeSystem:String = "threat"

/*: "btn_male_nv_sel" :*/
fileprivate let featureCoverMessage:String = "personal reward universal skinbtn_"
fileprivate let featureJawSystem:String = "enjoy manner sou poo_sel"

/*: "Male" :*/
fileprivate let viewDialogMessage:[Character] = ["M","a","l","e"]

/*: "#FF5372" :*/
fileprivate let layoutClearlyPlatform:String = "#Fwriting mid mine speed"
fileprivate let componentSweetAnonUtility:String = "F5toxic2"

/*: "btn_female_nv_nor" :*/
fileprivate let componentRegardingTitle:String = "port privacy icon alongbtn_"
fileprivate let serviceSpeakerDevice:String = "le_nblind want run package"

/*: "btn_female_nv_sel" :*/
fileprivate let featureMinimizePage:[Character] = ["b","t","n","_","f","e","m","a","l","e","_","n","v"]
fileprivate let kBookKey:String = "feedback black bird jus signal_sel"

/*: "Female" :*/
fileprivate let featureSupportAttentionMetrics:String = "net eastFemale"

/*: "Next" :*/
fileprivate let sessionTheoreticalUrl:[Character] = ["N","e","x","t"]

/*: "sex" :*/
fileprivate let appHappyKey:[UInt8] = [0x78,0x65,0x73]

/*: "RegisterSuccess" :*/
fileprivate let viewSingleUtility:[Character] = ["R","e","g","i","s","t","e","r","S","u","c","c"]
fileprivate let viewHighwayValue:[Character] = ["e","s","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ExpectViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingLoginGenderVC: TalkingBaseViewController {
class ExpectViewController: GroupThen {
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: displayPillStrongData.map{$0^214}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: popGesture(isOpen: false)
        retrieve(isOpen: false)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Personal information".localized
        self.title = (String(moduleTranslateUtility) + String(displayAtmosphereUtility) + widgetSoftlyApplicationEvent.replacingOccurrences(of: "low", with: "io")).localized
        //: PositionThen.share.userFillInfoMode.sex = "1"
        PositionThen.share.userFillInfoMode.sex = "1"
        //: self.setupSubviews()
        self.calculateSetup()
        //: self.setupSubViewsConstraint()
        self.eyeMuscleConstraint()
        //: self.bindInteraction()
        self.holdFast()
        //: func__checkNextBtnState()
        reportAwake()
    }

    //: deinit {
    deinit {}

    //: private lazy var logoTitleLabel: UILabel = {
    private lazy var logoTitleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17)
        label.font = .modelSize(type: .Semibold, fontSize: 17)
        //: label.textColor = .appTitleColor()
        label.textColor = .restoreAudience()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Gender".localized
        label.text = (String(themeQueryionTrackDevice)).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var genderTipLabel: UILabel = {
    private lazy var genderTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15)
        label.font = .modelSize(type: .Regular, fontSize: 15)
        //: label.textColor = .appValueColor()
        label.textColor = .valueColor()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Once gender is set, it Cannot be changed.".localized
        label.text = String(bytes: styleGallerySettings.map{shinePlatform(vid: $0)}, encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var maleButton: TalkingButton = {
    private lazy var maleButton: GrantRushTalkingButton = {
        //: let btn = TalkingButton(type: .custom)
        let btn = GrantRushTalkingButton(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.innerDoing(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#2ABBFF")!, forState: .selected)
        btn.innerDoing(color: UIColor(hex: (String(themePlayUrl)))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(appCommitSettings.suffix(4)) + "male" + String(moduleArcError.prefix(6)) + widgetFeeSystem.replacingOccurrences(of: "threat", with: "r"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_male_nv_sel"), for: .selected)
        btn.setImage(UIImage.titled(name: (String(featureCoverMessage.suffix(4)) + "male_nv" + String(featureJawSystem.suffix(4)))), for: .selected)
        //: btn.setTitle("Male".localized, for: .normal)
        btn.setTitle((String(viewDialogMessage)).localized, for: .normal)
        //: btn.setTitle("Male".localized, for: .selected)
        btn.setTitle((String(viewDialogMessage)).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.attachSize(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var femaleButton: TalkingButton = {
    private lazy var femaleButton: GrantRushTalkingButton = {
        //: let btn = TalkingButton(type: .custom)
        let btn = GrantRushTalkingButton(type: .custom)
        //: btn.isSelected = false
        btn.isSelected = false
        //: btn.spaceBetweenTitleAndImage = 20
        btn.spaceBetweenTitleAndImage = 20
        //: btn.setBackgroundColor(color: UIColor.init(red: 244/255.0, green: 244/255.0, blue: 244/255.0, alpha: 1.0), forState: .normal)
        btn.innerDoing(color: UIColor(red: 244 / 255.0, green: 244 / 255.0, blue: 244 / 255.0, alpha: 1.0), forState: .normal)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#FF5372")!, forState: .selected)
        btn.innerDoing(color: UIColor(hex: (String(layoutClearlyPlatform.prefix(2)) + componentSweetAnonUtility.replacingOccurrences(of: "toxic", with: "37")))!, forState: .selected)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_nor"), for: .normal)
        btn.setImage(UIImage.titled(name: (String(componentRegardingTitle.suffix(4)) + "fema" + String(serviceSpeakerDevice.prefix(4)) + "v_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_female_nv_sel"), for: .selected)
        btn.setImage(UIImage.titled(name: (String(featureMinimizePage) + String(kBookKey.suffix(4)))), for: .selected)
        //: btn.setTitle("Female".localized, for: .normal)
        btn.setTitle((String(featureSupportAttentionMetrics.suffix(6))).localized, for: .normal)
        //: btn.setTitle("Female".localized, for: .selected)
        btn.setTitle((String(featureSupportAttentionMetrics.suffix(6))).localized, for: .selected)
        //: btn.setTitleColor(.black, for: .normal)
        btn.setTitleColor(.black, for: .normal)
        //: btn.setTitleColor(.white, for: .selected)
        btn.setTitleColor(.white, for: .selected)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 17)
        btn.titleLabel?.font = UIFont.attachSize(fontSize: 17)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.modelSize(type: .Medium, fontSize: 17)
        //: btn.setTitle("Next".localized, for: .normal)
        btn.setTitle((String(sessionTheoreticalUrl)).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.totalelMention(colors: UIColor.motion(), size: CGSize(width: moduleAdjustPath - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginGenderVC {
extension ExpectViewController {
    //: private func func__checkNextBtnState() {
    private func reportAwake() {
        //: if PositionThen.share.userFillInfoMode.sex == "1" {
        if PositionThen.share.userFillInfoMode.sex == "1" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = true
            self.maleButton.isSelected = true
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
            //: } else if PositionThen.share.userFillInfoMode.sex == "2"{
        } else if PositionThen.share.userFillInfoMode.sex == "2" {
            //: self.commitButton.isEnabled = true
            self.commitButton.isEnabled = true
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = true
            self.femaleButton.isSelected = true
            //: } else {
        } else {
            //: self.commitButton.isEnabled = false
            self.commitButton.isEnabled = false
            //: self.maleButton.isSelected = false
            self.maleButton.isSelected = false
            //: self.femaleButton.isSelected = false
            self.femaleButton.isSelected = false
        }
    }

    //: private func func__pushToNextProgress() {
    private func query() {
        //        // 1.3 判断用户设备SIM卡归属地是否来自被剔除的国家或地区
        //        // 1.4 判断用户设备是否开启VPN
//        if PositionThen.share.appConfigMode.vpnSimCardCheck {
//
//            guard  !HarvestThen.isUsedProxy() && !HarvestThen.isUsedVPN() else {
//                self.func__showStatusBarErrorMsg(showMsg: "Network error. Check it and try again.".localized)
//                self.navigationController?.popToRootViewController(animated: true)
//                NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
//                RushThen.func__reportDeviceID()
//                return
//            }
//        }
        /// 清除后面已填写过资料
//        let sex = PositionThen.share.userFillInfoMode.sex
//        PositionThen.share.userFillInfoMode = UserInfoModel.init()
//        PositionThen.share.userFillInfoMode.sex = sex
//        let VC = ClangoringThen()
//        self.navigationController?.pushViewController(VC, animated: true)

        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show()
        WeatherMapThen.depressCalculate()
        //: TalkingLoginRequestTool.req_updateUserInfo(params: ["sex":PositionThen.share.userFillInfoMode.sex]) { succeed, result, errorModel in
        ScriptReactiveCompatible.detectFailure(params: [String(bytes: appHappyKey.reversed(), encoding: .utf8)!: PositionThen.share.userFillInfoMode.sex]) { succeed, result, _ in
            //: ProgressHUD.dismiss()
            WeatherMapThen.evolution()
            //: if succeed {
            if succeed {
                // 发送通知
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: viewNameSystem, object: nil, userInfo: result as! [String: Any])
                // 注册成功埋点
                //: TalkingAdjustManager.share.addEvent(key: "RegisterSuccess")
                SparkReactiveCompatible.share.bitEquity(key: (String(viewSingleUtility) + String(viewHighwayValue)))
                //: FirebaseAnalyticsManager.share.logEvent(name: "RegisterSuccess")
                FailAnalyticsManager.share.eventCompanyNameNurseLogParameters(name: (String(viewSingleUtility) + String(viewHighwayValue)))
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingLoginGenderVC {
extension ExpectViewController {
    // 添加视图
    //: private func setupSubviews() {
    private func calculateSetup() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.addSubview(logoTitleLabel)
        view.addSubview(logoTitleLabel)
        //: view.addSubview(genderTipLabel)
        view.addSubview(genderTipLabel)
        //: view.addSubview(maleButton)
        view.addSubview(maleButton)
        //: view.addSubview(femaleButton)
        view.addSubview(femaleButton)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func eyeMuscleConstraint() {
        //: logoTitleLabel.snp.makeConstraints { make in
        logoTitleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(34)
            make.top.equalTo(34)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: genderTipLabel.snp.makeConstraints { make in
        genderTipLabel.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(moduleAdjustPath - 60)
            //: make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
            make.top.equalTo(logoTitleLabel.snp.bottom).offset(6)
        }
        //: maleButton.snp.makeConstraints { make in
        maleButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: femaleButton.snp.makeConstraints { make in
        femaleButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.width.equalTo(actualWidth(w: 140))
            make.width.equalTo(actualWidth(w: 140))
            //: make.height.equalTo(actualWidth(w: 50))
            make.height.equalTo(actualWidth(w: 50))
            //: make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
            make.top.equalTo(genderTipLabel.snp.bottom).offset(20)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.width.equalTo(ScreenWidth-60)
            make.width.equalTo(moduleAdjustPath - 60)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(maleButton.snp.bottom).offset(40)
            make.top.equalTo(maleButton.snp.bottom).offset(40)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func holdFast() {
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__pushToNextProgress()
                self.query()

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: femaleButton.rx.tap
        femaleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: PositionThen.share.userFillInfoMode.sex = "2"
                PositionThen.share.userFillInfoMode.sex = "2"
                //: self.func__checkNextBtnState()
                self.reportAwake()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: maleButton.rx.tap
        maleButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: PositionThen.share.userFillInfoMode.sex = "1"
                PositionThen.share.userFillInfoMode.sex = "1"
                //: self.func__checkNextBtnState()
                self.reportAwake()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
