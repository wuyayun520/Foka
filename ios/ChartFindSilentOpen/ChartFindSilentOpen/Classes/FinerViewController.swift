
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let displayChannelId:[UInt8] = [0xfc,0xfb,0xfc,0xe1,0xbd,0xf6,0xfa,0xf1,0xf0,0xe7,0xaf,0xbc,0xb5,0xfd,0xf4,0xe6,0xb5,0xfb,0xfa,0xe1,0xb5,0xf7,0xf0,0xf0,0xfb,0xb5,0xfc,0xf8,0xe5,0xf9,0xf0,0xf8,0xf0,0xfb,0xe1,0xf0,0xf1]

/*: "Load..." :*/
fileprivate let appDemonstrateReserveConfig:String = "representative cur tell waist poLoad..."

/*: "Skip" :*/
fileprivate let k_frontTimer:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let themeFormatId:[UInt8] = [0x41,0x7d,0x7d,0x79,0x5d,0x66,0x66,0x65,0x27,0x47,0x68,0x7d,0x60,0x7f,0x6c,0x5d,0x66,0x43,0x7a,0x21,0x2e,0x7b,0x6c,0x6a,0x61,0x68,0x7b,0x6e,0x6c,0x2e,0x20]

private func differenceLevel(profile num: UInt8) -> UInt8 {
    return num ^ 9
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let screenChooseConfig:[UInt8] = [0xcd,0xd3,0xd4,0xde,0xd5,0xcd,0x94,0xd5,0xd4,0xea,0xdb,0xdd,0xdf,0xe9,0xd2,0xd5,0xcd,0x9c,0x9c,0xd5,0xd4,0xea,0xdb,0xdd,0xdf,0xe9,0xd2,0xd5,0xcd,0x92,0x93,0x81]

private func moreRun(word num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let k_leadingPlatform:[Character] = ["j","s","E","v","e","n","t","("]
fileprivate let k_woodTitle:[Character] = ["o"]
fileprivate let moduleRatingTitle:String = "pic at separationnPage"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let viewTicSettings:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let k_communicateName:String = "outdo coordinator numb star dogjsEv"
fileprivate let widgetRegionalText:[Character] = ["P","a","g"]
fileprivate let featureColorSettings:[Character] = ["e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let serviceMarginEvent:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let styleLicenseAlert:[Character] = ["c","a","n","G"]
fileprivate let layoutHelloValue:String = "max begin timber capture systemoBack"

/*: "PHPSESSID= :*/
fileprivate let featureBlockKey:String = "yaP"
fileprivate let featureReduceValue:[Character] = ["S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let moduleViolationValue:String = "lot exactly tip chin attach;uid="

/*: "Cookie" :*/
fileprivate let styleDefineKey:[Character] = ["C","o","o","k","i"]
fileprivate let k_justPlatform:String = "hence"

/*: "nav_back_black_nor" :*/
fileprivate let kErrorHelper:String = "crush cleannav_"
fileprivate let spacingSceneMetrics:String = "purchaselac"
fileprivate let viewEnjoyMessage:[Character] = ["r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let spacingPhoneConfig:String = "ico_intiaggression tam process"
fileprivate let coreNoonHelper:String = "marticlete"
fileprivate let themeOldTimer:[Character] = ["_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let spacingPersonalId:[UInt8] = [0x1f,0x14,0x18,0xe,0x16,0x1e,0x15,0xf,0x55,0x1c,0x1e,0xf,0x3e,0x17,0x1e,0x16,0x1e,0x15,0xf,0x8,0x39,0x2,0x2f,0x1a,0x1c,0x35,0x1a,0x16,0x1e,0x53,0x5c,0x19,0x14,0x1f,0x2,0x5c,0x52,0x20,0x4b,0x26,0x55,0x8,0xf,0x2,0x17,0x1e,0x55,0x19,0x1a,0x18,0x10,0x1c,0x9,0x14,0xe,0x15,0x1f,0x46,0x5c,0x9,0x1c,0x19,0x1a,0x53,0x4b,0x57,0x4b,0x57,0x4b,0x57,0x4b,0x52,0x5c]

/*: "Alert" :*/
fileprivate let layoutCurBrakeDevice:String = "player to effectAlert"

/*: "Cancle" :*/
fileprivate let k_solarPlatform:String = "Canclevalid rack score mouth clear"

/*: "Done" :*/
fileprivate let commonPromptName:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let componentWeekUtility:String = "forget suitable general don slidejs c"
fileprivate let themeRecoverUrl:String = "all exit utter bag"
fileprivate let colorThroughKey:String = "material beam duckd nam"

/*: , message =  :*/
fileprivate let sessionBucketConfig:String = "automatically kit, mes"
fileprivate let sessionVaryCastAlert:String = "long partsage = "

/*: "http://" :*/
fileprivate let featureCornerChannelAlert:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let componentSegmentConfig:[Character] = ["h"]
fileprivate let coreUtilitySettings:[Character] = ["t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let featurePreviousData:[UInt8] = [0xa,0x3b,0x29,0x20,0x7d,0x20,0x5d,0x30,0x5b,0x29,0x27,0x3d,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x6e,0x72,0x75,0x74,0x65,0x72,0x20,0x7b,0x20,0x29,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x28,0x70,0x61,0x6d,0x2e,0x29,0x27,0x20,0x3b,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x3d,0x20,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x72,0x61,0x76]

/*: "uid= :*/
fileprivate let coreMediumData:String = "at grav distributionuid="

/*: ;domain= :*/
fileprivate let viewMaximumCharacterEvent:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let viewEffKey:String = "thread still military particle infrastructure;path="
fileprivate let kYouMessage:[Character] = ["/"]

/*: "document.cookie=' :*/
fileprivate let componentScriptData:[Character] = ["d","o","c","u","m","e"]
fileprivate let spacingCapableId:String = "course commit mineral utter postnt.coo"

/*: ;domin= :*/
fileprivate let displayApplyHelper:[Character] = [";","d","o","m","i","n"]
fileprivate let layoutModifyTimer:String = "jump"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let componentRiskyData:[UInt8] = [0x36,0x39,0x7f,0x77,0x3c,0x30,0x30,0x34,0x36,0x3a,0x11,0x3e,0x32,0x3a,0x2c,0x71,0x36,0x31,0x3b,0x3a,0x27,0x10,0x39,0x77,0x78,0xf,0x17,0xf,0xc,0x1a,0xc,0xc,0x16,0x1b,0x78,0x76,0x7f,0x62,0x62,0x7f,0x72,0x6e,0x76,0x7f,0x24,0x7f,0x3b,0x30,0x3c,0x2a,0x32,0x3a,0x31,0x2b,0x71,0x3c,0x30,0x30,0x34,0x36,0x3a,0x62,0x78]

/*: "h5Domain =  :*/
fileprivate let serviceSnapName:String = "h5Ddevice shade break"
fileprivate let layoutBelowError:String = "omafit"

/*: "getAllCookies =  :*/
fileprivate let sessionCompositionYesFormat:String = "getAllago missing thought"
fileprivate let layoutMainOkUtility:String = "ies = street interested"

/*: "PHPSESSID" :*/
fileprivate let themeGolfProjectEvent:[Character] = ["P","H","P","S","E"]
fileprivate let styleIncomeSystem:String = "SSIless"

/*: "uid" :*/
fileprivate let layoutGuaranteeMetrics:String = "UID"

/*: "appCookies =  :*/
fileprivate let screenRunConfig:String = "component drownappCo"
fileprivate let styleToDevice:String = "safety bold nths = "

/*: "UID" :*/
fileprivate let colorRushError:String = "Uflu"

/*: "navigator.userAgent" :*/
fileprivate let componentWallBroadPreference:String = "nresidenceigat"
fileprivate let styleButtonDevice:String = "orange fail for clothes enableor.use"

/*: "xunwu_iphone" :*/
fileprivate let componentPartUtility:String = "xunwu_off forget oration makeup"
fileprivate let viewCurrentDevice:String = "IPHONE"

/*: " xunwu_device_id/ :*/
fileprivate let screenWarningTitle:String = "sod long receive xunwu_"
fileprivate let themeMeNutSettings:[Character] = ["d","e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let stylePopularEvent:[Character] = ["x","u","n","w","u","_","i","p","h","o"]
fileprivate let themeRoundEvent:[Character] = ["n","e"," ","x"]
fileprivate let moduleToneTitle:[Character] = ["u","n","w","u","_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let coreBestData:[Character] = [" ","x","u","n","w","u","_","p","a","c","k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let screenCreationName:[Character] = [" ","x","u","n","w","u","_","b","u","n","d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let appLaterSettings:String = "  westock composition exercise writing pressure"
fileprivate let colorCenterFinisSystem:String = "1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FinerViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class FinerViewController: OriginalKingViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: ReachConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: AntitypeTitleCounterpolation?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: AntitypeTitleCounterpolation?, webConfig: ReachConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: displayChannelId.map{$0^149}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        understandingPageShow()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        doUpwardlyFromDisguise()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(appDemonstrateReserveConfig.suffix(7)))
        //: addNotification()
        notification()
        //: func__checkConfig()
        busyFamily()
        //: func__setUpWebView()
        somePhoto()
        //: func__registerObserver()
        duck()
        //: func__beginStartRequest()
        cleanRequest()
        //: func_checkAppleIAP()
        passengerStyleIap()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: TalkingAppPushManager.share.func__pushToWebVC(webViewType: .DiscountPopup)
                ProdPushManager.share.ticInfo(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: componentSceneEvent, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            assessment(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.tributeChromaticColour(), .font: UIFont.modelSize(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_frontTimer)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension FinerViewController {
    /// 添加通知
    //: private func addNotification() {
    private func notification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(sufficientFor),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: commonNetworkId,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(understandingPageShow),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func sufficientFor() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: themeFormatId.map{differenceLevel(profile: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func understandingPageShow() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: screenChooseConfig.map{moreRun(word: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_leadingPlatform) + String(k_woodTitle) + String(moduleRatingTitle.suffix(5)) + "Show): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func doUpwardlyFromDisguise() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: viewTicSettings.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_communicateName.suffix(4)) + "ent(on" + String(widgetRegionalText) + String(featureColorSettings)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension FinerViewController {
    //: func func__registerObserver() {
    func duck() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(serviceMarginEvent)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(styleLicenseAlert) + String(layoutHelloValue.suffix(5))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(regularise), name: sessionOfPreference, object: nil)
    }

    //: func func__setUpWebView() {
    func somePhoto() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        pointAgent()
        //: func__addBridgeMethod()
        slim()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.layerColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            WeatherMapThen.sinceRisky(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(appSceneUrl)
            }
        }
    }

    //: func func__beginStartRequest() {
    func cleanRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.noRequestDeal()
            }
            //: } else {
        } else {
            //: func__startRequest()
            noRequestDeal()
        }
    }

    //: func func__startRequest() {
    func noRequestDeal() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = PositionThen.share.loginUid
        let uid = PositionThen.share.loginUid
        //: let ssid = PositionThen.share.loginSessionId
        let ssid = PositionThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (featureBlockKey.replacingOccurrences(of: "ya", with: "PH") + String(featureReduceValue)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(moduleViolationValue.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(styleDefineKey) + k_justPlatform.replacingOccurrences(of: "hence", with: "e")))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        rackLust()
        //: func__initRightButton()
        hang()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func passengerStyleIap() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            ManagerReactiveCompatible.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.printing(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ManagerReactiveCompatible.shared.rootTransactions()
        }
    }

    //: func func__initLeftButton() {
    func policeman() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.titled(name: (String(kErrorHelper.suffix(4)) + "back_" + spacingSceneMetrics.replacingOccurrences(of: "purchase", with: "b") + "k_no" + String(viewEnjoyMessage))), style: .plain, target: self, action: #selector(observeAction(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func hang() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(AntitypeTitleCounterpolation.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.titled(name: (String(spacingPhoneConfig.prefix(8)) + coreNoonHelper.replacingOccurrences(of: "article", with: "a") + String(themeOldTimer))), style: .plain, target: self, action: #selector(atFunc))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func observeAction(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        dig()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.famousTick() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: FinerViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! FinerViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! FinerViewController).understandingPageShow()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func busyFamily() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func competition(sucess: Bool) {
        //: ProgressHUD.dismiss()
        WeatherMapThen.evolution()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func rackLust() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: spacingPersonalId.map{$0^123}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func duringDit(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: url)
            ProdPushManager.share.rangeGalleryThirty(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func unneededQuality(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func regularise() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: sessionOfPreference, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func atFunc() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = IndigenousLanguageDataSource(frame: CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent))
        //: view.show()
        view.rid()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension FinerViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            WeatherMapThen.sinceRisky(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            competition(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        policeman()
        //: func__clearJSBgColor()
        rackLust()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            competition(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            competition(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(layoutCurBrakeDevice.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(k_solarPlatform.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(commonPromptName)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension FinerViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(componentWeekUtility.suffix(4)) + String(themeRecoverUrl.prefix(4)) + "metho" + String(colorThroughKey.suffix(5)) + "e = ") + "\(message.name)" + (String(sessionBucketConfig.suffix(5)) + String(sessionVaryCastAlert.suffix(7))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.scriptCover(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func slim() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ButterflyKeyRepresentable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func dig() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ButterflyKeyRepresentable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension FinerViewController {
    //: func func__addUserScript() {
    func horriblePlateNotice() {
        //: var htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(featureCornerChannelAlert))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(featureCornerChannelAlert)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(componentSegmentConfig) + String(coreUtilitySettings))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(componentSegmentConfig) + String(coreUtilitySettings)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = PositionThen.share.loginUserMode.userID
        let tempUid: String = PositionThen.share.loginUserMode.userID
        //: let tempSessionID: String = PositionThen.share.loginSessionId
        let tempSessionID: String = PositionThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: featurePreviousData.reversed(), encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(coreMediumData.suffix(4))) + "\(tempUid)" + (String(viewMaximumCharacterEvent)) + "\(htmlDomain)" + (String(viewEffKey.suffix(6)) + String(kYouMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(componentScriptData) + String(spacingCapableId.suffix(6)) + "kie=\'") + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (featureBlockKey.replacingOccurrences(of: "ya", with: "PH") + String(featureReduceValue)) + "\(tempSessionID)" + (String(displayApplyHelper) + layoutModifyTimer.replacingOccurrences(of: "jump", with: "=")) + "\(htmlDomain)" + (String(viewEffKey.suffix(6)) + String(kYouMessage))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(componentScriptData) + String(spacingCapableId.suffix(6)) + "kie=\'") + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: componentRiskyData.map{$0^95}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(coreMediumData.suffix(4))) + "\(tempUid)" + (String(viewMaximumCharacterEvent)) + "\(htmlDomain)" + (String(viewEffKey.suffix(6)) + String(kYouMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(componentScriptData) + String(spacingCapableId.suffix(6)) + "kie=\'") + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(coreMediumData.suffix(4))) + "\(tempSessionID)" + (String(viewMaximumCharacterEvent)) + "\(htmlDomain)" + (String(viewEffKey.suffix(6)) + String(kYouMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(componentScriptData) + String(spacingCapableId.suffix(6)) + "kie=\'") + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func assessment(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(serviceSnapName.prefix(3)) + layoutBelowError.replacingOccurrences(of: "fit", with: "i") + "n = ") + "\(htmlDomain)")
            //: let userID: String = PositionThen.share.loginUserMode.userID
            let userID: String = PositionThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(sessionCompositionYesFormat.prefix(6)) + "Cook" + String(layoutMainOkUtility.prefix(6))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(themeGolfProjectEvent) + styleIncomeSystem.replacingOccurrences(of: "less", with: "D")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (layoutGuaranteeMetrics.lowercased()), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.cleanRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.grav(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func grav(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(serviceSnapName.prefix(3)) + layoutBelowError.replacingOccurrences(of: "fit", with: "i") + "n = ") + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: HarvestThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(screenRunConfig.suffix(5)) + "okie" + String(styleToDevice.suffix(4))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (colorRushError.replacingOccurrences(of: "flu", with: "ID")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (layoutGuaranteeMetrics.lowercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(themeGolfProjectEvent) + styleIncomeSystem.replacingOccurrences(of: "less", with: "D")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(themeGolfProjectEvent) + styleIncomeSystem.replacingOccurrences(of: "less", with: "D"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.cleanRequest()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func pointAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((componentWallBroadPreference.replacingOccurrences(of: "residence", with: "av") + String(styleButtonDevice.suffix(6)) + "rAgent")) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (componentWallBroadPreference.replacingOccurrences(of: "residence", with: "av") + String(styleButtonDevice.suffix(6)) + "rAgent"))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(componentPartUtility.prefix(6)) + viewCurrentDevice.lowercased())) {
                //: let str = getUserAgentString()
                let str = digitalName()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.makeup { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(screenWarningTitle.suffix(7)) + String(themeMeNutSettings)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func digitalName() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(stylePopularEvent) + String(themeRoundEvent) + String(moduleToneTitle)) + "\(coreReadText)" + (String(coreBestData)) + "\(screenSaltConfig)" + (String(screenCreationName)) + "\(coreStreamMetrics)" + (String(appLaterSettings.prefix(4)) + "bkit/" + colorCenterFinisSystem.capitalized)
    }
}
