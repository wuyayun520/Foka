
//: Declare String Begin

/*: "fee" :*/
fileprivate let componentStyleValue:String = "FEE"

/*: "VIPFee" :*/
fileprivate let viewColorTripSeparatePath:String = "VIPFeeland whole member would"

/*: "chattingNum" :*/
fileprivate let moduleCowPreference:String = "chacurvecurveing"
fileprivate let moduleCoordinatorMetrics:String = "person channel mutualNum"

/*: "You're already in her live room" :*/
fileprivate let colorDrawValue:[UInt8] = [0x9a,0xb0,0xb6,0x68,0xb3,0xa6,0x61,0xa2,0xad,0xb3,0xa6,0xa2,0xa5,0xba,0x61,0xaa,0xaf,0x61,0xa9,0xa6,0xb3,0x61,0xad,0xaa,0xb7,0xa6,0x61,0xb3,0xb0,0xb0,0xae]

fileprivate func tagCurve(define num: UInt8) -> UInt8 {
    let value = Int(num) + 191
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "toUid" :*/
fileprivate let widgetAfterTimer:[UInt8] = [0x62,0x5d,0x43,0x57,0x52]

fileprivate func contentureCellTolerance(pitch num: UInt8) -> UInt8 {
    let value = Int(num) + 18
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "momentId" :*/
fileprivate let k_limitedSettings:[UInt8] = [0x19,0x1b,0x19,0x11,0x1a,0x0,0x3d,0x10]

private func animalHear(whether num: UInt8) -> UInt8 {
    return num ^ 116
}

/*: "extra" :*/
fileprivate let themeRateTimer:[UInt8] = [0xef,0xf2,0xfe,0xf8,0xeb]

/*: "uid" :*/
fileprivate let sessionEscapeText:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let componentSignalTitle:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let themeDynamicsGoSeriesDevice:[UInt8] = [0xfa,0xe0,0xdd,0xf6,0xe4,0xc6,0xe0,0xf6,0xe1]

private func sampleJus(re num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "userStatus" :*/
fileprivate let widgetSupportAlert:[UInt8] = [0x3e,0x3c,0x2e,0x3b,0x1c,0x3d,0x2a,0x3d,0x3e,0x3c]

fileprivate func litHer(them num: UInt8) -> UInt8 {
    let value = Int(num) + 55
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Account is restricted！" :*/
fileprivate let coreYesConfig:String = "Accoulibrary ta today bar able"
fileprivate let featureEnhanceMpUrl:String = "essituationr"
fileprivate let componentRoadSettings:String = "icted！strain lap label"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let moduleFierceRocketPage:[UInt8] = [0xeb,0xcc,0xc4,0xc1,0xc8,0xc9,0x8d,0xd9,0xc2,0x8d,0xc8,0xc3,0xd9,0xc8,0xdf,0x8d,0xd9,0xc5,0xc8,0x8d,0xce,0xc5,0xcc,0xd9,0x8d,0xdf,0xc2,0xc2,0xc0,0x83,0x8d,0xfd,0xc1,0xc8,0xcc,0xde,0xc8,0x8d,0xd9,0xdf,0xd4,0x8d,0xcc,0xca,0xcc,0xc4,0xc3,0x8d,0xc1,0xcc,0xd9,0xc8,0xdf]

private func exploreDimension(mine num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "url" :*/
fileprivate let appEarlyFormat:String = "utool"

/*: "userDetail" :*/
fileprivate let spacingOppositePage:String = "usplatformr"

/*: "&type=6" :*/
fileprivate let themePreparePadName:[Character] = ["&","t","y","p","e"]
fileprivate let componentRegulationTitle:String = "=6"

/*: "&type=7" :*/
fileprivate let k_bagPage:String = "auto flow convert scar&type"
fileprivate let componentBowLeaveTitle:String = "=7"

/*: "amount" :*/
fileprivate let viewOthersEnjoyName:[UInt8] = [0xb4,0xc0,0xc2,0xc8,0xc1,0xc7]

fileprivate func mpThin(grav num: UInt8) -> UInt8 {
    let value = Int(num) + 173
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "https://" :*/
fileprivate let coreLittleTingDevice:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http://" :*/
fileprivate let styleGraphicAlert:[Character] = ["h","t","t","p",":","/","/"]

/*: "transparency" :*/
fileprivate let styleHeEvent:[Character] = ["t","r","a","n","s","p","a","r","e","n","c","y"]

/*: "version= :*/
fileprivate let layoutSectionTelephoneLogger:String = "random ampleversion="

/*: &packageId= :*/
fileprivate let widgetSauceTimer:String = "&packreport hello"

/*: &bundleId= :*/
fileprivate let layoutOdeKey:[Character] = ["&","b","u","n","d","l","e","I"]
fileprivate let widgetBrushTitle:String = "d=crop chase certain dynamics section"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProdPushManager.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class TalkingAppPushManager: NSObject {
public class ProdPushManager: NSObject {
    //: @objc static public let share = TalkingAppPushManager()
    @objc public static let share = ProdPushManager()
    //: private override init() {}
    override private init() {}
}

//: extension TalkingAppPushManager {
extension ProdPushManager {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func noBrainerRand(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        themeSincePlatform.givingStream(eventID: layoutLikeUrl)
        //: guard TalkingSocketManager.shared.isFamous == false else {
        guard TopThen.shared.isFamous == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.colouration(showMsg: spacingQualityHundredSystem)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        MagnitudeReactiveCompatible.popularNet { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            BrandVideoManager.immobilise { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = SatelliteRecognizerDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(componentStyleValue.lowercased())].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(viewColorTripSeparatePath.prefix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(moduleCowPreference.replacingOccurrences(of: "curve", with: "t") + String(moduleCoordinatorMetrics.suffix(3)))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.famousTick()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func puncture() {
        //: PositionThen.share.userFillInfoMode = UserFillInfoModel.init()
        PositionThen.share.userFillInfoMode = UserInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = ExpectViewController()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = concealed()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func varyProminent(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = concealed()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: PromiseEmailVc.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: AcidViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = PromiseEmailVc()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func untilBuild(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.famousTick() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? EmplacementThen, InstrumentalityReactiveCompatible.aftermath().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.contentClick()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? RationalRecognizerDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.rubyRed()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = RationalRecognizerDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        getController()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func vidType(uid: String, enterType: TenantEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard MagnitudeReactiveCompatible.column() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(InstrumentalityReactiveCompatible.aftermath().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.colouration(showMsg: String(bytes: colorDrawValue.map{tagCurve(define: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        FeatureCircleAudienceManager().disjunct(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = MapModel.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.concealed()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: TenantObjectProtocol.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! TenantObjectProtocol).afterFlow()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            InstrumentalityReactiveCompatible.aftermath().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = TenantObjectProtocol()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                themeSincePlatform.givingStream(eventID: componentAcceptLogger, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                themeSincePlatform.givingStream(eventID: displaySceneUtility, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func halfApply(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = OpportunityViewDelegate(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = concealed()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func monthCompletion(chatID: String, isFrom: MapFromEnum = .Normal, completion: ((_ vc: ReverseBannerDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != FragThen.getXiaoMiID() {
        if chatID != FragThen.ringRowingVertical() { // 系统消息
            //: ProgressHUD.show()
            WeatherMapThen.depressCalculate()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: widgetAfterTimer.map{contentureCellTolerance(pitch: $0)}, encoding: .utf8)!: chatID, String(bytes: k_limitedSettings.map{animalHear(whether: $0)}, encoding: .utf8)!: "0", String(bytes: themeRateTimer.map{$0^138}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            ScriptRequestTool.floatingBridgeCompletion(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                WeatherMapThen.evolution()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = SubstanceHandyJSON.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = SubstanceHandyJSON.askDoing(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                UsufructuaryThen.dayCut(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: sessionEscapeText.reversed(), encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: componentSignalTitle.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: themeDynamicsGoSeriesDevice.map{sampleJus(re: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: widgetSupportAlert.map{litHer(them: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: commonThirdSettings,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.colouration(showMsg: (String(coreYesConfig.prefix(5)) + "nt is r" + featureEnhanceMpUrl.replacingOccurrences(of: "situation", with: "t") + String(componentRoadSettings.prefix(6))).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.manageChat(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: SubstanceHandyJSON())
                let chatVC = self.manageChat(chatID: chatID, isFrom: isFrom, userWrap: SubstanceHandyJSON())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: SubstanceHandyJSON ) -> TalkingPrivateChatController {
    func manageChat(chatID: String, isFrom: MapFromEnum = .Normal, userWrap: SubstanceHandyJSON) -> ReverseBannerDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = concealed()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ReverseBannerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ReverseBannerDelegate
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ReverseBannerDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func spot(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        WeatherMapThen.depressCalculate()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        ScriptRequestTool.markEnableCompletion(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            WeatherMapThen.evolution()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.colouration(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if FragThen.isGroupChat(groupId) {
            if FragThen.halogenSpringChatEmotion(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.recoverPrivacy(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = CommandControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.concealed()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.recoverPrivacy(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = CommandControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.concealed()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.colouration(showMsg: String(bytes: moduleFierceRocketPage.map{exploreDimension(mine: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func recoverPrivacy(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.concealed()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: CommandControllerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! CommandControllerDelegate
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func duringToast(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: PositionThen.share.loginUserMode.isTPAuth)
        let tpAuth = SwineInfluenzaSubscriptType(rawValue: PositionThen.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = PanelRecognizerDelegate()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = concealed()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = BetterRecognizerDelegate()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = concealed()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func streetSmart(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func strokeThirty() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = concealed()
        //: let vc = TalkingFeedbackVC.init()
        let vc = DecisionMedalFeedbackThen()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func selfInput(jumpModel: GrantMessageJumpTitleTheoryMeasurable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (appEarlyFormat.replacingOccurrences(of: "tool", with: "rl")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            rangeGalleryThirty(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (spacingOppositePage.replacingOccurrences(of: "platform", with: "e") + "Detail") {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func feather() {
        //: TalkingAppPushManager.share.func__pushToWebVC(webViewType: .SubscribePage)
        ProdPushManager.share.ticInfo(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func circleOfCurvature(type: ExpectUTF8Initializable, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = BalanceViewController()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        concealed()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension TalkingAppPushManager {
extension ProdPushManager {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func ticInfo(webViewType: AntitypeTitleCounterpolation) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        getThrough(webViewType: webViewType, webConfig: ReachConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func retranslatePrompt(popupCallIndex: Int, appendParams: String = (String(themePreparePadName) + componentRegulationTitle.capitalized)) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = DeadlineReactiveCompatible()
        //: view.show()
        view.guestAdjust()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.funcHarshMakeup(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                subscribeBasic(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func responsibility(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(k_bagPage.suffix(5)) + componentBowLeaveTitle.capitalized)) {
        //: let payWinType = PositionThen.share.appUserConfigMode.payWinType
        let payWinType = PositionThen.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = DeadlineReactiveCompatible()
        //: view.show()
        view.guestAdjust()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (PositionThen.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (PositionThen.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.funcHarshMakeup(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                subscribeBasic(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func subscribeBasic(appendParams: String = (String(k_bagPage.suffix(5)) + componentBowLeaveTitle.capitalized)) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = FamousManager.putBack(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: urlStr)
        ProdPushManager.share.rangeGalleryThirty(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        themeSincePlatform.lance(eventID: componentNameKey, parameterStr: [String(bytes: viewOthersEnjoyName.map{mpThin(grav: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func funcHarshMakeup(webViewType: AntitypeTitleCounterpolation, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = ReachConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        getThrough(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            k_rankText = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                themeSincePlatform.lance(eventID: clickEvent, parameterStr: [String(bytes: viewOthersEnjoyName.map{mpThin(grav: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func panelView() {
        //: guard !PositionThen.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !PositionThen.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = PositionThen.share.appConfigMode.urlH5Domain + PositionThen.share.appUserConfigMode.discountUrl
        let httpsUrl = PositionThen.share.appConfigMode.urlH5Domain + PositionThen.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(coreLittleTingDevice)), with: (String(styleGraphicAlert)))
        //: TalkingAppPushManager.share.func__pushToWebVC(urlStr: httpUrl)
        ProdPushManager.share.rangeGalleryThirty(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func getThrough(webViewType: AntitypeTitleCounterpolation, webConfig: ReachConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = FamousManager.putBack(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        someAttention(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func rangeGalleryThirty(urlStr: String?, webConfig: ReachConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? ReachConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        someAttention(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func someAttention(urlStr: String?, webViewType: AntitypeTitleCounterpolation?, webConfig: ReachConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(styleHeEvent))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(layoutSectionTelephoneLogger.suffix(8))) + "\(coreReadText)" + (String(widgetSauceTimer.prefix(5)) + "ageId=") + "\(screenSaltConfig)" + (String(layoutOdeKey) + String(widgetBrushTitle.prefix(2))) + "\(coreStreamMetrics)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = FinerViewController(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = concealed() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: FinerViewController.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! FinerViewController).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.birthShadow(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: moduleAdjustPath, height: moduleAdjustPath / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: moduleAdjustPath, height: componentMeEvent)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension TalkingAppPushManager {
public extension ProdPushManager {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func concealed() -> UIViewController? {
        //: return currentViewController()
        return famousTick()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct GrantMessageJumpTitleTheoryMeasurable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
