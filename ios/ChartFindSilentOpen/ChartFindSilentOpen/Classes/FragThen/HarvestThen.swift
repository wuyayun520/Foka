
//: Declare String Begin

/*: "http://" :*/
fileprivate let coreIonTitle:String = "http://decision pow"

/*: "https://" :*/
fileprivate let styleForeheadAnotherUtility:[Character] = ["h"]
fileprivate let k_aircraftValue:[Character] = ["t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let featureStrainText:String = "series mind attach collecthttp://"

/*: "serverSelctIndex =  :*/
fileprivate let coreHundredValue:String = "eventrv"
fileprivate let viewSampleEmotionSurvivorDevice:String = "ctInpan container or fighter mid"

/*: "serverUrlStr =  :*/
fileprivate let styleAllowApproveKey:String = "ssurfacervsurfacer"

/*: "app. :*/
fileprivate let commonSlowUtility:String = "app.pretty chase nature sound"

/*: .com" :*/
fileprivate let componentProblemUrl:String = ".comable required strain length weak"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let screenAssertConfig:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d"]
fileprivate let themeScarError:String = "properly button icon revenue differenceGift"

/*: "recharge/applePayNotify" :*/
fileprivate let colorOriginallyTitle:String = "recinterest"
fileprivate let spacingMaleCompetitionTimer:String = "nearby this aspect more/applePa"
fileprivate let featureSafetyPath:String = "yNotifyenable blind permission"

/*: "user/appleLogin" :*/
fileprivate let layoutDelayCheckAlert:String = "user/toward region feed majority status"
fileprivate let featureStreamSettings:String = "howp"
fileprivate let themeAssetHelper:String = "pleLoginaddress read center pill"

/*: "IM/getMyBlackList" :*/
fileprivate let appSatisfyMetrics:String = "petition agree memberIM/g"
fileprivate let screenColumnConfig:String = "lifetime writing processing sortkList"

/*: "mf/business/sendMsg" :*/
fileprivate let sessionMedalData:String = "mf/bempty layer rank"
fileprivate let viewVoicePlatform:String = "its skinss/s"

/*: "mf/business/sendGift" :*/
fileprivate let coreMeetMetrics:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let screenProcessingPreference:String = "mf/chwarn run honey"
fileprivate let screenAttractiveTimer:String = "eye until yesterday magnetic combinendMsg"

/*: "GJ21FB" :*/
fileprivate let styleInstructionUtility:String = "GJ2writF"
fileprivate let displaySuccessfullyAlert:String = "cut"

/*: "完成了检测,请求状态正常" :*/
fileprivate let sessionResidentPath:[Character] = ["\u{5b8c}","成","\u{4e86}","检","测",",","请","\u{6c42}","状","态","正","常"]

/*: "接口请求失败" :*/
fileprivate let colorCleanOrientDevice:[Character] = ["接","口","请","求","\u{5931}","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let k_emptyValue:String = "完成了\u{68c0}测"
fileprivate let featureTinSession:String = "\u{6362}s"
fileprivate let viewParaWorldTimer:[Character] = ["e","r","v","e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let featureStatePath:String = "wallTTPP"

/*: "__SCOPED__" :*/
fileprivate let screenSuiteName:[Character] = ["_","_"]
fileprivate let themeAssistantSettings:String = "ScostP"

/*: "tap" :*/
fileprivate let layoutPrepareAlert:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let coreEssentialLogger:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let moduleEditorBeforeBraceUtility:String = "ipfoundationec"

/*: "ppp" :*/
fileprivate let k_chinPreference:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let componentGuidelinePage:[Character] = ["f","u","n"]
fileprivate let appGreetPage:[Character] = ["c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let styleAspectSystem:String = "large harassment engage carrierhttps://"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let screenWordBeefId:[UInt8] = [0x90,0x9f,0x98,0x8d,0x89,0x89,0x8e,0x8f,0x96,0x8f,0x9e,0x8f,0x7d,0x8b,0x97,0x8f,0x6d,0x99,0x99,0x95,0x93,0x8f,0x9d,0x81,0x93,0x9e,0x92,0x7f,0x9c,0x96,0x4a,0x67,0x4a]

fileprivate func contactClearAnima(competition num: UInt8) -> UInt8 {
    let value = Int(num) - 42
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let screenAddedMessage:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let spacingAssessmentConfig:String = "phpsess"
fileprivate let spacingTabFormat:String = "Ithumb"

/*: "func__saveAllCookies =  :*/
fileprivate let kPiKey:String = "darkunc"
fileprivate let screenYouKindMetrics:String = "open capture quick competition menuveAl"
fileprivate let themeAssessmentTimer:String = "es = theoretical than video again notice"

/*: "func__saveAllCookies  error" :*/
fileprivate let appArticlePreference:String = "FUNC"
fileprivate let screenBusinessTimer:String = "asset low service ilk correcteAll"
fileprivate let widgetGoingName:String = "es  income product sun"
fileprivate let appCountryLogger:String = "eplusplusoplus"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HarvestThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum WaitTransformable: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class HarvestThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = HarvestThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        moon()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(coreIonTitle.prefix(7)))) || newValue.hasPrefix((String(styleForeheadAnotherUtility) + String(k_aircraftValue))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if widgetOkPlatform && !displayBlockConversationMetrics {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(featureStrainText.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(featureStrainText.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    way(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func moon() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = screenOutputConfig.integer(forKey: themeRouteLogger)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (coreHundredValue.replacingOccurrences(of: "event", with: "se") + "erSel" + String(viewSampleEmotionSurvivorDevice.prefix(4)) + "dex = ") + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = exitUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(coreIonTitle.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if widgetOkPlatform && !displayBlockConversationMetrics {
            //: serverPrefix = "http://"
            serverPrefix = (String(coreIonTitle.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (styleAllowApproveKey.replacingOccurrences(of: "surface", with: "e") + "UrlStr = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func exitUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func waist(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(commonSlowUtility.prefix(4))) + "\(componentFormalPath)" + (String(componentProblemUrl.prefix(4))), (String(commonSlowUtility.prefix(4))) + "\(componentFormalPath)" + (String(componentProblemUrl.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(screenAssertConfig) + String(themeScarError.suffix(4))),
         //: "recharge/applePayNotify",
         (colorOriginallyTitle.replacingOccurrences(of: "interest", with: "h") + "arge" + String(spacingMaleCompetitionTimer.suffix(8)) + String(featureSafetyPath.prefix(7))),
         //: "user/appleLogin"] + slowPathArr1
         (String(layoutDelayCheckAlert.prefix(5)) + featureStreamSettings.replacingOccurrences(of: "how", with: "a") + String(themeAssetHelper.prefix(8)))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(appSatisfyMetrics.suffix(4)) + "etMyBlac" + String(screenColumnConfig.suffix(5))),
         //: "mf/business/sendMsg",
         (String(sessionMedalData.prefix(4)) + "usine" + String(viewVoicePlatform.suffix(4)) + "endMsg"),
         //: "mf/business/sendGift",
         (String(coreMeetMetrics)),
         //: "mf/chatRoom/sendMsg"]
         (String(screenProcessingPreference.prefix(5)) + "atRoom/se" + String(screenAttractiveTimer.suffix(5)))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = FragThen.getSystemLangCode()
        let lang = FragThen.decideRevenue()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .ratingTitle(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (styleInstructionUtility.replacingOccurrences(of: "writ", with: "1") + displaySuccessfullyAlert.replacingOccurrences(of: "cut", with: "B")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension HarvestThen {
    //: func checkServerUrl() {
    func faceDown() {
        //: if PositionThen.share.networkStatus != .Unavailable {
        if PositionThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            columnSurvivor()
            //: } else {
        } else {
            //: func_starCodeTime()
            currentPage()
        }
    }

    //: func func_starCodeTime() {
    func currentPage() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.columnSurvivor()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func sauceAcross() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func columnSurvivor() {
        //: if PositionThen.share.networkStatus == .Unavailable {
        if PositionThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        sauceAcross()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        RushThen.reduce { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(sessionResidentPath)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(colorCleanOrientDevice)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.currentPage()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (k_emptyValue + ",需要更" + featureTinSession + String(viewParaWorldTimer)))
                    //: self.func__changeHostUrl()
                    self.secureUrl()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func secureUrl() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        sauceAcross()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        screenOutputConfig.set(serverSelctIndex, forKey: themeRouteLogger)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(coreIonTitle.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if widgetOkPlatform, !displayBlockConversationMetrics {
            //: serverPrefix = "http://"
            serverPrefix = (String(coreIonTitle.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension HarvestThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func inwardsUtiliseSoftProxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(featureStatePath.replacingOccurrences(of: "wall", with: "H") + "roxy")] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func yap() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(screenSuiteName) + themeAssistantSettings.replacingOccurrences(of: "cost", with: "CO") + "ED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(layoutPrepareAlert))) || keyStr.contains((String(coreEssentialLogger))) || keyStr.contains((moduleEditorBeforeBraceUtility.replacingOccurrences(of: "foundation", with: "s"))) || keyStr.contains((String(k_chinPreference))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension HarvestThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func way(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(componentGuidelinePage) + String(appGreetPage)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = getBackColumn()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(coreIonTitle.prefix(7))), with: (String(styleForeheadAnotherUtility) + String(k_aircraftValue)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(styleForeheadAnotherUtility) + String(k_aircraftValue))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(styleAspectSystem.suffix(8))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            quant(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        belowTimber()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func quant(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: screenWordBeefId.map{contactClearAnima(competition: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func getBackColumn() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(coreIonTitle.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if widgetOkPlatform, !displayBlockConversationMetrics {
            //: serverPrefix = "https://"
            serverPrefix = (String(styleForeheadAnotherUtility) + String(k_aircraftValue))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(commonSlowUtility.prefix(4))) + "\(componentFormalPath)" + (String(componentProblemUrl.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func serverRefuse(note _: Notification) {
        //: if PositionThen.share.loginUid.isEmptyString {
        if PositionThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            sumo()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            wee()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func sumo() {
        //: var htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(screenAddedMessage)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(screenAddedMessage))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(screenAddedMessage)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(screenAddedMessage))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(screenAddedMessage))), h5Cookie.keys.contains((String(screenAddedMessage))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(screenAddedMessage))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(screenAddedMessage))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))), h5Cookie.keys.contains((spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func wee() {
        //: let htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = PositionThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == PositionThen.share.loginUserMode.userID {
            if cookie.name == (String(screenAddedMessage)), cookie.value == PositionThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(screenAddedMessage)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(screenAddedMessage))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(screenAddedMessage))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(screenAddedMessage))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(spacingAssessmentConfig.uppercased() + spacingTabFormat.replacingOccurrences(of: "thumb", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func belowTimber() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (kPiKey.replacingOccurrences(of: "dark", with: "f") + "__sa" + String(screenYouKindMetrics.suffix(4)) + "lCooki" + String(themeAssessmentTimer.prefix(5))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            screenOutputConfig.set(cookieData, forKey: spacingListDoingEvent)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (appArticlePreference.lowercased() + "__sav" + String(screenBusinessTimer.suffix(4)) + "Cooki" + String(widgetGoingName.prefix(4)) + appCountryLogger.replacingOccurrences(of: "plus", with: "r")))
        }
    }

    //: func func__setAppCookies() {
    func behindSpark() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = screenOutputConfig.object(forKey: spacingListDoingEvent)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func yearDatabase() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        screenOutputConfig.removeObject(forKey: spacingListDoingEvent)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
