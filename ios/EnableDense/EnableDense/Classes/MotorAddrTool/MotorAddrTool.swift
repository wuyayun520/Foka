
//: Declare String Begin

/*: "http://" :*/
fileprivate let const_observeUrl:String = "http://report soon enough properly leading"

/*: "https://" :*/
fileprivate let app_itsUrl:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let user_hungId:String = "http://progress builder right ting style"

/*: "serverSelctIndex =  :*/
fileprivate let data_mirrorText:String = "sspecrv"
fileprivate let show_observerMsg:String = "score washlctI"
fileprivate let k_calledMessage:String = "rating=rating"

/*: "serverUrlStr =  :*/
fileprivate let mainStartTarId:[Character] = ["s","e","r","v"]
fileprivate let constOriginTitle:[Character] = ["e","r","U","r","l","S","t","r"," ","="," "]

/*: "app. :*/
fileprivate let notiMeetingMidMessage:String = "app.reference form"

/*: .com" :*/
fileprivate let show_personalId:String = ".comrule right"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let noti_transportMessage:String = "mf/chatRdescribe carry"
fileprivate let const_administrativeKey:[Character] = ["o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let show_resentPath:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N"]
fileprivate let main_stripHeTitle:String = "OTIFY"

/*: "user/appleLogin" :*/
fileprivate let show_dimensionLineHiMessage:String = "user/tip by"
fileprivate let k_modelTitle:String = "oovern"

/*: "IM/getMyBlackList" :*/
fileprivate let mainComputerComputeTitle:[Character] = ["I","M","/","g","e","t","M","y"]
fileprivate let data_succeedMsg:String = "BlackListyield limited story line"

/*: "mf/business/sendMsg" :*/
fileprivate let const_opportunityId:String = "arrow extent classmf/busin"
fileprivate let const_unknownName:String = "wrap tolerance piece bearess/se"

/*: "mf/business/sendGift" :*/
fileprivate let user_offArriveUrl:String = "mf/bbox announcement catch material busy"
fileprivate let user_injuryMessage:String = "s/senwant successfully"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let appFormalMatchFormat:String = "mf/chbrow previous hour method"
fileprivate let noti_waveCapitalUrl:String = "resource subom/se"

/*: "GJ21FB" :*/
fileprivate let data_tumbleTitle:String = "GJ2stretchB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let app_yaPath:String = "完\u{6210}了检测"

/*: "接口请求失败" :*/
fileprivate let constEnthusiasmMessage:String = "接\u{53e3}请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let showBushFormat:String = "完成了检\u{6d4b},需"
fileprivate let data_neverName:String = "要更sharp"
fileprivate let dataUsAssistKey:String = "sefirstv"

/*: "HTTPProxy" :*/
fileprivate let k_likeMessage:[Character] = ["H","T","T","P","P","r"]
fileprivate let data_hurryId:String = "oxbeautiful"

/*: "__SCOPED__" :*/
fileprivate let notiSuccessfullyTitle:[Character] = ["_","_","S"]
fileprivate let show_advertisingMessage:String = "COPED__visible remote"

/*: "tap" :*/
fileprivate let mainDecideStr:String = "tradep"

/*: "tun" :*/
fileprivate let dataRejectCircleFormat:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let show_angleKey:String = "IPSEC"

/*: "ppp" :*/
fileprivate let data_portraitUrl:String = "appearp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let show_relieveKey:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g"]
fileprivate let user_foodData:String = "Cookwriter brush general fit"
fileprivate let kPillowContent:String = "bot example big cloudies = "

/*: "https:// :*/
fileprivate let appTranslationValue:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let noti_planValue:[UInt8] = [0x28,0x37,0x30,0x25,0x21,0x21,0x26,0x27,0x2e,0x27,0x36,0x27,0x15,0x23,0x2f,0x27,0x5,0x31,0x31,0x2d,0x2b,0x27,0x35,0x19,0x2b,0x36,0x2a,0x17,0x34,0x2e,0xe2,0xff,0xe2]

fileprivate func withoutJump(support num: UInt8) -> UInt8 {
    let value = Int(num) - 194
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let showUnablePathTitle:String = "UIaction"

/*: "PHPSESSID" :*/
fileprivate let k_grayTitle:String = "translationPS"
fileprivate let show_resWorkOfId:String = "essid"

/*: "func__saveAllCookies =  :*/
fileprivate let const_shirtFormat:String = "reduceunc"
fileprivate let dataEntryName:String = "contain network handle happyeAllCo"
fileprivate let noti_bigMsg:String = "before=before"

/*: "func__saveAllCookies  error" :*/
fileprivate let main_mmNoticeMsg:[Character] = ["f","u","n","c","_","_","s","a"]
fileprivate let dataDignityWithFormat:[Character] = ["v","e","A","l","l","C","o","o","k","i","e","s"," "," "]
fileprivate let notiObserverContent:[Character] = ["e","r","r","o","r"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MotorAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum SubsumeExpressionConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class MotorAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = MotorAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        hairAfter()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(const_observeUrl.prefix(7)))) || newValue.hasPrefix((String(app_itsUrl))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if data_serverTitle && !app_underKey {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(user_hungId.prefix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(user_hungId.prefix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    funcCookiesModifyNeedMobileWork(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func hairAfter() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = notiMovieText.integer(forKey: const_buildFormat)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (data_mirrorText.replacingOccurrences(of: "spec", with: "e") + "erSe" + String(show_observerMsg.suffix(4)) + "ndex" + k_calledMessage.replacingOccurrences(of: "rating", with: " ")) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = ridGreet()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_observeUrl.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if data_serverTitle && !app_underKey {
            //: serverPrefix = "http://"
            serverPrefix = (String(const_observeUrl.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(mainStartTarId) + String(constOriginTitle)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func ridGreet() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func unused(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(notiMeetingMidMessage.prefix(4))) + "\(notiNetworkTitle)" + (String(show_personalId.prefix(4))), (String(notiMeetingMidMessage.prefix(4))) + "\(notiNetworkTitle)" + (String(show_personalId.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(noti_transportMessage.prefix(8)) + String(const_administrativeKey)),
         //: "recharge/applePayNotify",
         (String(show_resentPath) + main_stripHeTitle.lowercased()),
         //: "user/appleLogin"] + slowPathArr1
         (String(show_dimensionLineHiMessage.prefix(5)) + "appleL" + k_modelTitle.replacingOccurrences(of: "over", with: "gi"))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(mainComputerComputeTitle) + String(data_succeedMsg.prefix(9))),
         //: "mf/business/sendMsg",
         (String(const_opportunityId.suffix(8)) + String(const_unknownName.suffix(6)) + "ndMsg"),
         //: "mf/business/sendGift",
         (String(user_offArriveUrl.prefix(4)) + "usines" + String(user_injuryMessage.prefix(5)) + "dGift"),
         //: "mf/chatRoom/sendMsg"]
         (String(appFormalMatchFormat.prefix(5)) + "atRo" + String(noti_waveCapitalUrl.suffix(5)) + "ndMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = DetermineJoinReactiveCompatible.getSystemLangCode()
        let lang = DetermineJoinReactiveCompatible.exCatch()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .thanAccount(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (data_tumbleTitle.replacingOccurrences(of: "stretch", with: "1F")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension MotorAddrTool {
    //: func checkServerUrl() {
    func presentationTranslate() {
        //: if SteppingMotorThen.share.networkStatus != .Unavailable {
        if SteppingMotorThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            incumbent()
            //: } else {
        } else {
            //: func_starCodeTime()
            someReading()
        }
    }

    //: func func_starCodeTime() {
    func someReading() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.incumbent()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func identical() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func incumbent() {
        //: if SteppingMotorThen.share.networkStatus == .Unavailable {
        if SteppingMotorThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        identical()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        PercentageManagerRequest.motion { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (app_yaPath + ",\u{8bf7}\u{6c42}状态\u{6b63}常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (constEnthusiasmMessage))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.someReading()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (showBushFormat + data_neverName.replacingOccurrences(of: "sharp", with: "换") + dataUsAssistKey.replacingOccurrences(of: "first", with: "r") + "er url"))
                    //: self.func__changeHostUrl()
                    self.validUrl()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func validUrl() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        identical()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        notiMovieText.set(serverSelctIndex, forKey: const_buildFormat)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_observeUrl.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if data_serverTitle, !app_underKey {
            //: serverPrefix = "http://"
            serverPrefix = (String(const_observeUrl.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension MotorAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func attractiveness() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(k_likeMessage) + data_hurryId.replacingOccurrences(of: "beautiful", with: "y"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func basicGravity() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(notiSuccessfullyTitle) + String(show_advertisingMessage.prefix(7)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((mainDecideStr.replacingOccurrences(of: "trade", with: "ta"))) || keyStr.contains((String(dataRejectCircleFormat))) || keyStr.contains((show_angleKey.lowercased())) || keyStr.contains((data_portraitUrl.replacingOccurrences(of: "appear", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension MotorAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func funcCookiesModifyNeedMobileWork(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(show_relieveKey) + String(user_foodData.prefix(4)) + String(kPillowContent.suffix(6))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = onicial()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = SteppingMotorThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = SteppingMotorThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(const_observeUrl.prefix(7))), with: (String(app_itsUrl)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(app_itsUrl))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(appTranslationValue)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            funcUponSmart(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        rescueByCookies()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func funcUponSmart(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: noti_planValue.map{withoutJump(support: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func onicial() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_observeUrl.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if data_serverTitle, !app_underKey {
            //: serverPrefix = "https://"
            serverPrefix = (String(app_itsUrl))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(notiMeetingMidMessage.prefix(4))) + "\(notiNetworkTitle)" + (String(show_personalId.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func refrigeratorCookie(note _: Notification) {
        //: if SteppingMotorThen.share.loginUid.isEmptyString {
        if SteppingMotorThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            unlessVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            glass()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func unlessVersion() {
        //: var htmlDomain: String = SteppingMotorThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = SteppingMotorThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (showUnablePathTitle.replacingOccurrences(of: "action", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased()) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (showUnablePathTitle.replacingOccurrences(of: "action", with: "D")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased()) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))), h5Cookie.keys.contains((showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())), h5Cookie.keys.contains((k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func glass() {
        //: let htmlDomain: String = SteppingMotorThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = SteppingMotorThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == SteppingMotorThen.share.loginUserMode.userID {
            if cookie.name == (showUnablePathTitle.replacingOccurrences(of: "action", with: "D")), cookie.value == SteppingMotorThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased()) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (showUnablePathTitle.replacingOccurrences(of: "action", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased()) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(showUnablePathTitle.replacingOccurrences(of: "action", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(k_grayTitle.replacingOccurrences(of: "translation", with: "PH") + show_resWorkOfId.uppercased())] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func rescueByCookies() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (const_shirtFormat.replacingOccurrences(of: "reduce", with: "f") + "__sav" + String(dataEntryName.suffix(6)) + "okies" + noti_bigMsg.replacingOccurrences(of: "before", with: " ")) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            notiMovieText.set(cookieData, forKey: show_cornerText)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(main_mmNoticeMsg) + String(dataDignityWithFormat) + String(notiObserverContent)))
        }
    }

    //: func func__setAppCookies() {
    func acquirable() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = notiMovieText.object(forKey: show_cornerText)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func funcThroughRecording() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        notiMovieText.removeObject(forKey: show_cornerText)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
