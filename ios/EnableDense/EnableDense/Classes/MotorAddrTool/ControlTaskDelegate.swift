
//: Declare String Begin

/*: ; build: :*/
fileprivate let notiHeartMsg:[Character] = [";"," ","b","u","i","l","d"]
fileprivate let userPerformData:String = ":"

/*: ; iOS  :*/
fileprivate let constAgendaValue:String = "reason earn late explain; iOS"
fileprivate let mainPageMsg:[Character] = [" "]

/*: "audio" :*/
fileprivate let showPiViaAutomaticMsg:String = "audiinner"

/*: .wav" :*/
fileprivate let show_wrapName:String = ".wavliteral yet thanks from"

/*: "audio/wav" :*/
fileprivate let k_mistName:String = "audzz"

/*: .jpg" :*/
fileprivate let userDeliveryPath:String = ".jpgstill dit inside attribute curiosity"

/*: "image/jpeg" :*/
fileprivate let show_builderValue:String = "image/jorigin speak heel my healthy"
fileprivate let userElementStr:String = "pego"

/*: "://app." :*/
fileprivate let mainPocketLiteralData:String = ":/"
fileprivate let mainIncludeTaKey:String = "/app.develop tender report survival population"

/*: "data" :*/
fileprivate let const_residentMsg:String = "forwardta"

/*: ":null" :*/
fileprivate let constDecreaseUrl:[Character] = [":","n","u","l","l"]

/*: "Request failed, Try again later" :*/
fileprivate let noti_insuranceId:[UInt8] = [0xe,0x39,0x2d,0x29,0x39,0x2f,0x28,0x7c,0x3a,0x3d,0x35,0x30,0x39,0x38,0x70,0x7c,0x8,0x2e,0x25,0x7c,0x3d,0x3b,0x3d,0x35,0x32,0x7c,0x30,0x3d,0x28,0x39,0x2e]

private func wordPainting(dial num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "json error" :*/
fileprivate let constMentionStr:String = "jsfuture"
fileprivate let const_personalTitle:String = "by ting old all arean error"

/*: "platform=iphone&version= :*/
fileprivate let constSightFormat:String = "plresultf"
fileprivate let main_allowValue:[Character] = ["o","r","m","=","i","p","h","o","n","e"]
fileprivate let dataLiftMessage:[Character] = ["&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let mainFirmTitle:[Character] = ["&","p","a","c","k","a"]
fileprivate let show_speakerUrl:String = "occur facility interactiongeId="

/*: &bundleId= :*/
fileprivate let notiPushGoPath:String = "administrative challenge&bundl"
fileprivate let mainCarKey:[Character] = ["e","I","d","="]

/*: &lang= :*/
fileprivate let mainAllyParentName:String = "&lang=edge transportation standard"

/*: "/route/slowindex" :*/
fileprivate let constFatalCuteName:String = "report editor/route"
fileprivate let data_eventText:String = "windeshirt"

/*: "/route/index" :*/
fileprivate let notiOutlinePath:[Character] = ["/","r","o","u","t","e","/","i","n","d","e"]
fileprivate let user_missingUrl:[Character] = ["x"]

/*: "?path= :*/
fileprivate let showLevelKey:[Character] = ["?","p","a","t","h","="]

/*: "&GJNonceStr= :*/
fileprivate let userPowValue:String = "sure convert patron plan&GJN"
fileprivate let userDarkName:String = "r=lift field size scope host"

/*: "&params= :*/
fileprivate let showThemeBindData:String = "tare if&par"

/*: "token" :*/
fileprivate let kModelUrl:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let noti_remarkUtilityId:String = "请\u{6c42}成\u{529f}"

/*: "AAAAAAAA" :*/
fileprivate let show_costContent:String = "bearbearbear"
fileprivate let userMinPopText:String = "cover"

/*: "下载完成 -  :*/
fileprivate let main_cancelMessage:[Character] = ["\u{4e0b}","载","完","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let const_residentKey:String = "cap cut format/Docum"
fileprivate let kReplacementFormat:[Character] = ["e","n","t","s","/"]

/*: ".mp4" :*/
fileprivate let user_fragmentMessage:String = ".mp4filter required"

/*: "移动地址: :*/
fileprivate let data_engagePath:String = "移动地址:"

/*: "后台任务下载回来" :*/
fileprivate let const_beanText:String = "后台任务下载回\u{6765}"

/*: "下载成功 : :*/
fileprivate let main_sufficientMirrorData:String = "下载成功 :"

/*: "Net Error" :*/
fileprivate let mainStrokeUrl:String = "include till attention op satisfyNet Er"
fileprivate let app_siblingPath:String = "rdialogr"

/*: "下载失败 : :*/
fileprivate let app_plainUrl:String = "\u{4e0b}载失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ControlTaskDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let appOfTitle = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let userCurrencyName = "\(user_packageText)/\(data_screenUrl) (\(appBlockUrl)" + (String(notiHeartMsg) + userPerformData.capitalized) + "\(showAccuracyTitle)" + (String(constAgendaValue.suffix(5)) + String(mainPageMsg)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: RedErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let data_attributeId = ControlTaskDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class ControlTaskDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func doRate(model: MpRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.noticeJust() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.nearAssemblage(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.overwinter(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func nearAssemblage(model: MpRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.convertLimited(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.todayModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    ticktock(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: main_backgroundPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        revenueMsg(showMsg: main_backgroundPath)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (showPiViaAutomaticMsg.replacingOccurrences(of: "inner", with: "o")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.stampTap())+\(main_netPath)" + (String(show_wrapName.prefix(4))), mimeType: (k_mistName.replacingOccurrences(of: "zz", with: "io") + "/wav"))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(userDeliveryPath.prefix(4))), mimeType: (String(show_builderValue.prefix(7)) + userElementStr.replacingOccurrences(of: "go", with: "g")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    ticktock(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: main_backgroundPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        revenueMsg(showMsg: main_backgroundPath)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func overwinter(model: MpRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.convertLimited(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.todayModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: app_underKey ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = appOfTitle }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    ticktock(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: main_backgroundPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        revenueMsg(showMsg: main_backgroundPath)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = appOfTitle }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    ticktock(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: main_backgroundPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        revenueMsg(showMsg: main_backgroundPath)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func ticktock(model: MpRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            MotorAddrTool.share.funcCookiesModifyNeedMobileWork(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if app_underKey, model.requestServer.contains((mainPocketLiteralData.capitalized + String(mainIncludeTaKey.prefix(5)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.decideM(key: MotorAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (const_residentMsg.replacingOccurrences(of: "forward", with: "da")) + "\"" + (String(constDecreaseUrl)), with: "" + "\"" + (const_residentMsg.replacingOccurrences(of: "forward", with: "da")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !app_underKey {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(constDecreaseUrl)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<FireguardHandyJSON>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == SpringHangBinaryCount.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, RedErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    revenueMsg(showMsg: responseModel.msg ?? String(bytes: noti_insuranceId.map{wordPainting(dial: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case SpringHangBinaryCount.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: data_featureName, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: (constMentionStr.replacingOccurrences(of: "future", with: "o") + String(const_personalTitle.suffix(7))).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func convertLimited(model: MpRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (constSightFormat.replacingOccurrences(of: "result", with: "at") + String(main_allowValue) + String(dataLiftMessage)) + "\(constLikeName)" + (String(mainFirmTitle) + String(show_speakerUrl.suffix(5))) + "\(main_netPath)" + (String(notiPushGoPath.suffix(6)) + String(mainCarKey)) + "\(appBlockUrl)" + (String(mainAllyParentName.prefix(6))) + "\(MotorAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if app_underKey, serverUrl.contains((mainPocketLiteralData.capitalized + String(mainIncludeTaKey.prefix(5)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if MotorAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(constFatalCuteName.suffix(6)) + "/slo" + data_eventText.replacingOccurrences(of: "shirt", with: "x")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(notiOutlinePath) + String(user_missingUrl))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(showLevelKey)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(userPowValue.suffix(4)) + "onceSt" + String(userDarkName.prefix(2))) + "\(MotorAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.atPublish(key: MotorAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(showThemeBindData.suffix(4)) + "ams=") + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func todayModel(model: MpRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(userCurrencyName)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(kModelUrl)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func pullFlag(model: MpRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.convertLimited(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (noti_remarkUtilityId) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (show_costContent.replacingOccurrences(of: "bear", with: "AA") + userMinPopText.replacingOccurrences(of: "cover", with: "AA"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(main_cancelMessage)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(const_residentKey.suffix(6)) + String(kReplacementFormat)) + NSDate.stampTap() + (String(user_fragmentMessage.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (data_engagePath.capitalized) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (const_beanText))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = KeepBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func requestCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: dataAddressText.dramaticComposition(), with: notiNetworkTitle)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (main_sufficientMirrorData.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.Normal.rawValue, errorMsg: (String(mainStrokeUrl.suffix(6)) + app_siblingPath.replacingOccurrences(of: "dialog", with: "o")).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: (String(mainStrokeUrl.suffix(6)) + app_siblingPath.replacingOccurrences(of: "dialog", with: "o")).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (app_plainUrl) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func vocalizationCompletion(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: dataAddressText.dramaticComposition(), with: notiNetworkTitle)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (main_sufficientMirrorData.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.Normal.rawValue, errorMsg: (String(mainStrokeUrl.suffix(6)) + app_siblingPath.replacingOccurrences(of: "dialog", with: "o")).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, RedErrorResponse(errorCode: SpringHangBinaryCount.NetError.rawValue, errorMsg: (String(mainStrokeUrl.suffix(6)) + app_siblingPath.replacingOccurrences(of: "dialog", with: "o")).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (app_plainUrl) + "\(errMsg)")
            }
        }
    }
}
