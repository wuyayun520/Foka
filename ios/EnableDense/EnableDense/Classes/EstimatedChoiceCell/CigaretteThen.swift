
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let k_aliveValue:String = "res practice glass calendar ricesetP"
fileprivate let dataPositiveId:[Character] = ["a","s","s","w","o","r","d"]

/*: "user/appleLogin" :*/
fileprivate let app_tooValue:String = "user/hide include"
fileprivate let notiCourseKey:[Character] = ["a","p","p","l","e"]
fileprivate let mainOffUrl:[Character] = ["L","o","g","i","n"]

/*: "user/googleLogin" :*/
fileprivate let kBottomStreamTitle:String = "step thinkuser/"
fileprivate let userPlatformData:String = "withinn"

/*: "user/smsLogin" :*/
fileprivate let k_togetherText:[Character] = ["u","s","e","r","/","s","m","s","L","o","g"]
fileprivate let notiBallProcessorKey:[Character] = ["i","n"]

/*: "user/emailVcodeLogin" :*/
fileprivate let data_properlyUrl:String = "text income debate solaruser/"
fileprivate let dataThreadPanelId:String = "lVcodecalled head pic recording"

/*: "user/pwdLogin" :*/
fileprivate let app_angleStr:[Character] = ["u","s","e","r","/","p","w"]
fileprivate let user_easternRentName:String = "dLoginappear actual fill"

/*: "user/emailPwdLogin" :*/
fileprivate let constStretchFormat:String = "user/snap whole direct arc builder"
fileprivate let const_followAdvanceUrl:String = "wdLogiattach primary cost"
fileprivate let kGapKey:[Character] = ["n"]

/*: "mobile" :*/
fileprivate let dataCapacityName:[UInt8] = [0x65,0x6c,0x69,0x62,0x6f,0x6d]

/*: "areaCode" :*/
fileprivate let dataGoldUrl:[UInt8] = [0xcf,0xe0,0xd3,0xcf,0xb1,0xdd,0xd2,0xd3]

fileprivate func writerFormal(failure num: UInt8) -> UInt8 {
    let value = Int(num) + 146
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/sendLoginSms" :*/
fileprivate let mainDataMaterialName:[Character] = ["u","s","e","r","/","s","e","n","d","L","o"]
fileprivate let showHighlightText:String = "river disk moment tingginSms"

/*: "RSA加密失败" :*/
fileprivate let mainTellName:String = "RSA加密失败creation today clearly table"

/*: "password" :*/
fileprivate let kCarId:[UInt8] = [0x1,0xf2,0x4,0x4,0x8,0x0,0x3,0xf5]

fileprivate func behindTender(bot num: UInt8) -> UInt8 {
    let value = Int(num) - 145
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "vcode" :*/
fileprivate let app_examineValue:[UInt8] = [0x65,0x64,0x6f,0x63,0x76]

/*: "user/modifyPassword" :*/
fileprivate let constPublicationLookMsg:[Character] = ["u","s","e","r","/","m","o","d","i","f","y","P","a","s","s","w"]
fileprivate let user_missingData:[Character] = ["o","r","d"]

/*: "user/sendModifyPwdSms" :*/
fileprivate let userModestValue:[Character] = ["u","s","e","r","/","s","e","n","d","M","o","d","i","f"]
fileprivate let data_orangeUrl:String = "index face holder unknownyPwdSms"

/*: "user/sendModifyPwdEmail" :*/
fileprivate let main_resistancePath:String = "usestock"
fileprivate let kWhatStr:String = "sire generate ditifyPw"

/*: "mf/user/getMyInfo" :*/
fileprivate let noti_nowPath:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","M","y","I"]
fileprivate let notiTranslationMsg:[Character] = ["n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let noti_bottomMsg:[Character] = ["i","n","v"]
fileprivate let dataCanNextValue:String = "just dramaticite/b"
fileprivate let userMovieValueLivingStr:String = "itaper"

/*: "invite/isBind" :*/
fileprivate let constSegmentFormat:String = "burnvi"
fileprivate let main_controlStepKey:String = "own anima offsBind"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let showWantBindPresentationName:[Character] = ["m","f","/","u","s","e","r","/","C","h","e","c","k","I","n","v","i","t","e","C","o","d","e"]

/*: "mf/user/updateInfo" :*/
fileprivate let kTimingFormat:[Character] = ["m","f","/","u","s","e","r","/","u","p"]
fileprivate let app_lightId:String = "DA"
fileprivate let noti_itsKey:String = "teInfoinput opposite should toward"

/*: "email" :*/
fileprivate let userLevelName:[UInt8] = [0xa,0x2,0xe,0x6,0x3]

private func fillPractice(destroy num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "user/getEmailBindVCode" :*/
fileprivate let appExtraSpeedFormat:String = "user/wait examine priority feature ally"
fileprivate let kCreativeKey:String = "mailBinnarrow unknown jaw actual"
fileprivate let app_thumbYouPath:[Character] = ["d","V","C","o","d","e"]

/*: "user/sendLoginEmail" :*/
fileprivate let appWrittenMessage:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g"]
fileprivate let showBridgeKey:String = "proposal shareinEmail"

/*: "user/emailBind" :*/
fileprivate let app_braceFormat:String = "square click himuser/"
fileprivate let appExposureSaleStr:String = "emvisitor"
fileprivate let showStatusValue:String = "fice convertilBind"

/*: "user/logout" :*/
fileprivate let userCigaretteName:String = "user/path technique size"

/*: "app/countryList" :*/
fileprivate let data_totalTitle:String = "logical running missapp/cou"
fileprivate let data_modestStr:[Character] = ["n","t","r","y","L","i","s","t"]

/*: "user/getMobileBindVCode" :*/
fileprivate let const_peerCounterestedStr:String = "ucandid"
fileprivate let appFactorMessage:String = "er/gengagement telephone chance he po"
fileprivate let const_simplyGenderStr:String = "leBimultiple model dignity fice over"

/*: "user/mobileBind" :*/
fileprivate let data_midMessage:String = "user/mattach magnitude stop pan sale"
fileprivate let showGreetName:String = "eBindpriority draw chemical part rank"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CigaretteThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class CigaretteThen: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func justMy(type: BulletTextConvertible, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.black(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(k_aliveValue.suffix(4)) + String(dataPositiveId))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: LatPushManager.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                LatPushManager.share.restoreing(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            CigaretteThen.dataSpeaker(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func black(type: BulletTextConvertible, params: [String: String]) -> MpRequestModel {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (String(app_tooValue.prefix(5)) + String(notiCourseKey) + String(mainOffUrl))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (String(kBottomStreamTitle.suffix(5)) + "googleLo" + userPlatformData.replacingOccurrences(of: "within", with: "gi"))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(k_togetherText) + String(notiBallProcessorKey))
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(data_properlyUrl.suffix(5)) + "emai" + String(dataThreadPanelId.prefix(6)) + "Login")
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(app_angleStr) + String(user_easternRentName.prefix(6)))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (String(constStretchFormat.prefix(5)) + "emailP" + String(const_followAdvanceUrl.prefix(6)) + String(kGapKey))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func fragmentWith(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: dataCapacityName.reversed(), encoding: .utf8)!: reslutStr, String(bytes: dataGoldUrl.map{writerFormal(failure: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = MpRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(mainDataMaterialName) + String(showHighlightText.suffix(6)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func club(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: dataCapacityName.reversed(), encoding: .utf8)!: resultPhoneStr, String(bytes: dataGoldUrl.map{writerFormal(failure: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: kCarId.map{behindTender(bot: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            CigaretteThen.justMy(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func agentDay(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: kCarId.map{behindTender(bot: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: app_examineValue.reversed(), encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = MpRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(constPublicationLookMsg) + String(user_missingData))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func animal(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(userModestValue) + String(data_orangeUrl.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func sendSignalCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (main_resistancePath.replacingOccurrences(of: "stock", with: "r") + "/sendMod" + String(kWhatStr.suffix(5)) + "dEmail")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func dataSpeaker(_ type: BulletTextConvertible, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(noti_nowPath) + String(notiTranslationMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            notiMovieText.set(result, forKey: k_tagMessage)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<EchoModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: SteppingMotorThen.share.loginUserMode = userModel
                SteppingMotorThen.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                notiMovieText.set(type.rawValue, forKey: constLevelSinceRequestStr)
                //: if SteppingMotorThen.share.loginUserMode.updateInfo == false {
                if SteppingMotorThen.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: LatPushManager.share.func__pushToFillUserInfoVC()
                    LatPushManager.share.overQuantityeractionEnd()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: kTurnId, object: nil, userInfo: result as! [String: Any])

                //: if SteppingMotorThen.share.loginUserMode.remindBindEmail == true {
                if SteppingMotorThen.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: LatPushManager.share.func__pushToLockUserEmailVC(isShowBack: false)
                        LatPushManager.share.afterTo(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func moon(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (String(noti_bottomMsg) + String(dataCanNextValue.suffix(5)) + "indInv" + userMovieValueLivingStr.replacingOccurrences(of: "tap", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func inviter(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (constSegmentFormat.replacingOccurrences(of: "burn", with: "in") + "te/i" + String(main_controlStepKey.suffix(5)))

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func status(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(showWantBindPresentationName))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func aboveTaExamine(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(kTimingFormat) + app_lightId.lowercased() + String(noti_itsKey.prefix(6)))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.stampTap()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(app_askManagerPageUrl)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.margin(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func component(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: userLevelName.map{fillPractice(destroy: $0)}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = MpRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (String(appExtraSpeedFormat.prefix(5)) + "getE" + String(kCreativeKey.prefix(7)) + String(app_thumbYouPath))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(appWrittenMessage) + String(showBridgeKey.suffix(7)))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func a(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(app_braceFormat.suffix(5)) + appExposureSaleStr.replacingOccurrences(of: "visitor", with: "a") + String(showStatusValue.suffix(6)))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: app_examineValue.reversed(), encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func betweenCharacter(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: userLevelName.map{fillPractice(destroy: $0)}, encoding: .utf8)!: resultEmailStr, String(bytes: kCarId.map{behindTender(bot: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            CigaretteThen.justMy(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func split(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(userCigaretteName.prefix(5)) + "logout")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func report(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(noti_nowPath) + String(notiTranslationMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                notiMovieText.set(result, forKey: k_tagMessage)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<EchoModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: SteppingMotorThen.share.loginUserMode = userModel
                    SteppingMotorThen.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func resumeCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(data_totalTitle.suffix(7)) + String(data_modestStr))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func method(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: dataCapacityName.reversed(), encoding: .utf8)!: reslutStr, String(bytes: dataGoldUrl.map{writerFormal(failure: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = MpRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (const_peerCounterestedStr.replacingOccurrences(of: "candid", with: "s") + String(appFactorMessage.prefix(4)) + "etMobi" + String(const_simplyGenderStr.prefix(4)) + "ndVCode")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func opposite(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: appProduceMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: dataCapacityName.reversed(), encoding: .utf8)!: reslutStr, String(bytes: dataGoldUrl.map{writerFormal(failure: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: app_examineValue.reversed(), encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = MpRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(data_midMessage.prefix(6)) + "obil" + String(showGreetName.prefix(5)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(mainTellName.prefix(7))))
        }
    }
}
