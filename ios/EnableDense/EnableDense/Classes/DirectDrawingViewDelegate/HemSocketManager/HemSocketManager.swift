
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let notiCuteUrl:[Character] = ["S","O","C","K","E","T","_"]
fileprivate let noti_originalUrl:String = "addr"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let showMayMessage:[UInt8] = [0x11,0xd,0x1,0x9,0x7,0x16,0x1d,0x14,0xb,0x6,0x7,0xd,0x1,0xa,0x3,0x16,0x1d,0x10,0x7,0x13,0x17,0x7,0x11,0x16,0x1,0x3,0xe,0xe,0x1d,0xc,0xd,0x16,0xb,0x4,0xb,0x1,0x3,0x16,0xb,0xd,0xc]

private func ownerCoverageAgo(quick num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let k_capData:[UInt8] = [0xf,0x18,0x1e,0x14,0xb,0x18,0x2,0x1b,0x11,0x12,0x1c,0x9,0xe,0x1e,0xf,0x18,0x18,0x13,0x2,0x13,0x12,0x9,0x14,0x1b,0x14,0x1e,0x1c,0x9,0x14,0x12,0x13]

private func openingTransaction(pow num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "isCalling" :*/
fileprivate let mainManagerKey:String = "isCalliyourself net range hung"
fileprivate let userProviderTitle:String = "NG"
 
/*: "SOCKET 连接成功." :*/
fileprivate let main_wedPath:String = "SOCKgeneralT"

/*: "SOCKET 连接失败, error: :*/
fileprivate let user_addStr:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}","\u{5931}","败",","," ","e","r","r","o"]
fileprivate let appMistId:String = "r:attitude hem here engineering before"

/*: "SOCKET 失败 :*/
fileprivate let dataNearValue:[Character] = ["S","O","C","K","E"]
fileprivate let mainReceiveBusyId:[Character] = ["T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let k_directId:String = "\u{6b21}, 切换过S"
fileprivate let dataDitName:String = "inner turn windowocket域"
fileprivate let constAreaName:String = "名\u{91cd}新尝"

/*: "SOCKET 已关闭, code: :*/
fileprivate let k_maximumFormat:[Character] = ["S","O","C","K","E","T"," ","\u{5df2}","关","闭",","]
fileprivate let show_birdFirmText:String = "constraint translation clear its assert code:"

/*: , reason: :*/
fileprivate let const_contentMsg:String = ", reart hour"
fileprivate let const_slipName:String = "living resource noason:"

/*: /?sid= :*/
fileprivate let notiSearchValue:String = "/?sid=category never country family"

/*: &uid= :*/
fileprivate let appTarPath:String = "entry lab&uid="

/*: &platform=iphone&version= :*/
fileprivate let app_delayEnhanceMsg:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e"]
fileprivate let constNeverthelessFormat:String = "rsion=loop key feed"

/*: &packageId= :*/
fileprivate let appPingFoundMessage:[Character] = ["&","p","a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let dataComeBracePath:[Character] = ["1","v","1","s","e"]
fileprivate let notiPoData:[Character] = ["r","v","e","r","."]

/*: .com:7766" :*/
fileprivate let app_headStr:String = "chance confirm.com"

/*: "43.129.185.194:7767" :*/
fileprivate let kHundredChanceFormat:String = "43.12"
fileprivate let showUponId:String = ".1us:"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let mainChemicalFormat:[UInt8] = [0x9a,0x86,0x8a,0x82,0x8c,0x9d,0xe9,0x2e,0x74,0x58,0x2e,0x72,0x55,0xbb,0xac,0xa8,0xaa,0xa1,0xa8,0xab,0xa0,0xa5,0xa0,0xbd,0xb0,0x8a,0xa1,0xa8,0xa7,0xae,0xac,0xad,0x2e,0x52,0x58,0x2c,0x59,0x65,0x26,0x75,0x45,0x2f,0x5f,0x64,0x2e,0x74,0x58,0x2c,0x4c,0x7a,0x20,0x5e,0x64,0xe7]

private func optionRequestControl(appeal num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "cmd" :*/
fileprivate let show_approximatelyName:String = "cmdrown"

/*: "ping" :*/
fileprivate let constProductName:String = "picostg"

/*: "callHeartbeat" :*/
fileprivate let app_realizeValue:String = "creader"
fileprivate let show_byName:[Character] = ["l","l","H","e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let k_oppositionMessage:[Character] = ["r","e","c","i","e","v","e","d","D","a"]
fileprivate let show_denseName:String = "told"

/*: "liveHeartBeat" :*/
fileprivate let constNeverId:[Character] = ["l","i","v","e","H","e","a","r","t","B"]
fileprivate let app_recentId:String = "EAT"

/*: "🔗 关闭 socket" :*/
fileprivate let const_rejectContent:[Character] = ["\u{0001f517}"," ","关","\u{95ed}"]
fileprivate let dataBushName:[Character] = [" ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let show_appearanceTitle:String = "left sparkThe u"
fileprivate let showSpectrumValue:String = "house securitylogs out"

/*: "SOCKET  :*/
fileprivate let kRestrictionMessage:String = "socket"
fileprivate let data_attributeStr:[Character] = [" "]

/*: 重连. 重试次数： :*/
fileprivate let show_smallValue:String = "重连. 重试次"
fileprivate let constLogValue:[Character] = ["数","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let dataAverageComplaintStr:String = "regularCK"
fileprivate let const_ageValue:String = "ET 连\u{63a5}"
fileprivate let showPreserveText:String = "需\u{8981}重新连接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let appHeConceptNoticeFormat:[Character] = ["S","O","C","K","E","T"," ","正","在","连","接"," ","不","需","要","重","新","连","接"]
fileprivate let data_replacementFormat:String = "."

/*: "&resume=1" :*/
fileprivate let kLargeInjuryFormat:String = "some lose art full&resume=1"

/*: "&lang= :*/
fileprivate let main_wipeValue:String = "club universal hurry&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let user_interestKey:String = "socke"
fileprivate let dataElectTitle:String = "连接, \u{5730}址："

/*: "onNetDisabled" :*/
fileprivate let show_screenFormat:String = "stroke scanonNet"
fileprivate let showLabelContent:[Character] = ["D","i","s","a","b","l","e","d"]

/*: "errno" :*/
fileprivate let noti_insideWorkName:String = "talkrrno"

/*: "data" :*/
fileprivate let notiFollowTitle:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let showBeyondName:[Character] = ["S","O","C","K","E","T"," ","连","接","保","活","c","m","d",":"," "]
fileprivate let kBeautyValue:String = "ping.enthusiasm element low"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let app_networkData:[UInt8] = [0xaf,0xb3,0xbf,0xb7,0xb9,0xa8,0xdc,0x88,0x8e,0x88,0x9f,0x18,0x43,0x61,0x1a,0x48,0x47,0x9f,0x91,0x98,0xc6,0xdc,0x9f,0x9d,0x90,0x90,0xb4,0x99,0x9d,0x8e,0x88,0x9e,0x99,0x9d,0x88,0xd2]

private func fastSolid(option num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let constFeedUnderFamilyData:[UInt8] = [0x8,0x4,0xf8,0x0,0xfa,0x9,0xd5,0x21,0x1e,0x2b,0x1a,0x99,0x74,0x52,0x9b,0x69,0x70,0x18,0x22,0x19,0xef,0xd5,0x21,0x1e,0x2b,0x1a,0xfd,0x1a,0x16,0x27,0x29,0xf7,0x1a,0x16,0x29,0xe3]

fileprivate func freshStat(formal num: UInt8) -> UInt8 {
    let value = Int(num) - 181
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let user_videoValue:String = "onPchart over title insurance language"

/*: "msg" :*/
fileprivate let kExhibitCanStr:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let mainListId:String = "🔗 处理"
fileprivate let notiSayRunningName:[Character] = ["\u{6d88}","息","\u{ff1a}","c","m","d","N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let user_outletStr:String = "catch mm begin attention cost, er"

/*: , msg: :*/
fileprivate let noti_visitorMessage:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let mainFileTitle:[Character] = [","]
fileprivate let mainConstitutionalHappyUrl:String = "length form recover hurry data:"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let noti_sameValue:[Character] = ["S","O","C","K","E"]
fileprivate let main_clingStr:[Character] = ["T"," ","接","收"]
fileprivate let userProblemFormat:[Character] = ["异","常","消","息","：","c","m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let data_soupMsg:String = "sharp springonReceiv"
fileprivate let appRecordingValue:String = "outer"
fileprivate let user_generateKey:[Character] = ["d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let appFeeData:String = "across beonVide"
fileprivate let data_progressText:[Character] = ["o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let dataRecommendPath:[Character] = ["o","n","R","e","q","u"]
fileprivate let app_focusHourMemberMessage:[Character] = ["e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let kGivingTitle:String = "onCanlarge lie mini"
fileprivate let user_surePath:String = "chemicalll"

/*: "onRefuseCall" :*/
fileprivate let kComputePath:[Character] = ["o","n","R","e","f"]
fileprivate let main_agreeCellStr:[Character] = ["u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let user_moonPath:String = "onAccvisual next bottom spring"

/*: "onStopCall" :*/
fileprivate let userPassageStr:[Character] = ["o","n"]
fileprivate let kDecideStr:String = "Stopdismiss surgery address result sire"

/*: "freeCallTimes" :*/
fileprivate let const_diskText:String = "freeCabar multiple blank pair processor"
fileprivate let noti_groundRegionUrl:String = "confirm"

/*: "mfCoin" :*/
fileprivate let main_midRefuseData:String = "mfCoingift able equipment possible"

/*: "onMissCoin" :*/
fileprivate let constMaxContent:[Character] = ["o","n","M","i","s","s","C","o","i","n"]

/*: "acceptCall" :*/
fileprivate let const_towardMessage:String = "accecap"

/*: "stopCall" :*/
fileprivate let k_hugeUrl:String = "register pass bass open farstopCa"
fileprivate let constMotivationMsg:[Character] = ["l","l"]

/*: "requestCall" :*/
fileprivate let mainObtainUrl:String = "reevaluatees"

/*: "[Invite you to a video call...]" :*/
fileprivate let appGeneralName:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let notiMentionMessage:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let kBlindName:[UInt8] = [0x7,0x15,0x32,0x2a,0x35,0x28,0x39,0x7c,0x25,0x33,0x29,0x7c,0x28,0x33,0x7c,0x3d,0x7c,0x2a,0x33,0x35,0x3f,0x39,0x7c,0x3f,0x3d,0x30,0x30,0x72,0x72,0x72,0x1]

/*: "uid" :*/
fileprivate let dataCenterModelMsg:String = "mmid"

/*: "nickname" :*/
fileprivate let data_valueText:String = "nicpreserveame"

/*: "headPic" :*/
fileprivate let user_privacyName:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let userSameUrl:[Character] = ["o","n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let constBeginStr:String = "cancricel"

/*: "refuseCall" :*/
fileprivate let show_actionData:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let userHiddenMessage:[Character] = ["a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let userCollectKey:String = "onCalagent transform coordinate"
fileprivate let data_requireKey:String = "ivatioff"

/*: "onLiveWarning" :*/
fileprivate let mainYieldUrl:String = "onLiidentity gravity click"
fileprivate let userPrettyStr:String = "nfilter"

/*: "reason" :*/
fileprivate let app_recommendThreeSurvivalName:String = "miceason"

/*: "time" :*/
fileprivate let noti_serviceCameraMessage:String = "timrealize"

/*: "onStopLive" :*/
fileprivate let notiModestTitle:[Character] = ["o","n","S","t","o","p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let data_fadeMsg:String = "happy inonSend"
fileprivate let main_windowStr:String = "thumb white temporaryProgr"
fileprivate let data_computerStr:String = "amGiftletter imagination have material enjoy"

/*: "onGameFs" :*/
fileprivate let notiColumnContent:String = "month food driveonGameFs"

/*: "broadStatus" :*/
fileprivate let data_shallId:String = "broaothers"
fileprivate let noti_holderMessage:String = "ukit"

/*: "onCallChatMsg" :*/
fileprivate let mainContainerAppWhileId:[Character] = ["o","n","C","a"]
fileprivate let show_rewardUrl:[Character] = ["l","l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let kFoodReplyReplaceText:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n","P","o","p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let k_rowFormat:[Character] = ["o","n","U","s","e","r","B","l","o"]
fileprivate let kFemaleIncludeStr:[Character] = ["c","k"]

/*: "isRecharge" :*/
fileprivate let user_identifyMiniMsg:[Character] = ["i","s","R","e","c","h","a"]
fileprivate let noti_eachMsg:String = "othersge"

/*: "source" :*/
fileprivate let const_limitPanelOutName:String = "storyurc"
fileprivate let app_thumbPath:[Character] = ["e"]

/*: "invitePop" :*/
fileprivate let k_thenName:String = "that"
fileprivate let user_taskText:String = "nvitePopplain strain stop develop"

/*: "videoPop" :*/
fileprivate let const_supportKey:String = "VID"
fileprivate let mainRunUrl:String = "rise in crib agreeeoPop"

/*: "coin" :*/
fileprivate let app_harassmentId:String = "successfullyoin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HemSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let data_domainFormat = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let dataVersionText = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let k_robotText = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let dataCancelKey = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let notiMagnitudeFormat = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let const_withoutFormat = (String(notiCuteUrl) + noti_originalUrl.uppercased() + "_KEY")

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let user_inputPath = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let show_hugeMatchStr = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let mainTotalervalValue = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let notiUnderStr = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let noti_closeKey = NSNotification.Name(rawValue: String(bytes: showMayMessage.map{ownerCoverageAgo(quick: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let show_micNextName = NSNotification.Name(rawValue: String(bytes: k_capData.map{openingTransaction(pow: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol OrganizerObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func judgeData(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol MilitaryMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func capture(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func capacityData(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol MpCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func buildData(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func err(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func betweenOrientation(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func tv(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol BeatPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func totalBubble(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func rebroadcast(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func socketOrData(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class HemSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum ComparisonType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currPageVC: AnyObject?
    var currPageVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: LieLanguageManager.shared.direction == .rightToLeft ? 0 : dataShowDetailPopMessage,
        //: y: StatusBarHeight+190
        y: dataEnvironmentId + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: LieLanguageManager.shared.direction == .rightToLeft ? 0 : dataShowDetailPopMessage,
        //: y: StatusBarHeight+190
        y: dataEnvironmentId + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: OrganizerObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: MilitaryMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: MpCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: BeatPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var isPage = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = HemSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachability(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(explain),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(mainManagerKey.prefix(7)) + userProviderTitle.lowercased()))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (VersePowKingfisherCompatible.shared as! VersePowKingfisherCompatible).takeDown()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            
            .observeWeakly(Bool.self, "isPage")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (VersePowKingfisherCompatible.shared as! VersePowKingfisherCompatible).takeDown()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension HemSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        ReckonLogTool.commentAdvertising(msg: (main_wedPath.replacingOccurrences(of: "general", with: "E") + " 连接成\u{529f}."))
        //: sendPing()
        directPing()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: notiFileText,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: const_withoutFormat)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        blackPlace()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        ReckonLogTool.commentAdvertising(msg: (String(user_addStr) + String(appMistId.prefix(2))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = SteppingMotorThen.share.reachability?.connection, status != .unavailable else {
        guard let status = SteppingMotorThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == dataCancelKey {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = ofDoinge(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            ReckonLogTool.commentAdvertising(msg: (String(dataNearValue) + String(mainReceiveBusyId)) + "\(dataCancelKey)" + (k_directId + String(dataDitName.suffix(6)) + constAreaName + "试\u{ff0c}地址：") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.vidDoing(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.ingredientUniform(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        ReckonLogTool.commentAdvertising(msg: (String(k_maximumFormat) + String(show_birdFirmText.suffix(6))) + "\(code)" + (String(const_contentMsg.prefix(4)) + String(const_slipName.suffix(5))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.topologicalSpace(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == notiUnderStr {
            //: return
            return
        }
        //: if let status = SteppingMotorThen.share.reachability?.connection, status != .unavailable {
        if let status = SteppingMotorThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.vidDoing(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func ofDoinge(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = SteppingMotorThen.share.appConfigMode.ws
        self.linkAddrArr = SteppingMotorThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = motionWrapFire()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: const_withoutFormat)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = SteppingMotorThen.share.loginUid
        let uid = SteppingMotorThen.share.loginUid
        //: let sid = SteppingMotorThen.share.loginSessionId
        let sid = SteppingMotorThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(notiSearchValue.prefix(6))) + "\(sid)" + (String(appTarPath.suffix(5))) + "\(uid)" + (String(app_delayEnhanceMsg) + String(constNeverthelessFormat.prefix(6))) + "\(constLikeName)" + (String(appPingFoundMessage)) + "\(main_netPath)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func motionWrapFire() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return data_serverTitle ? [(String(dataComeBracePath) + String(notiPoData)) + "\(notiNetworkTitle)" + (String(app_headStr.suffix(4)) + ":7766")] : [(kHundredChanceFormat.capitalized + "9.185" + showUponId.replacingOccurrences(of: "us", with: "94") + "7767")]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension HemSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func reachability(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            ReckonLogTool.commentAdvertising(msg: String(bytes: mainChemicalFormat.map{optionRequestControl(appeal: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.topologicalSpace(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.vidDoing(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func explain() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.vidDoing(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.remarkCountData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension HemSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func directPing() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: data_domainFormat, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.commentObject(info: [(show_approximatelyName.replacingOccurrences(of: "drown", with: "d")): (constProductName.replacingOccurrences(of: "cost", with: "n"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func splitButton() {
        //: endTalkingPing()
        strike()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: dataVersionText, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isPage || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currPageVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currPageVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isPage || (self.strokeController()?.isKind(of: MotorViewDelegate.self) == false && self.strokeController()?.isKind(of: StatusCallerDelegate.self) == false) && (self.currPageVC?.isKind(of: MotorViewDelegate.self) == false && self.currPageVC?.isKind(of: StatusCallerDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.commentObject(info: [(show_approximatelyName.replacingOccurrences(of: "drown", with: "d")): (app_realizeValue.replacingOccurrences(of: "reader", with: "a") + String(show_byName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func strike() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func remarkCountData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(k_oppositionMessage) + show_denseName.replacingOccurrences(of: "old", with: "e"))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < appMicTitle {
                //: SteppingMotorThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                SteppingMotorThen.share.requestDisappear(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension HemSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func mobilePing() {
        //: endLivePing()
        anPing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: k_robotText, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.commentObject(info: [(show_approximatelyName.replacingOccurrences(of: "drown", with: "d")): (String(constNeverId) + app_recentId.lowercased())])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func anPing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension HemSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func blackPlace() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = OrganizerContainerView()
        //: let keyWindow = DetermineJoinReactiveCompatible.getWindow()
        let keyWindow = DetermineJoinReactiveCompatible.existent()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isPage
        self.containerView?.isHidden = !self.isPage
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func translationLicense() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard MaterialLiveManager.threadContext().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension HemSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func topologicalSpace(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(const_rejectContent) + String(dataBushName)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: notiUnderStr, reason: (String(show_appearanceTitle.suffix(5)) + "ser " + String(showSpectrumValue.suffix(8))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        strike()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            translationLicense()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func outpost() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = HemSocketManager.shared.ofDoinge(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.vidDoing(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func vidDoing(timeInterval: TimeInterval, type: ComparisonType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.connectSocketBarb(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func connectSocketBarb(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                ReckonLogTool.commentAdvertising(msg: (dataAverageComplaintStr.replacingOccurrences(of: "regular", with: "SO") + const_ageValue + "正\u{5e38} 不" + showPreserveText))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                ReckonLogTool.commentAdvertising(msg: (String(appHeConceptNoticeFormat) + data_replacementFormat.capitalized))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isPage {
        if self.isPage {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(kLargeInjuryFormat.suffix(9))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(main_wipeValue.suffix(6))) + "\(MotorAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        topologicalSpace(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = notiMagnitudeFormat
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        ReckonLogTool.commentAdvertising(msg: (user_interestKey.uppercased() + "T 开始" + dataElectTitle) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func commentObject(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if SteppingMotorThen.share.reachability?.connection == nil || SteppingMotorThen.share.reachability?.connection == .unavailable {
        if SteppingMotorThen.share.reachability?.connection == nil || SteppingMotorThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(show_approximatelyName.replacingOccurrences(of: "drown", with: "d")): (String(show_screenFormat.suffix(5)) + String(showLabelContent)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (noti_insideWorkName.replacingOccurrences(of: "talk", with: "e")): "\(mainTotalervalValue)",
                                       //: "data": [:]]
                                       (String(notiFollowTitle)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            ingredientUniform(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(show_approximatelyName.replacingOccurrences(of: "drown", with: "d"))] as? String == (constProductName.replacingOccurrences(of: "cost", with: "n")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                ReckonLogTool.commentAdvertising(msg: (String(showBeyondName) + String(kBeautyValue.prefix(5))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(show_approximatelyName.replacingOccurrences(of: "drown", with: "d"))] as? String == (app_realizeValue.replacingOccurrences(of: "reader", with: "a") + String(show_byName)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                ReckonLogTool.commentAdvertising(msg: String(bytes: app_networkData.map{fastSolid(option: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(show_approximatelyName.replacingOccurrences(of: "drown", with: "d"))] as? String == (String(constNeverId) + app_recentId.lowercased()) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                ReckonLogTool.commentAdvertising(msg: String(bytes: constFeedUnderFamilyData.map{freshStat(formal: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(show_approximatelyName.replacingOccurrences(of: "drown", with: "d"))] as? String == (constProductName.replacingOccurrences(of: "cost", with: "n")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(show_approximatelyName.replacingOccurrences(of: "drown", with: "d")): (String(user_videoValue.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (noti_insideWorkName.replacingOccurrences(of: "talk", with: "e")): "\(user_inputPath)",
                                           //: "data": [:]]
                                           (String(notiFollowTitle)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                ingredientUniform(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.vidDoing(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func ingredientUniform(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(show_approximatelyName.replacingOccurrences(of: "drown", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(noti_insideWorkName.replacingOccurrences(of: "talk", with: "e"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(kExhibitCanStr))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(notiFollowTitle))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (mainListId.capitalized + String(notiSayRunningName)) + "\(cmdName)" + (String(user_outletStr.suffix(4)) + "rNo:") + "\(errorNo)" + (String(noti_visitorMessage)) + "\(msg)" + (String(mainFileTitle) + String(mainConstitutionalHappyUrl.suffix(6))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        waitTar(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            ReckonLogTool.commentAdvertising(msg: (String(noti_sameValue) + String(main_clingStr) + String(userProblemFormat)) + "\(cmdName)" + (String(user_outletStr.suffix(4)) + "rNo:") + "\(errorNo)" + (String(noti_visitorMessage)) + "\(msg)" + (String(mainFileTitle) + String(mainConstitutionalHappyUrl.suffix(6))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: SteppingMotorThen.share.loginUserMode.freeCallTimes = 0
                SteppingMotorThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !SteppingMotorThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !SteppingMotorThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                FluThen.shared.generateBy()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.judgeData(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(data_soupMsg.suffix(8)) + appRecordingValue.replacingOccurrences(of: "outer", with: "e") + String(user_generateKey)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kMaxMsg, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(appFeeData.suffix(6)) + String(data_progressText)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.capture(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(dataRecommendPath) + String(app_focusHourMemberMessage)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.capacityData(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(kGivingTitle.prefix(5)) + "celC" + user_surePath.replacingOccurrences(of: "chemical", with: "a")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(kComputePath) + String(main_agreeCellStr)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(user_moonPath.prefix(5)) + "eptCall") { // 接收方发送acceptCall回执
            //: self.isPage = true
            self.isPage = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            splitButton()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.rebroadcast(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(userPassageStr) + String(kDecideStr.prefix(4)) + "Call") { // 接收方发送stopCall回执
            //: self.isPage = false
            self.isPage = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            strike()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(const_diskText.prefix(6)) + "llTime" + noti_groundRegionUrl.replacingOccurrences(of: "confirm", with: "s"))] as? Int {
                //: SteppingMotorThen.share.loginUserMode.freeCallTimes = freeCallTimes
                SteppingMotorThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: SteppingMotorThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            SteppingMotorThen.share.loginUserMode.mf_coin = json[(String(notiFollowTitle))][(String(main_midRefuseData.prefix(6)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            officialCalculate(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: showSampleData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (VersePowKingfisherCompatible.shared as! VersePowKingfisherCompatible).diligentLicenseDate()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(constMaxContent)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.tv(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (const_towardMessage.replacingOccurrences(of: "cap", with: "p") + "tCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isPage = true
            self.isPage = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            splitButton()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.buildData(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(k_hugeUrl.suffix(6)) + String(constMotivationMsg)) { // 接收方/拨打方 挂断了通话
            //: self.isPage = false
            self.isPage = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            strike()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.err(data: data)
            // 更新余额
            //: SteppingMotorThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            SteppingMotorThen.share.loginUserMode.mf_coin = json[(String(notiFollowTitle))][(String(main_midRefuseData.prefix(6)))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(const_diskText.prefix(6)) + "llTime" + noti_groundRegionUrl.replacingOccurrences(of: "confirm", with: "s"))] as? Int {
                //: SteppingMotorThen.share.loginUserMode.freeCallTimes = freeCallTimes
                SteppingMotorThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            officialCalculate(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: showSampleData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (VersePowKingfisherCompatible.shared as! VersePowKingfisherCompatible).diligentLicenseDate()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (mainObtainUrl.replacingOccurrences(of: "evaluate", with: "qu") + "tCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: noti_closeKey, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: SteppingMotorThen.share.recieve1v1TalkRequest(info: data)
                SteppingMotorThen.share.requestDisappear(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(k_oppositionMessage) + show_denseName.replacingOccurrences(of: "old", with: "e"))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: appGeneralName.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(notiFollowTitle))][(String(notiMentionMessage))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: kBlindName.map{$0^92}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                KeepBaseDelegate.tap(uid: String(json[(String(notiFollowTitle))][(dataCenterModelMsg.replacingOccurrences(of: "mm", with: "u"))].intValue),
                                      //: title: data["nickname"] as? String,
                                      title: data[(data_valueText.replacingOccurrences(of: "preserve", with: "kn"))] as? String,
                                      //: body: body,
                                      body: body,
                                      //: imageUrl: data["headPic"] as? String)
                                      imageUrl: data[(String(user_privacyName))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                KeepBaseDelegate.shared.domain()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(userSameUrl)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isPage = true
            self.isPage = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            splitButton()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(userSameUrl))] = "1"
            //: SteppingMotorThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            SteppingMotorThen.share.componentInfo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (VersePowKingfisherCompatible.shared as! VersePowKingfisherCompatible).takeDown()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (constBeginStr.replacingOccurrences(of: "rice", with: "e") + "Call") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isPage = false
            self.isPage = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            strike()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.betweenOrientation(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                KeepBaseDelegate.bot(identifier: String(json[(String(notiFollowTitle))][(dataCenterModelMsg.replacingOccurrences(of: "mm", with: "u"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                KeepBaseDelegate.shared.parcelProduce()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(show_actionData) + String(userHiddenMessage)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isPage = false
            self.isPage = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            strike()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.totalBubble(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(userCollectKey.prefix(5)) + "lMot" + data_requireKey.replacingOccurrences(of: "off", with: "on")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if SteppingMotorThen.share.loginUserMode.sex == Gender.female.rawValue {
            if SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.socketOrData?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(mainYieldUrl.prefix(4)) + "veWarni" + userPrettyStr.replacingOccurrences(of: "filter", with: "g")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            MaterialLiveManager.calendarBy(content: msg, reason: json[(String(notiFollowTitle))][(app_recommendThreeSurvivalName.replacingOccurrences(of: "mic", with: "r"))].stringValue, time: json[(String(notiFollowTitle))][(noti_serviceCameraMessage.replacingOccurrences(of: "realize", with: "e"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(notiModestTitle)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            MaterialLiveManager.threadContext().fieldTerms()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(data_fadeMsg.suffix(6)) + String(main_windowStr.suffix(5)) + String(data_computerStr.prefix(6))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.pointRate(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(notiColumnContent.suffix(8))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(data_shallId.replacingOccurrences(of: "others", with: "d") + "Stat" + noti_holderMessage.replacingOccurrences(of: "kit", with: "s"))] as? Int ?? -1
            //: if SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: show_micNextName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kMaxMsg, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: show_micNextName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(mainContainerAppWhileId) + String(show_rewardUrl)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            ProcessingReactiveCompatible.titleSequence().post(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(kFoodReplyReplaceText)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            FluThen.shared.posit(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(k_rowFormat) + String(kFemaleIncludeStr)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            MaterialLiveManager.threadContext().fieldTerms()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: data_featureName, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension HemSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func waitTar(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (mainObtainUrl.replacingOccurrences(of: "evaluate", with: "qu") + "tCall") || cmdName == (String(dataRecommendPath) + String(app_focusHourMemberMessage)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if MaterialLiveManager.threadContext().isLive == true {
                //: NEED_RESTORE_LIVE = true
                appFormatTitle = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                MaterialLiveManager.returnArray(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                MaterialLiveManager.threadContext().fieldTerms()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard appFormatTitle == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (constBeginStr.replacingOccurrences(of: "rice", with: "e") + "Call") || cmdName == (String(kGivingTitle.prefix(5)) + "celC" + user_surePath.replacingOccurrences(of: "chemical", with: "a")) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(show_actionData) + String(userHiddenMessage)) || cmdName == (String(kComputePath) + String(main_agreeCellStr)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(k_hugeUrl.suffix(6)) + String(constMotivationMsg)) || cmdName == (String(userPassageStr) + String(kDecideStr.prefix(4)) + "Call")
        {
            //: NEED_RESTORE_LIVE = false
            appFormatTitle = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = FavoriteReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                MaterialLiveManager.threadContext().offHandler()
            }
            //: alert.show()
            alert.linguisticContext()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func officialCalculate(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        RequestRatingManager.shared.undercoverOperation()
        //: guard SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue else {
        guard SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(user_identifyMiniMsg) + noti_eachMsg.replacingOccurrences(of: "others", with: "r"))] as? Bool == false {
            //: LatPushManager.share.func_pushToDiscountRechargeView()
            LatPushManager.share.discountMinimum()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(const_limitPanelOutName.replacingOccurrences(of: "story", with: "so") + String(app_thumbPath))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (k_thenName.replacingOccurrences(of: "that", with: "i") + String(user_taskText.prefix(8))) || source == (const_supportKey.lowercased() + String(mainRunUrl.suffix(5))) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(app_harassmentId.replacingOccurrences(of: "successfully", with: "c"))] as? Int) ?? 0 < 60 {
            //: LatPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            LatPushManager.share.iii(webViewType: .RechargeHalfPage)
        }
    }
}
