
//: Declare String Begin

/*: "msgType" :*/
fileprivate let const_rescueStr:String = "msgTypeor force butt mystery sweet"

/*: "tips" :*/
fileprivate let mainFormOnerId:String = "tbodys"

/*: "extra" :*/
fileprivate let userFadeId:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let data_destinationValue:[Character] = ["m","s","g","I","n","f","o"]

/*: "user" :*/
fileprivate let userBridgeUrl:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let mainHalfMsg:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let noti_choiceMsg:[UInt8] = [0x98,0x9f,0x95,0x9d,0x98,0x97,0x9b,0x93]

private func marginEqually(loose num: UInt8) -> UInt8 {
    return num ^ 246
}

/*: "headPic" :*/
fileprivate let const_statusMsg:[UInt8] = [0x63,0x69,0x50,0x64,0x61,0x65,0x68]

/*: "age" :*/
fileprivate let dataTapPath:[UInt8] = [0x65,0x67,0x61]

/*: "gift" :*/
fileprivate let mainSucceedPrettyIgnoreKey:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let data_whichMerelyName:String = "isGroupsource past self greet total"

/*: "WantGift" :*/
fileprivate let user_representValue:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let notiMutualStr:String = "TXT"

/*: "img" :*/
fileprivate let app_lackMessage:String = "idelay"

/*: "video" :*/
fileprivate let data_roundingId:String = "signaturedeo"

/*: "audio" :*/
fileprivate let show_sumPath:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let appBorderMinuteId:String = "mestateag"
fileprivate let const_quitMessage:String = "lab"

/*: "contentType" :*/
fileprivate let appEaseValue:[Character] = ["c","o","n","t","e","n","t","T","y"]
fileprivate let notiRelationTingStr:[Character] = ["p","e"]

/*: "ImgMsg" :*/
fileprivate let user_aspectUrl:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let user_mirrorData:String = "m"
fileprivate let mainLoopMessage:[Character] = ["m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let main_cancelUrl:String = "typlay"

/*: "pic" :*/
fileprivate let app_aCorrectValue:String = "missic"

/*: "isBurn" :*/
fileprivate let k_discPath:String = "isBurnbring visible calendar ting transition"

/*: "giftId" :*/
fileprivate let kEnterValue:[Character] = ["g"]
fileprivate let constPersonTitle:String = "administrative quality creationiftId"

/*: "params" :*/
fileprivate let notiHalfFormat:String = "pionams"

/*: "audioLength" :*/
fileprivate let show_wrapHairTitle:[Character] = ["a","u","d","i","o","L","e","n","g","t","h"]

/*: "len" :*/
fileprivate let data_calendarStr:String = "scaleen"

/*: "audioSandbox" :*/
fileprivate let userDoingeStr:String = "audioSanothing gender large dark remind"
fileprivate let data_soonIdenticalMessage:[Character] = ["n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let user_compositionJoinValue:[Character] = ["s","e","n","d","e","r","I"]
fileprivate let mainLoseMsg:String = "permission"

/*: "AudioMsg" :*/
fileprivate let kHowTitle:String = "AudioMsgproperly peer bucket represent"

/*: "content" :*/
fileprivate let kAdvanceFormat:String = "contprioritynt"

/*: "TxtMsg" :*/
fileprivate let showPresentationKey:String = "TxtMsgword thumb ok see kit"

/*: "message" :*/
fileprivate let dataAbsId:String = "currencyssage"

/*: "quoteMsgId" :*/
fileprivate let const_processorName:String = "qexamplet"
fileprivate let notiTenderPath:String = "assistant"

/*: "quoteMsgInfo" :*/
fileprivate let constReStr:String = "quoteMexamine day"
fileprivate let dataSoonRid:[Character] = ["s"]
fileprivate let dataDefinitionTunName:[Character] = ["g","I","n","f","o"]

/*: "rYMsgType" :*/
fileprivate let userVersionMessage:[Character] = ["r","Y","M","s","g"]
fileprivate let appUnitPath:String = "Typeone style action bottom"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let kThresholdKey:String = "publication attention place finishGJ:S"
fileprivate let show_messageValue:String = "round nobodyoGre"
fileprivate let main_personalKey:String = "tCustomway spectrum"

/*: "changeDirection" :*/
fileprivate let notiHerId:String = "occur connection pure feedchangeD"
fileprivate let main_badStr:String = "whenection"

/*: "system" :*/
fileprivate let const_mirrorStatUrl:[Character] = ["s","y","s","t","e"]
fileprivate let userFirePath:String = "waste"

/*: "title" :*/
fileprivate let kRunStr:String = "lateilatele"

/*: "icon" :*/
fileprivate let dataMultipleUrl:String = "rescueon"

/*: "all" :*/
fileprivate let constActionLipUrl:String = "drawl"

/*: "icon_chats_pcr" :*/
fileprivate let data_automaticallyText:String = "icon_able grant privacy rounding"
fileprivate let user_optionMessage:String = "s_pcdifference try root"
fileprivate let data_mixMessage:[Character] = ["r"]

/*: "status" :*/
fileprivate let appSilverMessage:String = "statdit"
fileprivate let constFingerName:String = "S"

/*: "msgIncome" :*/
fileprivate let notiExposureStr:String = "branch"
fileprivate let userPolicyFormat:String = "sgIncomedrag rise"

/*: "msgId" :*/
fileprivate let app_exclusiveMsg:String = "msgIdextension i on over street"

/*: "NaturalResourceRelatableTarget" :*/
fileprivate let mainExistStr:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","S"]
fileprivate let constPostSecondStr:String = "processaprocessus"

/*: "TMsgTranslatedContent" :*/
fileprivate let kMoonKey:String = "stretch approval through there rewardTMsgTr"
fileprivate let mainSessionEarnId:String = "edConresistance cast prompt"

/*: "formatType" :*/
fileprivate let dataConceptUnitName:[Character] = ["f","o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let appCountId:String = "plairem"

/*: "imageData" :*/
fileprivate let notiMapKey:String = "imballge"
fileprivate let appHoneyId:String = "Datareceive behind template"

/*: "VideoMsg" :*/
fileprivate let app_storyId:String = "VideoMmin enthusiasm"
fileprivate let kWritingData:String = "sdefine"

/*: "match" :*/
fileprivate let data_indexFormat:String = "dropatch"

/*: "userIntro" :*/
fileprivate let noti_adKey:[Character] = ["u","s","e"]
fileprivate let constWeeklyUrl:String = "rIntrocover uniform"

/*: "targetId" :*/
fileprivate let data_everyoneId:[Character] = ["t","a"]
fileprivate let showSpendKey:String = "examine walk soundrgetId"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let constSheUrl:[Character] = ["y","y","y","y","-","M","M","-","d","d"," ","H","H",":","m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let app_zzKey:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let showSourceId:String = "isReadbean chart"

/*: "audioData" :*/
fileprivate let main_birthdayMsg:String = "ball leading skip thirdaudioDat"
fileprivate let show_soupValue:String = "ping"

/*: "audioUri" :*/
fileprivate let user_pillowUrl:String = "AU"
fileprivate let constInstructionValue:String = "dioUriscience carry"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CookieChatManager.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum InscriptionGenerationEquatable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - CookieChatManager

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class CookieChatManager: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func buildTime() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> PhoneMessageThen? {
    public class func charm(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> PhoneMessageThen? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = keyWeight(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: FoodV2Listener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            FoodV2Listener.shared.spendConversion(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> PhoneMessageThen? {
    public class func keyWeight(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> PhoneMessageThen? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(const_rescueStr.prefix(7)))] = (mainFormOnerId.replacingOccurrences(of: "body", with: "ip"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(userFadeId)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = destinationDataMessageTotaleraction(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CookieCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = TareModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? GuidanceModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(data_destinationValue))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(userBridgeUrl))] = [
            //: "uid": SteppingMotorThen.share.loginUserMode.userID,
            (String(mainHalfMsg)): SteppingMotorThen.share.loginUserMode.userID,
            //: "nickname": SteppingMotorThen.share.loginUserMode.nickname,
            String(bytes: noti_choiceMsg.map{marginEqually(loose: $0)}, encoding: .utf8)!: SteppingMotorThen.share.loginUserMode.nickname,
            //: "headPic": SteppingMotorThen.share.loginUserMode.headPic,
            String(bytes: const_statusMsg.reversed(), encoding: .utf8)!: SteppingMotorThen.share.loginUserMode.headPic,
            //: "age": SteppingMotorThen.share.loginUserMode.age,
            String(bytes: dataTapPath.reversed(), encoding: .utf8)!: SteppingMotorThen.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(userFadeId)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = destinationDataMessageTotaleraction(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(const_rescueStr.prefix(7)))] as? String == (String(mainSucceedPrettyIgnoreKey)) {
            //: var data: PhoneMessageThen
            var data: PhoneMessageThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(data_whichMerelyName.prefix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = SlumThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = AutomatonReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = AutomatonReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(const_rescueStr.prefix(7)))] as? String == (String(user_representValue)) {
            //: let data = AlsoConferReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AlsoConferReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(const_rescueStr.prefix(7)))] as? String == (notiMutualStr.lowercased()) {
            //: var data: PhoneMessageThen
            var data: PhoneMessageThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(data_whichMerelyName.prefix(7)))] as? String == "1" { // 群聊
                //: data = GroupThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GroupThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(const_rescueStr.prefix(7)))] as? String == (app_lackMessage.replacingOccurrences(of: "delay", with: "mg")) {
            //: let data = TakeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TakeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(const_rescueStr.prefix(7)))] as? String == (data_roundingId.replacingOccurrences(of: "signature", with: "vi")) {
            //: let data = HashOutThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = HashOutThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(const_rescueStr.prefix(7)))] as? String == (String(show_sumPath)) {
            //: let data = BeatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BeatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(mainSucceedPrettyIgnoreKey))] != nil && (msgInfo[(appBorderMinuteId.replacingOccurrences(of: "state", with: "ss") + "eTyp" + const_quitMessage.replacingOccurrences(of: "lab", with: "e"))] as? Int) == 4 {
            //: let data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TareModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: PhoneMessageThen) -> PhoneMessageThen? {
    public class func purchase(withData cellData: PhoneMessageThen) -> PhoneMessageThen? {
        // 图片
        //: if let reCellData = cellData as? TakeCellData {
        if let reCellData = cellData as? TakeCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(DetermineJoinReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(DetermineJoinReactiveCompatible.charmGet())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(appEaseValue) + String(notiRelationTingStr))] = (String(user_aspectUrl))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(const_rescueStr.prefix(7)))] = (app_lackMessage.replacingOccurrences(of: "delay", with: "mg"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(user_mirrorData.replacingOccurrences(of: "m", with: "i") + String(mainLoopMessage))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(data_destinationValue))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(main_cancelUrl.replacingOccurrences(of: "play", with: "pe"))] = (String(user_aspectUrl))
            //: params["pic"] = filePath
            params[(app_aCorrectValue.replacingOccurrences(of: "miss", with: "p"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(k_discPath.prefix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(k_discPath.prefix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(kEnterValue) + String(constPersonTitle.suffix(5)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(kEnterValue) + String(constPersonTitle.suffix(5)))] = "0"
            }

            //: dic["params"] = params
            dic[(notiHalfFormat.replacingOccurrences(of: "ion", with: "ar"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = keyWeight(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? HashOutThen {
        if let reCellData = cellData as? HashOutThen {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? BeatCellData {
        if let reCellData = cellData as? BeatCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = CookieInfoManager.audio(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(show_wrapHairTitle))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(data_calendarStr.replacingOccurrences(of: "scale", with: "l"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(userDoingeStr.prefix(7)) + String(data_soonIdenticalMessage))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(userDoingeStr.prefix(7)) + String(data_soonIdenticalMessage))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(const_rescueStr.prefix(7)))] = (String(show_sumPath))
            //: msgInfo["senderId"] = SteppingMotorThen.share.loginUserMode.userID
            msgInfo[(String(user_compositionJoinValue) + mainLoseMsg.replacingOccurrences(of: "permission", with: "d"))] = SteppingMotorThen.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(data_destinationValue))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(main_cancelUrl.replacingOccurrences(of: "play", with: "pe"))] = (String(kHowTitle.prefix(8)))
            //: dic["params"] = params
            dic[(notiHalfFormat.replacingOccurrences(of: "ion", with: "ar"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = keyWeight(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ChatCellData {
        if let reCellData = cellData as? ChatCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(kAdvanceFormat.replacingOccurrences(of: "priority", with: "e"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(const_rescueStr.prefix(7)))] = (notiMutualStr.lowercased())
            //: dic["msgInfo"] = msgInfo
            dic[(String(data_destinationValue))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(main_cancelUrl.replacingOccurrences(of: "play", with: "pe"))] = (String(showPresentationKey.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(dataAbsId.replacingOccurrences(of: "currency", with: "me"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.present() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(const_processorName.replacingOccurrences(of: "example", with: "uo") + "eMsgI" + notiTenderPath.replacingOccurrences(of: "assistant", with: "d"))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(constReStr.prefix(6)) + String(dataSoonRid) + String(dataDefinitionTunName))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(notiHalfFormat.replacingOccurrences(of: "ion", with: "ar"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = keyWeight(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> PhoneMessageThen? {
    public class func effect(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> PhoneMessageThen? {
        //: var cellData: PhoneMessageThen?
        var cellData: PhoneMessageThen?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(userFadeId))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(data_destinationValue))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(userBridgeUrl))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? TareModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == SteppingMotorThen.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == SteppingMotorThen.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == SteppingMotorThen.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == SteppingMotorThen.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(userVersionMessage) + String(appUnitPath.prefix(4)))] as? String == (String(kThresholdKey.suffix(4)) + "ess:Aut" + String(show_messageValue.suffix(4)) + "et:Tx" + String(main_personalKey.prefix(7))) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(notiHerId.suffix(7)) + main_badStr.replacingOccurrences(of: "when", with: "ir"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = engineering(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(const_rescueStr.prefix(7)))] as? String == (String(const_mirrorStatUrl) + userFirePath.replacingOccurrences(of: "waste", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = TareModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? GuidanceModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CookieCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(kAdvanceFormat.replacingOccurrences(of: "priority", with: "e"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = TakeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TakeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = HashOutThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = HashOutThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SlumThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = AutomatonReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = AutomatonReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = remind(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(userFadeId))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(data_destinationValue))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? DetailedModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = AlsoConferReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AlsoConferReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = BeatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BeatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? BeatCellData {
            if let audioCellData = cellData as? BeatCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = tickModel(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = FairThen.contribution(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = FairThen.big(vapourous(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(kRunStr.replacingOccurrences(of: "late", with: "t"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(kAdvanceFormat.replacingOccurrences(of: "priority", with: "e"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(app_lackMessage.replacingOccurrences(of: "delay", with: "mg"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = LatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = GroupThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GroupThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? Model(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(dataMultipleUrl.replacingOccurrences(of: "rescue", with: "ic"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: SteppingMotorThen.share.loginUserMode.headPic ?? ""
                        SteppingMotorThen.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = DetailedModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(kAdvanceFormat.replacingOccurrences(of: "priority", with: "e"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(userFadeId))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = ChallengeModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (constActionLipUrl.replacingOccurrences(of: "draw", with: "al"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(data_automaticallyText.prefix(5)) + "chat" + String(user_optionMessage.prefix(4)) + String(data_mixMessage))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.editSmart(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(appSilverMessage.replacingOccurrences(of: "dit", with: "u") + constFingerName.lowercased())] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(notiExposureStr.replacingOccurrences(of: "branch", with: "m") + String(userPolicyFormat.prefix(8)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(appBorderMinuteId.replacingOccurrences(of: "state", with: "ss") + "eTyp" + const_quitMessage.replacingOccurrences(of: "lab", with: "e"))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(app_exclusiveMsg.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(mainExistStr) + constPostSecondStr.replacingOccurrences(of: "process", with: "t"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = NaturalResourceRelatableTarget(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(kMoonKey.suffix(6)) + "anslat" + String(mainSessionEarnId.prefix(5)) + "tent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = StumbleModel.narrow()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = StumbleModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = SlideHypothesisModel.openDoingDict()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = SlideHypothesisModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = EnthusiasmPillowModel.row()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func engineering(_ extraDic: [String: Any]) -> InscriptionGenerationEquatable {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(userFadeId))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(data_destinationValue))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? TareModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(dataConceptUnitName))] as? String == (appCountId.replacingOccurrences(of: "rem", with: "n")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(user_mirrorData.replacingOccurrences(of: "m", with: "i") + String(mainLoopMessage))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(notiMapKey.replacingOccurrences(of: "ball", with: "a") + String(appHoneyId.prefix(4)))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(app_storyId.prefix(6)) + kWritingData.replacingOccurrences(of: "define", with: "g")) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = ageLay(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(data_indexFormat.replacingOccurrences(of: "drop", with: "m"))] as? Int) == 1 || (msgInfo?[(data_indexFormat.replacingOccurrences(of: "drop", with: "m"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(const_rescueStr.prefix(7)))] as? String == (mainFormOnerId.replacingOccurrences(of: "body", with: "ip")) || (dic?[(appBorderMinuteId.replacingOccurrences(of: "state", with: "ss") + "eTyp" + const_quitMessage.replacingOccurrences(of: "lab", with: "e"))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(dataConceptUnitName))] as? String == "mf") && ((dic?[(appBorderMinuteId.replacingOccurrences(of: "state", with: "ss") + "eTyp" + const_quitMessage.replacingOccurrences(of: "lab", with: "e"))] as? Int) == 15 || (dic?[(appBorderMinuteId.replacingOccurrences(of: "state", with: "ss") + "eTyp" + const_quitMessage.replacingOccurrences(of: "lab", with: "e"))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(const_rescueStr.prefix(7)))] as? String == (String(noti_adKey) + String(constWeeklyUrl.prefix(6))) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(const_rescueStr.prefix(7)))] as? String == (String(user_representValue)) || (msgInfo?[(appBorderMinuteId.replacingOccurrences(of: "state", with: "ss") + "eTyp" + const_quitMessage.replacingOccurrences(of: "lab", with: "e"))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func vapourous(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.editSmart(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(userFadeId))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(data_destinationValue))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(userBridgeUrl))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(user_compositionJoinValue) + mainLoseMsg.replacingOccurrences(of: "permission", with: "d"))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == SteppingMotorThen.share.loginUserMode.userID {
        if senderId == SteppingMotorThen.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(user_compositionJoinValue) + mainLoseMsg.replacingOccurrences(of: "permission", with: "d"))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(data_everyoneId) + String(showSpendKey.suffix(6)))] = message.userID ?? (user?[(String(data_everyoneId) + String(showSpendKey.suffix(6)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(user_compositionJoinValue) + mainLoseMsg.replacingOccurrences(of: "permission", with: "d"))] = "\(msgInfo?[(String(mainHalfMsg))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(data_everyoneId) + String(showSpendKey.suffix(6)))] = "\(msgInfo?[(String(mainHalfMsg))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(app_exclusiveMsg.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(app_exclusiveMsg.prefix(5)))] = "\(msgInfo?[(String(app_exclusiveMsg.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.editSmart(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(app_exclusiveMsg.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(app_exclusiveMsg.prefix(5)))] = "\(customDic[(String(app_exclusiveMsg.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(app_exclusiveMsg.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(userDoingeStr.prefix(7)) + String(data_soonIdenticalMessage))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(userDoingeStr.prefix(7)) + String(data_soonIdenticalMessage))] = "\(msgInfo?[(String(userDoingeStr.prefix(7)) + String(data_soonIdenticalMessage))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(show_wrapHairTitle))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(show_wrapHairTitle))] = "\(msgInfo?[(String(show_wrapHairTitle))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(constSheUrl))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(app_zzKey))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == SteppingMotorThen.share.loginUserMode.userID ? "1" : "0"
        info?[(String(showSourceId.prefix(6)))] = senderId == SteppingMotorThen.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func tickModel(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = vapourous(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(app_exclusiveMsg.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func remind(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func destinationDataMessageTotaleraction(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func ageLay(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(const_rescueStr.prefix(7)))] as? String, msgType == (String(show_sumPath)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(appEaseValue) + String(notiRelationTingStr))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(kHowTitle.prefix(8)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(main_birthdayMsg.suffix(8)) + show_soupValue.replacingOccurrences(of: "ping", with: "a"))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(user_pillowUrl.lowercased() + String(constInstructionValue.prefix(6)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
