
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let user_performFormat:String = "IM退出team level pad care foundation"
fileprivate let showPackageData:String = "attitude listene:"

/*: , desc: :*/
fileprivate let dataFiftyAdmissionId:String = ", desc:protocol seek television computer"

/*: "IM登录失败，code: :*/
fileprivate let notiSequenceUrl:String = "enable"
fileprivate let noti_titleMultiName:String = "M\u{767b}录失败，"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let data_editName:[Character] = ["I","M","\u{83b7}","取","未","读","消","息","总","\u{6570}","失","败","，","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let showWhetherMsg:[Character] = ["c","r","u"]
fileprivate let k_whoFormat:[Character] = ["s","h","B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let main_partyId:[UInt8] = [0x29,0x28,0x2b,0x15,0x3c,0x34,0x29,0x2c,0x39]

fileprivate func resPriceCar(life num: UInt8) -> UInt8 {
    let value = Int(num) - 199
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let noti_aspectData:String = "foreheadaza"

/*: "roomNum" :*/
fileprivate let showAnimalFormat:String = "service"
fileprivate let main_likeMsg:[Character] = ["o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let appBulletData:String = "datam"

/*: "onlyOneRoomId" :*/
fileprivate let main_dialValue:[Character] = ["o","n","l","y","O","n","e","R"]
fileprivate let userWhichText:String = "water over mentallyoomId"

/*: "LIVE-CHAT" :*/
fileprivate let noti_memoryName:String = "computer cap popLIVE-"
fileprivate let dataRefreshlyTitle:[Character] = ["C","H","A","T"]

/*: "extra" :*/
fileprivate let user_viaId:String = "extrenjoy"

/*: "msgType" :*/
fileprivate let notiScoreName:[Character] = ["m","s","g","T","y","p"]
fileprivate let appFlatTitle:[Character] = ["e"]

/*: "newPkgGift" :*/
fileprivate let dataAnalysisYellowTitle:String = "newPkgGifbot chin from capture"
fileprivate let mainHandleMessage:[Character] = ["t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let show_passageKey:String = "ingredient round clearly bird approvalMF:Chat"
fileprivate let appCustomerFormat:String = "state absoluteMent"

/*: "uid" :*/
fileprivate let kZoneStr:[UInt8] = [0xba,0xae,0xa9]

fileprivate func blankIcon(simultaneously num: UInt8) -> UInt8 {
    let value = Int(num) - 69
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let kThemeCameraForeMsg:[UInt8] = [0x3e,0x3f,0x3d,0x38,0x3f,0x34,0x2,0x25,0x30,0x25,0x24,0x22]

private func coreSClear(psychological num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "isNewUser" :*/
fileprivate let data_protectionId:[UInt8] = [0x7,0x1d,0x20,0xb,0x19,0x3b,0x1d,0xb,0x1c]

private func greatestDrag(recording num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "c2c_ :*/
fileprivate let showBringPath:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let kGameMsg:[Character] = ["I","M","获","取","会","话","失","败","，","c","o","n","v","I","d"]
fileprivate let main_launchPath:String = ":"

/*: , code: :*/
fileprivate let noti_weeStr:String = ", codnumber distance"
fileprivate let constSparkId:String = "should woman year jump holde:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let mainComplyUsedMessage:[Character] = ["I","M","插","入","消","息","失","败","\u{ff0c}","t","a","r"]
fileprivate let const_dignityMsg:String = "getId:bean tail rice body assert"

/*: "changeDirection" :*/
fileprivate let user_devoteRiverKey:[Character] = ["c","h","a","n","g","e","D","i"]
fileprivate let notiUpgradelyUrl:String = "protectionection"

/*: "rYMsgType" :*/
fileprivate let dataTwoMessage:String = "failure board episoderYMs"

/*: "GJ:CallCustom" :*/
fileprivate let const_micFormat:[Character] = ["G","J",":","C","a","l","l"]
fileprivate let notiVidStr:String = "she girlCustom"

/*: "IM获取会话列表失败，code: :*/
fileprivate let userPlaceContent:[Character] = ["I","M","获","\u{53d6}","会","话","\u{5217}","表","失","败","，","c","o","d","e"]
fileprivate let app_browKey:[Character] = [":"]

/*: "msgInfo" :*/
fileprivate let show_survivalContent:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let user_yesterdayName:String = "quote"
fileprivate let dataBeneathKey:String = "edFoldbegin disabled asset inner"

/*: "group_ :*/
fileprivate let appDocName:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let noti_combinedHostMessage:[Character] = ["I","M","删","除","会","话","失"]
fileprivate let kWealthyValue:String = "败，cocamera always protection theme"

/*: "IM置顶会话失败，convId: :*/
fileprivate let k_albumMidValue:String = "IM置顶会\u{8bdd}\u{5931}\u{8d25}"
fileprivate let notiDiscMessage:[Character] = ["，","c","o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let data_selectStr:[Character] = ["S","D","K"," ","正"]
fileprivate let notiMerelyFormat:String = "在连接到服务\u{5668}"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let constHangValue:String = "clear attractiveSDK 已经"

/*: "IM连接服务器失败，code: :*/
fileprivate let data_weeklyMemoryStr:String = "IM连接\u{670d}务"
fileprivate let const_presentationMessage:String = "combined bot medal clothing rescode:"

/*: , error: :*/
fileprivate let show_goalMsg:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let constExSeekMsg:String = "location handleIM账号被"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let user_interactionUrl:[UInt8] = []

private func compositionClick(called num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "Offline Notification" :*/
fileprivate let noti_matchTelevisionStr:String = "network crush preparationOffli"
fileprivate let main_proposalFormat:String = "TIFIC"

/*: "Cancel" :*/
fileprivate let mainRenderValue:String = "Cancelhome timing star"

/*: "Re Login" :*/
fileprivate let kCaptureAdvancedFormat:[Character] = ["R","e"," "]
fileprivate let kMyPath:String = "concept record bright enough buttLogin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FoodV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol PlusManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func lawman(data: [SpendConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func signalInvitation(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func creatingByRemoval(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum FormDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class FoodV2Listener: NSObject {
@objcMembers public class FoodV2Listener: NSObject {
    //: public static let shared = FoodV2Listener()
    public static let shared = FoodV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [SpendConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [SpendConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [SpendConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [SpendConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [SpendConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: SpendConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.response()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func candid() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kGiftId, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func response() {
        //: func__removeAllDelegate()
        funcAreaDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.capacity()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func capacity() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = SpendConversationModel.income(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = SpendConversationModel.income(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func activity() -> [SpendConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if SteppingMotorThen.share.appStatus == AppSkinStatus.special.rawValue {
        if SteppingMotorThen.share.appStatus == EchoTermConvertible.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == DetermineJoinReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == DetermineJoinReactiveCompatible.facilityRank() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension FoodV2Listener {
extension FoodV2Listener {
    //: func func__removeAllDelegate() {
    func funcAreaDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func closeerPickDisplay(_ delegate: PlusManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func device(_ delegate: PlusManagerDelegate) {
        //: self.func__invokeDelegate {
        self.onceRestore {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func onceRestore(_ invocation: (PlusManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! PlusManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension FoodV2Listener {
extension FoodV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func instrumentalityDialog() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            ReckonLogTool.commentAdvertising(msg: (String(user_performFormat.prefix(4)) + "登\u{5f55}失败，cod" + String(showPackageData.suffix(2))) + "\(code)" + (String(dataFiftyAdmissionId.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.response()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func midday(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = SteppingMotorThen.share.appUserConfigMode.uSig
        let uSigStr = SteppingMotorThen.share.appUserConfigMode.uSig
        //: let uidStr = SteppingMotorThen.share.loginUserMode.userID
        let uidStr = SteppingMotorThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.hiddenMagnitude()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            ReckonLogTool.commentAdvertising(msg: (notiSequenceUrl.replacingOccurrences(of: "enable", with: "I") + noti_titleMultiName + "code:") + "\(code)" + (String(dataFiftyAdmissionId.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func speakOfTheDevil() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || SteppingMotorThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || SteppingMotorThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.revenueMsg(showMsg: main_backgroundPath)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension FoodV2Listener {
extension FoodV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func hiddenMagnitude() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            ReckonLogTool.commentAdvertising(msg: (String(data_editName)) + "\(code)" + (String(dataFiftyAdmissionId.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func conMsg() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        TimeTestedRequestTool.doFront { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(showWhetherMsg) + String(k_whoFormat))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: main_contentUrl, object: self, userInfo: [String(bytes: main_partyId.map{resPriceCar(life: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func panReceive(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        TimeTestedRequestTool.serviceCompletion { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(noti_aspectData.replacingOccurrences(of: "forehead", with: "pl"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(showAnimalFormat.replacingOccurrences(of: "service", with: "r") + String(main_likeMsg))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = SpendConversationModel.income(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(appBulletData.replacingOccurrences(of: "data", with: "nu"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(main_dialValue) + String(userWhichText.suffix(5)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension FoodV2Listener: V2TIMAdvancedMsgListener {
extension FoodV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(noti_memoryName.suffix(5)) + String(dataRefreshlyTitle)))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            EnhanceReactiveCompatible.executeShared().constraintMsg(msg: msg)
            //: return
            return
        }
        //: if msg.sender == SteppingMotorThen.share.loginUserMode.userID { return }
        if msg.sender == SteppingMotorThen.share.loginUserMode.userID { return }

        //: let dic = BotDialogChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = BotDialogChatManager.clever(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(user_viaId.replacingOccurrences(of: "enjoy", with: "a"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? TareModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: dataCloseValue, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(notiScoreName) + String(appFlatTitle))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(dataAnalysisYellowTitle.prefix(9)) + String(mainHandleMessage)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: dataWarningKey, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if BotDialogChatManager.checkVoiceMessage(message: msg) {
            if BotDialogChatManager.containment(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = CookieChatManager.vapourous(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                FairThen.big(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.resolve(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SwineInfluenzaReactiveCompatible.shared.bigDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SwineInfluenzaReactiveCompatible.shared.ta(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(show_passageKey.suffix(7)) + String(appCustomerFormat.suffix(4)) + "ionMsg") { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            SatelliteChatManager.share.textdrop(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: GenChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: GenChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != DetermineJoinReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != DetermineJoinReactiveCompatible.anthologist() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.onceRestore { $0.creatingByRemoval?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            PacketReactiveCompatible.shared.faraway(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: kZoneStr.map{blankIcon(simultaneously: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: kThemeCameraForeMsg.map{coreSClear(psychological: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: data_protectionId.map{greatestDrag(recording: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: dataRobotMeetingText,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            CookieInfoManager.sayTime(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            GenChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kTextStatusName, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: PhoneMessageThen, isPrivete: Bool) {
    @objc public func spendConversion(targetId: String, msgCellData: PhoneMessageThen, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.instill(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(showBringPath)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.secure(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.joinInput(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func instill(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = SteppingMotorThen.share.loginUserMode.userID
        let senderStr = SteppingMotorThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func resolve(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(user_devoteRiverKey) + notiUpgradelyUrl.replacingOccurrences(of: "protection", with: "r"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && LatPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(dataTwoMessage.suffix(4)) + "gType")].stringValue == (String(const_micFormat) + String(notiVidStr.suffix(6))) && isSelf && LatPushManager.share.topical()!.isKind(of: ControlRecognizerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: userRouteName) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: userRouteName)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension FoodV2Listener: V2TIMConversationListener {
extension FoodV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.secure(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.joinInput(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.secure(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.joinInput(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: SteppingMotorThen.share.unreadMessageNum = Int(totalUnreadCount)
        SteppingMotorThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.onceRestore {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.signalInvitation?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func joinInput(conVersationList: [SpendConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if CookieInfoManager.compassPoint().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                CookieInfoManager.compassPoint().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.onceRestore {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.lawman?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func mini(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [SpendConversationModel] = []

        // V2TIMConversation->SpendConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = SpendConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.take(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [SpendConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = FairHangInfoTable.putBold(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.terms(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension FoodV2Listener {
extension FoodV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func space(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.mini(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.joinInput(conVersationList: arr as? [SpendConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            ReckonLogTool.commentAdvertising(msg: (String(userPlaceContent) + String(app_browKey)) + "\(code)" + (String(dataFiftyAdmissionId.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func crossfireComputerSimulationReferenceSpreadPullInManager(targetId: String) -> SpendConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func secure(convList: [V2TIMConversation]) -> [SpendConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [SpendConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = SpendConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.take(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.terms(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func terms(convModel: SpendConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.capacity()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: FormDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - SteppingMotorThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - SteppingMotorThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = CookieInfoManager.dealMp(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = BotDialogChatManager.parseTXMessageData(data: elemData)
            let dict = BotDialogChatManager.clever(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(user_viaId.replacingOccurrences(of: "enjoy", with: "a"))][(String(show_survivalContent))][(user_yesterdayName.replacingOccurrences(of: "quote", with: "ne") + String(dataBeneathKey.prefix(6)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.stopType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.stopType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func stopType(convList: [SpendConversationModel], dataType: FormDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func person(targetId: String, dataType: FormDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(showBringPath)) + "\(targetId)" : (String(appDocName)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            CookieInfoManager.hedge(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(SteppingMotorThen.share.loginUserMode.userID, toUid: targetId)
            FairThen.animaGame(SteppingMotorThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.hiddenMagnitude()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func fieldFemale(convModel: SpendConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            ReckonLogTool.commentAdvertising(msg: (k_albumMidValue + String(notiDiscMessage)) + "\(convModel.conversationID)" + (String(noti_weeStr.prefix(5)) + String(constSparkId.suffix(2))) + "\(code)" + (String(dataFiftyAdmissionId.prefix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.stopType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.stopType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension FoodV2Listener: V2TIMSDKListener {
extension FoodV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(data_selectStr) + notiMerelyFormat))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(constHangValue.suffix(6)) + "成功\u{8fde}接\u{5230}服务器"))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        ReckonLogTool.commentAdvertising(msg: (data_weeklyMemoryStr + "器\u{5931}败，" + String(const_presentationMessage.suffix(5))) + "\(code)" + (String(show_goalMsg)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        ReckonLogTool.commentAdvertising(msg: (String(constExSeekMsg.suffix(5)) + "挤下\u{7ebf}。"))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        HemSocketManager.shared.topologicalSpace(userLoginOut: true)
        //: FoodV2Listener.shared.isConnection = false
        FoodV2Listener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (VersePowKingfisherCompatible.shared as! VersePowKingfisherCompatible).quantityerval()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: user_interactionUrl.map{compositionClick(called: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = WhiteAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if MotorAddrTool.share.interfaceLang == SubsumeExpressionConvertible.ar.rawValue || MotorAddrTool.share.interfaceLang == SubsumeExpressionConvertible.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            MotorAddrTool.share.interfaceLang == SubsumeExpressionConvertible.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.platformDisk(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        EnhanceAlertShow.valueNog()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        EnhanceAlertShow.valueNog(title: (String(noti_matchTelevisionStr.suffix(5)) + "ne No" + main_proposalFormat.lowercased() + "ation").localized, message: msg, leftBtnTitle: (String(mainRenderValue.prefix(6))).localized, rightBtnTitle: (String(kCaptureAdvancedFormat) + String(kMyPath.suffix(5))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            MaterialLiveManager.threadContext().fieldTerms()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            CigaretteThen.split { t in
                //: ProgressHUD.dismiss()
                DirectProgressHUD.thatDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: data_featureName, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            CigaretteThen.report { t in
                //: if t {
                if t {
                    //: FoodV2Listener.shared.func__LogingIn { _ in
                    FoodV2Listener.shared.midday { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if MaterialLiveManager.threadContext().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(MaterialLiveManager.threadContext().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    HemSocketManager.shared.outpost()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
