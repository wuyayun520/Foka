
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showAppropriateMedalStr:[UInt8] = [0xdd,0xe2,0xdd,0xe8,0x9c,0xd7,0xe3,0xd8,0xd9,0xe6,0xae,0x9d,0x94,0xdc,0xd5,0xe7,0x94,0xe2,0xe3,0xe8,0x94,0xd6,0xd9,0xd9,0xe2,0x94,0xdd,0xe1,0xe4,0xe0,0xd9,0xe1,0xd9,0xe2,0xe8,0xd9,0xd8]

fileprivate func topicMind(invest num: UInt8) -> UInt8 {
    let value = Int(num) + 140
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "roomId" :*/
fileprivate let mainResultFormat:String = "attention present then scene onroomId"

/*: "type" :*/
fileprivate let app_smartName:[UInt8] = [0x14,0x19,0x10,0x5]

private func proceedCombined(now num: UInt8) -> UInt8 {
    return num ^ 96
}

/*: "extra" :*/
fileprivate let constAgePartEnthusiasmData:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let notiHeadKey:[UInt8] = [0x41,0x4c,0x44,0x47,0x22,0x3c,0x50]

fileprivate func shFair(peer num: UInt8) -> UInt8 {
    let value = Int(num) - 215
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Send Gift" :*/
fileprivate let appNaturalData:String = "at clockSend G"
fileprivate let user_discUrl:String = "itradet"

/*: "View info" :*/
fileprivate let user_analyzeUrl:String = "source through giftView "
fileprivate let kPutText:[Character] = ["i","n","f","o"]

/*: "Report" :*/
fileprivate let app_enoughWithinId:[Character] = ["R","e","p","o","r","t"]

/*: "uid" :*/
fileprivate let main_shadeEarlyMsg:[UInt8] = [0xa6,0xba,0xb7]

private func cuteBorder(request num: UInt8) -> UInt8 {
    return num ^ 211
}

/*: "name" :*/
fileprivate let data_aboveId:[UInt8] = [0xe0,0xef,0xe3,0xeb]

private func womanHighlight(subject num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "Can't send blank message" :*/
fileprivate let data_beanStr:String = "Can\'t"
fileprivate let appHimId:[Character] = [" ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let mainTurnUrl:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let kPartnerFormat:[UInt8] = [0xde,0xd2,0xd3,0xc9,0xd8,0xd3,0xc9]

/*: "msgType" :*/
fileprivate let data_kingValue:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let k_analyzeData:String = "tillxtill"

/*: "isGroup" :*/
fileprivate let dataExtendText:String = "license frameisGroup"

/*: "jumps" :*/
fileprivate let appIconStr:String = "jumpobserve"

/*: "message" :*/
fileprivate let user_connectionData:[UInt8] = [0x1c,0x14,0x22,0x22,0x10,0x16,0x14]

fileprivate func premiumCoat(attach num: UInt8) -> UInt8 {
    let value = Int(num) + 81
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let noti_requestValue:String = "show mic character satisfy arriveTxtMs"
fileprivate let app_usMsg:String = "never"

/*: "toUid" :*/
fileprivate let show_atMsg:String = "toUidsilver sure guard"

/*: "msgInfo" :*/
fileprivate let appVisualStr:String = "msgInfofinger claim"

/*: "params" :*/
fileprivate let mainExtentRootDailyMsg:[UInt8] = [0xa9,0xb8,0xab,0xb8,0xb4,0xaa]

private func changeRepresentative(stretch num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "contentType" :*/
fileprivate let constLackContent:[UInt8] = [0x2,0xe,0xf,0x15,0x4,0xf,0x15,0x35,0x18,0x11,0x4]

private func theTalk(find num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "ImgMsg" :*/
fileprivate let appPhotoPath:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let show_meDiscMessage:String = "replymg"

/*: "imageUri" :*/
fileprivate let showMultipleIconOccurData:[UInt8] = [0x14,0x18,0xc,0x12,0x10,0x0,0x1d,0x14]

fileprivate func anotherSpan(collect num: UInt8) -> UInt8 {
    let value = Int(num) + 85
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let appThirdContent:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let appSweetAlterId:String = "compute smoothmsgId"

/*: "totalMfCoin" :*/
fileprivate let showBranchId:String = "suspend addition patterntotalM"
fileprivate let app_advertisingPath:[Character] = ["n"]

/*: "giftId" :*/
fileprivate let kLieValue:String = "giftIdtoward verse his ignore"

/*: "giftNum" :*/
fileprivate let appGoodMsg:[Character] = ["g","i","f","t","N","u"]
fileprivate let constSurgeryTitle:[Character] = ["m"]

/*: "pkgItemsetId" :*/
fileprivate let user_rollFormat:String = "mind exactly moon loop edgepkgI"
fileprivate let mainElectronicKey:String = "level sh resigntemsetId"

/*: "msg" :*/
fileprivate let user_fireName:String = "alwaysg"

/*: "tips" :*/
fileprivate let main_pillowName:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let dataPingTitle:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let mainFlushTitle:String = "nrollckn"
fileprivate let k_clearStr:String = "akey"

/*: "headPic" :*/
fileprivate let const_enterpriseMsg:[UInt8] = [0x26,0x2b,0x2f,0x2a,0x1e,0x27,0x2d]

/*: "All Numbers" :*/
fileprivate let app_needFormat:String = "operate define chance positAl"
fileprivate let userAlterMessage:String = "topic disk observe others thanl Num"

/*: "sendNum" :*/
fileprivate let main_labelRequireData:String = "half environment big curvesendNu"
fileprivate let data_workerUrl:[Character] = ["m"]

/*: "toUser" :*/
fileprivate let notiCombineMessage:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let userScaleUrl:[Character] = ["m","s","g","I","n","c","o"]
fileprivate let data_effKey:[Character] = ["m","e"]

/*: "isConnection" :*/
fileprivate let constCoverageStr:[Character] = ["i","s"]
fileprivate let dataMinuteUrl:String = "allow gold makerConnec"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FoodRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class FoodRecognizerDelegate: BodyLatViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = FieldModelType() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = SpendConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = FieldModelType.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showAppropriateMedalStr.map{topicMind(invest: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SteppingMotorThen.share.appConfigMode.disableShootScreen {
            if SteppingMotorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StrokeKeepThen(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        resistanceLab()
        //: createUIConstraints()
        pictureScale()
        //: bindInteraction()
        beforeKind()
        //: addTapGestureRecognizer()
        yellowConvert()
        //: func__chatRoom_insertTipMsg()
        gender()
        //: chatSettings()
        middleCrush()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            load()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.primaryDoingce(join: false)
        //: self.destroyTipsTimer()
        self.wipeOutDirectTimer()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: DetermineViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = DetermineViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(RefreshlyView.self, forCellReuseIdentifier: RefreshlyView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(SinceViewCell.self, forCellReuseIdentifier: SinceViewCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(AttributeThen.self, forCellReuseIdentifier: AttributeThen.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ThereChatThen.self, forCellReuseIdentifier: ThereChatThen.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: RequestView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(CommentPlusViewCell.self, forCellReuseIdentifier: RequestView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: FieldInputView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = FieldInputView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: UnderBotReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = UnderBotReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: GiftView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = GiftView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: CurveBirdSEyeViewReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = CurveBirdSEyeViewReactiveCompatible(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: JoinThen = {
        //: let v = TalkingSomeoneAtMeView()
        let v = JoinThen()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func middleCrush() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        SatelliteChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        primaryDoingce(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func primaryDoingce(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(mainResultFormat.suffix(6))): self.conversationData.groupID, String(bytes: app_smartName.map{proceedCombined(now: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        TimeTestedRequestTool.quoteContext(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - WirePlaceUnderlyingDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension FoodRecognizerDelegate: WirePlaceUnderlyingDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> PhoneMessageThen! {
    func hair(_: DetermineViewController!, reDelete data: V2TIMMessage!) -> PhoneMessageThen! {
        //: let dic = BotDialogChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = BotDialogChatManager.clever(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = CookieChatManager.effect(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: PhoneMessageThen!) -> RequestView! {
    func with(_: DetermineViewController!, display data: PhoneMessageThen!) -> RequestView! {
        //: if data.isKind(of: GroupThen.self) {
        if data.isKind(of: GroupThen.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = RefreshlyView(style: .default, reuseIdentifier: RefreshlyView.className())
            //: let cellData = data as! GroupThen
            let cellData = data as! GroupThen
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.windowAcross(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.renderPush(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: TakeCellData.self) {
        if data.isKind(of: TakeCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = SinceViewCell(style: .default, reuseIdentifier: SinceViewCell.className())
            //: let cellData = data as! TakeCellData
            let cellData = data as! TakeCellData
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: SlumThen.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = AttributeThen(style: .default, reuseIdentifier: AttributeThen.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! SlumThen
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: CookieCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ThereChatThen(style: .default, reuseIdentifier: ThereChatThen.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! CookieCellData
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: RequestView!) {
    func destroy(_: DetermineViewController!, description cell: RequestView!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(SteppingMotorThen.share.loginUid) {
            if userModel.uid == Int(SteppingMotorThen.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                windowAcross(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                level(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: RequestView!) {
    func available(_: DetermineViewController!, full cell: RequestView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SinceViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! TakeCellData
            let imageCellData = cell.messageData as! TakeCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(noti_cornerKey)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = GenreModelType()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = ContentTheoryDialogMeasurable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [GenreModelType] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = ControlDataSource(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.strokeController()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: RefreshlyView.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! RefreshlyView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = BotDialogChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = BotDialogChatManager.clever(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(constAgePartEnthusiasmData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: notiHeadKey.map{shFair(peer: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HemSpecialJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LatPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LatPushManager.share.policyLicense(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ThereChatThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ThereChatThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = BotDialogChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = BotDialogChatManager.clever(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(constAgePartEnthusiasmData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: notiHeadKey.map{shFair(peer: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HemSpecialJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LatPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LatPushManager.share.policyLicense(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: RequestView!) {
    func failure(_: DetermineViewController!, margin cell: RequestView!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        chicoryRoot(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func wipeOutDirectTimer() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func level(user: Model) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = AttributePopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.organiseList(cellTitleList: [(String(appNaturalData.suffix(6)) + user_discUrl.replacingOccurrences(of: "trade", with: "f")).localized, "@", (String(user_analyzeUrl.suffix(5)) + String(kPutText)).localized, (String(app_enoughWithinId)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.oarlock(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.redTableDHote(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.windowAcross(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.motivation(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func oarlock(user: Model) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.settingCan()
        //: let model = TalkingChatRoomMemberModel()
        let model = StatusStrokeModelType()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.intervalernationality(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func redTableDHote(user: Model) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: main_shadeEarlyMsg.map{cuteBorder(request: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: data_aboveId.map{womanHighlight(subject: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.draftCopyOff(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func windowAcross(uid: Int) {
        //: LatPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        LatPushManager.share.postKey(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func motivation(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = UnderViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.straddleResult(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func adminModel(msgStr: String, quoteModel _: MedalFiniteModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            revenueMsg(showMsg: (data_beanStr + String(appHimId)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: notiHeadKey.map{shFair(peer: $0)}, encoding: .utf8)!: (String(mainTurnUrl)), String(bytes: main_shadeEarlyMsg.map{cuteBorder(request: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: kPartnerFormat.map{$0^189}, encoding: .utf8)!: temStr, (String(data_kingValue)): (k_analyzeData.replacingOccurrences(of: "till", with: "t")), (String(dataExtendText.suffix(7))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(appIconStr.replacingOccurrences(of: "observe", with: "s"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: user_connectionData.map{premiumCoat(attach: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: app_smartName.map{proceedCombined(now: $0)}, encoding: .utf8)!: (String(noti_requestValue.suffix(5)) + app_usMsg.replacingOccurrences(of: "never", with: "g")),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(mainResultFormat.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(show_atMsg.prefix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appVisualStr.prefix(7))): msgInfo, String(bytes: mainExtentRootDailyMsg.map{changeRepresentative(stretch: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            chicoryRoot(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func burnAvailable(img: UIImage) {
        //: let imagePath = TakeCellData.getChatImagePath(image: img)
        let imagePath = TakeCellData.per(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: constLackContent.map{theTalk(find: $0)}, encoding: .utf8)!: (String(appPhotoPath)), (String(data_kingValue)): (show_meDiscMessage.replacingOccurrences(of: "reply", with: "i")), String(bytes: showMultipleIconOccurData.map{anotherSpan(collect: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(appThirdContent)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: app_smartName.map{proceedCombined(now: $0)}, encoding: .utf8)!: (String(appPhotoPath)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(mainResultFormat.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(appVisualStr.prefix(7))): msgInfo, String(bytes: mainExtentRootDailyMsg.map{changeRepresentative(stretch: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            chicoryRoot(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: PhoneMessageThen, reSend: Bool) {
    private func chicoryRoot(msgData: PhoneMessageThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            revenueMsg(showMsg: main_backgroundPath)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.center(since: msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(appThirdContent))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(appThirdContent))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(appThirdContent))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        TimeTestedRequestTool.param(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.footing(msgData, customSystem: nil, render: NSNumber(value: 0), forefront: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.recess(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(appVisualStr.prefix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.footing(msgData, customSystem: msgInfo[(String(appSweetAlterId.suffix(5)))] as? String, render: NSNumber(value: 0), forefront: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(showBranchId.suffix(6)) + "fCoi" + String(app_advertisingPath))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(showBranchId.suffix(6)) + "fCoi" + String(app_advertisingPath))] as! NSNumber
                //: SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func recess(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard SteppingMotorThen.share.loginUserMode.status != 1 else {
            guard SteppingMotorThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    revenueMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: LatPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            LatPushManager.share.ownerBag(clickEvent: k_foundErrorPath, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.advancedDoing()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            latitude(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = FluBulgeOutReactiveCompatible(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath))
            //: view.show()
            view.makeupNetwork()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                revenueMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension FoodRecognizerDelegate: ConferThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func assets(giftModel _: MyGiftModel, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func gapSecond() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func narrowRange(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = SpecificDataSource()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.draftCopyOff(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func spend(actionType: HugeEquatable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            database()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func centerNum(giftModel _: MyGiftModel, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func output(giftModel: MyGiftModel, giftNum: String, model: StatusStrokeModelType) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            revenueMsg(showMsg: main_backgroundPath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(show_atMsg.prefix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(kLieValue.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(appGoodMsg) + String(constSurgeryTitle))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(mainResultFormat.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(user_rollFormat.suffix(4)) + String(mainElectronicKey.suffix(8)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        TimeTestedRequestTool.subjectMatter(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.recess(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.decisionComplete(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.combineProgress(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func eEr(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.dataConverterSizeBottom(true)
            self.msgView.dataConverterSizeBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func equity(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.dataConverterSizeBottom(true)
            self.msgView.dataConverterSizeBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func gestureDark(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.gestureDark(tap)
    }

    //: func func__starRecordBtnDown() {}
    func someDownward() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func funcWeekly(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension FoodRecognizerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func database() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        LabelMpThen.rangeDoing(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SpendBassPickTool.systemVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: LatPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                LatPushManager.share.topical()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.burnAvailable(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func stage() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        LabelMpThen.commitBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.burnAvailable(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func fossilization(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(mainResultFormat.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    //: func func__TipsNotification() {
    func cantileverBridge() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(face(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: app_halfMessage,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func face(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: CookieCellData = userinfo[(user_fireName.replacingOccurrences(of: "always", with: "ms"))] as! CookieCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.dataConverterSizeBottom(true)
        self.msgView.dataConverterSizeBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func latitude(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: kPartnerFormat.map{$0^189}, encoding: .utf8)!: tipStr, (String(data_kingValue)): (String(main_pillowName))]
        //: var message: PhoneMessageThen?
        var message: PhoneMessageThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = CookieChatManager.keyWeight(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.dataConverterSizeBottom(true)
        self.msgView.dataConverterSizeBottom(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func gender() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard SteppingMotorThen.share.appUserConfigMode.rTips.count > 0 else { return }
            guard SteppingMotorThen.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: SteppingMotorThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.latitude(tipStr: SteppingMotorThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func combineProgress(extral: [String: Any?], model: StatusStrokeModelType, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(appVisualStr.prefix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(dataExtendText.suffix(7)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(data_kingValue))] = (String(dataPingTitle))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(appVisualStr.prefix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: main_shadeEarlyMsg.map{cuteBorder(request: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (mainFlushTitle.replacingOccurrences(of: "roll", with: "i") + k_clearStr.replacingOccurrences(of: "key", with: "me")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: const_enterpriseMsg.map{$0^78}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(app_needFormat.suffix(2)) + String(userAlterMessage.suffix(5)) + "bers").localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(dataPingTitle))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(dataPingTitle))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(mainFlushTitle.replacingOccurrences(of: "roll", with: "i") + k_clearStr.replacingOccurrences(of: "key", with: "me"))] = "\(giftdic[(String(main_labelRequireData.suffix(6)) + String(data_workerUrl))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(notiCombineMessage))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = CookieChatManager.keyWeight(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.center(since: cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(userScaleUrl) + String(data_effKey))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(userScaleUrl) + String(data_effKey))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.footing(cellData, customSystem: msgInfoDic[(String(appSweetAlterId.suffix(5)))] as? String, render: msgIncome, forefront: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        pan(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(showBranchId.suffix(6)) + "fCoi" + String(app_advertisingPath))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(showBranchId.suffix(6)) + "fCoi" + String(app_advertisingPath))] as! NSNumber
            //: SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: SteppingMotorThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.disclike(needReload: false, mf_coin: SteppingMotorThen.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.acceptResPremium()
    }

    //: func initCachEffectData() {
    func load() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.request(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func pan(giftDic: [String: Any], model: StatusStrokeModelType) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.panel(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in HimGiftManager.share.people() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [HrArrModel]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.recording(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.appropriate(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func social(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in HimGiftManager.share.people() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [HrArrModel]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.recording(model: giftModel as! RepresentModelType)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.uniformSignal(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension FoodRecognizerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func resistanceLab() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func pictureScale() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(show_turnStr)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(show_turnStr)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - k_depthUrl)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func beforeKind() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = SpecificDataSource()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! PhoneMessageThen).isKind(of: GroupThen.self) {
                    if (cellData as! PhoneMessageThen).isKind(of: GroupThen.self) {
                        //: let groupCellData = cellData as! GroupThen
                        let groupCellData = cellData as! GroupThen
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(appSweetAlterId.suffix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: FoodV2Listener.shared.rx
        FoodV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(constCoverageStr) + String(dataMinuteUrl.suffix(6)) + "tion"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
