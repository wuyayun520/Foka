
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userDiscourseName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let mainRemovalPerspectiveKey:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let const_allegedKey:String = "memory archeadPic"

/*: "nickname" :*/
fileprivate let constAlongsideName:String = "excknam"
fileprivate let data_signMessage:String = "shared"

/*: "birthday" :*/
fileprivate let noti_parentUrl:String = "birthanalysisy"

/*: "videoUrl" :*/
fileprivate let show_earnMessage:[Character] = ["v","i","d"]
fileprivate let k_boxMsg:String = "region business constrainteoUrl"

/*: "location" :*/
fileprivate let const_rationalMsg:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let k_birdPoId:[UInt8] = [0xfc,0xf2,0xfb]

private func contributionIon(hello num: UInt8) -> UInt8 {
    return num ^ 159
}

/*: "refuseCall" :*/
fileprivate let appThemeName:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let constFiftyData:[UInt8] = [0xc6,0xc3,0xd6,0xc3]

private func distancePan(purchase num: UInt8) -> UInt8 {
    return num ^ 162
}

/*: "Call interruption" :*/
fileprivate let userShadowDrawingPath:String = "motivation rem choose chemical banCall"
fileprivate let kSireValue:String = "ptutilityn"

/*: "stopCall" :*/
fileprivate let dataNameKey:String = "streadingp"

/*: "logId" :*/
fileprivate let mainClothingCustomChangePath:[UInt8] = [0xa3,0xa6,0x9e,0x80,0x9b]

fileprivate func makeTake(tab num: UInt8) -> UInt8 {
    let value = Int(num) - 55
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let userSubmitGeneticUrl:String = "librarynce"

/*: "No answer" :*/
fileprivate let data_attractiveName:String = "No aop related"

/*: "duration" :*/
fileprivate let const_canText:String = "dproperlyati"
fileprivate let data_approximatelyContent:String = "osufficient"

/*: "She has hung up" :*/
fileprivate let kAgainstId:String = "letter enterprise sweet closed cityShe ha"
fileprivate let main_recommendSmallLackId:String = "outside spend somethingng up"

/*: "cancelSex" :*/
fileprivate let mainHeavyHappyName:String = "mustnc"

/*: "He has hung up" :*/
fileprivate let constReadNorSourceId:String = "member middleHe has"
fileprivate let k_installationId:String = "presentation advice produce hung up"

/*: "endType" :*/
fileprivate let data_identityName:[Character] = ["e"]
fileprivate let kDialogPath:String = "ndTypehi seek hair stick current"

/*: "stopSex" :*/
fileprivate let app_conversionStr:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let k_togetherName:[UInt8] = [0xbb,0x9c,0x81,0x87,0x94,0x94,0x9b,0x91,0x9b,0x97,0x9c,0x86,0xd2,0x95,0x9d,0x9e,0x96,0xd2,0x91,0x9d,0x9b,0x9c,0x81,0xde,0xd2,0x91,0x93,0x9e,0x9e,0xd2,0x9a,0x93,0x81,0xd2,0x97,0x9c,0x96,0x97,0x96]

private func pendingBubble(alongside num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "The other hung up the phone" :*/
fileprivate let show_hideText:String = "The otpart notice"
fileprivate let notiTopFormat:String = "hung uborder definition wait"
fileprivate let user_sayKey:String = "commercial over fill identical scenee phon"
fileprivate let userBeneathMessage:String = "min"

/*: "type" :*/
fileprivate let app_yearName:String = "shepe"

/*: "jumpUrl" :*/
fileprivate let kExistName:String = "jumpUrldismiss track message"

/*: "Cancel" :*/
fileprivate let main_aboutStr:String = "beneath dignity select feedbackCancel"

/*: "Get gold coins" :*/
fileprivate let data_okTitle:String = "Get golplayer floor opening outer street"
fileprivate let const_arcData:[Character] = ["d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let k_wrapPath:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let show_illegalSaleMessage:[Character] = ["r","e","f","u","s"]
fileprivate let app_scriptMessage:String = "application positive want reason latereSex"

/*: "onAcceptCall" :*/
fileprivate let main_clingValue:String = "onAccepstrain someone doc"
fileprivate let userHorseStr:[Character] = ["t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let dataCommitPrettyTitle:[Character] = ["a","c","c","e","p","t","C"]
fileprivate let user_bagUrl:String = "originll"

/*: "Calls are cancelled" :*/
fileprivate let data_shakeRatingMsg:String = "Calls priority to"
fileprivate let appStrikeFormat:String = "carrynce"

/*: "The call has ended" :*/
fileprivate let user_clipTitle:String = "customer host combine openingThe ca"
fileprivate let dataButtPhasePath:String = "as endwarn esteem floor reserve squeeze"
fileprivate let notiAbsoluteKey:String = "titled"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StatusCallerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let userLengthPath = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class StatusCallerDelegate: BodyLatViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: BassChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CapTakePlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: MinThen? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        HemSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        HemSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        HemSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userDiscourseName.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! CurveReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.pinWith()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            atAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.translate()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        adjudicate()
        //: setupSubViewsConstraint()
        aboutConstraint()
        //: initNotificationObserver()
        ageCustom()
        //: req_callGetUserInfo()
        smartWithDown()
        //: timeoutRules()
        extra()
        //: resumeTalk()
        partnerMedia()
        //: setUpBGMPlayer()
        volleyballPlayer()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SteppingMotorThen.share.appConfigMode.disableShootScreen {
            if SteppingMotorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StrokeKeepThen(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isPage = false
        HemSocketManager.shared.isPage = false
        //: TalkingSocketManager.shared.isCalling = false
        HemSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.everyCry()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: PowView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = PowView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: WhoCoordinatorChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = WhoCoordinatorChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func smartWithDown() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        VerseCloudDelegate.conversionDown(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: mainRemovalPerspectiveKey.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(const_allegedKey.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(constAlongsideName.replacingOccurrences(of: "ex", with: "ni") + data_signMessage.replacingOccurrences(of: "shared", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(noti_parentUrl.replacingOccurrences(of: "analysis", with: "da"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(show_earnMessage) + String(k_boxMsg.suffix(5)))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(const_rationalMsg))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.meInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func atAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LabelMpThen.streetSmartFor(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.revenueMsg(showMsg: noti_ageMovieMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (String(appThemeName)), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainRemovalPerspectiveKey.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.underBirthday()
                //: return
                return
            }

            //: self.stopBGM()
            self.capableness()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CapTakePlayer.quantityPlay(type: .Response)
            //: self.startBGM()
            self.pinWith()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func everyCry() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.capableness()
        //: TalkingSocketManager.shared.callerDelegate = nil
        HemSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        HemSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        HemSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func underBirthday(animated: Bool = true) {
        //: self.stopBGM()
        self.capableness()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func talkWriterFinish() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.realised(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func partnerMedia() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            talkWriterFinish()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func ageCustom() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(noticeNetwork(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(scope(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: notiFileText,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        userLengthPath.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func noticeNetwork(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            underBirthday()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func scope(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard HemSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        HemSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        HemSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        HemSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.belowNumbererestInput(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension StatusCallerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.revenueMsg(showMsg: (String(userShadowDrawingPath.suffix(4)) + " interru" + kSireValue.replacingOccurrences(of: "utility", with: "io")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (dataNameKey.replacingOccurrences(of: "reading", with: "o") + "Call"), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (userSubmitGeneticUrl.replacingOccurrences(of: "library", with: "ca") + "lCall"), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (String(appThemeName)), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainRemovalPerspectiveKey.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.deformation()
                //: TalkingSocketManager.shared.currPageVC = nil
                HemSocketManager.shared.currPageVC = nil
            }
            //: popCurrentViewController()
            underBirthday()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.modifyFire(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func volleyballPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? MilitaryCoatSwitchsetTarget.Call : MilitaryCoatSwitchsetTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CapTakePlayer.quantityPlay(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func pinWith() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.hailPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func capableness() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.theatregoer()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func extra() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? dataSenseSystemTimeUrl : self.chatModel?.nameShadow()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            arrival()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.arrival()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func arrival() {
        //: guard !TalkingSocketManager.shared.isPage else { return }
        guard !HemSocketManager.shared.isPage else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.revenueMsg(showMsg: (String(data_attractiveName.prefix(4)) + "nswer").localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (userSubmitGeneticUrl.replacingOccurrences(of: "library", with: "ca") + "lCall"), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (String(appThemeName)), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainRemovalPerspectiveKey.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        HemSocketManager.shared.commentObject(info: socketDict)
        //: popCurrentViewController()
        underBirthday()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func style(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(const_canText.replacingOccurrences(of: "properly", with: "ur") + data_approximatelyContent.replacingOccurrences(of: "sufficient", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.propertyCon(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.judgeShow()
            //: createTalkRoom()
            talkWriterFinish()
            //: self.stopBGM()
            self.capableness()
        }
    }
}

// MARK: - MpCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension StatusCallerDelegate: MpCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func betweenOrientation(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kAgainstId.suffix(6)) + "s hu" + String(main_recommendSmallLackId.suffix(5))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(mainHeavyHappyName.replacingOccurrences(of: "must", with: "ca") + "elSex")].stringValue == WalkMilitaryOutputStreamable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constReadNorSourceId.suffix(6)) + String(k_installationId.suffix(8))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.revenueMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.deformation()
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }

        //: popCurrentViewController()
        underBirthday()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func buildData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.capableness()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.judgeShow()
        //: self.createTalkRoom()
        self.talkWriterFinish()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func err(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(data_identityName) + String(kDialogPath.prefix(6)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(kAgainstId.suffix(6)) + "s hu" + String(main_recommendSmallLackId.suffix(5))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(app_conversionStr))].stringValue == WalkMilitaryOutputStreamable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(constReadNorSourceId.suffix(6)) + String(k_installationId.suffix(8))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: k_togetherName.map{pendingBubble(alongside: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(show_hideText.prefix(6)) + "her " + String(notiTopFormat.prefix(6)) + "p th" + String(user_sayKey.suffix(6)) + userBeneathMessage.replacingOccurrences(of: "min", with: "e")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.revenueMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.deformation()
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.betweenTing()
        //: self.popCurrentViewController()
        self.underBirthday()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: showSampleData, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func tv(msg: String, data: [String: Any]) {
        //: guard SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(app_yearName.replacingOccurrences(of: "she", with: "ty"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(kExistName.prefix(7)))] as? String else { return }
            //: let httpsUrl = SteppingMotorThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = SteppingMotorThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = HangConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: LatPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            LatPushManager.share.beforeRemarkConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            EnhanceAlertShow.valueNog(title: nil,
                                      //: message: msg,
                                      message: msg,
                                      //: leftBtnTitle: "Cancel".localized,
                                      leftBtnTitle: (String(main_aboutStr.suffix(6))).localized,
                                      //: rightBtnTitle: "Get gold coins".localized) {
                                      rightBtnTitle: (String(data_okTitle.prefix(7)) + String(const_arcData)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                EnhanceAlertShow.stripAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: LatPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                LatPushManager.share.ownerBag(clickEvent: constSucceedId, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func socketOrData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(k_wrapPath))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.statementPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.medalCompare(point: addBean)
        }
    }
}

// MARK: - BeatPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension StatusCallerDelegate: BeatPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func totalBubble(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kAgainstId.suffix(6)) + "s hu" + String(main_recommendSmallLackId.suffix(5))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(show_illegalSaleMessage) + String(app_scriptMessage.suffix(4)))].stringValue == WalkMilitaryOutputStreamable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constReadNorSourceId.suffix(6)) + String(k_installationId.suffix(8))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.revenueMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.deformation()
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }
        //: popCurrentViewController()
        underBirthday()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func rebroadcast(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.capableness()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.judgeShow()
        //: self.createTalkRoom()
        self.talkWriterFinish()
    }
}

// MARK: - OrganizerObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension StatusCallerDelegate: OrganizerObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func judgeData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(appThemeName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.revenueMsg(showMsg: (String(data_attractiveName.prefix(4)) + "nswer").localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.deformation()
                //: TalkingSocketManager.shared.currPageVC = nil
                HemSocketManager.shared.currPageVC = nil
            }
            //: self.popCurrentViewController()
            self.underBirthday()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(main_clingValue.prefix(7)) + String(userHorseStr)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue else { return }
            //: LatPushManager.share.func__jumpToWebRecharge(sufficient: false)
            LatPushManager.share.ownerBag(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(main_clingValue.prefix(7)) + String(userHorseStr)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.revenueMsg(showMsg: msg)
            //: self.popCurrentViewController()
            self.underBirthday()
        }
    }
}

// MARK: - AnalysisViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension StatusCallerDelegate: AnalysisViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func preparationSearchion() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (String(dataCommitPrettyTitle) + user_bagUrl.replacingOccurrences(of: "origin", with: "a")), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainRemovalPerspectiveKey.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func adBecome() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.revenueMsg(showMsg: (String(data_shakeRatingMsg.prefix(6)) + "are c" + appStrikeFormat.replacingOccurrences(of: "carry", with: "a") + "lled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (userSubmitGeneticUrl.replacingOccurrences(of: "library", with: "ca") + "lCall"), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.revenueMsg(showMsg: (String(data_shakeRatingMsg.prefix(6)) + "are c" + appStrikeFormat.replacingOccurrences(of: "carry", with: "a") + "lled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (String(appThemeName)), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainRemovalPerspectiveKey.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.revenueMsg(showMsg: (String(user_clipTitle.suffix(6)) + "ll h" + String(dataButtPhasePath.prefix(6)) + notiAbsoluteKey.replacingOccurrences(of: "title", with: "e")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            HemSocketManager.shared.commentObject(info: [String(bytes: k_birdPoId.map{contributionIon(hello: $0)}, encoding: .utf8)!: (dataNameKey.replacingOccurrences(of: "reading", with: "o") + "Call"), String(bytes: constFiftyData.map{distancePan(purchase: $0)}, encoding: .utf8)!: [String(bytes: mainClothingCustomChangePath.map{makeTake(tab: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.betweenTing()
        //: self.popCurrentViewController()
        self.underBirthday()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func counteractionHandle(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.expectedRefuse(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func changeVia(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.worker(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func quash() {
        //: TalkingSocketManager.shared.currPageVC = self
        HemSocketManager.shared.currPageVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = MinThen.tickAbout()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.strokeController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.gestureTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.discTime(talkingTime)
        //: popCurrentViewController()
        underBirthday()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func magnification(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.discTime(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func liveWith(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.hairDesign(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension StatusCallerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func adjudicate() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func aboutConstraint() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
