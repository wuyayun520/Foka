
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_springName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let userWipeGoingFormat:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let kNightName:String = "headPby sufficient language esteem waste"
fileprivate let kBadContent:String = "ififty"

/*: "nickname" :*/
fileprivate let k_justMsg:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let data_styleId:String = "birselect"

/*: "videoUrl" :*/
fileprivate let data_tabVisitorMsg:[Character] = ["v","i"]
fileprivate let appBallName:[Character] = ["d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let showAllowMsg:String = "descriptionver"

/*: "location" :*/
fileprivate let data_recordingFormat:[Character] = ["l","o"]
fileprivate let show_lineKey:String = "ctaretion"

/*: "cmd" :*/
fileprivate let mainPictureLovelyMessage:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let app_discoverStr:String = "nativefuse"

/*: "data" :*/
fileprivate let show_storyKey:[UInt8] = [0x84,0x81,0x94,0x81]

fileprivate func animalToday(history num: UInt8) -> UInt8 {
    let value = Int(num) + 224
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let mainAttributeMsg:String = "river request segment format attributeCall"
fileprivate let notiTakeMessage:[Character] = [" ","i","n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let mainSunId:String = "patron mystery clock mic bitstopCall"

/*: "logId" :*/
fileprivate let main_framePath:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let show_latValue:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let appPairFormat:[Character] = ["N","o"," ","a","n","s","w","e"]
fileprivate let main_attractiveWeHourMessage:String = "reading"

/*: "duration" :*/
fileprivate let appBringId:String = "duratiosize"

/*: "She has hung up" :*/
fileprivate let notiScheduleMoviePath:String = "She hhand image button trigger her"
fileprivate let show_toneName:String = "replacement"
fileprivate let data_positTransferValue:String = "you dialog items hu"

/*: "cancelSex" :*/
fileprivate let noti_maxMsg:String = "beautifulanbeautifulel"
fileprivate let userHungSpecId:String = "late living across step knockSex"

/*: "He has hung up" :*/
fileprivate let user_utilityMsg:[Character] = ["H","e"," "]
fileprivate let kSolarTitle:String = "handle lift trigger scale earnhas hu"

/*: "endType" :*/
fileprivate let userSignaturePath:String = "endTypemain publish moon logical path"

/*: "stopSex" :*/
fileprivate let kTopStr:String = "female moment listen kindstopS"
fileprivate let appWayId:String = "income"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let noti_backMessage:[UInt8] = [0x8a,0xaf,0xb4,0xb6,0xa7,0xa7,0xaa,0xa4,0xaa,0xa6,0xaf,0xb5,0x61,0xa8,0xb0,0xad,0xa5,0x61,0xa4,0xb0,0xaa,0xaf,0xb4,0x6d,0x61,0xa4,0xa2,0xad,0xad,0x61,0xa9,0xa2,0xb4,0x61,0xa6,0xaf,0xa5,0xa6,0xa5]

fileprivate func noModeDit(far num: UInt8) -> UInt8 {
    let value = Int(num) + 191
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let kBoardPath:String = "The identify refrigerator"
fileprivate let mainFlushFatalPutId:String = "r hungcorrect break strip publication perspective"
fileprivate let app_dominantTitle:String = "smooth living rese phone"

/*: "type" :*/
fileprivate let k_noseFormat:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let app_sendMessage:[Character] = ["j","u","m","p","U"]
fileprivate let mainAdviceValue:String = "bulletl"

/*: "Cancel" :*/
fileprivate let mainCapableName:[Character] = ["C","a","n","c","e"]
fileprivate let const_scrambleFormat:String = "once"

/*: "Get gold coins" :*/
fileprivate let constAgainValue:String = "fade privateGet gold"
fileprivate let showDimensionOkIgnoreTitle:String = "earn"
fileprivate let userUsData:[Character] = ["c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let dataLargeBrandPath:String = "stick"
fileprivate let show_leaveGameMessage:String = "far relation instrument random washddBean"

/*: "refuseSex" :*/
fileprivate let dataParentName:String = "refuseScoordinator nevertheless am wide"
fileprivate let main_cornerStr:[Character] = ["e","x"]

/*: "onAcceptCall" :*/
fileprivate let app_thirdData:String = "onAccmini cur spark clock constraint"
fileprivate let mainAssertValue:String = "eptCallplan extension"

/*: "acceptCall" :*/
fileprivate let user_ageMsg:[Character] = ["a","c","c","e","p","t"]
fileprivate let const_displayTitle:[Character] = ["C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let data_errorUrl:String = "fair representCalls "
fileprivate let app_constitutionalContent:[Character] = ["c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let userRefTransmissionName:String = "administer bid creation representative slowThe c"
fileprivate let userRouteTitle:String = "s endedbucket capital"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MotorViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let k_purchasePackageFormat = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class MotorViewDelegate: BodyLatViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: BassChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: CapTakePlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: VideoMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: GuesstimateThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        HemSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        HemSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        HemSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_springName.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! CurveReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.appearBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            precocious()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            artGallery(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.everyCreateData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        outfall()
        //: setupSubViewsConstraint()
        faceConstraint()
        //: initNotificationObserver()
        hang()
        //: req_callGetUserInfo()
        somaInfo()
        //: timeoutRules()
        timeout()
        //: setUpBGMPlayer()
        will()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.clothing()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SteppingMotorThen.share.appConfigMode.disableShootScreen {
            if SteppingMotorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StrokeKeepThen(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isPage = false
        HemSocketManager.shared.isPage = false
        //: TalkingSocketManager.shared.isCalling = false
        HemSocketManager.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.outsize()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: EstimatedView = {
        //: let v = TalkingBGVideoView()
        let v = EstimatedView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.addOutput(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: HimThen = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = HimThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: ConversionRecognizerDelegate = {
        //: let v = TalkingVideoWindowView()
        let v = ConversionRecognizerDelegate()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = dataShowDetailPopMessage - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if LieLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: dataEnvironmentId + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.pocketSized()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: VerseCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = VerseCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func somaInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        VerseCloudDelegate.conversionDown(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: userWipeGoingFormat.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kNightName.prefix(5)) + kBadContent.replacingOccurrences(of: "fifty", with: "c"))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(k_justMsg))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(data_styleId.replacingOccurrences(of: "select", with: "t") + "hday")].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(data_tabVisitorMsg) + String(appBallName))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(showAllowMsg.replacingOccurrences(of: "description", with: "co"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(data_recordingFormat) + show_lineKey.replacingOccurrences(of: "tare", with: "a"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.ask()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.modest()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func modest() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = CoatCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = CoatCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.runningOf(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.failExecute(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func precocious() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        LabelMpThen.noWait { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.revenueMsg(showMsg: noti_ageMovieMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (app_discoverStr.replacingOccurrences(of: "native", with: "re") + "Call"), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: userWipeGoingFormat.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.observerCalculate()
                //: return
                return
            }

            //: self.startPreview()
            self.clothing()
            //: self.stopBGM()
            self.group()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = CapTakePlayer.quantityPlay(type: .Response)
            //: self.startBGM()
            self.appearBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func clothing() {
        //: self.videoManager.startPreview()
        self.videoManager.jumpDoing()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func outsize() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.group()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        ProcessingReactiveCompatible.signal()
        //: TalkingSocketManager.shared.callerDelegate = nil
        HemSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        HemSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        HemSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func observerCalculate(animated: Bool = true) {
        //: self.stopBGM()
        self.group()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func pocketSized() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.twinkle()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modest()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func overlip() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.audiencePublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modest()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.random()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func random() {
        //: guard SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = GuesstimateThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.attributeAnimation()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func hang() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(length(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(motivationIn(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: userMicUrl,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(anonymousNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: notiFileText,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        k_purchasePackageFormat.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func length(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            observerCalculate()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func anonymousNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard HemSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        HemSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        HemSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        HemSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.refuseWith()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func motivationIn(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modest()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension MotorViewDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.revenueMsg(showMsg: (String(mainAttributeMsg.suffix(4)) + String(notiTakeMessage)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (String(mainSunId.suffix(8))), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: main_framePath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (String(show_latValue)), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: main_framePath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (app_discoverStr.replacingOccurrences(of: "native", with: "re") + "Call"), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: userWipeGoingFormat.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.deformation()
                //: TalkingSocketManager.shared.currPageVC = nil
                HemSocketManager.shared.currPageVC = nil
            }
            //: popCurrentViewController()
            observerCalculate()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func will() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? MilitaryCoatSwitchsetTarget.Call : MilitaryCoatSwitchsetTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = CapTakePlayer.quantityPlay(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func appearBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.hailPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func group() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.theatregoer()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? dataSenseSystemTimeUrl : self.chatModel?.nameShadow()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            createMentally()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.createMentally()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func createMentally() {
        //: guard !TalkingSocketManager.shared.isPage else { return }
        guard !HemSocketManager.shared.isPage else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.revenueMsg(showMsg: (String(appPairFormat) + main_attractiveWeHourMessage.replacingOccurrences(of: "reading", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (String(show_latValue)), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: main_framePath.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (app_discoverStr.replacingOccurrences(of: "native", with: "re") + "Call"), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: userWipeGoingFormat.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        HemSocketManager.shared.commentObject(info: socketDict)
        //: popCurrentViewController()
        observerCalculate()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func modifyFromUnprocessedRawData(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: main_framePath.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(appBringId.replacingOccurrences(of: "size", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.lyric(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.exFirst()
            //: createTalkRoom()
            overlip()
            //: self.stopBGM()
            self.group()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func artGallery(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.twinkle()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.addOutput(show: false)
        //: self.stopBGM()
        self.group()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.exFirst()
        //: self.createTalkRoom()
        self.overlip()
    }
}

// MARK: - MpCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension MotorViewDelegate: MpCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func betweenOrientation(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(notiScheduleMoviePath.prefix(5)) + show_toneName.replacingOccurrences(of: "replacement", with: "a") + String(data_positTransferValue.suffix(4)) + "ng up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(noti_maxMsg.replacingOccurrences(of: "beautiful", with: "c") + String(userHungSpecId.suffix(3)))].stringValue == WalkMilitaryOutputStreamable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(user_utilityMsg) + String(kSolarTitle.suffix(6)) + "ng up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.revenueMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.deformation()
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }

        //: popCurrentViewController()
        observerCalculate()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func buildData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        artGallery(logId: json[String(bytes: main_framePath.reversed(), encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func err(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(userSignaturePath.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(notiScheduleMoviePath.prefix(5)) + show_toneName.replacingOccurrences(of: "replacement", with: "a") + String(data_positTransferValue.suffix(4)) + "ng up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kTopStr.suffix(5)) + appWayId.replacingOccurrences(of: "income", with: "ex"))].stringValue == WalkMilitaryOutputStreamable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(user_utilityMsg) + String(kSolarTitle.suffix(6)) + "ng up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: noti_backMessage.map{noModeDit(far: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kBoardPath.prefix(4)) + "othe" + String(mainFlushFatalPutId.prefix(6)) + " up th" + String(app_dominantTitle.suffix(7))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.revenueMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.deformation()
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.invitation()
        //: popCurrentViewController()
        observerCalculate()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func tv(msg: String, data: [String: Any]) {
        //: guard SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(k_noseFormat))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(app_sendMessage) + mainAdviceValue.replacingOccurrences(of: "bullet", with: "r"))] as? String else { return }
            //: let httpsUrl = SteppingMotorThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = SteppingMotorThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = HangConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: LatPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            LatPushManager.share.beforeRemarkConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            EnhanceAlertShow.valueNog(title: nil,
                                      //: message: msg,
                                      message: msg,
                                      //: leftBtnTitle: "Cancel".localized,
                                      leftBtnTitle: (String(mainCapableName) + const_scrambleFormat.replacingOccurrences(of: "once", with: "l")).localized,
                                      //: rightBtnTitle: "Get gold coins".localized) {
                                      rightBtnTitle: (String(constAgainValue.suffix(8)) + showDimensionOkIgnoreTitle.replacingOccurrences(of: "earn", with: " ") + String(userUsData)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                EnhanceAlertShow.stripAlert()
                //: } rightBlock: {
            } rightBlock: {
                //: LatPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                LatPushManager.share.ownerBag(clickEvent: constSucceedId, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func conversion(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.dataDoing(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func socketOrData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(dataLargeBrandPath.replacingOccurrences(of: "stick", with: "a") + String(show_leaveGameMessage.suffix(6)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.sign(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.scanPoint(point: addBean)
        }
    }
}

// MARK: - BeatPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MotorViewDelegate: BeatPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func totalBubble(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(notiScheduleMoviePath.prefix(5)) + show_toneName.replacingOccurrences(of: "replacement", with: "a") + String(data_positTransferValue.suffix(4)) + "ng up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(dataParentName.prefix(7)) + String(main_cornerStr))].stringValue == WalkMilitaryOutputStreamable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(user_utilityMsg) + String(kSolarTitle.suffix(6)) + "ng up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.revenueMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.deformation()
            //: TalkingSocketManager.shared.currPageVC = nil
            HemSocketManager.shared.currPageVC = nil
        }

        //: popCurrentViewController()
        observerCalculate()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func rebroadcast(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        artGallery(logId: json[String(bytes: main_framePath.reversed(), encoding: .utf8)!].intValue)
    }
}

// MARK: - OrganizerObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension MotorViewDelegate: OrganizerObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func judgeData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (app_discoverStr.replacingOccurrences(of: "native", with: "re") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.revenueMsg(showMsg: (String(appPairFormat) + main_attractiveWeHourMessage.replacingOccurrences(of: "reading", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.deformation()
                //: TalkingSocketManager.shared.currPageVC = nil
                HemSocketManager.shared.currPageVC = nil
            }
            //: popCurrentViewController()
            observerCalculate()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(app_thirdData.prefix(5)) + String(mainAssertValue.prefix(7))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue else { return }
            //: LatPushManager.share.func__jumpToWebRecharge(sufficient: false)
            LatPushManager.share.ownerBag(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(app_thirdData.prefix(5)) + String(mainAssertValue.prefix(7))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.revenueMsg(showMsg: msg)
            //: popCurrentViewController()
            observerCalculate()
        }
    }
}

// MARK: - LabelViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension MotorViewDelegate: LabelViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func nearDirect() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (String(user_ageMsg) + String(const_displayTitle)), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: userWipeGoingFormat.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func timework() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.revenueMsg(showMsg: (String(data_errorUrl.suffix(6)) + "are can" + String(app_constitutionalContent)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (String(show_latValue)), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: main_framePath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.revenueMsg(showMsg: (String(data_errorUrl.suffix(6)) + "are can" + String(app_constitutionalContent)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (app_discoverStr.replacingOccurrences(of: "native", with: "re") + "Call"), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: userWipeGoingFormat.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.revenueMsg(showMsg: (String(userRefTransmissionName.suffix(5)) + "all ha" + String(userRouteTitle.prefix(7))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            HemSocketManager.shared.commentObject(info: [String(bytes: mainPictureLovelyMessage.reversed(), encoding: .utf8)!: (String(mainSunId.suffix(8))), String(bytes: show_storyKey.map{animalToday(history: $0)}, encoding: .utf8)!: [String(bytes: main_framePath.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        observerCalculate()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func my(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.flow(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func too(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.addOutput(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.cameraCos(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.modest()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func miniWindow() {
        //: TalkingSocketManager.shared.currPageVC = self
        HemSocketManager.shared.currPageVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = VideoMiniView.toView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.setWritten()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.botMode()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.strokeController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        observerCalculate()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func gallery() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension MotorViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func outfall() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.toOrigin(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func faceConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
