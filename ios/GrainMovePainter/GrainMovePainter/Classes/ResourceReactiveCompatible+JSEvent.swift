
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let data_previousValue:[UInt8] = [0xc6,0xd4,0xd3,0xc7,0xd8,0xd4,0xc6,0xce,0xc5,0xd9,0xd8,0xc3,0xd5,0xce,0xc3,0xd4,0xd7,0xc3,0xd4,0xc2,0xd9,0xd2,0xde,0xd8,0xdf,0xce,0xdf,0xde,0xc5,0xd8,0xd7,0xd8,0xd2,0xd0,0xc5,0xd8,0xde,0xdf]

private func formalFormer(normal num: UInt8) -> UInt8 {
    return num ^ 145
}

/*: "goodsId" :*/
fileprivate let appNatureBelowUrl:String = "stage beauty expressiongoodsId"

/*: "source" :*/
fileprivate let kAmName:String = "sacceleraterce"

/*: "type" :*/
fileprivate let dataYearPath:[Character] = ["t","y","p","e"]

/*: "title" :*/
fileprivate let showTimingStr:String = "titlfield"

/*: "url" :*/
fileprivate let app_formerText:String = "urnarrow"

/*: "money" :*/
fileprivate let constShouldDismissFormat:String = "mwhetherey"

/*: "Other" :*/
fileprivate let data_collectionStr:String = "Otherpermission charm back aspect"

/*: "scene" :*/
fileprivate let show_dialogValue:String = "scinveste"

/*: "show" :*/
fileprivate let show_littlePath:[Character] = ["s","h","o","w"]

/*: "target" :*/
fileprivate let notiGoldName:[Character] = ["t","a","r","g","e","t"]

/*: "eventName" :*/
fileprivate let data_logicalValue:String = "eloadent"

/*: "jsonString" :*/
fileprivate let app_naturalKey:String = "J"
fileprivate let user_satisfyPath:String = "lay method stocksonS"

/*: "coin" :*/
fileprivate let k_observerAId:[Character] = ["c","o","i","n"]

/*: "uid" :*/
fileprivate let dataAllowUrl:String = "UID"

/*: "closeWeb" :*/
fileprivate let noti_exploreData:[Character] = ["c","l","o","s","e","W","e","b"]

/*: "未实现的js事件： :*/
fileprivate let const_geneticUrl:[Character] = ["未","实","现","的","j","s","\u{4e8b}"]
fileprivate let constConductData:String = "added："

/*: "PurchaseClick" :*/
fileprivate let mainSlimId:String = "Purwealthy explain wee"
fileprivate let show_planStr:String = "chasufficient"

/*: "Retry After or Go to " :*/
fileprivate let main_scrambleMsg:String = "Retrfirm spring illegal here"
fileprivate let app_eliteHoldMsg:String = "ter ago force build correlation"
fileprivate let mainGenderId:String = "to content handle pop gravity snap"

/*: "Feedback" :*/
fileprivate let noti_noticeStreetId:[Character] = ["F","e","e","d","b","a","c"]
fileprivate let appDriveCalledId:String = "filter"

/*: " to contact us" :*/
fileprivate let k_wastePeopleStr:[Character] = [" ","t","o"," ","c","o","n","t","a","c","t"," ","u","s"]

/*: "Apple" :*/
fileprivate let mainTicketWaterValue:[Character] = ["A","p","p","l","e"]

/*: " apple支付充值失败： :*/
fileprivate let notiBrandStatAnnouncementData:[Character] = [""," ","a","p","p","l","e"]
fileprivate let const_alterContent:String = "支\u{4ed8}充值失败："

/*: "payResultCallback();" :*/
fileprivate let data_regionValue:[Character] = ["p","a","y","R","e","s","u","l","t","C","a"]
fileprivate let data_actualValue:String = "LLBAC"

/*: "USD" :*/
fileprivate let user_windowName:String = "Usurvival"

/*: "amount" :*/
fileprivate let appPackageUrl:[UInt8] = [0xdb,0xd7,0xd5,0xcf,0xd4,0xce]

private func resolveAgree(off num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let user_weeFormat:[UInt8] = [0x29,0x65,0x75,0x72,0x74,0x28,0x73,0x75,0x74,0x61,0x74,0x53,0x74,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x74,0x6f,0x4e,0x6d,0x65,0x74,0x73,0x79,0x53,0x74,0x65,0x67]

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let const_warningMiddleUrl:[UInt8] = [0xf3,0xf1,0xe0,0xc7,0xed,0xe7,0xe0,0xf1,0xf9,0xda,0xfb,0xe0,0xfd,0xf2,0xfd,0xf7,0xf5,0xe0,0xfd,0xfb,0xfa,0xe0,0xc7,0xe0,0xf5,0xe0,0xe1,0xe7,0xbc,0xf2,0xf5,0xf8,0xe7,0xf1,0xbd]

/*: "mfBean" :*/
fileprivate let mainObtainName:[UInt8] = [0xdc,0xd7,0xf3,0xd4,0xd0,0xdf]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ResourceReactiveCompatible+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let show_valueLogData = NSNotification.Name(rawValue: String(bytes: data_previousValue.map{formalFormer(normal: $0)}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension ResourceReactiveCompatible {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func materialRefreshRefuse(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = CookieWalkSubqueryConvertible(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.sender(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            actionMakeup()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            succeedCorrupt(productId: json[(String(appNatureBelowUrl.suffix(7)))].stringValue, source: json[(kAmName.replacingOccurrences(of: "accelerate", with: "ou"))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(String(dataYearPath))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                userImageRejectId = notiDiskContent
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(String(dataYearPath))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                userImageRejectId = notiDiskContent
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            modifyReplacement(type: json[(String(dataYearPath))].stringValue, productId: json[(String(appNatureBelowUrl.suffix(7)))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            sustain(title: json[(showTimingStr.replacingOccurrences(of: "field", with: "e"))].stringValue, url: json[(app_formerText.replacingOccurrences(of: "narrow", with: "l"))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            costScene(price: Double(json[(constShouldDismissFormat.replacingOccurrences(of: "whether", with: "on"))].stringValue) ?? 0, payMode: (String(data_collectionStr.prefix(5))), scene: json[(show_dialogValue.replacingOccurrences(of: "invest", with: "en"))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            upConstraint(show: json[(String(show_littlePath))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            breachTab(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: LatPushManager.share.func__pushToPriveteChatVC(chatID: DetermineJoinReactiveCompatible.getCustomerServiceID())
            LatPushManager.share.generate(chatID: DetermineJoinReactiveCompatible.facilityRank())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(String(notiGoldName))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: LatPushManager.share.func__pushToSubscribePageWebVC()
                LatPushManager.share.selectReply()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = EncompassViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                raisinCookie()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                directManual(type: ComparisonOnsetTarget.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: kOnKey,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                directManual(type: ComparisonOnsetTarget.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                directManual(type: ComparisonOnsetTarget.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                directManual(type: ComparisonOnsetTarget.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = CigaretteDataSource()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                directManual(type: ComparisonOnsetTarget.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(String(dataYearPath))].intValue
            //: changeNotifaStatus(type: type)
            colorType(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            activeAgent()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            refuse(eventName: json[(data_logicalValue.replacingOccurrences(of: "load", with: "v") + "Name")].stringValue, jsonStr: json[(app_naturalKey.lowercased() + String(user_satisfyPath.suffix(4)) + "tring")].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            stater(coin: json[(String(k_observerAId))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            packageMoon()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            vibrate()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            PercentageManagerRequest.answerCompletion { _, _, _ in
                //: if SteppingMotorThen.share.loginUserMode.jumpType == 1 {
                if SteppingMotorThen.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.sender(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: LatPushManager.share.func__pushToRandomVideoVC(isBeginRand: false)
                        LatPushManager.share.native(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: LatPushManager.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            LatPushManager.share.generate(chatID: json[(dataAllowUrl.lowercased())].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: LatPushManager.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            LatPushManager.share.postKey(uid: json[(dataAllowUrl.lowercased())].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: LatPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            LatPushManager.share.iii(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: let view = TalkingRechargeDetentionView.init()
            let view = AppearView()
            //: view.dtype = json["type"].intValue
            view.dtype = json[(String(dataYearPath))].intValue
            //: view.closeWeb = json["closeWeb"].boolValue
            view.closeWeb = json[(String(noti_exploreData))].boolValue
            //: view.show()
            view.insidePlace()
            //: view.closWebClock = { [weak self] in
            view.closWebClock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: self.func__closeAction(sender: nil)
                self.sender(sender: nil)
            }

        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            belemUrl(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (String(const_geneticUrl) + constConductData.replacingOccurrences(of: "added", with: "件")) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func modifyReplacement(type _: String, productId: String, payType: EchoUnderType) {
        //: applePay(productId: productId, payType: payType)
        succeedCorrupt(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func succeedCorrupt(productId: String, source: Int = -1, payType: EchoUnderType) {
        //: if SteppingMotorThen.share.loginUid.isEmptyString {
        if SteppingMotorThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        AlsoAnalyticsManager.share.magnitude(name: (String(mainSlimId.prefix(3)) + show_planStr.replacingOccurrences(of: "sufficient", with: "se") + "Click"))

        //: ProgressHUD.show()
        DirectProgressHUD.discountShow()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        LabelManager.shared.beginAndHandle(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            DirectProgressHUD.thatDismiss()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    DirectProgressHUD.suggestToast((String(main_scrambleMsg.prefix(4)) + "y Af" + String(app_eliteHoldMsg.prefix(4)) + "or Go " + String(mainGenderId.prefix(3))) + "\"" + (String(noti_noticeStreetId) + appDriveCalledId.replacingOccurrences(of: "filter", with: "k")) + "\"" + (String(k_wastePeopleStr)).localized)
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    sweepUnderTheRug(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    costScene(price: reportMoney, payMode: (String(mainTicketWaterValue)), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(notiBrandStatAnnouncementData) + const_alterContent) + "\(status.rawValue)")
                }
            }
        }
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func sustain(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = MagnitudeercalateTalkingButton()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.detergentSaveer(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(slideBring), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(app_formerText.replacingOccurrences(of: "narrow", with: "l")): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func slideBring(button: MagnitudeercalateTalkingButton) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(app_formerText.replacingOccurrences(of: "narrow", with: "l"))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        funcUponRequest()
    }

    /// 退出登录
    //: private func needLogin() {
    private func actionMakeup() {
        //: guard Int(SteppingMotorThen.share.loginUid) ?? 0 > 0 else {
        guard Int(SteppingMotorThen.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: data_featureName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func sweepUnderTheRug(price: Double, payType: EchoUnderType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(data_regionValue) + data_actualValue.lowercased() + "k();")) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        costScene(price: price, payMode: (String(mainTicketWaterValue)), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func costScene(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appChangePath, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appAgeRecordText, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: notiDataText, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: noti_displayKey, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            AgentThen.share.marginalCost(price: price, currency: (user_windowName.replacingOccurrences(of: "survival", with: "SD")))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            AgentThen.share.ribbonCurrency(price: price, currency: (user_windowName.replacingOccurrences(of: "survival", with: "SD")))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        AlsoAnalyticsManager.share.purchaseCounterruptCurrency(payType: payMode, price: price, currency: (user_windowName.replacingOccurrences(of: "survival", with: "SD")))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if userImageRejectId.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            kSkipContent.mightHaveBeen(eventID: userImageRejectId, parameterStr: [String(bytes: appPackageUrl.map{resolveAgree(off: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: show_valueLogData, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func upConstraint(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func raisinCookie() {
        //: LatPushManager.share.func__pushUserVerifyController(toast: nil)
        LatPushManager.share.mouthSave(toast: nil)
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func directManual(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func colorType(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            PercentageManagerRequest.distributionCost { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            PercentageManagerRequest.answerCompletion { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            LabelMpThen.disableStar { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: user_weeFormat.reversed(), encoding: .utf8)! : String(bytes: const_warningMiddleUrl.map{$0^148}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func activeAgent() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        PercentageManagerRequest.answerCompletion { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if MaterialLiveManager.threadContext().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: userStyleOkTopPath, object: nil, userInfo: [String(bytes: mainObtainName.map{$0^177}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func stater(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            PercentageManagerRequest.missStep { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(SteppingMotorThen.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(SteppingMotorThen.share.loginUserMode.mf_coin)! + value
            //: SteppingMotorThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            SteppingMotorThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func packageMoon() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dataWarningKey, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func refuse(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        kSkipContent.coincideStr(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func vibrate() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
