
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constFilterUrl:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Voice greeting" :*/
fileprivate let data_whetherFormat:String = "temp tillVoice"
fileprivate let mainAlreadyTransportationUrl:String = "lessng"

/*: "Add" :*/
fileprivate let noti_taskId:String = "Addscience be guidance para"

/*: "type" :*/
fileprivate let userGiftData:String = "typzz"

/*: "addStatus" :*/
fileprivate let appWriteNearbyStr:String = "addStgap wealthy"

/*: "list" :*/
fileprivate let userGoalText:String = "lieventt"

/*: "audio" :*/
fileprivate let notiProduceerFormat:[Character] = ["a","u","d","i","o"]

/*: "length" :*/
fileprivate let constReadyValue:String = "ledocumentgth"

/*: "url" :*/
fileprivate let kFoodValue:[Character] = ["u","r","l"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let main_productId:[UInt8] = [0xc,0x2a,0x3c,0x3c,0x3a,0x2c,0x2c,0x39,0x2a,0x33,0x33,0x26,0x7f,0x3e,0x3b,0x3b,0x3a,0x3b,0x73,0x7f,0x2f,0x33,0x3a,0x3e,0x2c,0x3a,0x7f,0x28,0x3e,0x36,0x2b,0x7f,0x39,0x30,0x2d,0x7f,0x3e,0x2f,0x2f,0x2d,0x30,0x29,0x3e,0x33]

/*: "Are you sure to delete this message？" :*/
fileprivate let k_warnTitle:[UInt8] = [0x8a,0xbb,0xae,0x69,0xc2,0xb8,0xbe,0x69,0xbc,0xbe,0xbb,0xae,0x69,0xbd,0xb8,0x69,0xad,0xae,0xb5,0xae,0xbd,0xae,0x69,0xbd,0xb1,0xb2,0xbc,0x69,0xb6,0xae,0xbc,0xbc,0xaa,0xb0,0xae,0x38,0x5,0xe8]

fileprivate func chorusRegular(assert num: UInt8) -> UInt8 {
    let value = Int(num) + 183
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let appOfStr:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let userThinText:[Character] = ["D","e","l","e","t","e"]

/*: "Please wait for the audit result" :*/
fileprivate let user_cosPath:[UInt8] = [0x86,0xa2,0x9b,0x97,0xa9,0x9b,0x56,0xad,0x97,0x9f,0xaa,0x56,0x9c,0xa5,0xa8,0x56,0xaa,0x9e,0x9b,0x56,0x97,0xab,0x9a,0x9f,0xaa,0x56,0xa8,0x9b,0xa9,0xab,0xa2,0xaa]

fileprivate func mpWord(effect num: UInt8) -> UInt8 {
    let value = Int(num) + 202
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The upper limit has been reached" :*/
fileprivate let mainFireHistoryAddedTitle:[UInt8] = [0x6b,0x57,0x5a,0x1f,0x4a,0x4f,0x4f,0x5a,0x4d,0x1f,0x53,0x56,0x52,0x56,0x4b,0x1f,0x57,0x5e,0x4c,0x1f,0x5d,0x5a,0x5a,0x51,0x1f,0x4d,0x5a,0x5e,0x5c,0x57,0x5a,0x5b]

private func whetherRobot(outlet num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "filePath" :*/
fileprivate let const_talkName:String = "move fair markfilePath"

/*: "time" :*/
fileprivate let show_sustainUrl:String = "tianalyze"

/*: "#FF506D" :*/
fileprivate let constRegularBodyName:String = "more disagree finger#FF50"
fileprivate let noti_donName:[Character] = ["6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let noti_keepMessage:String = "btn_him entity"
fileprivate let const_weValue:[Character] = ["g","e","_","d","e","l","e","t","e","_","n","o","r"]

/*: "Currently mute" :*/
fileprivate let constGoingMsg:String = "measurement re validCurr"
fileprivate let noti_descriptionText:String = "accessible rescue meeting submity mute"

/*: "Edit Title" :*/
fileprivate let dataRatingUrl:String = "motivation iEd"
fileprivate let const_estimatedText:[Character] = ["i","t"," ","T","i","t","l","e"]

/*: "remark" :*/
fileprivate let app_ticketStr:[Character] = ["r","e","m","a","r"]
fileprivate let app_cleanNoPath:String = "into"

/*: "Click the button below to add\ncontent" :*/
fileprivate let constCityPath:[UInt8] = [0x8a,0xa5,0xa0,0xaa,0xa2,0xe9,0xbd,0xa1,0xac,0xe9,0xab,0xbc,0xbd,0xbd,0xa6,0xa7,0xe9,0xab,0xac,0xa5,0xa6,0xbe,0xe9,0xbd,0xa6,0xe9,0xa8,0xad,0xad,0xc3,0xaa,0xa6,0xa7,0xbd,0xac,0xa7,0xbd]

private func duringCustom(forehead num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let show_courseAverageStr:[UInt8] = [0xc,0x29,0x29,0x6d,0x20,0x38,0x21,0x39,0x24,0x3d,0x21,0x28,0x6d,0x3b,0x22,0x24,0x2e,0x28,0x6d,0x2a,0x3f,0x28,0x28,0x39,0x24,0x23,0x2a,0x3e,0x61,0x6d,0x2c,0x23,0x29,0x6d,0x39,0x25,0x28,0x6d,0x3e,0x34,0x3e,0x39,0x28,0x20,0x6d,0x3a,0x24,0x21,0x21,0x6d,0x3e,0x28,0x23,0x29,0x6d,0x3b,0x22,0x24,0x2e,0x28,0x6d,0x20,0x28,0x3e,0x3e,0x2c,0x2a,0x28,0x3e,0x6d,0x2f,0x34,0x6d,0x39,0x38,0x3f,0x23,0x3e,0x76,0x47,0xe,0x21,0x24,0x2e,0x26,0x6d,0x39,0x25,0x28,0x6d,0x2f,0x38,0x2f,0x2f,0x21,0x28,0x6d,0x39,0x22,0x6d,0x3d,0x21,0x2c,0x34,0x6d,0x39,0x25,0x28,0x6d,0x3b,0x22,0x24,0x2e,0x28,0x61,0x6d,0x3e,0x21,0x24,0x29,0x28,0x6d,0x39,0x22,0x6d,0x39,0x25,0x28,0x6d,0x3b,0x22,0x24,0x2e,0x28,0x6d,0x2c,0x3f,0x28,0x2c,0x6d,0x39,0x22,0x6d,0x29,0x28,0x21,0x28,0x39,0x28,0x6d,0x24,0x39,0x61,0x6d,0x2c,0x23,0x29,0x6d,0x39,0x25,0x28,0x6d,0x3b,0x22,0x24,0x2e,0x28,0x6d,0x39,0x24,0x39,0x21,0x28,0x6d,0x2e,0x2c,0x23,0x6d,0x2f,0x28,0x6d,0x28,0x29,0x24,0x39,0x28,0x29,0x63]

private func observerPlayer(processor num: UInt8) -> UInt8 {
    return num ^ 77
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SincePocketAudioVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class SincePocketAudioVc: BodyLatViewController {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: FairAudioView?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: AbbreviationThen?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: ComparisonView?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constFilterUrl.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(data_whetherFormat.suffix(5)) + " gree" + mainAlreadyTransportationUrl.replacingOccurrences(of: "less", with: "ti")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.originalAlways()
        //: designView()
        productRewardView()
        //: getAudioListData()
        current()
        //: setMutedDetection()
        system()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(jump(notification:)), name: noti_closeKey, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(mp(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(databaseByStage),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: noti_closeKey,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.guide()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [WishGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [WishGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(noti_taskId.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.opPoint(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.originInstance(colors: UIColor.o(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fee), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension SincePocketAudioVc {
    //: func getAudioListData() {
    func current() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(userGiftData.replacingOccurrences(of: "zz", with: "e"))] = 1
        //: ProgressHUD.show()
        DirectProgressHUD.discountShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        GenRequestTool.skinListCompletion(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            DirectProgressHUD.thatDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.mainOf()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(appWriteNearbyStr.prefix(5)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(userGoalText.replacingOccurrences(of: "event", with: "s"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [WishGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<WishGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func attitude(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(userGiftData.replacingOccurrences(of: "zz", with: "e"))] = 1
        //: dict["audio"] = data
        dict[(String(notiProduceerFormat))] = data
        //: dict["length"] = Int(time)
        dict[(constReadyValue.replacingOccurrences(of: "document", with: "n"))] = Int(time)

        //: ProgressHUD.show()
        DirectProgressHUD.discountShow()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        GenRequestTool.sequence(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            DirectProgressHUD.thatDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(String(kFoodValue))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.pointRate(showMsg: String(bytes: main_productId.map{$0^95}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.current()
                //: self.recordView?.dismiss()
                self.recordView?.history()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func precession(index: Int) {
        //: let config = ShowAlertConfig()
        let config = WhiteAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.opPoint(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        EnhanceAlertShow.columnPress(message: String(bytes: k_warnTitle.map{chorusRegular(assert: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(appOfStr)).localized, rightBtnTitle: (String(userThinText)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            EnhanceAlertShow.stripAlert()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            EnhanceAlertShow.stripAlert()
            //: self.stopPlay()
            self.databaseByStage()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: WishGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            GenRequestTool.inOneCase(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.current()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension SincePocketAudioVc {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func jump(notification _: NSNotification) {
        //: stopPlay()
        databaseByStage()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func mp(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            databaseByStage()
        }
    }

    //: @objc func addButtonClick() {
    @objc func fee() {
        //: guard TalkingSocketManager.shared.isPage == false else {
        guard HemSocketManager.shared.isPage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.revenueMsg(showMsg: showFileUrl)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        LabelMpThen.streetSmartFor(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.revenueMsg(showMsg: noti_ageMovieMsg)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.revenueMsg(showMsg: String(bytes: user_cosPath.map{mpWord(effect: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.revenueMsg(showMsg: String(bytes: mainFireHistoryAddedTitle.map{whetherRobot(outlet: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.databaseByStage()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = FairAudioView(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.awake()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(const_talkName.suffix(8)))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.attitude(data: data!, time: dict[(show_sustainUrl.replacingOccurrences(of: "analyze", with: "me"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension SincePocketAudioVc {
    //: func setMutedDetection() {
    func system() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        AgentMotorThen.shared.liveCloseicial()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        AgentMotorThen.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension SincePocketAudioVc: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = ComparisonView.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: ComparisonView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! ComparisonView

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.used(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.chemicalAgent(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.chemicalAgent(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            AgentMotorThen.shared.liveCloseicial()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.bean(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.databaseByStage()
            //: self.editTitle(index: indexPath.section)
            self.header(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(userThinText)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.precession(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(constRegularBodyName.suffix(5)) + String(noti_donName)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.precession(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.hr(name: (String(noti_keepMessage.prefix(4)) + "messa" + String(const_weValue))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = RenderImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func bean(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isPage else {
        guard !HemSocketManager.shared.isPage else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.revenueMsg(showMsg: showFileUrl)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.guide()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? ComparisonView
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.chemicalAgent(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.revenueMsg(showMsg: (String(constGoingMsg.suffix(4)) + "entl" + String(noti_descriptionText.suffix(6))).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = AbbreviationThen.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.guide()
        //: self.player?.play(urlStr: model.content)
        self.player?.recover(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? ComparisonView
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.chemicalAgent(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func header(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = MpViewDelegate(title: (String(dataRatingUrl.suffix(2)) + String(const_estimatedText)).localized, leftBtnTitle: (String(appOfStr)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.picturesqueness()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            DirectProgressHUD.discountShow()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: WishGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(String(app_ticketStr) + app_cleanNoPath.replacingOccurrences(of: "into", with: "k"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            GenRequestTool.pair(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DirectProgressHUD.thatDismiss()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func databaseByStage() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.guide()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.chemicalAgent(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension SincePocketAudioVc: AnalysisReactiveCompatible {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func spectrum(status: SpecialValueConvertible) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            databaseByStage()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            databaseByStage()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension SincePocketAudioVc {
    //: private func setupEmptyDataView() {
    private func mainOf() {
        //: var style = EmptyStyle()
        var style = EnhanceEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: constCityPath.map{duringCustom(forehead: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .originalAlways()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func productRewardView() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(ComparisonView.self, forCellReuseIdentifier: ComparisonView.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.uniformColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.promptReload(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: show_courseAverageStr.map{observerPlayer(processor: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
