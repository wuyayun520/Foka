
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainProgressTitle:[UInt8] = [0x7,0x0,0x7,0x1a,0x46,0xd,0x1,0xa,0xb,0x1c,0x54,0x47,0x4e,0x6,0xf,0x1d,0x4e,0x0,0x1,0x1a,0x4e,0xc,0xb,0xb,0x0,0x4e,0x7,0x3,0x1e,0x2,0xb,0x3,0xb,0x0,0x1a,0xb,0xa]

private func laterDocument(us num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "F5F5F5" :*/
fileprivate let const_productionValue:String = "f5f5f"
fileprivate let showResolveMessage:String = "round"

/*: "#128CFF" :*/
fileprivate let appBehindMsg:[Character] = ["#","1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let kPatronFormat:[Character] = ["F","e","e","d","b","a","c"]
fileprivate let data_modeAnniversaryId:String = "window"

/*: "#8370FF" :*/
fileprivate let constWeeklyData:String = "#8370"
fileprivate let show_compriseTitle:String = "ff"

/*: "Quick Greeting" :*/
fileprivate let constAThreadPath:String = "Quick ball program identical"
fileprivate let main_slipMessage:String = "TING"

/*: "Beauty Photos" :*/
fileprivate let mainSingleSpotExFormat:String = "Beauty dent resistance script"
fileprivate let noti_whyMsg:[Character] = ["P"]
fileprivate let k_suiteId:[Character] = ["h","o","t","o","s"]

/*: "url" :*/
fileprivate let notiAnimalMsg:String = "bodyl"

/*: "unlockGift" :*/
fileprivate let kArrowLogKey:[Character] = ["u","n","l","o","c","k","G","i","f"]
fileprivate let app_includeMessage:String = "quantity"

/*: "isBurn" :*/
fileprivate let dataPresentRegionMessage:[UInt8] = [0x4c,0x56,0x67,0x50,0x57,0x4b]

private func merelyWoman(technique num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "giftId" :*/
fileprivate let k_positiveFormat:[Character] = ["g","i","f","t","I"]
fileprivate let data_scopeTitle:String = "favorite"

/*: "rYMsgType" :*/
fileprivate let appLateName:String = "rYMsweek wait ply"

/*: "GJ:CallCustom" :*/
fileprivate let main_targetMsg:String = "future network enhance abs advanceGJ:Cal"

/*: "changeDirection" :*/
fileprivate let notiPitchId:String = "closedange"
fileprivate let appFormatHeelName:String = "tiogroup"

/*: "extra" :*/
fileprivate let userHelloFormat:String = "curvetra"

/*: "msgInfo" :*/
fileprivate let mainBridgeMessage:String = "msgInfoloose engage right going restriction"

/*: "toUid" :*/
fileprivate let kPromptFormat:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let noti_mysteryMessage:[Character] = ["w","a","n","t","G","i","f"]
fileprivate let showScreenName:[Character] = ["t"]

/*: "jumpKey" :*/
fileprivate let data_girlArrivalDonStr:String = "entity skin extra holderjumpKey"

/*: "truePersonAuth" :*/
fileprivate let notiLatePath:[Character] = ["t","r","u","e","P","e","r","s"]
fileprivate let data_generalContent:[Character] = ["o"]
fileprivate let k_pressName:[Character] = ["n","A","u","t","h"]

/*: "logType" :*/
fileprivate let showMapBlackPath:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let notiDetectTitle:[UInt8] = [0x99,0x87,0x93,0xa0,0x8d,0x84,0x91]

private func greenStandard(light num: UInt8) -> UInt8 {
    return num ^ 244
}

/*: "txt" :*/
fileprivate let appTextMessage:String = "speakt"

/*: "type" :*/
fileprivate let showPromptDataId:[Character] = ["t","y","p","e"]

/*: "QuickMsg" :*/
fileprivate let noti_detailGalleryValue:[Character] = ["Q","u","i","c","k","M","s"]
fileprivate let showEnjoyStr:String = "display"

/*: "params" :*/
fileprivate let constPlanAlreadyTitle:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "content" :*/
fileprivate let k_uniformValue:[UInt8] = [0x84,0x88,0x89,0x93,0x82,0x89,0x93]

private func mainSign(pin num: UInt8) -> UInt8 {
    return num ^ 231
}

/*: "txtId" :*/
fileprivate let appHeartBookTitle:String = "old remindertxtId"

/*: "message" :*/
fileprivate let notiThinkHiUrl:[UInt8] = [0xee,0xe6,0xf0,0xf0,0xe2,0xe4,0xe6]

/*: "TxtMsg" :*/
fileprivate let k_tickUniformText:String = "TxtMsgtrust fall"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let mainAdvertisingName:[UInt8] = [0x65,0x72,0x75,0x64,0x65,0x63,0x6f,0x72,0x50,0x20,0x2e,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x64,0x61,0x6f,0x6c,0x6e,0x77,0x6f,0x64,0x20,0x6f,0x6c,0x6c,0x65,0x68,0x20,0x65,0x63,0x69,0x6f,0x56]

/*: "length" :*/
fileprivate let main_nativeMsg:[Character] = ["l","e","n","g","t"]
fileprivate let showWhoUrl:String = "layer"

/*: "audio" :*/
fileprivate let app_noticeContent:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let show_activityName:String = "arobotio"
fileprivate let kOppositeContent:String = "Idsilver oval this session follow"

/*: "audioLength" :*/
fileprivate let constPracticeTitle:[UInt8] = [0x41,0x55,0x44,0x49,0x4f,0x2c,0x45,0x4e,0x47,0x54,0x48]

fileprivate func contactArcMust(root num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let appNorId:[Character] = ["a","u","d","i","o","S","a","n","d","b"]
fileprivate let kWhetherRevenueValue:String = "ochoose"

/*: "senderId" :*/
fileprivate let notiCrushStr:[UInt8] = [0x8,0x1e,0x15,0x1f,0x1e,0x9,0x32,0x1f]

private func greetSolar(should num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "AudioMsg" :*/
fileprivate let main_bodyFormat:String = "AudioMsgoutput before"

/*: "Can't send blank message" :*/
fileprivate let showCoordinateData:String = "Can\'t"
fileprivate let data_computerLetterToleranceKey:String = " senrid blind news talk section"
fileprivate let noti_forkName:String = "port samplek mes"

/*: "quoteMsgId" :*/
fileprivate let const_customerPath:String = "quover"
fileprivate let user_genderBassName:[Character] = ["e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let constWasteBasicValue:String = "another pointquoteMs"
fileprivate let mainPositionText:String = "age refuse your norgInfo"

/*: "contentType" :*/
fileprivate let show_openingMsg:[UInt8] = [0x9e,0x92,0x93,0x89,0x98,0x93,0x89,0xa9,0x84,0x8d,0x98]

private func titleureFuture(hurry num: UInt8) -> UInt8 {
    return num ^ 253
}

/*: "ImgMsg" :*/
fileprivate let kGapValue:String = "outside dateImgMsg"

/*: "img" :*/
fileprivate let data_listSpeedReminderFormat:String = "imdue"

/*: "imageUri" :*/
fileprivate let app_listenerText:[UInt8] = [0xfd,0xf9,0xf5,0xf3,0xf1,0xc1,0xe6,0xfd]

/*: "customSend" :*/
fileprivate let notiSpecialHourOppositeTitle:[UInt8] = [0xf5,0xe3,0xe5,0xe2,0xf9,0xfb,0xc5,0xf3,0xf8,0xf2]

private func jarLarge(conversation num: UInt8) -> UInt8 {
    return num ^ 150
}

/*: "pic" :*/
fileprivate let noti_operationValue:String = "pirid"

/*: "VideoMsg" :*/
fileprivate let constVideoMediaSearchStr:String = "VideoMsgwee direct recommend"

/*: "video" :*/
fileprivate let const_sizeContent:String = "videvoice"

/*: "coverImg" :*/
fileprivate let data_senseHourName:[UInt8] = [0xa6,0xaa,0xb3,0xa0,0xb7,0x8c,0xa8,0xa2]

private func attitudeTerms(res num: UInt8) -> UInt8 {
    return num ^ 197
}

/*: "videoUrl" :*/
fileprivate let notiNoteContent:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let dataMonthStr:[UInt8] = [0x8f,0x9e,0x99,0x8a,0x9f,0x82,0x84,0x85]

private func minimumSuccess(electronic num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "len" :*/
fileprivate let main_heelUrl:[UInt8] = [0x39,0x30,0x3b]

private func exceptLength(maker num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "Documents/User/voice/" :*/
fileprivate let app_pathMsg:String = "would here option doDocume"
fileprivate let data_ownUrl:String = "compare piece factor birder/v"

/*: "Documents/User/record/" :*/
fileprivate let mainProviderFormat:String = "Documewipe business lovely"
fileprivate let const_devoteMessage:[Character] = ["n","t","s","/","U","s","e","r","/"]
fileprivate let showSpeakStr:String = "next say pagerecord/"

/*: "msgIncome" :*/
fileprivate let dataAnyoneTitle:String = "thanks rowmsgIncome"

/*: "mfBean" :*/
fileprivate let notiTagPath:[UInt8] = [0x8f,0x84,0xa0,0x87,0x83,0x8c]

private func filingAwake(team num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "messageType" :*/
fileprivate let noti_wealthyNumbId:String = "yearessa"

/*: "WantGift" :*/
fileprivate let dataBeyondPath:String = "calendar behind correlation spread threeWant"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let dataMissText:String = "_Privaa remove maybe devote income"
fileprivate let main_likeStr:[Character] = ["p","s","T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let notiApplicationPath:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let userViewUrl:[Character] = ["t","o","t","a","l","M","f","C"]
fileprivate let appAtKey:[Character] = ["o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let userScheduleFormat:[Character] = ["a","t","t","a"]
fileprivate let app_neverData:String = "chSysMsganalysis mini"

/*: "intimatePhoto" :*/
fileprivate let show_mysteryText:String = "iesteemtima"

/*: "totalIntimate" :*/
fileprivate let userReadingMessage:[Character] = ["t","o","t","a","l","I"]
fileprivate let app_outputClaimTitle:String = "NTIMATE"

/*: "uid" :*/
fileprivate let const_weightMessage:[UInt8] = [0xf4,0xe8,0xe5]

private func hairLibrary(button num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "intimate" :*/
fileprivate let constForwardStr:[UInt8] = [0xb,0x10,0x16,0xb,0xf,0x3,0x16,0x7]

fileprivate func peopleReject(shrink num: UInt8) -> UInt8 {
    let value = Int(num) + 94
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let app_editorTitle:[UInt8] = [0xed,0xcf,0xc0,0x89,0xda,0x8e,0xdb,0xdd,0xcb,0x8e,0xda,0xc6,0xcb,0x8e,0xc8,0xdb,0xc0,0xcd,0xda,0xc7,0xc1,0xc0,0x8e,0xca,0xdb,0xcb,0x8e,0xda,0xc1,0x8e,0xd7,0xc1,0xdb,0xdc,0x8e,0xc2,0xcf,0xcd,0xc5,0x8e,0xc1,0xc8,0x8e,0xc7,0xc0,0xda,0xc7,0xc3,0xcf,0xcd,0xd7,0x8e,0xc2,0xcb,0xd8,0xcb,0xc2,0x80]

private func entryDense(illegal num: UInt8) -> UInt8 {
    return num ^ 174
}

/*: "giftNum" :*/
fileprivate let user_strokeMsg:String = "giftNumdisk small beat where"

/*: "pkgItemsetId" :*/
fileprivate let constOppositionTitle:String = "peasterng"
fileprivate let user_separationText:[Character] = ["I","t","e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let const_contentFinitePath:String = "frarraym"

/*: "wantGiftId" :*/
fileprivate let mainDevotePerformId:String = "raw arc outletwantGi"

/*: "WantGiftMsg" :*/
fileprivate let userAnswerValue:String = "WantGiftincrease take shall sub mirror"
fileprivate let dataResentWhatMultiText:String = "Msgresult alleged put"

/*: "gift" :*/
fileprivate let user_targetUrl:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let constSectionFormat:String = "press work certainView"
fileprivate let const_onlyId:String = "hidden raw library info"

/*: "Notes" :*/
fileprivate let showMemoryData:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let main_fallStr:[Character] = ["R","e","p","o","r"]
fileprivate let constReserveTitle:String = "flow"

/*: "View read receipts?" :*/
fileprivate let noti_applyValue:String = "work shallView "
fileprivate let userTotalerruptKey:String = "ECEI"

/*: "She" :*/
fileprivate let dataSuggestName:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let constCollectYouData:String = "point bind appearance brand program%@ is "
fileprivate let constHaveKey:String = "word topic too"
fileprivate let show_resistanceFormat:String = "gift"
fileprivate let const_creativeFormat:String = "ERSION"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let userMutualKey:String = "Shoot library pending mask global force"
fileprivate let appFoundationMsg:String = "panel target slim at l"
fileprivate let mainQuitId:String = " 5 span box report bucket"

/*: "Upload failed" :*/
fileprivate let noti_prepareId:String = "Uplokind heart"
fileprivate let user_equipmentKey:String = "lrecommendd"

/*: "播放状态: :*/
fileprivate let userCharacterMsg:String = "播放状\u{6001}:"

/*: "c2c_" :*/
fileprivate let user_personalMessage:String = "tract exit bringc2c_"

/*: "msg" :*/
fileprivate let showDebatePath:String = "msyesterday"

/*: "tips" :*/
fileprivate let main_devilText:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let appReinWriterStr:[UInt8] = [0x6c,0x6c,0x61,0x63,0x20,0x61,0x20,0x40,0x25,0x20,0x65,0x76,0x69,0x67,0x20,0x6f,0x74,0x20,0x65,0x72,0x75,0x74,0x61,0x65,0x66,0x20,0x74,0x61,0x68,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x6e,0x61,0x63,0x20,0x75,0x6f,0x59]

/*: "cellData" :*/
fileprivate let userMinuteTitle:String = "cellDagold threshold mine"
fileprivate let kLiveFormat:String = "seriesa"

/*: "System notification" :*/
fileprivate let constBackValue:String = "s"
fileprivate let appBrideText:[Character] = ["y","s","t","e","m"," ","n","o","t","i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ControlRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum PocketEpisodeFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ControlRecognizerDelegate: BodyLatViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = ComprehendReactiveCompatible()
    private var userWrap = ComprehendReactiveCompatible() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = PocketEpisodeFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ComprehendReactiveCompatible = ComprehendReactiveCompatible()) {
    init(chatID: String, isFrom: PocketEpisodeFromEnum = .Normal, userWrap: ComprehendReactiveCompatible = ComprehendReactiveCompatible()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ControlRecognizerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        GenChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == DetermineJoinReactiveCompatible.getXiaoMiID() {
        if targetId == DetermineJoinReactiveCompatible.anthologist() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == DetermineJoinReactiveCompatible.getCustomerServiceID() {
        } else if targetId == DetermineJoinReactiveCompatible.facilityRank() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainProgressTitle.map{laterDocument(us: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SteppingMotorThen.share.appConfigMode.disableShootScreen {
            if SteppingMotorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StrokeKeepThen(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        animationCollection()
        //: createUI()
        accelerate()
        //: createUIConstraints()
        ok()
        //: bindInteraction()
        bindAndAction()
        //: addTapGestureRecognizer()
        yellowConvert()
        //: func__requestChatInfo()
        originBy()
        //: func__getFillDraftText()
        totalry()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        WalkPlayerDelegate.shared.serviceMove()
        //: func__addVoiceNotification()
        property()
        //: addReadPeceiptNotification()
        satisfy()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            visibleCur()
            //: func__TipsNotification()
            geneticProfiling()
            //: if SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue {
            if SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue {
                //: func__setReplyTipsView()
                remainView()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        popular(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        popular(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            generateName()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            RequestRatingManager.shared.undercoverOperation()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: DetermineViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = DetermineViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (const_productionValue.uppercased() + showResolveMessage.replacingOccurrences(of: "round", with: "5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(WalkMessageCell.self, forCellReuseIdentifier: WalkMessageCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(SinceViewCell.self, forCellReuseIdentifier: SinceViewCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(FluViewCell.self, forCellReuseIdentifier: FluViewCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(ClothReactiveCompatible.self, forCellReuseIdentifier: ClothReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(DialogMessageCell.self, forCellReuseIdentifier: DialogMessageCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ThereChatThen.self, forCellReuseIdentifier: ThereChatThen.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(PowThen.self, forCellReuseIdentifier: PowThen.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(WhiteStatusMsgCell.self, forCellReuseIdentifier: WhiteStatusMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(FairViewCell.self, forCellReuseIdentifier: FairViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: RequestView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(CommentPlusViewCell.self, forCellReuseIdentifier: RequestView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: DirectorCurveErrorDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = DirectorCurveErrorDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.successBuild()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: FieldInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: SpendViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = FieldInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(appBehindMsg)))!, .font: UIFont.opPoint(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kPatronFormat) + data_modeAnniversaryId.replacingOccurrences(of: "window", with: "k")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(premiumRegeneration), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: UnderBotReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = UnderBotReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: CurveBirdSEyeViewReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = CurveBirdSEyeViewReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.opPoint(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (constWeeklyData.capitalized + show_compriseTitle.uppercased())), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(constAThreadPath.prefix(6)) + "Gree" + main_slipMessage.lowercased()).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.nameure().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(databaseColor), for: .touchUpInside)
        //: btn.isHidden = !SteppingMotorThen.share.appUserConfigMode.showDirectGreet || SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue || SteppingMotorThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !SteppingMotorThen.share.appUserConfigMode.showDirectGreet || SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue || SteppingMotorThen.share.appStatus == EchoTermConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: ViewThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = ViewThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: SpecifyView = {
        //: let v = TalkingSelectTranslationView()
        let v = SpecifyView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.speakerWithView(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: PeerGroupThen = {
        //: let v = TalkingTranslationResultView()
        let v = PeerGroupThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.adminModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.opPoint(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (constWeeklyData.capitalized + show_compriseTitle.uppercased())), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(mainSingleSpotExFormat.prefix(7)) + String(noti_whyMsg) + String(k_suiteId)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.nameure().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(locationClick), for: .touchUpInside)
        //: btn.isHidden = SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue || SteppingMotorThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue || SteppingMotorThen.share.appStatus == EchoTermConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: IncludingDataSource = {
        //: let v = TalkingLiveRoomGamesView()
        let v = IncludingDataSource()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: AgentHugeDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = AgentHugeDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: CapScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = CapScreenView()
        //: v.isHidden = (SteppingMotorThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (SteppingMotorThen.share.appStatus == EchoTermConvertible.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: MpView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = MpView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.below()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ControlRecognizerDelegate: SoupReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func speakerMid(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue {
                if SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue {
                if SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.attentionVoiceFuturism(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ControlRecognizerDelegate: WirePlaceUnderlyingDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func databaseColor() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        GenChatManager.guidanceSystem(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.revenueMsg(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.lab(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func locationClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        GenChatManager.state(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = GroundDataSource()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().salute(urlStr: dict?[(notiAnimalMsg.replacingOccurrences(of: "body", with: "ur"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(kArrowLogKey) + app_includeMessage.replacingOccurrences(of: "quantity", with: "t"))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = RocketModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: dataPresentRegionMessage.map{merelyWoman(technique: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(k_positiveFormat) + data_scopeTitle.replacingOccurrences(of: "favorite", with: "d"))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.befittingModel(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> PhoneMessageThen! {
    func hair(_: DetermineViewController!, reDelete data: V2TIMMessage!) -> PhoneMessageThen! {
        //: let dic = BotDialogChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = BotDialogChatManager.clever(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = CookieChatManager.effect(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: CellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(appLateName.prefix(4)) + "gType")].stringValue == (String(main_targetMsg.suffix(6)) + "lCustom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(notiPitchId.replacingOccurrences(of: "closed", with: "ch") + "Direc" + appFormatHeelName.replacingOccurrences(of: "group", with: "n"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(userHelloFormat.replacingOccurrences(of: "curve", with: "ex"))][(String(mainBridgeMessage.prefix(7)))][(String(kPromptFormat))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: BeatCellData.self) == true ||
        if cellData?.isKind(of: BeatCellData.self) == true ||
            //: cellData?.isKind(of: TakeCellData.self) == true ||
            cellData?.isKind(of: TakeCellData.self) == true ||
            //: cellData?.isKind(of: ChatCellData.self) == true ||
            cellData?.isKind(of: ChatCellData.self) == true ||
            //: cellData?.isKind(of: HashOutThen.self) == true {
            cellData?.isKind(of: HashOutThen.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            GenChatManager.layView(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            GenChatManager.statusApplication(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: PhoneMessageThen!) -> RequestView! {
    func with(_: DetermineViewController!, display data: PhoneMessageThen!) -> RequestView! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = globalOwner(data: data)

        //: if data.isKind(of: ChatCellData.self) {
        if data.isKind(of: ChatCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = WalkMessageCell(style: .default, reuseIdentifier: WalkMessageCell.className())
            //: let cellData = data as! ChatCellData
            let cellData = data as! ChatCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.present() {
                //: if cellData.msgModel.quoteMsgInfo.uid == SteppingMotorThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == SteppingMotorThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = SteppingMotorThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = SteppingMotorThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: TakeCellData.self) {
        if data.isKind(of: TakeCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = SinceViewCell(style: .default, reuseIdentifier: SinceViewCell.className())
            //: let cellData = data as! TakeCellData
            let cellData = data as! TakeCellData
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: HashOutThen.self) {
        if data.isKind(of: HashOutThen.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = FluViewCell(style: .default, reuseIdentifier: FluViewCell.className())
            //: let cellData = data as! HashOutThen
            let cellData = data as! HashOutThen
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AutomatonReactiveCompatible.self) {
        if data.isKind(of: AutomatonReactiveCompatible.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = ClothReactiveCompatible(style: .default, reuseIdentifier: ClothReactiveCompatible.className())
            //: let cellData = data as! AutomatonReactiveCompatible
            let cellData = data as! AutomatonReactiveCompatible
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: AlsoConferReactiveCompatible.self) {
        if data.isKind(of: AlsoConferReactiveCompatible.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = DialogMessageCell(style: .default, reuseIdentifier: DialogMessageCell.className())
            //: let cellData = data as! AlsoConferReactiveCompatible
            let cellData = data as! AlsoConferReactiveCompatible
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = MyGiftModel()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.funcRedFrom(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(noti_mysteryMessage) + String(showScreenName)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: BeatCellData.self) {
        if data.isKind(of: BeatCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = WhiteStatusMsgCell(style: .default, reuseIdentifier: WhiteStatusMsgCell.className())
            //: let cellData = data as! BeatCellData
            let cellData = data as! BeatCellData
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: CellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = PowThen(style: .default, reuseIdentifier: PowThen.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! CellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: CookieCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ThereChatThen(style: .default, reuseIdentifier: ThereChatThen.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! CookieCellData
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: LatCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = FairViewCell(style: .default, reuseIdentifier: FairViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! LatCellData
            //: cell.fill(with: cellData)
            cell.panWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: RequestView!) {
    func destroy(_: DetermineViewController!, description cell: RequestView!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != DetermineJoinReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != DetermineJoinReactiveCompatible.facilityRank() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: LatPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            LatPushManager.share.postKey(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: RequestView!) {
    func available(_: DetermineViewController!, full cell: RequestView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SinceViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! TakeCellData
            let imageCellData = cell.messageData as! TakeCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = EchoThen(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.gravityNo(imageCellData, metier: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.untilClick(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiBottomMsg)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.socialUnitView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.designMaximum(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                browse()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(noti_cornerKey)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = GenreModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ContentTheoryDialogMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [GenreModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = ControlDataSource(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                browse()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: FluViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! HashOutThen
            let videoCellData = cell.messageData as! HashOutThen
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = DialogCoordinatorViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                browse()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = DirectDrawingViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.handle(videoCellData, off: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.untilClick(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: notiBottomMsg)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.socialUnitView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.designMaximum(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                browse()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WhiteStatusMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WhiteStatusMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! BeatCellData, cellView: audioCell)
            play(cellData: audioCell.cellData as! BeatCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: WalkMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! WalkMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = BotDialogChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = BotDialogChatManager.clever(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(userHelloFormat.replacingOccurrences(of: "curve", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(data_girlArrivalDonStr.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(data_girlArrivalDonStr.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(notiLatePath) + String(data_generalContent) + String(k_pressName)) {
                    //: LatPushManager.share.func__pushUserVerifyController(toast: nil)
                    LatPushManager.share.mouthSave(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HemSpecialJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LatPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LatPushManager.share.policyLicense(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(appLateName.prefix(4)) + "gType")] as? String == (String(main_targetMsg.suffix(6)) + "lCustom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(mainBridgeMessage.prefix(7)))][(String(showMapBlackPath))].intValue == 1 {
                    //: checkAndCallVoice()
                    onicialVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    third()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ThereChatThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ThereChatThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = BotDialogChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = BotDialogChatManager.clever(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(userHelloFormat.replacingOccurrences(of: "curve", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(data_girlArrivalDonStr.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<HemSpecialJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: LatPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    LatPushManager.share.policyLicense(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: PowThen.self) {
            //: LatPushManager.share.func__pushToUserDetailVC(uid: targetId)
            LatPushManager.share.postKey(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: RequestView!) {
    func failure(_: DetermineViewController!, margin cell: RequestView!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: MpCapCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SinceViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! SinceViewCell
            //: cellData = imageCell.cellData as! TakeCellData
            cellData = imageCell.cellData as! TakeCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: FluViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! FluViewCell
            //: cellData = videoCell.cellData as! HashOutThen
            cellData = videoCell.cellData as! HashOutThen
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WhiteStatusMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WhiteStatusMsgCell
            //: cellData = audioCell.cellData as! BeatCellData
            cellData = audioCell.cellData as! BeatCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: WalkMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! WalkMessageCell
            //: cellData = textCell.cellData as! ChatCellData
            cellData = textCell.cellData as! ChatCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: PhoneMessageThen? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: PhoneMessageThen? = CookieChatManager.purchase(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: HashOutThen.self) {
                if reCellData!.isKind(of: HashOutThen.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    talk(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    funcToCast(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: PhoneMessageThen!) {
    func inscriptionDate(_: DetermineViewController!, handle cellData: PhoneMessageThen!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = GenChatManager.recognize(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: PhoneMessageThen!) {
    func linkPost(_: DetermineViewController!, gesture cellData: PhoneMessageThen!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        GenChatManager.hiddenMarginId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: PhoneMessageThen) -> PhoneMessageThen {
    private func globalOwner(data: PhoneMessageThen) -> PhoneMessageThen {
        //: let isSelf = data.msgModel.user.uid == Int(SteppingMotorThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(SteppingMotorThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if SteppingMotorThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if SteppingMotorThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = SteppingMotorThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = SteppingMotorThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = SteppingMotorThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = SteppingMotorThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func medianProgram() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (appTextMessage.replacingOccurrences(of: "speak", with: "tx"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(showPromptDataId)): (String(noti_detailGalleryValue) + showEnjoyStr.replacingOccurrences(of: "display", with: "g"))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.funcToCast(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func lab(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: k_uniformValue.map{mainSign(pin: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: k_uniformValue.map{mainSign(pin: $0)}, encoding: .utf8)!: content, (String(appHeartBookTitle.suffix(5))): greetId, String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (appTextMessage.replacingOccurrences(of: "speak", with: "tx"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: notiThinkHiUrl.map{$0^131}, encoding: .utf8)!: content, (String(appHeartBookTitle.suffix(5))): greetId, (String(showPromptDataId)): (String(k_tickUniformText.prefix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.funcToCast(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            ConferSessionDelegate.shared.tipAudio(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.revenueMsg(showMsg: String(bytes: mainAdvertisingName.reversed(), encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        GenChatManager.visible(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(main_nativeMsg) + showWhoUrl.replacingOccurrences(of: "layer", with: "h"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (String(app_noticeContent)),
                               //: "audioId": greetId,
                               (show_activityName.replacingOccurrences(of: "robot", with: "ud") + String(kOppositeContent.prefix(2))): greetId,
                               //: "audioLength": length,
                               String(bytes: constPracticeTitle.map{contactArcMust(root: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x")): model.unZipFilePath,
                               //: "senderId": SteppingMotorThen.share.loginUserMode.userID]
                               String(bytes: notiCrushStr.map{greetSolar(should: $0)}, encoding: .utf8)!: SteppingMotorThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(show_activityName.replacingOccurrences(of: "robot", with: "ud") + String(kOppositeContent.prefix(2))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: constPracticeTitle.map{contactArcMust(root: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(showPromptDataId)): (String(main_bodyFormat.prefix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.funcToCast(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func adminModel(msgStr: String, quoteModel: MedalFiniteModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            revenueMsg(showMsg: (showCoordinateData + String(data_computerLetterToleranceKey.prefix(4)) + "d blan" + String(noti_forkName.suffix(5)) + "sage").localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: k_uniformValue.map{mainSign(pin: $0)}, encoding: .utf8)!: temStr, String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (appTextMessage.replacingOccurrences(of: "speak", with: "tx"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: notiThinkHiUrl.map{$0^131}, encoding: .utf8)!: temStr, (String(showPromptDataId)): (String(k_tickUniformText.prefix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(mainBridgeMessage.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(const_customerPath.replacingOccurrences(of: "over", with: "ot") + String(user_genderBassName))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(constWasteBasicValue.suffix(7)) + String(mainPositionText.suffix(5)))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? MedalFiniteModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcToCast(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func befittingModel(img: UIImage, sendModel: RocketModel) {
        //: let imagePath = TakeCellData.getChatImagePath(image: img)
        let imagePath = TakeCellData.per(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: show_openingMsg.map{titleureFuture(hurry: $0)}, encoding: .utf8)!: (String(kGapValue.suffix(6))), String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (data_listSpeedReminderFormat.replacingOccurrences(of: "due", with: "g")), String(bytes: app_listenerText.map{$0^148}, encoding: .utf8)!: imagePath, String(bytes: notiSpecialHourOppositeTitle.map{jarLarge(conversation: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(noti_operationValue.replacingOccurrences(of: "rid", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(showPromptDataId)): (String(kGapValue.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataPresentRegionMessage.map{merelyWoman(technique: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_positiveFormat) + data_scopeTitle.replacingOccurrences(of: "favorite", with: "d")): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcToCast(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: PhoneMessageThen?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func talk(msgData: PhoneMessageThen?, sendModel: RocketModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: show_openingMsg.map{titleureFuture(hurry: $0)}, encoding: .utf8)!: (String(constVideoMediaSearchStr.prefix(8))), String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (const_sizeContent.replacingOccurrences(of: "voice", with: "o")), String(bytes: notiSpecialHourOppositeTitle.map{jarLarge(conversation: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(showPromptDataId)): (String(constVideoMediaSearchStr.prefix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: data_senseHourName.map{attitudeTerms(res: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: notiNoteContent.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: dataMonthStr.map{minimumSuccess(electronic: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataPresentRegionMessage.map{merelyWoman(technique: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_positiveFormat) + data_scopeTitle.replacingOccurrences(of: "favorite", with: "d")): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = CookieChatManager.keyWeight(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.center(since: uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.archeozoic(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.clear(cellData, showShare: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.archeozoic(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.detail(dict, data: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        funcToCast(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func funcWeekly(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (String(app_noticeContent)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: constPracticeTitle.map{contactArcMust(root: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x")): voicePath,
                       //: "senderId": SteppingMotorThen.share.loginUserMode.userID]
                       String(bytes: notiCrushStr.map{greetSolar(should: $0)}, encoding: .utf8)!: SteppingMotorThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (String(showPromptDataId)): (String(main_bodyFormat.prefix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: main_heelUrl.map{exceptLength(maker: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcToCast(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: PhoneMessageThen, reSend: Bool) {
    private func funcToCast(msgData: PhoneMessageThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            revenueMsg(showMsg: main_backgroundPath)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(kPromptFormat))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: HashOutThen.self) == false && msgData.isKind(of: AlsoConferReactiveCompatible.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: HashOutThen.self) == false, msgData.isKind(of: AlsoConferReactiveCompatible.self) == false, params[(String(showPromptDataId))] as? String != (String(noti_detailGalleryValue) + showEnjoyStr.replacingOccurrences(of: "display", with: "g")) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.center(since: msgData)
        }
        //: let recordTime = BotDialogChatManager.getMessageInsertTime()
        let recordTime = BotDialogChatManager.incur()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((noti_operationValue.replacingOccurrences(of: "rid", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(noti_operationValue.replacingOccurrences(of: "rid", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(noti_operationValue.replacingOccurrences(of: "rid", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(String(showPromptDataId))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(main_bodyFormat.prefix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(appNorId) + kWhetherRevenueValue.replacingOccurrences(of: "choose", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(app_pathMsg.suffix(6)) + "nts/Us" + String(data_ownUrl.suffix(4)) + "oice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = userTitleKey + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(mainProviderFormat.prefix(6)) + String(const_devoteMessage) + String(showSpeakStr.suffix(7)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = appConversationValue + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(show_activityName.replacingOccurrences(of: "robot", with: "ud") + String(kOppositeContent.prefix(2)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(app_noticeContent))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        TimeTestedRequestTool.stageTransaction(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(noti_detailGalleryValue) + showEnjoyStr.replacingOccurrences(of: "display", with: "g")) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.footing(msgData, customSystem: nil, render: NSNumber(value: 0), forefront: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.untilClick(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: const_explainId)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(appHeartBookTitle.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    GenChatManager.visible(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.shrink(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(mainBridgeMessage.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(dataAnyoneTitle.suffix(9)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(dataAnyoneTitle.suffix(9)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if MaterialLiveManager.threadContext().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: userStyleOkTopPath, object: nil, userInfo: [String(bytes: notiTagPath.map{filingAwake(team: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.pop(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(noti_wealthyNumbId.replacingOccurrences(of: "year", with: "m") + "geType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(noti_detailGalleryValue) + showEnjoyStr.replacingOccurrences(of: "display", with: "g")) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!] = (String(dataBeyondPath.suffix(4)) + "Gift")
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kSkipContent.coincideStr(eventID: notiAgentText)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(noti_detailGalleryValue) + showEnjoyStr.replacingOccurrences(of: "display", with: "g")) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!] = (appTextMessage.replacingOccurrences(of: "speak", with: "tx"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.center(since: updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(SteppingMotorThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(SteppingMotorThen.share.loginUserMode.userID)" + (String(dataMissText.prefix(6)) + "teChatTi" + String(main_likeStr)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if SteppingMotorThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.male.rawValue, type != (String(noti_detailGalleryValue) + showEnjoyStr.replacingOccurrences(of: "display", with: "g")) {
                //: self.removeReplyTipsView()
                self.trackDecide()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.footing(updateMsgData, customSystem: msgInfo[(String(notiApplicationPath))] as? String, render: msgIncome, forefront: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            GenChatManager.statusApplication(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(userViewUrl) + String(appAtKey))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(userViewUrl) + String(appAtKey))] as! NSNumber
                //: SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(userScheduleFormat) + String(app_neverData.prefix(8)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(userScheduleFormat) + String(app_neverData.prefix(8)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.saveTitle(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((show_mysteryText.replacingOccurrences(of: "esteem", with: "n") + "tePhoto")) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(show_mysteryText.replacingOccurrences(of: "esteem", with: "n") + "tePhoto")] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? StumbleModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.gravityNo(updateMsgData, metier: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((const_sizeContent.replacingOccurrences(of: "voice", with: "o"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(const_sizeContent.replacingOccurrences(of: "voice", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? SlideHypothesisModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.handle(updateMsgData, off: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        manager()

        //: updateSession()
        incite()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func incite() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        CookieInfoManager.with(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func untilClick(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard SteppingMotorThen.share.loginUserMode.status != 1 else {
            guard SteppingMotorThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    revenueMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: LatPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            LatPushManager.share.ownerBag(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.advancedDoing()
            //: SteppingMotorThen.share.loginUserMode.freeMsgTimes = ""
            SteppingMotorThen.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.successBuild()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = FluBulgeOutReactiveCompatible(frame: CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath))
            //: view.show()
            view.makeupNetwork()
            //: } else if errorCode == 60003, !SteppingMotorThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !SteppingMotorThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            FluThen.shared.generateBy()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                revenueMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func designMaximum(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(mainBridgeMessage.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.pop(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func early() {
        //: if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.magnitudery(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(SteppingMotorThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(SteppingMotorThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (notiMovieText.array(forKey: notiBusyValue)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.historyAnimation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                notiMovieText.set(intimateCacheData, forKey: notiBusyValue)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func sideSlip(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = BotDialogChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = BotDialogChatManager.clever(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(userHelloFormat.replacingOccurrences(of: "curve", with: "ex"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(mainBridgeMessage.prefix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(userReadingMessage) + app_outputClaimTitle.lowercased())]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.party(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func pop(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(userReadingMessage) + app_outputClaimTitle.lowercased())), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(userReadingMessage) + app_outputClaimTitle.lowercased())] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.party(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func party(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = CookieInfoManager.dealMp(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.magnitudery(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.totalimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.totalimateAssemblageAllSet(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.totalimateAssemblageAllSet(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        CookieInfoManager.via(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: const_weightMessage.map{hairLibrary(button: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: constForwardStr.map{peopleReject(shrink: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kShowKey,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ControlRecognizerDelegate: ConferThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func gapSecond() {
        //: guard SteppingMotorThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.utterness(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func narrowRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func spend(actionType: HugeEquatable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    draft()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    succeedPlay()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                draft()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if SteppingMotorThen.share.loginUserMode.sex == Gender.female.rawValue {
            if SteppingMotorThen.share.loginUserMode.sex == WalkMilitaryOutputStreamable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if SteppingMotorThen.share.loginUserMode.loungePlus {
                if SteppingMotorThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.sprechgesangTerminated(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                succeedPlay()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    eyeglasses()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    succeedPlay()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                eyeglasses()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.dentalApplianceMoon(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func succeedPlay() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.revenueMsg(showMsg: String(bytes: app_editorTitle.map{entryDense(illegal: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func output(giftModel _: MyGiftModel, giftNum _: String, model _: StatusStrokeModelType) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func centerNum(giftModel: MyGiftModel, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        funcRedFrom(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: AlsoConferReactiveCompatible?, fromStr: String?) {
    func funcRedFrom(giftModel: MyGiftModel, giftNum: String, cellData: AlsoConferReactiveCompatible?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            revenueMsg(showMsg: main_backgroundPath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(kPromptFormat))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(k_positiveFormat) + data_scopeTitle.replacingOccurrences(of: "favorite", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(user_strokeMsg.prefix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(constOppositionTitle.replacingOccurrences(of: "eastern", with: "k") + String(user_separationText))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(const_contentFinitePath.replacingOccurrences(of: "array", with: "o"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        TimeTestedRequestTool.busyCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.untilClick(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: k_foundErrorPath)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.decisionComplete(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.socialUnitView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.incite()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(mainBridgeMessage.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(userReadingMessage) + app_outputClaimTitle.lowercased())), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(userReadingMessage) + app_outputClaimTitle.lowercased())] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.party(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.keyMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kSkipContent.coincideStr(eventID: dataFormalMsg)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func assets(giftModel: MyGiftModel, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kSkipContent.coincideStr(eventID: userBusyUrl)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            revenueMsg(showMsg: main_backgroundPath)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= SteppingMotorThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            succeedPlay()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(mainDevotePerformId.suffix(6)) + "ftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(showPromptDataId))] = (String(userAnswerValue.prefix(8)) + String(dataResentWhatMultiText.prefix(3)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (String(dataBeyondPath.suffix(4)) + "Gift")]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(mainBridgeMessage.prefix(7))): msgInfo, String(bytes: constPlanAlreadyTitle.reversed(), encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = CookieChatManager.keyWeight(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcToCast(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func eEr(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(constProgressValue - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.dataConverterSizeBottom(true)
            self.msgView.dataConverterSizeBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func equity(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.dataConverterSizeBottom(true)
            self.msgView.dataConverterSizeBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func gestureDark(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.gestureDark(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                planAnimation(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func someDownward() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if WalkPlayerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            WalkPlayerDelegate.shared.aStopPlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func socialUnitView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(mainBridgeMessage.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!] = (String(user_targetUrl))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(mainBridgeMessage.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = CookieChatManager.keyWeight(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.center(since: cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(dataAnyoneTitle.suffix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(dataAnyoneTitle.suffix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.footing(cellData, customSystem: msgInfoDic[(String(notiApplicationPath))] as? String, render: msgIncome, forefront: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        mark(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(userViewUrl) + String(appAtKey))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(userViewUrl) + String(appAtKey))] as! NSNumber
            //: SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SteppingMotorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: SteppingMotorThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.disclike(needReload: false, mf_coin: SteppingMotorThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func visibleCur() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.request(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func mark(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.appropriate(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func progressRoot(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.uniformSignal(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func atMaximumId() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func onicialVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.decisionRecover { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.incite()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func third() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.underErase { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.incite()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func premiumRegeneration() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = LieEstimatedFeedbackVc()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        browse()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func destinationClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = AttributePopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.organiseList(cellTitleList: [(String(constSectionFormat.suffix(4)) + String(const_onlyId.suffix(5))).localized, (String(showMemoryData)).localized, (String(main_fallStr) + constReserveTitle.replacingOccurrences(of: "flow", with: "t")).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .quitTo()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: LatPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                LatPushManager.share.postKey(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                below()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = UnderViewDelegate(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.straddleResult(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func below() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = WhoThereNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.hostLess(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.family(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func dorsalVertebraCoatButtonClick() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            planAnimation(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            browse()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func solarNotification(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = CoordinatorModelType.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.removeQuick(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func satisfy() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(global(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kTextStatusName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(consolidate),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: noti_displayKey,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func global(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard SteppingMotorThen.share.loginUserMode.loungePlus == true else { return }
        guard SteppingMotorThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func consolidate() {
        //: SteppingMotorThen.share.loginUserMode.loungePlus = true
        SteppingMotorThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func announcementLabel() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? PhoneMessageThen else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? PhoneMessageThen else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if GenChatManager.empower(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.version(tipStr: (String(noti_applyValue.suffix(5)) + "read r" + userTotalerruptKey.lowercased() + "pts?").localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ControlRecognizerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func draft() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        LabelMpThen.rangeDoing(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = SpendBassPickTool.systemVideo(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.demonstrateMap(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func livePhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        LabelMpThen.commitBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.demonstrateMap(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func demonstrateMap(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = RocketModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.befittingModel(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = ControlDataSource(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = RocketModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.befittingModel(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        browse()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func eyeglasses() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard GenChatManager.switcherVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == WalkMilitaryOutputStreamable.male.rawValue ? "He".localized : (String(dataSuggestName)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.revenueMsg(showMsg: (String(constCollectYouData.suffix(6)) + "using" + String(constHaveKey.suffix(4)) + " low a " + show_resistanceFormat.replacingOccurrences(of: "gift", with: "v") + const_creativeFormat.lowercased()).publicTransport(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.longshot()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func longshot() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = SpendBassPickTool.systemVideo(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            SpendBassPickTool.low(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.visitorBack(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.revenueMsg(showMsg: (String(userMutualKey.prefix(6)) + "video" + String(appFoundationMsg.suffix(5)) + "east" + String(mainQuitId.prefix(4)) + "econds").localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func visitorBack(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: GenEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PocketViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = RocketModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.talk(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        DirectProgressHUD.thatDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        browse()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: PhoneMessageThen, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func archeozoic(msgData: PhoneMessageThen, sendModel: RocketModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ConferInsertListener.shared.whitehead(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.revenueMsg(showMsg: (String(noti_prepareId.prefix(4)) + "ad fai" + user_equipmentKey.replacingOccurrences(of: "recommend", with: "e")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.footing(msgData, customSystem: nil, render: NSNumber(value: 0), forefront: .Msg_Status_Fail)
                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.talk(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    //: func func__addVoiceNotification() {
    func property() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(nameChemicalAgent),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(appBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(ond(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(appearance(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: app_greetKey,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(sideSlip(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: dataCloseValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func popular(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            WalkPlayerDelegate.shared.serviceMove()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            WalkPlayerDelegate.shared.us()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func nameChemicalAgent() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        popular(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func appBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        popular(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func ond(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            popular(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            popular(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: BeatCellData, cellView: TalkingChatAudioMsgCell) {
    func play(cellData: BeatCellData, cellView: WhiteStatusMsgCell) {
        //: guard TalkingSocketManager.shared.isPage == false else {
        guard HemSocketManager.shared.isPage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.revenueMsg(showMsg: showFileUrl)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? PhoneMessageThen)!.isKind(of: BeatCellData.self) == false {
            if (indexModel as? PhoneMessageThen)!.isKind(of: BeatCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! BeatCellData
            let audioModel = indexModel as! BeatCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = CookieInfoManager.audio(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        WalkPlayerDelegate.shared.aStopPlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if WalkPlayerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (userCharacterMsg) + "\(WalkPlayerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if WalkPlayerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                WalkPlayerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        WalkPlayerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        WalkPlayerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: PhoneMessageThen = self.msgView.uiMsgs.object(at: index) as! PhoneMessageThen
                let subModel: PhoneMessageThen = self.msgView.uiMsgs.object(at: index) as! PhoneMessageThen
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: BeatCellData.self) {
                if subModel.isKind(of: BeatCellData.self) {
                    //: let audioModel = subModel as! BeatCellData
                    let audioModel = subModel as! BeatCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = CookieInfoManager.audio(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    FairThen.motor(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        WalkPlayerDelegate.shared.choke(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func originBy() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.funcCostWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.lab(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? PhoneMessageThen
                let indexModel = model as? PhoneMessageThen
                //: if indexModel?.isKind(of: BeatCellData.self) == true || indexModel?.isKind(of: TakeCellData.self) == true || indexModel?.isKind(of: ChatCellData.self) == true || indexModel?.isKind(of: HashOutThen.self) == true {
                if indexModel?.isKind(of: BeatCellData.self) == true || indexModel?.isKind(of: TakeCellData.self) == true || indexModel?.isKind(of: ChatCellData.self) == true || indexModel?.isKind(of: HashOutThen.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.manager()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.wash()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.announcementLabel()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func funcCostWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.underStack(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.corner(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.early()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == SteppingMotorThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == SteppingMotorThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == SteppingMotorThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == SteppingMotorThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.family(userWrap)
    }

    //: func func__getFillDraftText() {
    func totalry() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_personalMessage.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.gapSecond()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func generateName() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_personalMessage.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    //: func func__TipsNotification() {
    func geneticProfiling() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(atOk(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: app_halfMessage,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func atOk(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: CookieCellData = userinfo[(showDebatePath.replacingOccurrences(of: "yesterday", with: "g"))] as! CookieCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.dataConverterSizeBottom(true)
        self.msgView.dataConverterSizeBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func version(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: k_uniformValue.map{mainSign(pin: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: notiDetectTitle.map{greenStandard(light: $0)}, encoding: .utf8)!: (String(main_devilText))]
        //: var message: PhoneMessageThen?
        var message: PhoneMessageThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = CookieChatManager.charm(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = CookieChatManager.keyWeight(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.dataConverterSizeBottom(true)
        self.msgView.dataConverterSizeBottom(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func wash() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        GenChatManager.customerBringHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.version(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func saveTitle(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.version(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func manager() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        GenChatManager.physicsLab(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.version(tipStr: String(bytes: appReinWriterStr.reversed(), encoding: .utf8)!.publicTransport(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func appearance(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: PhoneMessageThen = dict?["cellData"] as! PhoneMessageThen
        let cellData: PhoneMessageThen = dict?[(String(userMinuteTitle.prefix(6)) + kLiveFormat.replacingOccurrences(of: "series", with: "t"))] as! PhoneMessageThen
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.dataConverterSizeBottom(true)
        self.msgView.dataConverterSizeBottom(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ControlRecognizerDelegate: ReliableThen {
    //: func func__setReplyTipsView() {
    func remainView() {
        //: let key =  "\(SteppingMotorThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(SteppingMotorThen.share.loginUserMode.userID)" + (String(dataMissText.prefix(6)) + "teChatTi" + String(main_likeStr)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.setUponSight()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            speakerMid(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            trackDecide()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func belowDot(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        adminModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        trackDecide()
    }

    //: func removeReplyTipsView() {
    func trackDecide() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            speakerMid(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func animationCollection() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: main_contentStorageWarningMsg, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: dataViewPath - constProgressValue, width: dataShowDetailPopMessage, height: dataViewPath)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: dataShowDetailPopMessage,
                                     //: height: ScreenHeight)
                                     height: dataViewPath)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func planAnimation(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.strokeController() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: DirectViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: dataViewPath - constProgressValue,
                                     //: width: ScreenWidth,
                                     width: dataShowDetailPopMessage,
                                     //: height: ScreenHeight)
                                     height: dataViewPath)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ControlRecognizerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func accelerate() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (constBackValue.uppercased() + String(appBrideText)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func ok() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? constProgressValue : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.vernalEquinox(width: contentView.width,
                                      //: height: contentView.height,
                                      height: contentView.height,
                                      //: corners: [.topLeft, .topRight],
                                      corners: [.topLeft, .topRight],
                                      //: cornerRadii: .init(width: 8, height: 8))
                                      cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? userLastMessage : show_turnStr)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + k_depthUrl)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? userLastMessage : show_turnStr)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + k_depthUrl)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + k_depthUrl)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func bindAndAction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.dorsalVertebraCoatButtonClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.destinationClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.spend(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kSkipContent.coincideStr(eventID: notiWindowContent, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
