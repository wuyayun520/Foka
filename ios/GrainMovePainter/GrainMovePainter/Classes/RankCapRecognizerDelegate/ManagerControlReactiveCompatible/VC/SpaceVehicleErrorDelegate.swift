
//: Declare String Begin

/*: "cmd" :*/
fileprivate let notiContactData:[UInt8] = [0xc9,0xd3,0xca]

fileprivate func claimYet(put num: UInt8) -> UInt8 {
    let value = Int(num) + 154
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let showExtentPickUrl:[Character] = ["c","a"]
fileprivate let mainResourceContent:String = "nlivingl"

/*: "data" :*/
fileprivate let user_finiteId:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let mainOffRealizeForeheadMessage:String = "complete clean method loglogId"

/*: "#322870" :*/
fileprivate let main_stickText:[Character] = ["#","3","2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let const_distinguishMessage:String = "Tap please hearing"
fileprivate let constStockTitle:String = "in medal arrow asset doubleart "
fileprivate let app_evaluatePeopleStr:String = "vvid"
fileprivate let kLiftValue:[Character] = ["d","e","o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let show_quoteStr:[Character] = ["i","c","o","n","_","c","l"]
fileprivate let appStickValue:String = "againsts"
fileprivate let k_bottomExitAdditionalFormat:[Character] = ["e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let constSpreadStr:[Character] = ["i","c","o","n","_","v"]
fileprivate let appResidentManagerWrittenId:[Character] = ["i","e","w","s","_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let noti_genderFormat:[Character] = ["%","@"," ","c","h"]
fileprivate let const_platKey:[Character] = ["a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let main_tradeMessage:String = "icon_lmanager information"

/*: "icon_rank_coin" :*/
fileprivate let mainSuggestText:String = "icoformal"
fileprivate let user_aspectPureValue:String = "k_coinactual double found layer"

/*: "vip_btn_top" :*/
fileprivate let constNumbererruptMessage:String = "virow"
fileprivate let k_eventDefinitionPath:String = "_btnsuper privacy"

/*: "btn_nearby_settings_nor" :*/
fileprivate let kCorrectUrl:[Character] = ["b","t","n","_","n","e","a","r","b","y"]
fileprivate let appNetPath:String = "_setaccessible giving service"
fileprivate let show_procedureTitle:String = "_norcommit pic response clear"

/*: "%@ Coins/min" :*/
fileprivate let data_recessKey:String = "anyone series convert reduce%@ Coi"

/*: "get json error" :*/
fileprivate let show_noticeMsg:[Character] = ["g","e","t"," ","j","s","o","n"," ","e"]
fileprivate let app_lessFormStr:String = "objectobjectoobject"

/*: "matchId" :*/
fileprivate let user_mineSumUrl:String = "matchIdcontext be fall relation"

/*: "errno" :*/
fileprivate let notiGovernKey:[UInt8] = [0x24,0x31,0x31,0x2d,0x2e]

fileprivate func replyAllRecognize(ally num: UInt8) -> UInt8 {
    let value = Int(num) - 191
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let user_shotMsg:String = "VIPFeincrease application"
fileprivate let notiToneFormat:String = "come"

/*: "Matching now" :*/
fileprivate let constMissingPath:String = "end currentlyMatch"

/*: "userTouch" :*/
fileprivate let app_stillStr:String = "admission evaluate refrigeratoruserT"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let k_orientationName:String = "mf/frrepresent edge destroy media outlet"
fileprivate let const_knockMsg:String = "no press processor straightll/get"
fileprivate let show_presentPath:[Character] = ["o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let k_defineStr:[UInt8] = [0xe9,0xfd,0xe0,0xe2,0xdb,0xf6,0xff,0xea]

private func doingceSecret(volume num: UInt8) -> UInt8 {
    return num ^ 143
}

/*: "You've been barred from receiving calls" :*/
fileprivate let user_launchName:[UInt8] = [0x92,0xa4,0xbe,0xec,0xbd,0xae,0xeb,0xa9,0xae,0xae,0xa5,0xeb,0xa9,0xaa,0xb9,0xb9,0xae,0xaf,0xeb,0xad,0xb9,0xa4,0xa6,0xeb,0xb9,0xae,0xa8,0xae,0xa2,0xbd,0xa2,0xa5,0xac,0xeb,0xa8,0xaa,0xa7,0xa7,0xb8]

/*: "source" :*/
fileprivate let k_directMapExtendValue:[UInt8] = [0x3e,0x3a,0x40,0x3d,0x2e,0x30]

fileprivate func harvestVoice(comply num: UInt8) -> UInt8 {
    let value = Int(num) + 53
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let kConvertStr:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let constAskStr:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let userThemPath:String = "frcur"
fileprivate let kIonStr:[Character] = ["m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let kWomanArcName:String = "requesbring"
fileprivate let const_refLengthText:[Character] = ["C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let app_flatTitle:[Character] = ["o","n","V","i","d"]
fileprivate let constKitPath:String = "eoMatchimport roll partner attractive"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let const_packUrl:[UInt8] = [0x67,0x6e,0x69,0x6d,0x6f,0x63,0x6e,0x69,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x76,0x69,0x65,0x63,0x65,0x52,0x20,0x5d,0x6f,0x65,0x64,0x69,0x76,0x20,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41,0x5b]

/*: "refuseCall" :*/
fileprivate let userImmediateTailVersionStr:String = "resmarts"

/*: "onCancelCall" :*/
fileprivate let const_cribMessage:String = "onCancbrain remove bounce"
fileprivate let noti_windowFormat:String = "task behavior situation candidelCall"

/*: "retryAfter" :*/
fileprivate let notiEnjoySqueezeMsg:String = "retryAworker new minimize submit"

/*: "onRequestCall" :*/
fileprivate let dataClearStr:[Character] = ["o"]
fileprivate let show_whiteMsg:String = "remain examinenReq"

/*: "failType" :*/
fileprivate let dataPositiveName:String = "shape own creationfailType"

/*: "callWaitingTime" :*/
fileprivate let notiExceptMessage:String = "decrease capacity idea clipcallW"
fileprivate let show_arrayData:[Character] = ["a","i","t","i","n","g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let show_writeStr:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let constBirdBrainData:String = "Free 1attach brow try"
fileprivate let user_presentTranslateValue:String = "raw equity min"

/*: "%@/min" :*/
fileprivate let main_mysteryStr:String = "%@/minparent insurance format strength"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SpaceVehicleErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class SpaceVehicleErrorDelegate: BodyLatViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.freshPlay()
            }
        }
        //: self.resetCamera()
        self.sinceDoing()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        prepareGreet()
        //: setupSubViewsConstraint()
        moonOffConstraint()
        //: bindInteraction()
        bindFrom()
        //: videoManager.startPreview(isMatch: true)
        videoManager.jumpDoing(isMatch: true)
        //: setPlayer(isRandon: false)
        springtime(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            takeIcon()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            HemSocketManager.shared.commentObject(info: [String(bytes: notiContactData.map{claimYet(put: $0)}, encoding: .utf8)!: (String(showExtentPickUrl) + mainResourceContent.replacingOccurrences(of: "living", with: "ce") + "Call"), String(bytes: user_finiteId.reversed(), encoding: .utf8)!: [(String(mainOffRealizeForeheadMessage.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        acceptJust()
        //: if TalkingSocketManager.shared.isPage == false {
        if HemSocketManager.shared.isPage == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            HemSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            HemSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            HemSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            HemSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: VerseCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = VerseCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = BassChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .quitTo()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(takeIcon), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: dataShowDetailPopMessage + 1, height: dataViewPath + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(main_stickText)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(const_distinguishMessage.prefix(4)) + "to st" + String(constStockTitle.suffix(4)) + app_evaluatePeopleStr.replacingOccurrences(of: "vid", with: "i") + String(kLiftValue)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.hr(name: (String(show_quoteStr) + appStickValue.replacingOccurrences(of: "against", with: "o") + String(k_bottomExitAdditionalFormat))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(stepExposure), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: MagnitudeercalateTalkingButton = {
        //: let btn = TalkingButton()
        let btn = MagnitudeercalateTalkingButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.hr(name: (String(constSpreadStr) + String(appResidentManagerWrittenId))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.hr(name: (String(constSpreadStr) + String(appResidentManagerWrittenId))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.opPoint(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(noti_genderFormat) + String(const_platKey)).publicTransport(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.hr(name: (String(main_tradeMessage.prefix(6)) + "ounge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.hr(name: (mainSuggestText.replacingOccurrences(of: "formal", with: "n") + "_ran" + String(user_aspectPureValue.prefix(6))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.opPoint(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.hr(name: (constNumbererruptMessage.replacingOccurrences(of: "row", with: "p") + String(k_eventDefinitionPath.prefix(4)) + "_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: MagnitudeercalateTalkingButton = {
        //: let btn = TalkingButton()
        let btn = MagnitudeercalateTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(enterAsk), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.hr(name: (String(kCorrectUrl) + String(appNetPath.prefix(4)) + "tings" + String(show_procedureTitle.prefix(4)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.hr(name: (mainSuggestText.replacingOccurrences(of: "formal", with: "n") + "_ran" + String(user_aspectPureValue.prefix(6))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .colorObserver()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(data_recessKey.suffix(6)) + "ns/min").publicTransport(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.opPoint(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: FavoriteReactiveCompatible = //: return FavoriteReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension SpaceVehicleErrorDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func springtime(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = EpisodeThen.default.noGoArea(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(show_noticeMsg) + app_lessFormStr.replacingOccurrences(of: "object", with: "r")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func vacateErrno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(user_mineSumUrl.prefix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: notiContactData.map{claimYet(put: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: notiGovernKey.map{replyAllRecognize(ally: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ManagerControlReactiveCompatible.counterest(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.totalCount()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(user_mineSumUrl.prefix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func signFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ManagerControlReactiveCompatible.discourse { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(user_shotMsg.prefix(5)) + notiToneFormat.replacingOccurrences(of: "come", with: "e"))].intValue
            //: self.refreshVideoCallPrice()
            self.freshPlay()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension SpaceVehicleErrorDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func enterAsk() {
        //: LatPushManager.share.func__pushToSubscribeAlert()
        LatPushManager.share.ordinalNumber()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func stepExposure() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        gravityMargin()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kSkipContent.coincideStr(eventID: k_ofName)

        //: if SteppingMotorThen.share.appConfigMode.displayMatchV4PopUp, SteppingMotorThen.share.loginUserMode.freeCallTimes > 0,
        if SteppingMotorThen.share.appConfigMode.displayMatchV4PopUp, SteppingMotorThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.linguisticContext()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        strokeController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func takeIcon() {
        //: guard TalkingSocketManager.shared.isPage == false else {
        guard HemSocketManager.shared.isPage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.revenueMsg(showMsg: showFileUrl)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        subsequentlyClean()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(constMissingPath.suffix(5)) + "ing now").localized
        //: setPlayer(isRandon: true)
        springtime(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if SteppingMotorThen.share.loginUserMode.randomVideoType == 2 {
        if SteppingMotorThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kSkipContent.coincideStr(eventID: app_availableMsg)
            //: req_getVideoInfo()
            semblance()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kSkipContent.coincideStr(eventID: mainSoundValue)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            HemSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            HemSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            HemSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            HemSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            primary()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func primary(cmd: String = (String(app_stillStr.suffix(5)) + "ouch"), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        acceptJust()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        vacateErrno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func totalCount() {
        // 停止定时器
        //: destroyWaitingTimer()
        acceptJust()
        //: showCloseBtn()
        gravityMargin()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(const_distinguishMessage.prefix(4)) + "to st" + String(constStockTitle.suffix(4)) + app_evaluatePeopleStr.replacingOccurrences(of: "vid", with: "i") + String(kLiftValue)).localized
        //: setPlayer(isRandon: false)
        springtime(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isPage == false {
        if HemSocketManager.shared.isPage == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            HemSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            HemSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            HemSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            HemSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        freshPlay()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func acceptJust() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func semblance() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(k_orientationName.prefix(5)) + "eeCa" + String(const_knockMsg.suffix(6)) + "Vide" + String(show_presentPath))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: k_defineStr.map{doingceSecret(volume: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.totalCount()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: SteppingMotorThen.share.loginUserMode.randomVideoType = 1
                    SteppingMotorThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.takeIcon()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.revenueMsg(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = HemModel.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = LatThen()
                //: player.setMute(bEnable: true)
                player.top(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.clearView(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                RobotManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                LabelMpThen.noWait { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.faceBy()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func faceBy() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kSkipContent.coincideStr(eventID: notiPlainStr)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard RobotManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.revenueMsg(showMsg: String(bytes: user_launchName.map{$0^203}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            RobotManager.shared.chronologicalRecord(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.totalCount()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = BeObjectProtocol()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.strokeController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.totalCount()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func subsequentlyClean() {
        //: if SteppingMotorThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if SteppingMotorThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(SteppingMotorThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(SteppingMotorThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func gravityMargin() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func sinceDoing() {
        //: guard TalkingSocketManager.shared.isPage == false else {
        guard HemSocketManager.shared.isPage == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.jumpDoing()
        }
    }
}

// MARK: - MilitaryMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension SpaceVehicleErrorDelegate: MilitaryMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func capture(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = DrawingHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(user_mineSumUrl.prefix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: k_directMapExtendValue.map{harvestVoice(comply: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: kConvertStr.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: constAskStr.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = HangCount.randow
        //: if SteppingMotorThen.share.loginUserMode.freeCallTimes > 0 && SteppingMotorThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if SteppingMotorThen.share.loginUserMode.freeCallTimes > 0 && SteppingMotorThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (userThemPath.replacingOccurrences(of: "cur", with: "o") + String(kIonStr)))
            //: TalkingSocketManager.shared.isFreeCall = true
            HemSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: notiContactData.map{claimYet(put: $0)}, encoding: .utf8)!: (kWomanArcName.replacingOccurrences(of: "bring", with: "t") + String(const_refLengthText)), String(bytes: user_finiteId.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        HemSocketManager.shared.commentObject(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            HemSocketManager.shared.commentObject(info: [String(bytes: notiContactData.map{claimYet(put: $0)}, encoding: .utf8)!: (String(showExtentPickUrl) + mainResourceContent.replacingOccurrences(of: "living", with: "ce") + "Call"), String(bytes: user_finiteId.reversed(), encoding: .utf8)!: [(String(mainOffRealizeForeheadMessage.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.primary(cmd: (String(app_flatTitle) + String(constKitPath.prefix(7))))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func capacityData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(mainOffRealizeForeheadMessage.suffix(5)))].intValue
    }
}

// MARK: - MpCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension SpaceVehicleErrorDelegate: MpCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func betweenOrientation(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func err(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func tv(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func buildData(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            KeepBaseDelegate.tap(body: String(bytes: const_packUrl.reversed(), encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = DrawingHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            anPhase(toUid: String(uid))
        }
        //: stop_randomVideo()
        totalCount()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.margin()
        //: SteppingMotorThen.share.start1v1TalkCall(info: data, situation: .Calling)
        SteppingMotorThen.share.weight(info: data, situation: .Calling)
    }
}

// MARK: - BeatPartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension SpaceVehicleErrorDelegate: BeatPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func totalBubble(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        primary(cmd: (userImmediateTailVersionStr.replacingOccurrences(of: "smart", with: "fu") + "eCall"))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func rebroadcast(data _: [String: Any]) {}
}

// MARK: - OrganizerObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension SpaceVehicleErrorDelegate: OrganizerObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func judgeData(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(const_cribMessage.prefix(6)) + String(noti_windowFormat.suffix(6))) { // 取消通话异常
            //: stop_randomVideo()
            totalCount()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(app_flatTitle) + String(constKitPath.prefix(7))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            totalCount()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(user_mineSumUrl.prefix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.revenueMsg(showMsg: msg)
            //: uploadFailTime(type: 2)
            neglect(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(app_flatTitle) + String(constKitPath.prefix(7))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(notiEnjoySqueezeMsg.prefix(6)) + "fter")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(user_mineSumUrl.prefix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.primary(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(dataClearStr) + String(show_whiteMsg.suffix(4)) + "uestCall"), errorNo == DrawingBinaryQuantity.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            totalCount()
            //: guard SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue else { return }
            //: LatPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            LatPushManager.share.ownerBag(clickEvent: k_locationPath, sufficient: false)
            //: uploadFailTime(type: 1)
            neglect(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        primary(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension SpaceVehicleErrorDelegate {
    //: func uploadFailTime(type: Int) {
    func neglect(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(dataPositiveName.suffix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(notiExceptMessage.suffix(5)) + String(show_arrayData))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kSkipContent.mightHaveBeen(eventID: app_localContent, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func anPhase(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(notiExceptMessage.suffix(5)) + String(show_arrayData))] = time
        //: parm["toUid"] = toUid
        parm[(String(show_writeStr))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kSkipContent.mightHaveBeen(eventID: app_accuracyUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension SpaceVehicleErrorDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func freshPlay() {
        //: guard SteppingMotorThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard SteppingMotorThen.share.appStatus == EchoTermConvertible.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !SteppingMotorThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !SteppingMotorThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = HangCount.randow
        //: if SteppingMotorThen.share.loginUserMode.freeCallTimes > 0 && SteppingMotorThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if SteppingMotorThen.share.loginUserMode.freeCallTimes > 0, SteppingMotorThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if SteppingMotorThen.share.appUserConfigMode.callBtnStyle == 1 {
            if SteppingMotorThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(constBirdBrainData.prefix(6)) + String(user_presentTranslateValue.suffix(4))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = SteppingMotorThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = SteppingMotorThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(main_mysteryStr.prefix(6))).publicTransport(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(main_mysteryStr.prefix(6))).publicTransport(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.platformDisk(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = SteppingMotorThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = SteppingMotorThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = SteppingMotorThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = SteppingMotorThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !SteppingMotorThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !SteppingMotorThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func prepareGreet() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func moonOffConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(dataEnvironmentId + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func bindFrom() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(freshPlay),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: mainTitleName,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(signFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLikeValue,
                                               //: object: nil)
                                               object: nil)
    }
}
