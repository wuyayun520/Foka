
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let main_manageName:[Character] = ["m","f","/","m","o"]
fileprivate let showMediaId:[Character] = ["m","e","n","t","/","l","i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let kAdminValue:[Character] = ["m","f","/","m","o","m","e","n","t","/"]
fileprivate let showCellGeneralName:[Character] = ["m","o","m","e","n","t","s"]

/*: "mf/moment/like" :*/
fileprivate let dataCountMessage:String = "match attitude putmf/m"
fileprivate let data_secondPath:String = "lgoverne"

/*: "mf/crush/send" :*/
fileprivate let userPhotoValue:[Character] = ["m","f","/","c"]
fileprivate let notiGuidanceText:String = "product button venture shrinkrush/send"

/*: "mf/moment/view" :*/
fileprivate let user_loopKey:String = "mf/momeenhance notice"
fileprivate let noti_livingFormat:String = "enjoy blue backnt/view"

/*: "mf/momentReply/list" :*/
fileprivate let user_courseText:String = "rise lovely false ablemf/momen"
fileprivate let userElectricMsg:String = "tRepfind may oval"

/*: "mf/momentReply/save" :*/
fileprivate let data_pageValue:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p"]
fileprivate let constAskReduceName:[Character] = ["l","y","/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let constCurveFormat:String = "version failure advancedmf/mom"
fileprivate let noti_appropriateContent:String = "greatest performideoSign"

/*: "dynamicPic.zip" :*/
fileprivate let k_enterpriseFormat:[Character] = ["d","y","n","a","m","i","c","P"]
fileprivate let constBirthdayFormat:[Character] = ["i","c",".","z","i","p"]

/*: "pic" :*/
fileprivate let main_feedbackMessage:[UInt8] = [0x2d,0x34,0x3e]

private func stopCome(appear num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "content" :*/
fileprivate let mainExecutiveMsg:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let kCalculateName:String = "detailed silver limitmf/m"
fileprivate let const_lookValue:[Character] = ["a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let notiIonBrainDealName:[UInt8] = [0x77,0x65,0x69,0x76,0x20,0x6f,0x74,0x20,0x73,0x63,0x69,0x6d,0x61,0x6e,0x79,0x44,0x20,0x79,0x4d,0x20,0x6f,0x74,0x20,0x6f,0x47,0x20,0x2c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53,0x20,0x68,0x73,0x69,0x6c,0x62,0x75,0x50]

/*: "mf/moment/saveVideo" :*/
fileprivate let appRentRemContent:String = "shake generate land hung businessmf/mo"
fileprivate let data_definitionTitle:String = "sareade"

/*: .png" :*/
fileprivate let data_soonTitle:[Character] = [".","p","n","g"]

/*: "dynamicPic" :*/
fileprivate let const_arrayKey:String = "advicena"

/*: "freeImage" :*/
fileprivate let mainMaterialMsg:String = "holder over sure yet wavefreeIm"
fileprivate let noti_mmDevoteMsg:String = "atool"

/*: "mf/momentReply/replylist" :*/
fileprivate let const_sheBeautyKey:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l"]
fileprivate let userFoundationMessage:[Character] = ["y","/","r","e","p"]
fileprivate let constRetText:String = "lylisdrop"

/*: "mf/moment/delete" :*/
fileprivate let const_sharpText:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let constDiscountMessage:String = "mask feedback chin snap/delete"

/*: "mf/momentReply/delete" :*/
fileprivate let main_itsMsg:String = "direct shared moviemf/m"
fileprivate let show_representId:[Character] = ["l","y","/"]
fileprivate let mainEndMessage:String = "dpadete"

/*: "mf/moment/pin" :*/
fileprivate let user_recommendText:String = "mf/mbottom correct delay label general"
fileprivate let appGreetPath:String = "/pininteraction pack manual utility"

/*: "mf/moment/remindNum" :*/
fileprivate let userProviderValue:String = "mf/momresign air my street speaker"
fileprivate let dataPinId:String = "about receive public top disabledent/re"
fileprivate let mainDomainName:String = "mindNummeasurement injury"

/*: "mf/moment/remindList" :*/
fileprivate let userOkAgentName:String = "mf/mbranch gallery"
fileprivate let notiFollowingValue:String = "called deny seek sub/rem"

/*: "page" :*/
fileprivate let kWhoPlanData:[UInt8] = [0x65,0x67,0x61,0x70]

/*: "cos/getCredential" :*/
fileprivate let main_ventureHomeCandidFormat:String = "spec daily clearly minute scalecos/"
fileprivate let user_fairPath:String = "DENTIAL"

/*: "type" :*/
fileprivate let showSaveUrl:String = "typbuild"

/*: "user/reportUserInfo" :*/
fileprivate let show_animaFormat:[Character] = ["u","s","e","r","/","r","e","p","o","r","t","U","s"]
fileprivate let notiPortionValue:[Character] = ["e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let mainDestinationValue:String = "ubush"

/*: "deviceId" :*/
fileprivate let notiProductCuteLeftKey:String = "guard others unlessdeviceI"
fileprivate let appNameKey:[Character] = ["d"]

/*: "deviceName" :*/
fileprivate let k_donPath:[Character] = ["d","e","v","i","c","e","N","a","m","e"]

/*: "iphone" :*/
fileprivate let noti_clipData:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let constThanStr:String = "bottom engage run posit recentfileUrl"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DrawingRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class DrawingRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func afterFilterCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(main_manageName) + String(showMediaId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func detailCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(kAdminValue) + String(showCellGeneralName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func paramsLikePsychologicalMomentCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(dataCountMessage.suffix(4)) + "oment/" + data_secondPath.replacingOccurrences(of: "govern", with: "ik"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func quashEnable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(userPhotoValue) + String(notiGuidanceText.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func language(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(user_loopKey.prefix(7)) + String(noti_livingFormat.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func completion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(user_courseText.suffix(8)) + String(userElectricMsg.prefix(4)) + "ly/list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func misreckoning(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(data_pageValue) + String(constAskReduceName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func circle(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(constCurveFormat.suffix(6)) + "ent/v" + String(noti_appropriateContent.suffix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func transfer(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        from()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            daily(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(k_enterpriseFormat) + String(constBirthdayFormat)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: main_feedbackMessage.map{stopCome(appear: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(mainExecutiveMsg))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = MpRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(kCalculateName.suffix(4)) + "oment/s" + String(const_lookValue))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.pointRate(showMsg: String(bytes: notiIonBrainDealName.reversed(), encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.from()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func document(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(appRentRemContent.suffix(5)) + "ment/" + data_definitionTitle.replacingOccurrences(of: "read", with: "v") + "Video")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func daily(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.program()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(data_soonTitle))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((const_arrayKey.replacingOccurrences(of: "advice", with: "dy") + "micPic"))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func from() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(mainMaterialMsg.suffix(6)) + noti_mmDevoteMsg.replacingOccurrences(of: "tool", with: "ge")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func logical(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(const_sheBeautyKey) + String(userFoundationMessage) + constRetText.replacingOccurrences(of: "drop", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func res(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(const_sharpText) + String(constDiscountMessage.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func reason(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(main_itsMsg.suffix(4)) + "omentRep" + String(show_representId) + mainEndMessage.replacingOccurrences(of: "pad", with: "el"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func city(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(user_recommendText.prefix(4)) + "oment" + String(appGreetPath.prefix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func nearContact(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(userProviderValue.prefix(6)) + String(dataPinId.suffix(6)) + String(mainDomainName.prefix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func onceRemind(page: Int, completion: @escaping (_ data: [ModelTransformable]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(userOkAgentName.prefix(4)) + "oment" + String(notiFollowingValue.suffix(4)) + "indList")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: kWhoPlanData.reversed(), encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [ModelTransformable] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<ModelTransformable>.deserialize(from: arr as? Array) as? [ModelTransformable] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func inmate(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(main_ventureHomeCandidFormat.suffix(4)) + "getCre" + user_fairPath.lowercased())
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(showSaveUrl.replacingOccurrences(of: "build", with: "e")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func distribute(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = MpRequestModel()
        //: reqModel.requestServer = SteppingMotorThen.share.appConfigMode.reportDomain
        reqModel.requestServer = SteppingMotorThen.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(show_animaFormat) + String(notiPortionValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = SteppingMotorThen.share.loginUserMode.userID
        params[(mainDestinationValue.replacingOccurrences(of: "bush", with: "id"))] = SteppingMotorThen.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(notiProductCuteLeftKey.suffix(7)) + String(appNameKey))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(k_donPath))] = (String(noti_clipData))
        //: params["type"] = 1
        params[(showSaveUrl.replacingOccurrences(of: "build", with: "e"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(constThanStr.suffix(7)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        data_attributeId.doRate(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
