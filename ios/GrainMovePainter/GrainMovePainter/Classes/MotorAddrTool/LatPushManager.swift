
//: Declare String Begin

/*: "fee" :*/
fileprivate let const_simplyMapKey:String = "sideee"

/*: "VIPFee" :*/
fileprivate let notiInjuryStr:String = "VIPFeespark date task"

/*: "chattingNum" :*/
fileprivate let kLivingDayData:String = "ccreatett"

/*: "You're already in her live room" :*/
fileprivate let kCapableId:[UInt8] = [0xbc,0x8a,0x90,0xc2,0x97,0x80,0xc5,0x84,0x89,0x97,0x80,0x84,0x81,0x9c,0xc5,0x8c,0x8b,0xc5,0x8d,0x80,0x97,0xc5,0x89,0x8c,0x93,0x80,0xc5,0x97,0x8a,0x8a,0x88]

private func chemicProcessing(formal num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "toUid" :*/
fileprivate let kWeMessage:[UInt8] = [0x5e,0x45,0x7f,0x43,0x4e]

private func totalerestCorner(roll num: UInt8) -> UInt8 {
    return num ^ 42
}

/*: "momentId" :*/
fileprivate let mainTechniqueId:[UInt8] = [0x60,0x62,0x60,0x68,0x63,0x79,0x44,0x69]

private func publishGround(mix num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "extra" :*/
fileprivate let appThroughUrl:[UInt8] = [0x61,0x72,0x74,0x78,0x65]

/*: "uid" :*/
fileprivate let noti_enterKey:[UInt8] = [0xc4,0xd8,0xd5]

private func sirenSong(near num: UInt8) -> UInt8 {
    return num ^ 177
}

/*: "onlineStatus" :*/
fileprivate let user_bindName:[UInt8] = [0xb4,0xb3,0xb1,0xae,0xb3,0xaa,0x98,0xb9,0xa6,0xb9,0xba,0xb8]

fileprivate func slimLarge(translation num: UInt8) -> UInt8 {
    let value = Int(num) - 69
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let const_alreadyTitle:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "userStatus" :*/
fileprivate let showAttitudeData:[UInt8] = [0x47,0x41,0x57,0x40,0x61,0x46,0x53,0x46,0x47,0x41]

private func gestureDent(slip num: UInt8) -> UInt8 {
    return num ^ 50
}

/*: "Account is restricted！" :*/
fileprivate let main_miniValue:[Character] = ["A","c","c","o","u","n","t"," ","i","s"," ","r","e","s","t","r","i","c"]
fileprivate let app_memoryPath:[Character] = ["t","e","d","！"]

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let kModelContent:[UInt8] = [0xc9,0xee,0xe6,0xe3,0xea,0xeb,0xaf,0xfb,0xe0,0xaf,0xea,0xe1,0xfb,0xea,0xfd,0xaf,0xfb,0xe7,0xea,0xaf,0xec,0xe7,0xee,0xfb,0xaf,0xfd,0xe0,0xe0,0xe2,0xa1,0xaf,0xdf,0xe3,0xea,0xee,0xfc,0xea,0xaf,0xfb,0xfd,0xf6,0xaf,0xee,0xe8,0xee,0xe6,0xe1,0xaf,0xe3,0xee,0xfb,0xea,0xfd]

private func fitReason(anonymous num: UInt8) -> UInt8 {
    return num ^ 143
}

/*: "url" :*/
fileprivate let show_relatedValue:String = "urinteraction"

/*: "userDetail" :*/
fileprivate let dataWhoName:String = "USER"
fileprivate let notiCommentFormerContent:String = "Detailsoon admin ready wee"

/*: "&type=6" :*/
fileprivate let userGuidanceMsg:[Character] = ["&","t","y","p","e","=","6"]

/*: "&type=7" :*/
fileprivate let main_visualHereMessage:[Character] = ["&","t","y","p","e"]
fileprivate let dataEaseName:String = "=ok"

/*: "amount" :*/
fileprivate let noti_universalMessage:[UInt8] = [0x74,0x6e,0x75,0x6f,0x6d,0x61]

/*: "https://" :*/
fileprivate let user_skilledValue:String = "star app ready distinguish hemhttps:/"
fileprivate let const_additionalPocketValue:[Character] = ["/"]

/*: "http://" :*/
fileprivate let data_visibleWhoName:String = "http:return leading supportive composition"
fileprivate let notiDiskFormat:[Character] = ["/","/"]

/*: "transparency" :*/
fileprivate let dataServiceMsg:[Character] = ["t","r","a","n","s","p","a","r","e","n","c","y"]

/*: "version= :*/
fileprivate let showCommentValue:String = "historyer"
fileprivate let mainConsumerStr:[Character] = ["s","i","o","n","="]

/*: &packageId= :*/
fileprivate let const_missMsg:String = "section category&pac"

/*: &bundleId= :*/
fileprivate let userMeetingValue:[Character] = ["&","b","u","n","d","l","e","I","d","="]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LatPushManager.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class LatPushManager: NSObject {
public class LatPushManager: NSObject {
    //: @objc static public let share = LatPushManager()
    @objc public static let share = LatPushManager()
    //: private override init() {}
    override private init() {}
}

//: extension LatPushManager {
extension LatPushManager {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func native(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        kSkipContent.coincideStr(eventID: data_topFormat)
        //: guard TalkingSocketManager.shared.isPage == false else {
        guard HemSocketManager.shared.isPage == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.revenueMsg(showMsg: showFileUrl)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        LabelMpThen.noWait { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            ManagerControlReactiveCompatible.discourse { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = SpaceVehicleErrorDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(const_simplyMapKey.replacingOccurrences(of: "side", with: "f"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(notiInjuryStr.prefix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(kLivingDayData.replacingOccurrences(of: "create", with: "ha") + "ingNum")].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.strokeController()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func overQuantityeractionEnd() {
        //: SteppingMotorThen.share.userFillInfoMode = UserFillInfoModel.init()
        SteppingMotorThen.share.userFillInfoMode = CommentInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = RobotGenderVc()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = topical()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func afterTo(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = topical()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: OrganizerRecognizerDelegate.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: AlongsideViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = OrganizerRecognizerDelegate()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func postKey(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.strokeController() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? ControlViewDelegate, MaterialLiveManager.threadContext().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.miniPlace()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? SpringViewController {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.searched()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = SpringViewController(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        browse()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func transportationSystem(uid: String, enterType: EchoEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard LabelMpThen.liveTitle() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(MaterialLiveManager.threadContext().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.revenueMsg(showMsg: String(bytes: kCapableId.map{chemicProcessing(formal: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        ControlBodyReactiveCompatible().becomeCompletion(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = FluModelType.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.topical()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: NoticeManagerDelegate.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! NoticeManagerDelegate).behindFire()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            MaterialLiveManager.threadContext().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = NoticeManagerDelegate()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                kSkipContent.coincideStr(eventID: appMeMsg, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                kSkipContent.coincideStr(eventID: show_nameKey, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func jostleWithView(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = DirectViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = topical()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func generate(chatID: String, isFrom: PocketEpisodeFromEnum = .Normal, completion: ((_ vc: ControlRecognizerDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != DetermineJoinReactiveCompatible.getXiaoMiID() {
        if chatID != DetermineJoinReactiveCompatible.anthologist() { // 系统消息
            //: ProgressHUD.show()
            DirectProgressHUD.discountShow()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: kWeMessage.map{totalerestCorner(roll: $0)}, encoding: .utf8)!: chatID, String(bytes: mainTechniqueId.map{publishGround(mix: $0)}, encoding: .utf8)!: "0", String(bytes: appThroughUrl.reversed(), encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            TimeTestedRequestTool.descriptionPin(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                DirectProgressHUD.thatDismiss()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = ComprehendReactiveCompatible.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = ComprehendReactiveCompatible.motionDic(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                CookieInfoManager.prepareWith(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: noti_enterKey.map{sirenSong(near: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: user_bindName.map{slimLarge(translation: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: const_alreadyTitle.reversed(), encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: showAttitudeData.map{gestureDent(slip: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: dataRobotMeetingText,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.revenueMsg(showMsg: (String(main_miniValue) + String(app_memoryPath)).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.dit(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: ComprehendReactiveCompatible())
                let chatVC = self.dit(chatID: chatID, isFrom: isFrom, userWrap: ComprehendReactiveCompatible())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ComprehendReactiveCompatible ) -> TalkingPrivateChatController {
    func dit(chatID: String, isFrom: PocketEpisodeFromEnum = .Normal, userWrap: ComprehendReactiveCompatible) -> ControlRecognizerDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = topical()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ControlRecognizerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ControlRecognizerDelegate
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ControlRecognizerDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func aId(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        DirectProgressHUD.discountShow()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        TimeTestedRequestTool.acceptScript(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            DirectProgressHUD.thatDismiss()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.revenueMsg(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if DetermineJoinReactiveCompatible.isGroupChat(groupId) {
            if DetermineJoinReactiveCompatible.worldToday(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.premiumSearch(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = FoodRecognizerDelegate(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.topical()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.premiumSearch(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = FoodRecognizerDelegate(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.topical()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.revenueMsg(showMsg: String(bytes: kModelContent.map{fitReason(anonymous: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func premiumSearch(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.topical()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: FoodRecognizerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! FoodRecognizerDelegate
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func mouthSave(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: SteppingMotorThen.share.loginUserMode.isTPAuth)
        let tpAuth = PositiveSubscriptType(rawValue: SteppingMotorThen.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = BeatViewController()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = topical()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = EncompassViewController()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = topical()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func populationProfileUid(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func pushBack() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = topical()
        //: let vc = TalkingFeedbackVC.init()
        let vc = LieEstimatedFeedbackVc()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func policyLicense(jumpModel: HemSpecialJumpModel) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (show_relatedValue.replacingOccurrences(of: "interaction", with: "l")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            beforeRemarkConfig(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (dataWhoName.lowercased() + String(notiCommentFormerContent.prefix(6))) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func selectReply() {
        //: LatPushManager.share.func__pushToWebVC(webViewType: .SubscribePage)
        LatPushManager.share.medianType(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func restoreing(type: BulletTextConvertible, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = EchoRecognizerDelegate()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        topical()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension LatPushManager {
extension LatPushManager {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func medianType(webViewType: FairAndSquareMirrorPath) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        choiceConfig(webViewType: webViewType, webConfig: HangConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func validated(popupCallIndex: Int, appendParams: String = (String(userGuidanceMsg))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = AnalysisBalanceView()
        //: view.show()
        view.finishWith()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.iii(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                ordinalNumber(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func ownerBag(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(main_visualHereMessage) + dataEaseName.replacingOccurrences(of: "ok", with: "7"))) {
        //: let payWinType = SteppingMotorThen.share.appUserConfigMode.payWinType
        let payWinType = SteppingMotorThen.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = AnalysisBalanceView()
        //: view.show()
        view.finishWith()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (SteppingMotorThen.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (SteppingMotorThen.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.iii(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                ordinalNumber(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func ordinalNumber(appendParams: String = (String(main_visualHereMessage) + dataEaseName.replacingOccurrences(of: "ok", with: "7"))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = SolarBatteryManager.alleged(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: LatPushManager.share.func__pushToWebVC(urlStr: urlStr)
        LatPushManager.share.beforeRemarkConfig(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        kSkipContent.mightHaveBeen(eventID: notiDiskContent, parameterStr: [String(bytes: noti_universalMessage.reversed(), encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func iii(webViewType: FairAndSquareMirrorPath, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = HangConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        choiceConfig(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            userImageRejectId = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                kSkipContent.mightHaveBeen(eventID: clickEvent, parameterStr: [String(bytes: noti_universalMessage.reversed(), encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func discountMinimum() {
        //: guard !SteppingMotorThen.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !SteppingMotorThen.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = SteppingMotorThen.share.appConfigMode.urlH5Domain + SteppingMotorThen.share.appUserConfigMode.discountUrl
        let httpsUrl = SteppingMotorThen.share.appConfigMode.urlH5Domain + SteppingMotorThen.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(user_skilledValue.suffix(7)) + String(const_additionalPocketValue)), with: (String(data_visibleWhoName.prefix(5)) + String(notiDiskFormat)))
        //: LatPushManager.share.func__pushToWebVC(urlStr: httpUrl)
        LatPushManager.share.beforeRemarkConfig(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func choiceConfig(webViewType: FairAndSquareMirrorPath, webConfig: HangConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = SolarBatteryManager.alleged(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        intervaloDataConverter(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func beforeRemarkConfig(urlStr: String?, webConfig: HangConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? HangConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        intervaloDataConverter(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func intervaloDataConverter(urlStr: String?, webViewType: FairAndSquareMirrorPath?, webConfig: HangConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(dataServiceMsg))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (showCommentValue.replacingOccurrences(of: "history", with: "v") + String(mainConsumerStr)) + "\(constLikeName)" + (String(const_missMsg.suffix(4)) + "kageId=") + "\(main_netPath)" + (String(userMeetingValue)) + "\(appBlockUrl)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = ResourceReactiveCompatible(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = topical() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: ResourceReactiveCompatible.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! ResourceReactiveCompatible).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.capEvaluate(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataShowDetailPopMessage / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataShowDetailPopMessage, height: dataViewPath)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension LatPushManager {
public extension LatPushManager {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func topical() -> UIViewController? {
        //: return currentViewController()
        return strokeController()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct HemSpecialJumpModel: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
